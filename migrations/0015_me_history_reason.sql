-- 0015 — 모드 하향의 사유가 앉을 자리 (ADR-027 §구현 명세)
--
-- ADR-019 부수 규칙 2가 "하향에도 Override와 같은 마찰(사유 + 대기)"을 정했는데
-- 사유를 적을 곳이 없었다. `me_history`는 주석이 이미 "변경 궤적을 분석 입력으로 쓴다(§3)"고
-- 말한다 — 모드 변경도 설정 변경이므로 여기 들어간다.
--
-- **새 테이블을 만들지 않는다.** 만들면 §6.5 자기 보정이 읽을 곳이 둘로 갈린다.
--
-- NULL 허용이다: 상향·동일과 일반 me 변경에는 사유가 없다(ADR-027 — 상향은 자유).
ALTER TABLE me_history ADD COLUMN reason TEXT;
