<?xml version="1.0" encoding="utf-8"?>
<!-- android/app/src/main/res/xml/widget_add_task_info.xml
     위젯 메타 (T-46 · ADR-041).

     `updatePeriodMillis="0"` — **주기 갱신을 하지 않는다.** 그릴 데이터가 없으므로
     깨울 이유도 없다. 갱신 스케줄은 데이터를 그리기로 정한 뒤에 만든다(ADR-041 §근거).

     `resizeMode="none"` — 1×1 고정. 늘려도 그릴 것이 없다.

     ⚠️ `targetCellWidth`·`targetCellHeight`·`description`·`previewLayout`은 API 31+ 속성이다.
        그 아래에서는 조용히 무시되고 `minWidth`·`minHeight`가 대신 판정한다 — 그래서 둘 다 둔다. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="40dp"
    android:minHeight="40dp"
    android:targetCellWidth="1"
    android:targetCellHeight="1"
    android:resizeMode="none"
    android:widgetCategory="home_screen"
    android:initialLayout="@layout/widget_add_task"
    android:previewLayout="@layout/widget_add_task"
    android:description="@string/widget_add_task_description"
    android:updatePeriodMillis="0" />
