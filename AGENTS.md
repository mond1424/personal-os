# AGENTS.md — Antigravity CLI 작업 규약

⚠️ **이 파일이 이 리포에서 Antigravity가 읽는 유일한 규약이다.**

Antigravity는 `GEMINI.md`(도구 전용 오버라이드)와 `~/.gemini/AGENTS.md`(전역)도 자동으로 읽는다.
**둘 다 두지 않는다.**

```
GEMINI.md              만들지 않는다 — 이 파일과 두 벌이 되고, 갈라지는 순간 어느 쪽이
                       이겼는지 로그에 안 남는다
~/.gemini/AGENTS.md    ⚠️ 리포 밖이라 이 층이 못 본다. 여기에 이 프로젝트 규칙을 적으면
                       "적는 층과 지키는 층이 달라" 반드시 낡는다 — `CLAUDE.md`
                       §사람이 하는 것의 상태는 적지 않는다 와 같은 실패다
.agents/               규약 조각을 흩지 않는다
```

⚠️ **`agy`는 무엇이 로드됐는지 말해 주지 않는다.** `agy 1.1.23`의 하위 명령은
`agent(s)`·`changelog`·`help`·`install`·`mcp`·`mic-serve`·`models`·`plugin(s)`·`update`뿐이고,
**설정을 열어 보이는 명령이 없다.** 그러니 **자리를 직접 센다:**

```powershell
# 위임 전. 하나라도 있으면 작업을 시작하지 말고 보고한다
Test-Path GEMINI.md, .agents, ~/GEMINI.md, ~/.agents, ~/.gemini/AGENTS.md
```

**결정론적이고 모델을 부르지 않는다** — 확인법이 판단을 하면 그것도 갈라지는 자리다.
⚠️ `~/.gemini` 자체는 있어도 된다(Antigravity 설치·상태 디렉터리다). **`AGENTS.md`가 그 안에
있는지만** 본다.

⚠️ **`.gitignore`는 앞의 둘만 막는다.** 홈 디렉터리의 셋은 리포 밖이라 git이 모르고,
**그래서 이 확인이 위임 절차에 남는다.**

⚠️ **이 자리에 원래 `agy inspect`라고 적혀 있었다** — 문서에서 읽고 옮겼을 뿐 **돌려보지 않았다.**
`CLAUDE.md` §사람이 하는 것의 상태가 말하는 그대로다: **적힌 확인법이 안 돌면
그 절은 거짓말을 하는 것이고, 상태를 적어 두는 것과 같은 실패다.**
`--dry-run`의 exit 9, `versionCode` 고정에 이어 **세 번째**다.

`CLAUDE.md`가 코드 규약의 원본이고 이 파일은 그것을 **가리킨다.**
**규약을 복사하면 반드시 갈라진다** — 이 리포가 기준선 숫자·배포 상태·하루 경계에서
차례로 물린 그 실패다.

**이 앱은 사용자가 폰에 설치해 매일 쓰고 있다. 회귀 하나가 새벽에 드러난다.**
Cloudflare Worker (Hono / TS) + D1 + `[assets]` 정적 서빙 + Capacitor Android 셸.

---

## 시작 전에 반드시 읽는다

| 순서 | 파일 | 무엇 |
|---|---|---|
| 1 | `CLAUDE.md` | **코드 규약의 원본.** 아키텍처 원칙·검사 명령·함정 |
| 2 | `STATE.md` | 현재 상태. 마이그레이션 적용 여부·미해결 |
| 3 | `AGENT-CHAIN.md` | 층별 권한과 경계 |
| 4 | 티켓 (`docs/tickets/T-NN-*.md`) | 지금 할 일 |

**전체 프로젝트를 재탐색하지 않는다.** 티켓의 범위에 적힌 파일부터 읽고,
호출부·의존 함수는 필요성이 확인될 때만 넓힌다. 파일을 다시 읽을 때는 필요한 구간만.

---

## 너의 위치

체인의 **마지막**이다. 지시는 Claude Code에서 오고, 결과도 Claude Code로 돌아간다.
사용자에게 직접 결론을 내지 않는다.

이 자리에 오는 일은 **설계가 이미 확정됐고 배선만 남은 것**이다.
설계 판단이 필요하다고 느껴지면, 그건 티켓이 잘못됐다는 신호다 — 멈추고 보고한다.

---

## 절대 하지 않는 것

1. **티켓 범위 밖 파일 수정** — 필요하면 고치지 말고 멈춰서 보고한다
2. **커밋 · 푸시** — git은 Claude Code 전담이다
3. **`STATE.md` · `APP-BUILD.md` · `APP-PLAN.md` · `APP-ADR.md` · 설계 문서 수정** — 읽기만
4. **마이그레이션 SQL 작성 · DB 트리거 수정** — 불변성의 최종 강제선이다
5. **`wrangler.toml`의 `database_id` 수정**
6. **`wrangler deploy` · `wrangler secret put` · `--remote` 마이그레이션** — 사용자만
7. **파생값 저장** — Todo/Done/Missed·이월 횟수·대기 일수·달성률·'지금'은 전부 조회 시 계산

---

## 물러서는 규칙

**확신이 부족하면 추측으로 범위를 넓히지 말고 멈춘다.** 넓게 훑어서 얻는 것보다,
관계없는 파일을 건드려서 잃는 것이 크다.

브라우저·폰에서 직접 확인해야 하는 것(렌더 결과·실제 폭·터치 동작·알람이 새벽에 울렸는가)은
**추측하지 말고 확인 절차를 적어 올린다.** 사용자가 실측한다.

---

## 검사 — 끝내기 전에 반드시

| 명령 | 하는 일 |
|---|---|
| `npm run typecheck` | `tsc --noEmit` |
| `npm run smoke` | HTTP 계층 서버 검사 (node:sqlite 셰임) |
| `npm run front` | 격리 러너 `e2e.mjs`(임시 D1 + jsdom). 실 DB 불변 |
| `npm run verify` | 위 셋을 한 번에 |

- `e2e.mjs`에서 `spawnSync ETIMEDOUT`이 뜨면 **진짜 hang이다.** 무시하지 않는다 —
  러너의 모든 대기에 상한이 있으므로 실패는 어디서 막혔는지 이름을 말한다. 그 줄을 보고한다
- front는 간헐 플레이크가 있다 — 재실행으로 확인한다
- **검사가 옛 동작을 검사하고 있으면 검사를 고치고 그 사실을 보고한다**

---

## 보고 형식 — 이대로 쓴다

```
티켓: T-07
바꾼 파일: public/app.js, public/index.html, test/front.mjs
기준선: typecheck 통과 · smoke 213 → 216 · front 167 → 170 · 실패 0
설계와 어긋난 점: 없음
막힌 것: 없음
```

- **"통과했습니다"는 보고가 아니다.** 숫자 앞뒤를 쓴다
- 숫자가 안 맞으면 원인을 찾기 전에 끝내지 않는다
- 설계와 어긋나 보이는 것을 발견하면 **고치지 말고 쓴다** — 판단은 위층의 몫이다

---

## 함정 — 실제로 물렸던 것들

전체 목록은 `CLAUDE.md`와 `README0722.md`에 있다. 프런트를 만진다면 최소한 이것들:

1. **`scrollIntoView` 금지** — `.phone`이 `overflow:hidden`이라 셸이 밀린다. 위치는 `scrollTop`만
2. **트랙 위치는 % `transform`** — 손가락분만 px(`clientWidth||380` 폴백). jsdom은 `clientWidth=0`
3. **jsdom 제스처는 좌표를 `MouseEvent` 생성자로** — 나중에 붙이면 `dx=NaN` → 검사 거짓 통과
4. **`boot()` 중복 실행 가드(`booted`)를 지우지 않는다** — 스와이프 한 번에 탭 두 칸
5. **색은 CSS 변수만** — 다크 대응은 항상 짝(`[data-theme="dark"]` + `@media prefers-color-scheme:dark`)
6. **마감된 날은 트리거가 동결** — 프런트는 `day_status`로 판단한다. 추측하면 409
7. **전역 클래스명 충돌 — 세 번 물렸다.** 짧고 일반적인 이름(`tt`·`wseg`·`warn`·`ok`·`on`)을 쓰기 전
   반드시 `grep`으로 기존 사용처를 확인한다. **CSS 규칙과 JS 선택자 양쪽을 본다**

## 서버를 만진다면

- **SQL은 `db/index.ts`에만.** 도메인 규칙·트랜잭션 순서는 `services/`
- **라우트는 리터럴 경로를 와일드카드보다 앞에** 둔다 — 두 번 물렸다
  (`/api/lm/{sections,import-me,item/:id}`가 `/api/lm/:section`보다 앞)
- 불변성은 API가 아니라 **DB 트리거가 최종 강제**하고, Worker는 그 거부를 `409/400`으로 번역만 한다
- 새 마이그레이션이 생기면 `test/smoke.ts`의 하드코딩 스키마 목록에도 파일명을 넣는다
  (단, 마이그레이션 작성 자체는 네 일이 아니다)
