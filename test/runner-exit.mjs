// 러너의 종료 코드 — **여기가 유일한 자리다.** `front.mjs`가 내고 `e2e.mjs`가 읽는다.
//
// ★ **왜 코드로 가르는가.** `e2e.mjs`는 `stdio:"inherit"`로 자식을 띄운다(진행이 실시간으로
// 보이는 것이 그 선택의 값이다). 그래서 부모는 자식의 stdout을 **읽지 않는다** — 요약 줄이
// 나왔는지 알 길이 종료 코드뿐이다. 문자열을 파싱하려고 파이프로 바꾸면 150초 동안 화면이
// 침묵한다. **한 곳에만 두거나 아예 두지 않는다**(CLAUDE.md)의 그 규칙으로 상수는 여기 하나다.
//
// ⚠️ **1을 쓰지 않는다.** node가 죽을 때 기본으로 내는 값이 1이라, 1을 '검사 실패'에 쓰면
// **요약을 낸 빨간불과 요약도 못 낸 죽음이 같은 값**이 된다 — T-66에서 배터리가 거짓말을 한
// 자리가 정확히 그것이다(요약 없음을 "아무도 안 죽었다"로 읽었다).
export const EXIT_OK = 0;        // 요약을 냈다 · 실패 0
export const EXIT_FAILED = 2;    // 요약을 냈다 · 실패 N — **정상적인 빨간불**
export const EXIT_ABORTED = 3;   // 요약을 냈다 · 다만 도중까지다 (잡히지 않은 예외로 중단)
// 그 밖의 nonzero·신호 = **요약을 못 냈다.** 예약하지 않는다 — `e2e.mjs`가 그렇게 이름 붙인다.
