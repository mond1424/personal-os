# BRIEF-AGENCY-0810 v2 — 사용자 주체성 축소: 입력 이관 · 감각 계층

> **적용 정오 (2026-08-10, 리포 실사 후)** — 본문 번호는 작성 시점 원격 기준(ADR ~026 · 마이그레이션 ~0009)이다. 적용 시점 리포는 **ADR-027(모드 하향 벡터, 8/5) · 마이그레이션 0015**까지 진행돼 있었다. 실제 반영은 **ADR-027~033 → 028~034 · 0012_cal_sync → 0016_cal_sync · 0011_notify → 착수 시 배정**으로 재배정했다. W5 진입 게이트(Guard 루프 닫힘)는 이미 충족(`5a1ff41`). 본문 숫자는 기록 보존을 위해 고치지 않는다.

작성 2026-08-10 (v2 — 같은 날 감각 계층 확장) · 기준 APP-PLAN(7/29) · APP-ADR(~026) · STATE.md(7/23)
이 문서는 APP-PLAN·APP-ADR 갱신 지시서다. §2의 ADR 전문을 APP-ADR에 추가하고, §3의 지시대로 APP-PLAN을 수정한다. v1 대비 변경: ADR-031~033(감각 계층) · Phase 8 · 보류 기록(§7) 추가. ADR-027~030은 v1에서 사용자 승인 완료, 무변경.

---

## 0. 배경 — 문제 정의

앱 사용 경험이 실생활에 충분히 도달하지 못하고 있다. 원인은 개별 기능이 아니라 전제다 — **시스템이 작동하려면 사용자가 성실해야 한다.** 사용자가 센서(직접 기록)이자 실행기(알림 수용)를 겸하고, 사용자가 멈추면 시스템도 멈춘다. 외부 구속의 부재를 해결하려는 시스템이 사용자의 성실성에 의존하는 구조적 모순.

프로젝트의 출발 동기였던 "알아서 돌아가는 구조"가 이뤄지지 않은 이유도 같다 — 구조가 잘못된 게 아니라 **감각기관이 없다.** 현재 구조는 기록계(사용자 손이 유일한 입력원)로 설계됐고, 8월에 만드는 네이티브 앱이 감각기관을 꽂을 소켓이 된다. 필요한 것은 재작성이 아니라 증설이다.

주체성이 하중을 지는 지점:

| # | 지점 | 처리 |
|---|---|---|
| ① | 일정 입력 — 폰 기본 캘린더와 이중 입력 | ADR-028 (8월 W5 읽기 / 9월 쓰기) |
| ② | 기록 입력 — 할일·memo·성찰, 앱을 열어야 함 | ADR-030, 9월 |
| ③ | 보호 규칙 부착 — protect_* 설정 UI 없음 | ADR-025 + ADR-029 |
| ④ | outcome 수동 확정 | v1 수동 유지 (§9 #1 미확정, 변경 없음) |
| ⑤ | lifestyle 데이터(수면·활동) — 입력 자체가 없음 | ADR-031, 9월+ |
| ⑥ | 외부 정보(학교 공지·메신저) — 사람이 읽고 옮김 | ADR-032·033, 9월+ |

①~④ = **입력 이관**(기존 입력 노동의 이동·소멸, Phase 7). ⑤~⑥ = **감각 계층**(새 입력원 증설, Phase 8).

---

## 1. 결정 요약

| 결정 | 내용 | ADR |
|---|---|---|
| 원칙 | 행동 데이터는 시스템이 수집, 성찰 데이터는 하루 1회로 압축. 결정권은 남기고 입력 노동을 없앤다 | 027 |
| 캘린더 | **양방향 동기화. 단 동기화 창 = 열린 날만.** 8월 = 읽기, 9월 = 쓰기 | 028 |
| 보호 규칙 | 자동 부착이 아니라 **자동 제안** — 결정론 키워드 + 1탭 확인. 사전 서약(§6.2) 보존 | 029 |
| 기록 경로 | share-target · 알림 액션 · RemoteInput 마감 — 9월, 기존 엔드포인트 재사용 | 030 |
| 건강 데이터 | **Health Connect 로컬 읽기.** 워치는 전제가 아니라 정밀도 업그레이드. 식사는 자동화 불가 — 명시적 제외 | 031 |
| 학교 공지 | Worker cron이 fetch·diff·키워드 매칭 → 제안. 공개 게시판만 | 032 |
| 메신저 | 알림 캡처(NotificationListenerService) → 기기 필터 → model_low 추출 → 제안 | 033 |

공통 형태: **수집은 로컬·결정론 (비용 0), AI는 필터 뒤 소량에만, 확정은 사용자 1탭.** ADR-021(발동)·029(서약)의 사상이 감각 계층 전체에 일관 적용된다.

---

## 2. 신규 ADR 초안 — APP-ADR에 추가

목록 표에 다음 7행 추가:

```
| [027](#adr-027) | 입력의 무게중심을 시스템으로 — 주체성 축소 원칙 | `Accepted` | 전 Phase |
| [028](#adr-028) | 캘린더 양방향 동기화 — 동기화 창은 열린 날만 | `Accepted` | 7 |
| [029](#adr-029) | 보호 규칙은 자동 부착이 아니라 자동 제안 | `Accepted` | 7 |
| [030](#adr-030) | 기록 경로를 OS 표면으로 — share-target·알림 액션 | `Accepted` | 7 |
| [031](#adr-031) | 건강 데이터는 Health Connect에서 로컬로 읽는다 | `Accepted` | 8 |
| [032](#adr-032) | 학교 공지는 Worker가 수집한다 — fetch·diff·키워드 제안 | `Accepted` | 8 |
| [033](#adr-033) | 메신저는 알림 캡처로 읽는다 — 기기에서 필터, 제안만 | `Accepted` | 8 |
```

---

<a id="adr-027"></a>
## ADR-027. 입력의 무게중심을 시스템으로 — 주체성 축소 원칙

**Status** `Accepted` · **Phase** 전 Phase · 2026-08-10
> ⚠️ **설계문서 갱신 필요.** v0.9는 사용자를 유일한 입력원으로 전제한다. ADR-018의 §6.4 갱신과 함께 v1.0에서 다룬다.

### 맥락
실사용에서 확인된 것: 앱을 직접 켜서 기록하고 알림을 수용해야 하는 수동적 역할이 실생활 영향의 상한을 만든다. 폰 기본 캘린더와의 이중 입력, 복잡한 구성 등 접근성 비용이 기록 자체를 막는다. 시스템의 존재 이유(외부 구속)가 사용자의 성실성(내부 구속)에 의존하는 모순.

### 결정
데이터를 두 종류로 나누고 다르게 다룬다.

| 종류 | 예 | 정책 |
|---|---|---|
| **행동 데이터** | 일정 · 기기 사용 · 화면 on/off · foreground app · 수면·활동 | **시스템이 수집한다.** 사용자 입력은 예외 경로 |
| **성찰 데이터** | score · feelings · 미루기 사유 · Override 사유 | **자동화하지 않는다.** 대신 하루 1회 순간으로 압축, 도달 채널에서 바로 응답 |

부수 원칙: **입력 노동은 없애되 결정권은 남긴다.** 시스템이 관찰하고 제안하며, 확정은 사용자가 한다(1탭). 특히 사전 서약(§6.2)이 걸리는 결정은 자동화 금지 — ADR-029.

### 근거
- 감지 계열(ADR-018·025)은 이미 이 방향이다. 이 ADR은 그것을 입력 전반의 원칙으로 승격한다
- 성찰 데이터를 자동화하지 않는 이유: score·feelings는 관찰이 아니라 자기 보고다. 추정치로 채우면 분석·자기 보정(§6.5)의 입력이 오염된다
- 결정권을 남기는 이유: Guard의 정당성은 "미리 정한 나"의 서약에서 나온다. 서약 없는 개입은 부과이고, 부과는 도구 이탈(§6.3)로 귀결된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 위젯·알림 개선으로 대응 | 증상 완화. 이중 입력과 수동 기록이라는 구조가 남는다 |
| 성찰 데이터도 AI 추정으로 자동화 | 자기 보고를 추정으로 대체하면 보정 입력이 오염된다. §6.5의 전제 훼손 |
| 전면 자동화(확정까지) | 사전 서약 원칙 위반. Guard 정당성 붕괴 |

### 감수하는 비용
동기화·권한·제안 UI라는 새 표면이 생긴다. 각 표면의 비용은 하위 ADR(028~033)에서 개별 평가한다.

### 재검토 트리거
자동 수집이 만든 데이터의 품질이 수동 입력보다 낮다고 확인될 때(중복·오분류가 수정 노동을 만들 때).

---

<a id="adr-028"></a>
## ADR-028. 캘린더 양방향 동기화 — 동기화 창은 열린 날만

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
일정의 자연스러운 입력처는 폰 기본 캘린더다. personal-os가 자체 일정 입력을 요구하는 한 이중 입력이 남고, 이중 입력은 어느 한쪽의 방치로 끝난다. 한편 personal-os의 events는 보호 규칙(protect_*)이 걸리는 곳이라 캘린더를 버릴 수도 없다.

양방향 동기화의 일반적 비용은 충돌 해소다(ADR-015가 CRDT를 과잉이라 기각한 이유). 그러나 이 도메인에는 일반 동기화에 없는 자산이 있다 — **마감된 날은 불변**이라는 규칙이 이미 있다.

### 결정
**양방향 동기화. 단 동기화 창 = 열린 날(오늘 포함)~미래 N일(기본 60, settings).**

```
열린 날   : 캘린더 ↔ personal-os 양방향. 캘린더가 편집 표면
마감된 날 : 동기화 영구 중단. personal-os 기록이 유일한 원본
           (이후 캘린더에서 과거를 고쳐도 무시)
```

**"열린 날은 캘린더와 공유하는 현재, 마감된 날은 personal-os만의 과거."** 귀속일 마감이 동기화 이탈 시점을 겸한다 — 새 개념을 만들지 않고 기존 불변성이 동기화 경계가 된다.

세부:

| 항목 | 결정 |
|---|---|
| 필드 스코프 | 제목 · 날짜 · 시각/종일만. protect_*는 앱 전용(캘린더로 안 나감). 위치·참석자·알림은 동기화 안 함 |
| 식별 | `events.ext_src('devcal')`·`ext_uid`. 반복 일정은 `<eventId>:<날짜>`로 인스턴스 단위 |
| 충돌 | 갱신 시각 LWW. 창이 좁고 단일 사용자라 실충돌은 드묾 — 해소 UI 없음 |
| 반복 일정 | `CalendarContract.Instances`로 창 범위만 전개해 개별 행으로. 앱→반복원본 수정은 금지(캘린더에서만) |
| 날짜 귀속 | 캘린더의 벽시계 날짜 그대로. 귀속일 재계산 금지 — 일정·보호 판정은 벽시계의 것(APP-PLAN 함정과 정합) |
| 삭제(캘린더→앱) | 열린 날 mirror 삭제. guard 이력 참조 시 삭제 대신 protect 해제+보존(개입 이력 불변) |
| 실행 주체 | **기기.** CalendarContract는 기기만 본다. 서버는 멱등 upsert 엔드포인트만 제공 |
| 시점 | 앱 열 때 + 하루 1회 보호 일정 pull **직전**(캘린더의 시험이 제안→서약→알람 예약 경로에 타도록) + 수동 새로고침 |
| 대상 캘린더 | 사용자가 기기 캘린더 목록에서 선택(1개 이상, settings) |

**단계화 — 8월은 읽기 방향만.**

| | 캘린더→앱 (읽기) | 앱→캘린더 (쓰기) |
|---|---|---|
| 시점 | **8월 W5 전반** | 9월 |
| 가치 | 이중 입력 소멸 + 보호 제안 재료 = 주체성 축소의 본체 | 앱 생성 일정의 캘린더 노출 |

읽기 단계 동안 devcal-소스 event는 **앱에서 읽기 전용**(수정은 캘린더에서). 두 저장소가 갈라질 경로를 쓰기 방향이 붙기 전까지 물리적으로 차단한다.

### 근거
- 동기화 창 한정이 양방향의 비용 문제를 구조로 푼다. 충돌 창이 "열린 날"로 좁혀지고, 과거 이력(양이 무한히 쌓이는 쪽)은 동기화 대상 자체가 아니다
- 마감 시점 확정은 설계 §1.3의 자연스러운 확장이다 — daily가 마감되며 그 날의 일정도 personal-os 기록으로 확정된다
- 캘린더가 입력 표면이 되면 보호 규칙의 재료(시험·마감 일정)가 등록 노동 없이 도착한다. ADR-025의 지적("보호 일정을 붙이는 UI도 없다")이 UI 신설이 아니라 흡수로 풀린다
- 읽기/쓰기 단계 분리로 8월 추가분이 최소화된다. 읽기는 서버 쪽이 upsert 하나라 Guard 크리티컬 패스와 접점이 없다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 캘린더=미래의 원본, 단방향 읽기만(원안) | 앱에서 만든 일정이 캘린더에 안 보인다. 표면이 둘로 남는 반쪽 해소 |
| personal-os 원본 유지 + 가져오기 | 이중 입력 부분 잔존. 접근성 문제의 근본 해결이 아님 |
| 전 기간 양방향(창 무제한) | 과거 편집 충돌이 불변성과 정면충돌. ADR-015가 기각한 복잡도가 그대로 돌아옴 |
| Google Calendar API 경유 | 계정 종속·네트워크 의존·쿼터. CalendarContract는 로컬이라 오프라인 정합(ADR-021 방향)과 일치 |

### 감수하는 비용
- 재설치 시 기기 쪽 매핑이 사라져 재연결 필요. `(ext_src, ext_uid)` 유니크 인덱스가 서버 쪽 중복은 막는다. 기존 수동 입력분과의 병합은 (날짜, 제목) 휴리스틱 1회
- 여러 날짜에 걸친 종일 일정은 v1에서 시작일 1건으로 축약
- 쓰기 방향이 붙기 전까지 devcal event는 앱에서 편집 불가 — 한 달 감수
- 마이그레이션 번호 소비: **0012_cal_sync**. 인증(Phase 0)은 0013으로 밀림

### 재검토 트리거
9월 write-back 착수 시(LWW로 부족한 충돌이 실제로 관측되는지). 창 크기 N은 실사용 조정.

---

<a id="adr-029"></a>
## ADR-029. 보호 규칙은 자동 부착이 아니라 자동 제안

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
ADR-028로 시험·마감 일정이 캘린더에서 자동으로 들어온다. 다음 질문: 보호 규칙(protect_*)도 자동으로 붙일 것인가? 주체성 축소를 끝까지 밀면 자동 부착이지만, Guard의 정당성은 사전 서약(§6.2)에서 나온다.

### 결정
**결정론 키워드 매칭으로 제안하고, 확정은 사용자 1탭.**

```
가져온 일정 제목 ⊇ 키워드(settings `guard_kw`, 기본: 시험·마감·면접·발표·접수)
  → Today에 제안 카드 1회: "8/21 정보처리기사 — 보호 걸까요? [보호 설정 / 안 함]"
  → [보호 설정] = protect_* 기본값 부여 + 알람 예약 경로 진입
  → 무응답 시 다음 열림에 재노출, event 시작 후 소멸. 응답은 기록
```

카드는 outcome 확정 카드(APP-PLAN 4주)와 같은 패턴을 재사용한다. 신규 UI 최소.

### 근거
- 서약이 없는 개입은 부과다. 새벽에 발동한 Guard가 "내가 건 것"이 아니면 Override가 아니라 이탈을 부른다 — §6.3
- 매칭은 결정론이다. AI 분류를 넣지 않는 이유는 ADR-021과 같다 — 제안 경로가 서드파티 가용성에 걸릴 이유가 없다. 오분류 비용도 비대칭적으로 안전하다: 과잉 제안은 1탭 거절, 누락은 기존 수동 경로
- 1탭이면 주체성 비용은 사실상 0이고 서약의 형식은 보존된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 완전 자동 부착 + 사후 해제 | 사전 서약 위반. 해제가 Override 마찰과 충돌하는 역설(자기가 안 건 보호를 마찰 내고 풀어야 함) |
| AI로 일정 성격 분류 | 제안 품질 이득 대비 가용성·비용. 키워드로 시작하고 10월 데이터로 판단 |
| 제안 없이 수동 유지 | ADR-025가 지적한 "보호 일정이 드물다" 문제가 그대로. 발동 루프가 계속 굶는다 |

### 감수하는 비용
키워드 목록 관리가 사용자 몫으로 남는다(기본값 제공으로 완화). 제안 카드 무시가 쌓이면 카드 자체가 소음 — 재노출은 열림당 1회로 제한.

### 재검토 트리거
10월. 제안 수락률이 기록되므로, 수락률이 매우 높으면(서약이 형식화되면) 자동 부착+통지로 내리는 것을 그때 다시 판단한다.

---

<a id="adr-030"></a>
## ADR-030. 기록 경로를 OS 표면으로 — share-target · 알림 액션

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
기록(할일·memo·하루 마감)은 앱을 열고 화면을 찾아 들어가야 한다. 위젯(Phase 5)이 일부를 풀지만 9월이고, 위젯도 결국 "보러 가는" 표면이다. OS에는 이미 도달해 있는 표면이 있다 — 공유 시트와 알림.

### 결정
기록 진입점 3종을 네이티브 표면에 만든다. **전부 9월. 전부 기존 엔드포인트 재사용(ADR-012 준용).**

| 표면 | 동작 | 서버 |
|---|---|---|
| share-target | 아무 앱에서 공유 → [memo / 할 일] 선택 → 저장 | `addMemo`·기존 task 생성. 신규 없음 |
| 알림 액션 | 리마인더 알림에 [완료]·[미루기] 버튼 | `complete`·`defer`. 409는 앱 열기로 폴백 |
| RemoteInput 마감 | 하루 마감 알림에서 score·한 줄 응답 → 마감 | 기존 close 경로 |

### 근거
- ADR-027의 성찰 데이터 압축("하루 1회, 도달 채널에서 바로")의 실행 형태가 RemoteInput 마감이다
- 공유 시트는 기록 마찰의 최저점이다 — 보던 것을 보내는 행위라 맥락 전환이 없다. **식사 기록(ADR-031이 자동화 불가로 판정)의 상한 경로이기도 하다**
- 기존 엔드포인트 재사용 이유는 ADR-012와 동일: 도메인 규칙(409·state·귀속일)이 한 곳에 산다
- 9월인 이유: 알림 액션·RemoteInput은 0011(notify) 위에 서고, 8월 W3~5는 Guard가 쓴다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Daily 위젯으로 해결 | RemoteViews에 텍스트 입력이 없어 어차피 알림 RemoteInput 우회가 필요(APP-PLAN Phase 5). 그럼 알림이 본체다 |
| 8월 W5 편입 | W5는 Guard 밀림 대비 버퍼 + 캘린더 읽기(ADR-028)가 선점 |
| 앱 내 입력 개선(진입 단축) | 앱을 연다는 전제가 남는다. ADR-027 위반 |

### 감수하는 비용
알림 액션의 409 처리·RemoteInput의 마감 실패 전달이 알림 UI 제약 안에서 거칠다. 실패 시 앱 열기 폴백으로 통일.

### 재검토 트리거
ADR-010 재검토(9월, 위젯 순서)와 함께 본다 — RemoteInput 마감이 자리 잡으면 Daily 위젯의 존재 이유가 좁아진다.

---

<a id="adr-031"></a>
## ADR-031. 건강 데이터는 Health Connect에서 로컬로 읽는다

**Status** `Accepted` · **Phase** 8 · 2026-08-10

### 맥락
lifestyle 데이터(수면·활동)를 세세한 입력 없이 얻어 분석·제시에 쓰고 싶다. 장애물로 보였던 것: 스마트워치 부재, API 비용. 실제로는 둘 다 전제가 아니다.

### 결정
**Android Health Connect를 유일한 건강 데이터 소스로 한다. 기기 로컬 읽기, 서버측 건강 API·클라우드 SDK 사용 안 함.**

| 층 | 내용 |
|---|---|
| 1차 (지금) | 폰 화면 on/off 기반 수면 추정 — §6.6에 이미 있는 항, W2 감지 수집이 재료. 워치 불필요 |
| 2차 (HC) | Health Connect에서 수면 세션(단계 포함)·걸음·심박 읽기. Samsung Health 등이 쓰는 데이터를 유형별 권한 승인 후 로컬로 |
| 우선순위 | HC에 실측 수면이 있으면 추정치보다 우선. `risk_snapshot`·분석 입력에 출처(`estimated`/`hc`) 표기 |

**식사는 자동 수집 대상에서 명시적으로 제외한다.** 어떤 소비자 기기도 식사를 자동 감지하지 못한다. 식사 기록의 상한은 초저마찰 수동 — share-target 사진/한 줄(ADR-030).

### 근거
- HC는 로컬 API라 네트워크 0 · API 비용 0 — ADR-021의 로컬 원칙과 정합
- Samsung Health SDK 직접 연동 대신 HC를 경유하면 제조사 종속이 없고, 워치를 나중에 들여도 코드 변경이 없다(워치가 HC에 쓰기 시작할 뿐)
- 워치는 전제조건이 아니라 정밀도 업그레이드다. 폰 추정(무료·이미 계획됨)으로 시작해, 추정치가 분석에 유효한지부터 확인하는 순서가 싸다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Samsung Health SDK 직접 연동 | 제조사 종속·심사. HC가 같은 데이터를 표준 경로로 준다 |
| 서버측 건강 플랫폼 API | 네트워크·비용·계정 종속. 로컬로 충분 |
| 식사 자동 감지 | 기술적으로 불가. 가능한 척 설계하면 빈 파이프라인만 남는다 |
| 워치 구매를 전제로 설계 | 전제가 아니다. 폰 추정으로 시작 가능 |

### 감수하는 비용
- HC 권한 승인 흐름(데이터 유형별)이라는 온보딩 표면 추가
- 추정치와 실측치의 이원화 — 출처 표기로 분석 오염 방지
- 데이터 품질이 기기·수면 습관(폰을 머리맡에 두는가)에 따라 다름

### 재검토 트리거
10월 분석에서 수면 항의 기여도 평가. 추정치가 무용하면 워치 도입을 그때 판단(반대가 아니라).

---

<a id="adr-032"></a>
## ADR-032. 학교 공지는 Worker가 수집한다 — fetch·diff·키워드 제안

**Status** `Accepted` · **Phase** 8 · 2026-08-10

### 맥락
일정·마감 정보가 학교 사이트 공지에 먼저 뜬다. 사람이 읽고 옮기는 노동이 주체성 하중 ⑥의 절반이다.

### 결정
**기존 cron에 수집기를 태운다.** 등록한 공개 게시판 URL을 fetch → 저장된 해시와 diff로 신규 글 판별 → 제목 키워드 매칭(결정론) → 아웃박스(0011) 적재 + 제안 카드 → 1탭으로 일정화(ADR-029 경로 합류).

**로그인 벽 뒤(포털·LMS)는 v1 제외.** 공개 게시판만.

### 근거
- cron·아웃박스·제안 카드·키워드 매칭 전부 기존 자산이다. 새로 짓는 것은 fetch·diff·파서뿐
- 공개 페이지라 권한·구조 문제가 없다 — "구조적으로 막혀 있을 것"이라는 예상과 달리 이 항목은 그냥 일이다
- 키워드 결정론으로 시작하는 이유는 ADR-029와 같다. 본문 AI 분석은 매칭된 글에만 사후로(비용 통제)

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| LMS 로그인 자동화 | 세션 유지·2FA·구조 변경에 취약. 유지보수가 편익을 넘는다 — v2 재검토 |
| 전 글 AI 분석 | 비용. 제목 키워드 필터 후 소량에만 |
| 제안 없이 자동 일정 등록 | ADR-029 위반. 오수집이 캘린더를 오염 |

### 감수하는 비용
게시판 HTML 구조가 바뀌면 파서가 깨진다 — 사이트별 파서는 소모품으로 취급(깨지면 고치는 운영 비용). 대상 게시판 목록 관리는 사용자.

### 재검토 트리거
공개 게시판만으로 커버리지가 부족하다고 확인될 때 로그인 벽을 다시 판단.

---

<a id="adr-033"></a>
## ADR-033. 메신저는 알림 캡처로 읽는다 — 기기에서 필터, 제안만

**Status** `Accepted` · **Phase** 8 · 2026-08-10

### 맥락
일정·과제 공지가 카톡 단톡방으로 온다. 카카오톡에는 메시지를 읽는 API가 없다 — 주체성 하중 ⑥의 나머지 절반이자, "권한 등 구조적 문제가 있을 것"이라 예상됐던 항목. 실제로는 표준 우회 경로가 있고 권한 계급도 이미 확보한 급이다.

### 결정
**`NotificationListenerService`로 수신 알림 텍스트를 캡처한다. 처리는 전량 기기 로컬, 서버에는 필터 통과분만.**

```
알림 캡처 (기기)
 → 등록한 방/발신자 + 키워드 필터 (결정론, 기기 로컬)
 → 통과분만 서버로 → model_low 일정 추출 → 제안 카드 1탭 (ADR-029 경로 합류)
 → 비매칭 원문은 저장하지 않는다
```

### 근거
- 카톡 공식 API 부재에서 유일하게 실행 가능한 경로다
- 권한 계급이 UsageStats와 동일하다(특수 접근 + 사이드로드 제한된 설정 1회 해제) — 이미 다루는 계급
- 필터 후 소량이라 AI 비용이 통제된다(`ai_daily_cap` 계열). 오추출 비용은 1탭 거절로 비대칭 안전(ADR-029와 동형)
- "비매칭 미저장"은 ADR-022가 전체 활동 로깅을 기각한 것과 같은 사생활 최소화 원칙

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 카카오 API | 존재하지 않는다 |
| 접근성 서비스로 화면 읽기 | 권한 과잉 — ADR-018이 AccessibilityService를 기각한 논리 그대로 |
| 전 메시지 서버 저장 후 분석 | 사생활 과잉 + 비용. 필터가 기기에 있어야 하는 이유 |

### 감수하는 비용
- **무음 방은 캡처 불가**(알림이 안 뜬다). 알림 미리보기 설정에도 종속 — 커버리지가 구조적으로 부분적
- model_low 추출 품질 한계 — 제안이므로 치명적이지 않음
- 감각 계층 중 불확실성이 가장 크다 — Phase 8 내 착수 순서 마지막

### 재검토 트리거
실사용에서 캡처 누락률이 높으면(핵심 방이 무음인 경우) 이 경로의 기대치를 낮추고 share-target 수동(ADR-030)을 보완 경로로 승격.

---

## 3. APP-PLAN 수정 지시

**3-1. §1 목표** — 2순위를 다음으로 교체:

> **2순위 — 감각 계층(Phase 8) · 입력 이관(Phase 7). 위젯은 10월.** 캘린더 읽기만 8월 W5, Phase 8은 9월 1주 착수, 위젯·FCM·인증은 10월. (2026-08-10 우선순위 교체: "알아서 돌아가는 구조"가 목적인 만큼 감각기관 > 표시 표면)

**3-2. §3 "8월 밖으로 보낸 것"** — 목록에 추가: `캘린더 쓰기 방향 · share-target · 알림 액션 · RemoteInput 마감 (Phase 7) · Health Connect · 공지 수집기 · 메신저 캡처 (Phase 8)`

**3-3. §3 실행 순서 도식** — 9~11월 병렬 항목을 다음 우선순위로 교체:

> ① **Phase 8 — 감각 계층** (9월 1주 착수, HC → 공지 → 메신저, 하나씩)
> ② Phase 7 잔여 — 캘린더 write-back · 기록 경로 3종
> ③ Guard v1 잔여 — 오버레이(9월 초 유지) · 규칙 반복 수정(본체, 상시)
> ④ **10월로**: 위젯(Phase 5) · FCM(Phase 4 잔여) · 인증(Phase 0) — 위젯이 FCM·인증의 수요처이므로 셋이 같이 이동
> ⑤ Compose 스파이크 — 여유 시

**주의: ADR-010·기존 §3의 "위젯 9월"과 상충 — 이 우선순위가 우선한다** (감각 계층 > 표시 표면, 2026-08-10 사용자 확정).

**3-4. §5 5주(8/26~31)** — 다음으로 교체:

> ### 5주 (8/26~31) — 캘린더 읽기 (Phase 7 전반) + 버퍼
>
> **선행 조건: 4주 종료 시점에 Guard 루프가 닫혀 있을 것.** 아니면 이 주 전체가 Guard 버퍼로 환원되고 캘린더는 9월로 — 감축 0순위.
>
> - 마이그레이션 **0012_cal_sync** (§4) + `POST /api/cal/sync` 멱등 upsert
> - 기기: READ_CALENDAR 권한 + 대상 캘린더 선택 + Instances 창 조회 → diff push
> - devcal-소스 event 읽기 전용 표시
> - 보호 키워드 제안 카드 (ADR-029, outcome 카드 패턴 재사용)
> - 기존 수동 입력분 병합: (날짜, 제목) 일치 시 연결
>
> **기준: 폰 캘린더에 시험을 넣으면, 앱에 나타나고, 보호 제안 카드가 뜨고, 1탭으로 서약되어 알람이 예약된다.**

**3-5. §5 감축 순서** — 맨 앞에 삽입: `0. 캘린더 읽기 전체 (9월로. Guard가 4주 안에 안 닫히면 자동 발동)`

**3-6. Phase 7 신설** — 정체성: **입력 이관** — 기존 입력 노동의 이동·소멸. 내용 = ADR-028·029·030 요약. 8월 = 캘린더 읽기, 9월 = 쓰기 + 기록 경로 3종.

**3-7. Phase 8 신설** — 정체성: **감각 계층** — 새 입력원 증설. 내용 = ADR-031·032·033 요약. **8월 작업 0 · 9월 1주 즉시 착수.** Guard 경로와 접점이 없는 독립 모듈이라 실사용 시작과 병행 가능. 착수 순서는 불확실성 오름차순: HC(로컬 API, 싸다 — Fit4 도착 시점과 정합) → 공지 수집기(파서 노동) → 메신저 캡처(커버리지 불확실). 앞 모듈이 자리 잡은 뒤 다음 모듈. 공통 형태: 수집 로컬·결정론 → 필터 → (필요시 model_low) → 제안 카드 1탭. 마이그레이션 번호는 착수 시 배정.

**3-8. Phase 0** — `마이그레이션 0012` → **`0013`** 으로 정정 (0012는 cal_sync가 소비).

**3-9. §6 (9~11월) 관찰 표** — 행 추가:

> | 제안 카드 수락률 (보호·공지·메신저) | 제안 임계·키워드 목록 · ADR-029 자동화 수위 |
> | 수면 추정치 vs HC 실측(도입 시) 정합 | §6.6 수면 항 가중치 · 워치 도입 판단 |

**3-10. 부록 A** — 체크리스트에 추가: `[ ] (W5 이후) 캘린더 권한·대상 캘린더 선택 확인 · 폰 캘린더 일정 → 앱 반영 확인`

---

## 4. 마이그레이션 0012_cal_sync 초안

```sql
-- 0012_cal_sync.sql
ALTER TABLE events ADD COLUMN ext_src TEXT;      -- 'devcal' | NULL(앱 생성)
ALTER TABLE events ADD COLUMN ext_uid TEXT;      -- CalendarContract event id, 반복은 '<id>:<날짜>'
ALTER TABLE events ADD COLUMN ext_updated TEXT;  -- 마지막 반영한 캘린더 측 갱신 시각 (LWW)
CREATE UNIQUE INDEX idx_events_ext
  ON events(ext_src, ext_uid) WHERE ext_src IS NOT NULL;
```

`test/smoke.ts` 스키마 목록에 파일명 등록 (CLAUDE.md 규칙). Phase 8은 8월 마이그레이션 없음 — HC·공지·메신저 데이터는 착수 시점에 스키마 배정(`risk_snapshot`은 JSON이라 수면 출처 표기는 무마이그레이션).

---

## 5. 검증

8월(W5) 분량만:
- smoke +8 내외 — upsert 멱등 · 마감된 날 skip · LWW(구갱신 무시) · 반복 인스턴스 개별 upsert · 삭제→mirror 제거 · guard 참조 시 protect 해제+보존 · 키워드 매칭 제안 생성 · 비매칭 무제안
- front +3 내외 — devcal event 읽기 전용 · 제안 카드 [보호 설정/안 함] · 재노출 열림당 1회

Phase 8 검증은 각 모듈 착수 시 정의.

## 6. 함정

- **동기화는 pull 직전에.** 순서가 뒤집히면 캘린더의 시험이 그날 알람 예약을 놓친다
- 새벽(00~05시) 일정의 날짜는 캘린더 벽시계 그대로 — 귀속일 재계산 금지. Guard 벽시계 함정과 같은 구간
- 여러 캘린더 계정(구글 다중·삼성) — 대상 선택 없이 전체 동기화하면 잡동사니가 쏟아진다. 선택이 선행
- 멀티데이 종일 일정은 시작일 1건으로 축약 (v1)
- 재설치 시 기기 매핑 소실 — 서버 유니크 인덱스가 중복 삽입은 막지만, 재연결 로직 전까지 수동 확인
- **W5 진입 게이트를 지켜라.** Guard 루프가 안 닫혔으면 이 문서의 8월 분량은 전부 9월이다 (감축 0순위)
- (Phase 8 선행 경고) 무음 방·알림 미리보기 꺼짐은 메신저 캡처의 구조적 사각 — 기대치를 미리 낮춰 둘 것
- (Phase 8) Fit 시리즈는 Wear OS가 아니라 Samsung Health 경유로만 데이터가 나온다 — **Fit4 구매 전, Samsung Health→Health Connect 동기화에 수면 세션이 실제로 도착하는지 확인**(ADR-031의 전제)

## 7. 보류 기록 · 남는 리스크

**보류 (명시적 결정, 2026-08-10):**
- **Guard 구조 개선** — 사용자가 세부 구조를 아직 소화하지 못했고 실측 비용이 크다. 개선 논의는 이해 회복 이후로 보류. 8월 계획(W3~4)은 현행대로 진행
- **설계문서 v1.0 재구성** — 사용자가 직접 작성한다(이해 회복 트랙). 축 = **감각 계층**(입력원을 "사용자 손"에서 "센서들"로). 입력 = §6.4 갱신(ADR-018) + ADR-027~033. Guard §6은 이 작업에서 사용자가 재유도하며 이해를 복구한다
- **Kotlin 전환** — 12월 유지(ADR-017). 단 Phase 7·8이 네이티브 모듈 비중을 키우므로 재검토 트리거("네이티브 > 웹") 조기 발화 가능성 있음 — 발화해도 경로는 동일(아래는 이식, 위는 재설계)

**남는 리스크:**
- 양방향의 실비용은 9월 write-back에서 나온다. 8월 읽기 단계는 안전하지만, 쓰기가 붙는 순간 LWW로 부족한 사례가 나올 수 있다 — ADR-028 재검토 트리거
- 8월 편입 자체의 리스크. W3(개입)이 가장 불확실한 주로 남아 있고, 오늘(8/10) 기준 W1 게이트·W2 결과가 이 문서에 반영되지 않았다. W5 진입 게이트(§3-4)가 유일한 방어선
- 감각 계층은 표면이 3개 늘어나는 일이다. Phase 8 착수는 반드시 하나씩(HC → 공지 → 메신저), 앞 모듈이 실사용에서 자리 잡은 뒤에
