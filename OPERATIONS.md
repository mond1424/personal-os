# OPERATIONS — 사용자 운영 가이드

에이전트 셋(Cowork · Claude Code · Codex CLI)을 굴리면서 **사용자가 하는 일**만 모은 문서.
경계와 권한은 `AGENT-CHAIN.md`, 코드 규약은 `CLAUDE.md`, 현재 상태는 `STATE.md`.

---

## 0. 원칙 — 내용을 나르지 않는다

**전달 매체는 리포 파일이다.** 지시는 티켓 파일에 있고, 보고도 같은 파일에 쓰인다.
세 에이전트가 전부 같은 파일을 읽으므로, 옮길 것은 **"시작해"라는 신호뿐**이다.

긴 텍스트를 복사해 나르기 시작하면 그 순간부터 사본이 갈라진다.

---

## 1. 한 사이클

```
① 나 → Cowork        "○○ 하고 싶어" · "T-NN 티켓 써줘"
② 나 → Claude Code   "T-NN 티켓이 들어왔다. 읽고 판단해라"
③ 나 → Codex CLI     "AGENTS.md 읽고 T-NN 수행해라"        (Claude Code가 넘긴 경우만)
④ 나 → Claude Code   "T-NN 보고가 들어왔다. 검토해라"
⑤ 나 → Cowork        "T-NN 끝났어"
⑥ 나                 승인 → 마이그레이션 · deploy · 빌드 · 폰 확인
```

②에서 Claude Code가 "이건 내가 한다"고 하면 ③을 건너뛴다.
위임 금지 영역(트리거·마이그레이션·귀속일·Guard 발동 경로)은 항상 그렇다.

---

## 2. 붙여넣는 문장

### Claude Code — 티켓 배정

```
docs/tickets/T-NN-*.md 티켓이 들어왔다. 읽고 판단해라.
직접 할 것인지 Codex에 넘길 것인지 정하고 이유를 말해라.
넘긴다면 APP-BUILD.md 락을 걸어라.
```

### Codex CLI — 구현 지시

```
AGENTS.md를 먼저 읽어라. 그다음 docs/tickets/T-NN-*.md를 읽고 그대로 수행해라.
끝나면 티켓 파일의 '보고' 절에 결과를 채워 넣어라.
```

**이게 Codex에 주는 지시의 전부다.** 길어진다면 티켓이 부실한 것이지,
설명으로 메울 일이 아니다 — 티켓을 다시 받는다.

### Claude Code — 검토·마무리

```
T-NN 보고가 티켓 파일에 들어왔다. 검토해라.
통과면 STATE.md·APP-BUILD.md 갱신하고 commit & push.
```

### Cowork — 최종 검토

```
T-NN 끝났어
```

---

## 3. 나만 하는 것

에이전트가 대신하지 않는다.

| 언제 | 무엇 |
|---|---|
| 마이그레이션이 추가됐을 때 | `npx wrangler d1 migrations apply personal-os --local`<br>→ `npx wrangler d1 migrations apply personal-os --remote` |
| 서버 코드가 바뀌었을 때 | `npm run deploy` |
| `android/` 아래가 바뀌었을 때 | APK 재빌드·설치 (`GUARD-DEV-LOOP.md`) |
| 항상 | **폰 실측** — 렌더 결과 · 실제 폭 · 터치 동작 · 알람이 새벽에 울렸는가 |
| 항상 | 최종 승인 |

**순서를 지킨다: 마이그레이션이 배포보다 먼저, `--local`이 `--remote`보다 먼저.**

### 폰 실측이 대체 불가인 이유

코드에서 안 보이는 결함이 실기기 덤프 한 줄에서 나온다.
`fired_at: "2026-07-29T20:09:15Z"` — 이 `Z` 하나가 귀속일이 전날로 밀리는 결함이었고,
diff로는 보이지 않았다. **에이전트가 못 보는 것을 보는 층이 사용자다.**

---

## 3.5 실측 — 2026-08-03 전부 완료

> **T-01 · T-02 · T-04 · T-05 · T-09 · T-11 — 확인 절차 전 항목 통과.**
> `G.state()` 넷(`notifications`·`fullScreenIntent`·`overlay`·`batteryUnrestricted`)이
> `true`인 상태에서 확인했다. 아래 목록은 **다음 빌드의 참고용**으로 남긴다.
>
> **남은 것 하나 — 밤 03:00 알람(S1.3 ③).** 자기 전에 걸고 충전기를 꽂지 않는다.
>
> ⚠️ 이번 설치는 서명 불일치로 **삭제 후 재설치**였다(T-13). 다음 빌드 전에 고친다.

### (지난 목록 — 다음 빌드 때 재사용)

원격 환경에서 쌓인 것들. **코드 검증은 전부 통과했고, 눈으로 본 적이 없는 것만 남았다.**
아래 순서대로 걸으면 세 티켓이 한 번에 닫힌다. APK 재빌드 불필요 — 전부 프런트다.

### 2026-07-30 · T-01 · T-02 · T-05 — **①②③ 화면 확인 완료 (08-01)**

> 남은 것은 **콘솔 확인 하나**(③의 `G.sync()` → `G.listAlarms()`)와 **T-04·T-09 실측**이다.
> 화면은 다 봤고, 알람까지 이어지는지가 아직 미확인이다 — 그게 T-05의 진짜 완료 조건이다.


```
① Me › Education
   □ 비어 있을 때 안내 문구가 뜬다                        T-01
   □ 추가 → 라벨이 한국어다 (과목명·학기·학점·선수과목)   T-02
   □ 목록에 뜬다 · 수정하면 갱신된다 · 삭제하면 사라진다   T-01
   □ status 3색이 다크모드에서 읽힌다  ← 검사로 대체 불가  T-01

② 캘린더 › 날짜 팝업
   □ 일정 제목이 굵고(600) 행을 채운다 · × 가 오른쪽에 붙어 있다   T-05
     (제목이 button으로 바뀌며 스타일이 빠졌던 자리 — 되살렸으나 미확인)
   □ 마감된 날의 일정 제목을 고치면 409 토스트가 뜬다               T-05
     (PATCH /api/events/:id 가 UI에서 처음 닿는다. 409가 정상 동작이다)

③ 보호 규칙 — 경로 A가 사는지
   □ 09:00 일정에 보호를 켠다 → 데드라인이 01:30                   T-05
   □ 수면을 300분으로 → 02:30으로 바뀐다                           T-05
   □ 목록·캘린더에 보호 표시가 보인다                              T-05
   □ 앱 콘솔: await G.sync() → await G.listAlarms()
     그 일정의 발동이 예약됐다   ← T-05의 진짜 완료 조건
   □ 보호를 푼다 → 표시가 사라지고 재동기화 후 예약도 사라진다
```

**③의 마지막이 가장 중요하다.** 화면이 아니라 알람까지 이어져야 시각 발동 경로가 산다.

### 2026-08-03 · T-09 · T-11 — deploy·APK 후

```
④ Me › Goals   (T-09 · deploy만, APK 불필요)
   □ 빈 상태 문구 → 목표 추가 → 목록에 뜬다
   □ kind='constraint' + dday_label 있는 기간에 연결 → 디데이가 뜬다
   □ kind='period' 기간에 연결 → 디데이가 안 뜬다
   □ 하단 바에서 Me가 4탭과 구분되어 보인다     ← 검사로 대체 불가
   □ 탭 스와이프가 그대로 (한 번에 두 칸 안 넘어간다 — 함정 4)

⑤ Guard 이탈 대가   (T-11 · APK 재빌드 필요)
   await G.testNotify({ level: 3 });
   □ [알겠습니다] → 화면이 닫힌다
   □ 폰을 계속 쓴다 → 5분 뒤 같은 Level이 다시 뜬다   ← 이 티켓의 전부
   □ 두 번째도 [알겠습니다] → 5분 뒤 또 온다 (2회째)
   □ 세 번째는 오지 않는다 (밤당 상한)
   □ [알겠습니다] 후 화면을 끄고 5분 → 오지 않는다
   □ 무음 모드에서 Level 4 → 소리·진동 없이 화면만 뜬다
   □ (await Api.guardEvents())[0] 에 cause: "recheck:accepted"
   막히면 await G.recheckStatus()
```

### 기록 · 아직 안 한 것

```
□ 밤 03:00 알람 실측 (S1.3 ③) — 자기 전에 걸고 충전기 안 꽂는다
  await G.cancelAlarms(); await G.scheduleAt({ hhmm: "03:00", level: 3 });
  아침에 await G.listAlarms() → count: 0 이면 소비된 것
□ 감지 발동 임계값 재검토 — 실사용 2주 뒤. 하룻밤 3회를 넘으면 과하다(ADR-025)
```

---

## 4. 승인 전에 확인할 것

Claude Code나 Cowork가 "끝났다"고 할 때, 다음이 없으면 승인하지 않는다.

1. **숫자** — `typecheck 통과 · smoke A → B · front C → D · 실패 0`
   "통과했습니다"만 오면 되돌려보낸다
2. **설계와 어긋난 점** — 없으면 "없음"이라고 적혀 있어야 한다
3. **막힌 것** — 미해결이 있으면 `STATE.md`에 남았는지

숫자가 안 맞는데 "플레이크 같다"고 넘어가려 하면 **원인을 찾을 때까지 승인하지 않는다.**

---

## 5. 동시에 돌리지 않는다

**같은 리포에 실행 에이전트를 둘 붙이지 않는다.** Claude Code와 Codex가 동시에
파일을 만지면 머지 비용이 병렬로 버는 것보다 크다.

`APP-BUILD.md` 맨 위의 락 줄이 비어 있어야 새 작업을 시작한다.

```
WIP: (없음)
```

---

## 6. 막혔을 때

**한 칸 위로만 올린다.**

| 증상 | 어디로 |
|---|---|
| Codex가 범위 밖을 고쳐야 한다고 한다 | Claude Code에 "범위 판단해라" |
| Claude Code가 설계 판단을 요구한다 | Cowork에 "이 결정 필요하대" |
| Cowork가 실측을 요구한다 | 폰에서 직접 확인 |

층을 건너뛰면 위층이 모르는 변경이 생기고, 그것이 `STATE.md`에 없으면
다음 세션이 통째로 어긋난다.

---

## 7. 세션을 마칠 때

Claude Code에 한 마디면 된다.

```
정리하자
```

`STATE.md` 갱신 → 구조가 바뀌었으면 `docs/api-surface.md` 재생성 →
마이그레이션이 추가됐으면 `docs/schema-current.sql` 재덤프 → commit & push.

**push하지 않으면 다음 세션이 낡은 코드를 본다.**
