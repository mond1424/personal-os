# AGENT-CHAIN — 누가 무엇을 결정하고, 무엇을 넘기는가

이 프로젝트는 **한 사람이 폰에 설치해 실사용 중인 앱**이다. 회귀 하나가 새벽에 드러난다.
여러 에이전트가 붙을 때 생기는 사고는 실력 부족이 아니라 **경계가 없어서** 난다 —
같은 파일을 둘이 만지고, 결정 기록을 모르고 기각된 대안을 다시 짜고, STATE가 갈라진다.

이 문서가 그 경계다. 코드 규약은 `CLAUDE.md`, 현재 상태는 `STATE.md`.

---

## 1. 위계

```
사용자
  │  의도 · 승인 · 배포 · secret · 폰 실측
  ▼
Cowork Claude          설계 · 판단 · 티켓 발행 · 최종 검토      [코드를 쓰지 않는다]
  │  티켓
  ▼
Claude Code            회귀 민감 영역 직접 구현 · verify · 커밋 · 1차 검토
  │  하위 티켓
  ▼
Antigravity CLI        범위가 명확한 구현만
```

**한 칸씩만 오간다.** Antigravity는 사용자에게 직접 결론을 내지 않고 Claude Code에 보고한다.
Claude Code는 ADR급 판단을 스스로 확정하지 않고 위로 올린다.

건너뛰기가 허용되는 유일한 경우: **사용자는 언제든 어느 층에나 직접 지시할 수 있다.**
그때는 지시받은 층이 위층에 사실을 알린다 — 모르는 변경이 STATE에 없는 것이 가장 나쁘다.

---

## 2. 층별 권한

### 사용자 — 최종 권한

혼자만 할 수 있는 것(에이전트가 대신하지 않는다):

- `npx wrangler d1 migrations apply --remote` · `npm run deploy` · `wrangler secret put`
- `git push` 최종 승인
- **폰 실측** — 렌더 결과·실제 폭·터치 동작·알람이 새벽에 울렸는가
- 설계 변경 승인

### Cowork Claude — 설계와 판단

- 설계 문서(`personal-agent-design`)·`APP-PLAN.md`·`APP-ADR.md`·티켓의 **유일한 편집자**
- 최종 검토 — 숫자가 아니라 **설계 정합**을 본다
- **코드를 쓰지 않는다.** `src/` · `public/` · `android/` · `test/` · `migrations/` ·
  `STATE.md` · `APP-BUILD.md` · git — 전부 건드리지 않는다.
  파일을 고치는 것 자체는 기술적으로 가능하다. **가능하지만 하지 않는다** —
  경계가 능력에 걸려 있으면 급할 때마다 "이건 내가 빨리 고치는 게 낫지"로 무너진다.
- **검증 숫자를 만들 수 없다**(`npm run verify`가 이 층에서 돌지 않는다).
  그래서 숫자는 항상 아래층에서 받는다. 자기가 고친 것을 아래층이 검증하면 **위계가 거꾸로 돈다**

### Claude Code — 구현 감독

- **`STATE.md`·`APP-BUILD.md`·git의 유일한 편집자**
- 회귀 민감 영역을 직접 구현한다(§4 위임 금지 목록)
- Antigravity 티켓을 분해해 내려보내고, 돌아온 것을 1차 검토한다
- `npm run verify`를 돌리고 숫자를 만든다. **숫자를 만드는 층은 여기 하나뿐이다**
- 세션 종료 규칙(STATE 갱신 → api-surface 재생성 → commit·push)의 주인

### Antigravity CLI — 구현

- **티켓에 적힌 파일만** 고친다
- 범위 밖이 필요하면 **고치지 말고 멈춰서 보고한다** — 거기가 판단이 필요한 지점이라는 신호다
- 설계 문서·ADR·STATE·APP-BUILD를 **쓰지 않는다**(읽기만)
- 커밋하지 않는다

---

## 3. 파일 소유권 — 동시 편집 사고를 막는 유일한 장치

| 대상 | 주인 | 나머지는 |
|---|---|---|
| `personal-agent-design*.md` · `APP-PLAN.md` · `APP-ADR.md` | Cowork | 제안만 |
| `STATE.md` · `APP-BUILD.md` · `docs/api-surface.md` · `docs/schema-current.sql` | Claude Code | 읽기만 |
| `docs/tickets/*.md` | Cowork 발행 · Claude Code 분해 | Antigravity는 읽기만 |
| `CLAUDE.md` · `AGENTS.md` · `OPERATIONS.md` · 이 문서 | Cowork | 제안만 |
| `src/` · `public/` · `android/` · `test/` · `migrations/` | **티켓 락 보유자** | 손대지 않는다 |
| `README0722.md` | Claude Code | 제안만 |
| git (commit · push) | Claude Code | 하지 않는다 |

⚠️ **`CLAUDE.md` §함정과 `README0722.md` §함정은 한 벌인데 주인이 다르다.**
요약이 원본을 가리킨다고 적혀 있으므로 **한쪽만 고치면 그 순간 문서가 거짓말을 한다** —
T-47이 실제로 그 상태를 만들었다(요약은 맞고 원본이 틀린 하루).
**한쪽을 고쳐야 하면 다른 쪽 주인에게 올린다.** 자기 것만 고치고 끝내지 않는다.

**"손대지 않는다"는 능력이 아니라 규율이다.** 위층은 아래층의 파일을 고칠 수 있다 —
고치지 않기로 한 것이다. 한 번 예외를 두면 락이 무의미해지고,
자기가 고친 것을 아래층이 검증하는 구조가 된다.

### 락

`APP-BUILD.md` 맨 위 한 줄. 비어 있으면 아무도 안 들고 있다는 뜻이다.

```
WIP: T-07 (codex, 07-30 14:20) — public/app.js, public/index.html
```

**락은 커밋되지 않은 상태를 표시한다.** 시작할 때 걸고 **커밋 직전에 푼다** —
순 diff가 0이라 이력에 남지 않고, 그 사이 작업 트리에만 존재한다.

**누가 하든 건다.** 직접 하니까 생략하는 것이 아니다 —
락은 아는 상대를 막는 장치가 아니라 **"이 파일들이 지금 움직이고 있다"는 공개 표시**다.
생략해도 되는 유일한 경우는 없다: 사용자가 다른 창에 지시를 붙여 넣는 순간이
정확히 그 표시가 필요한 순간이고, 세션이 중간에 죽으면 락 줄이 무엇이 떠 있었는지 알려 준다.

**같은 리포에 실행 에이전트를 둘 동시에 붙이지 않는다.** 병렬로 버는 것보다 머지 비용이 크다.

### 락이 못 막는 것 — 순차 누적

**미커밋 코드가 남은 채 다음 티켓을 착수하지 않는다.**

락은 *동시에 둘이 만지는 것*을 막는다. 그러나 **한 층이 순서대로 넷을 쌓는 것**은 못 막는다.
두 번 물렸다:

- **T-08** — T-08·T-09·T-11·T-12가 한 트리에 섞여, 불합격인 T-08을 stash로 도려내야 했다
- **T-16** — T-15가 미검토·미커밋인데 T-16을 착수시켜 기준선이 의미를 잃었다

섞이면 커밋을 hunk로 갈라야 하고, **그때 불합격분이 딸려 들어갈 위험이 생긴다.**
앞 티켓의 **코드**가 커밋된 뒤에 다음을 낸다.

**두 층에 동시에 내지 않는다.** 파일이 안 겹쳐도 안 된다 —
`npm run verify`는 **트리 전체**를 돌리므로 숫자가 누구 것인지 알 수 없어진다.
실제로 T-26·T-27을 병렬로 내자 한쪽은 남의 변경 때문에 verify를 못 돌렸고,
다른 쪽의 `front +1`은 자기 것이 아니었다. **발행자가 규칙을 만들어 놓고 자기가 어겼다.**

**문서는 코드가 아니다.** 티켓·계획·규약(`docs/tickets/*` · `CLAUDE.md` · 이 문서 · `OPERATIONS.md`)은
작업 중에 갱신되는 것이 정상이다 — §보고·§검토를 채우는 것이 절차의 일부다.
코드 파일과 겹치지 않으므로 hunk를 가를 일도 없다.

이 구분이 없으면 규칙이 **자기 절차를 막는다** — 실제로 T-17 착수가 문서 7건 때문에 차단됐다.
막은 판단은 옳았고 **규칙이 부정확했다.**

---

## 4. 위임 금지 — Claude Code 이상만 만진다

| 영역 | 왜 |
|---|---|
| DB 트리거 · `migrations/*.sql` | 불변성의 최종 강제선. 뚫으면 설계가 무너진다 |
| 귀속일 계산(`lib/time.ts`) | 2026-07-30 UTC 결함이 예다. `"...Z"` 한 글자였고 diff로는 안 보였다 |
| Guard 발동 경로(`services/guard.ts` · `guard/*.kt`) | 실패가 새벽에만 드러난다 |
| `wrangler.toml`의 `database_id` | 건드리지 않는다 |
| 파생값 저장 여부가 걸린 변경 | 아키텍처 원칙 1 |

**판단 근거**: 고치는 데 드는 시간보다 **왜 그렇게 돼 있는지 설명하는 데 드는 시간이 더 크면** 넘기지 않는다.

반대로 **설계가 이미 확정됐고 배선만 남은 것**은 전부 Antigravity로 간다 — UI 폼, 테스트 추가, 반복 리팩터.

---

## 5. 아래로 흐르는 것 — 티켓

템플릿은 `docs/tickets/_TEMPLATE.md`. **티켓은 템플릿에서 시작한다** — 손으로 쓰면 절이 빠진다.
티켓 없이 구현 지시를 내리지 않는다.

**티켓에 API·기본값·형식·폴백을 쓸 때는 그 파일을 읽고 쓴다.** 기억으로 쓴 값은
아래층에서 함정이 되어 나타난다 — 설정 화이트리스트, 시각 없는 일정의 폴백,
서버가 쓰는 정규식. 전부 파일을 열었으면 보였을 것들이다.
과제만 주면 이미 기각된 대안을 다시 구현해 온다 — **이 프로젝트는 가치가 코드보다 결정 기록에 몰려 있다.**

### 내기 전에 한 번 걸어본다

**§금지를 전부 지키면서 §완료 조건을 전부 통과하는 경로가 실제로 있는가.**
절을 하나씩 채우는 것으로는 이것이 확인되지 않는다 — 각 절은 따로 옳고 **합쳐서 모순일 수 있다.**

두 번 물렸고 **둘 다 발행자의 실수였다:**

| | 티켓이 쓴 문장 | 맞대봤으면 보였을 것 |
|---|---|---|
| T-08 | "하향에 확인만 붙이고 막지 않는다" | ADR-019 부수 규칙 1·2와 정면 충돌 |
| T-08′ | "서버를 건드리지 않는다" | 화면이 PUT 전에 하향인지 알 방법이 없다 |

T-08′에서 발행자는 `modes()`가 `{modes, active}`뿐인 것을 **읽었다.**
읽고도 "그럼 화면은 무엇으로 판정하나"를 묻지 않았다. **읽기가 아니라 검증이 빠진 것이다.**

특히 **"~를 건드리지 않는다"는 확인 없이 쓰지 않는다.** 범위를 좁히는 것은 좋은 습관이지만,
*안 건드려도 되는지*를 확인하지 않고 쓰면 아래층이 불가능한 조건을 받는다.

**"없다"도 같은 부류다** — *"마이그레이션 없다" · "무변경" · "값만 늘어난다"*.
T-31 초안이 *"`ai_verdict`는 문자열이고 값만 늘어난다"*고 썼는데 **닫힌 `CHECK`였다.**
그대로 갔으면 위반이 400이 되고, 기기의 `flush()`가 400을 '재시도 무의미'로 보고 버려
**이유가 아니라 발동 행 자체가 사라진다** — 그것도 네트워크가 나쁜 밤에만,
즉 **그 티켓이 관측하려던 바로 그 밤에만.** 관측을 늘리려던 티켓이 관측을 지울 뻔했다.

**칼럼·제약을 말할 때는 `docs/schema-current.sql`을 읽는다.** 그 스냅샷이 존재하는 이유가
정확히 이것이고, T-31 초안은 컬럼을 말하면서 그 파일을 안 봤다.

**"아무것도 안 한다"가 "원래대로 둔다"와 같지 않을 수 있다.** T-34 초안이
*"아무것도 안 닫았으면 기본 동작에 맡긴다"*라고 썼는데, Capacitor는 `backButton` 리스너를
다는 순간 **기본 동작을 끈다** — 글자 그대로 구현하면 **앱에 갇힌다.**
플랫폼이 기본을 유지한다는 전제도 **확인해야 하는 전제다.**

**"0이다"를 세는 검사에는 "몇이다"를 세는 짝을 둔다.**
없음을 확인하는 검사는 **검사 자체가 죽어도 0이 나온다.**
T-23에서 실제로 났다 — 주석을 걷어내는 정규식이 18줄을 먹어 진짜 호출을 지웠는데
"`services/` 아래 0건"은 그대로 초록이었고, **"진입 계층에 2건"이라는 양성 대조만 빨간불**이었다.

**판단 시점의 기록은 그 시점 이후를 담지 않는다.** 기준과 상한은 다른 것이고 **둘 다 필요하다** —
*"언제를 기준으로 보나"*를 고쳐도 *"어디까지 보나"*가 열려 있으면 샌다.
T-32 티켓이 조회 **기준**만 지적했는데, 담당이 상한(`ts <= firedAt`)도 필요함을 찾았다:
큐가 늦게 오면 발동 **이후**에 생긴 Log가 이미 DB에 있고, 담으면
**스냅샷이 판단 시점에 없던 것을 안다.** 그 스냅샷으로 10월에 가중치를 유도하면
*"그 순간 알 수 있었던 것"*이 아니라 *"나중에 알게 된 것"*으로 배우게 된다.

**대조는 결과가 달라지는 값으로 짠다.** 짝을 두는 것만으로는 부족하다 —
**대조값이 실패값과 같은 결과를 내면 검사가 죽어도 초록이다.**
T-31이 캐시 대조를 `deny`로 짰더니 `IN` 조건을 통째로 지워도 293/0이었다:
`guard.ts:392`가 `=== "approve"`만 보므로 캐시가 `unavailable`을 잘못 집어도 **결과가 `deny`와 같다.**
`approve`로 바꾸자 감도가 생겼다. **담당이 자기 검사를 스스로 떨어뜨려 찾았다.**

**대칭이 있는 것은 양쪽을 각각 적는다.** 좌·우, 상향·하향, 앞·뒤.
한쪽만 검사하면 **반대쪽이 우연히 맞는 것과 구별되지 않는다** —
T-21의 검사는 오른쪽 회전을 틀린 변이는 잡았지만 왼쪽 변이는 통과시켰다.

**코드의 자리를 말하는 것은 전부 그 자리를 읽고 쓴다** — 티켓이든 ADR이든 **한 줄짜리 메모든.**
ADR-035 초안이 *"새 작업을 추가하면 내일로"*라고 썼는데, 실제로는 작업이 생기는 경로가 셋이고
**날짜 규칙이 서로 달랐다.** 대기 경로는 애초에 날짜가 없어 그 ADR이 할 일이 없었고,
"Level 4 중"의 정의도 살아 있는 화면 참조로 잡으면 **차단이 영원히 안 걸릴** 구조였다.
**설계 판단이 틀린 것이 아니라 그 판단이 놓일 자리를 안 본 것이다.**

이 규칙을 *ADR에도 걸린다*로 적었더니 **바로 다음 메모에서 같은 것을 틀렸다.**
T-28 준비 메모가 자리 넷을 적었는데 그중 셋이 **피커를 여는 곳**이었고, 고른 날짜가 실제로
붙는 곳은 `assignDate` 하나였다. 그대로 나갔으면 구현자가 **여는 곳 셋을 고치고 붙는 곳을 놓친다.**
규칙에 문서 종류를 적은 것이 잘못이다 — **틀리는 것은 문서의 격이 아니라 확인하지 않은 자리다.**

**여는 자리와 붙는 자리를 구별한다.** 하나가 여럿으로 갈라져 한 곳에 모이는 모양이면
**모이는 쪽에 건다.** 갈라진 쪽을 세면 하나를 빠뜨렸을 때 조용히 새고, 검사도 같이 속는다.

**금지는 행위를 적는다. 이름을 적으면 무해한 사용까지 걸린다.**
T-08′ 검토가 `max_level`을 화면에 찍는 한 줄을 불합격으로 판정했다 —
발행자가 "`max_level`이 `app.js`에 없어야 한다"라고 써서, *판정에 쓰지 말라*는 취지가
*읽지도 말라*로 넓어진 것이다. 아래층은 문자대로 지켰고 그것이 옳다.
**애매한 금지의 비용은 아래층이 아니라 발행자가 진다.**

### 티켓이 틀렸을 때

**티켓의 누락은 발행자의 실수다.** 처리는 층마다 다르다.

- **Antigravity는 멈춘다.** 판단 권한이 없는 자리다. 범위를 넓혀 메우면 그 판단을 아무도 검토하지 않는다
- **Claude Code는 메우고 보고한다.** 세 조건을 전부 만족할 때만:
  ① 설계 문서·ADR이 **이미 요구하는 것**일 것 (새 결정이 아니다)
  ② 범위 확장이 최소일 것
  ③ 보고에 **무엇을 왜 넓혔는지** 쓸 것

②③만으로는 부족하다. ①이 없으면 그것은 누락을 메우는 것이 아니라 **새 결정**이고,
새 결정은 위로 올라간다.

티켓에 반드시 들어가는 것:

1. **범위** — 고칠 파일 목록. 이 밖은 손대지 않는다
2. **금지** — 이유가 붙은 것들
3. **완료 조건** — 숫자로. `smoke 213 → 216 · front 167 · 실패 0`
4. **읽을 것** — 관련 ADR 번호 · 설계 절 · 함정 번호

### 실측 산출물은 문서가 아니라 파일로 남긴다

**문서에 적힌 실측은 다음 사람이 재구성한다. 재구성본은 형식이 맞아도 값이 다르다.**

T-41이 `.ics` fixture를 ADR의 실측 기록에서 되살렸다. 형식 셋(`METHOD` 위치 ·
`CALSCALE` 유무 · `DTSTAMP`)은 마침 안 쓰는 자리라 지났지만, **값 둘이 달랐다** —
`SUMMARY`가 넉 자인 것과 `DTSTART`의 시각. **값을 쓰는 검사였으면 거기서 갈렸다.**

**받은 파일은 리포에 넣는다**(`docs/samples/`). 그리고 **사본을 뜨지 않는다** —
두 벌이 되면 갈라지고 **갈라진 쪽이 원본 행세를 한다.** 기준선 숫자를 한 곳에만
두는 것과 같은 이유다. 조립이 필요하면 **원본에서 잘라 쓰고, 조립 결과가 원본과
같은지를 검사가 못 박는다.**

**"눈으로 확인한다"는 확인 절차가 아니다.** 같은 이유로 §확인 절차에 *"눈으로"*가
들어가면 그것은 회귀를 못 막는다 — 문구·마크업이라도 스캐너로 센다(T-42).

---

## 6. 위로 흐르는 것 — 보고 형식

```
티켓: T-07
바꾼 파일: public/app.js, public/index.html, test/front.mjs
기준선: typecheck 통과 · smoke 213 → 216 · front 167 → 170 · 실패 0
설계와 어긋난 점: 없음
막힌 것: 없음
```

- **"통과했습니다"는 보고가 아니다.** 숫자 앞뒤를 쓴다
- 검사가 옛 동작을 검사하고 있었으면 **검사를 고치고 그 사실을 쓴다**
- 숫자가 안 맞으면 원인을 찾기 전에 끝내지 않는다
- 어긋난 점이 있으면 **고치지 말고 쓴다** — 설계 판단은 위층의 몫이다

---

## 7. 멈추고 올려야 하는 순간

### Antigravity → Claude Code

- 티켓 범위 밖 파일을 고쳐야 한다
- 설계 문서·ADR과 어긋나 보인다
- 검사가 옛 동작을 검사하고 있다
- 숫자가 안 맞고 원인을 못 찾겠다
- 확신이 부족하다 — **추측으로 범위를 넓히지 않는다**

### Claude Code → Cowork

- ADR급 결정이 필요하다(기각한 대안이 있고, 그 기록이 남아야 한다)
- 설계 문서와 구현이 어긋난 것을 발견했다
- 티켓 자체가 틀렸다

### Cowork → 사용자

- 마이그레이션 원격 적용 · deploy · secret
- 설계 변경
- 폰에서 직접 봐야만 알 수 있는 것

---

## 8. 검토 — diff 읽기가 아니다

돌아온 결과를 세 가지와 대조한다.

1. **설계 위반** — 파생값을 저장했나 · 트리거를 우회했나 · 귀속일을 기기 날짜로 냈나 · SQL이 `db/index.ts` 밖에 있나
2. **함정 재발** — `README0722.md` 요약 11개. 특히 `scrollIntoView` · 전역 클래스명 · 마이그레이션 순서
3. **숫자의 의미** — 늘었어도 **잘못된 동작을 지키고 있을 수 있다**

셋 다 통과해야 사용자에게 승인을 요청한다.

**실측 확인은 검토를 대체하지 않는다.** 둘은 다른 질문에 답한다:

| 실측 | 증상이 사라졌는가 |
|---|---|
| **검토** | **전수였는가 · 검사가 하드코딩과 구별되는가 · 설계와 어긋나지 않는가** |

T-15에서 사용자가 "증상 사라짐"을 확인했고 그것을 검토 통과로 받아 다음 티켓을 냈다.
**전수 조사가 조건이었는데 그것이 확인되지 않은 채 다음이 쌓였다.**

**검토 결과는 티켓 파일에 쓴다.** 대화로만 말한 승인은 아래층에 닿지 않는다 —
사용자가 나르지 않으면 없는 것이고, 나르면 사본이 갈라진다(§0의 이유 그대로).
범위 확장 승인·재작업 사유·보류 판단은 전부 해당 티켓의 절로 남는다.

**세 번째가 가장 무겁다.** 구현자는 자기가 통과시킬 검사를 쓴다 — 악의가 아니라
같은 이해로 짜기 때문이다. 그래서 검사의 합격 기준은 **티켓이 아니라 검토에서 강제한다.**
미리 적어 두는 것으로는 해결되지 않는다.

검사 하나를 볼 때 묻는 것: **구현을 잘못했다면 이 검사가 빨간불이 되는가.**
아니라면 그 검사는 숫자만 올린다.

### ★ 그 물음의 짝 — 다른 것을 잘못했을 때도 빨간불이 되는가

**검사 둘이 같은 자리를 보면 서로의 몫을 못 센다.** 변이 하나가 둘을 함께 죽이고,
**그러면 어느 결함인지 못 읽는다.** T-52·T-56·T-58·T-60에서 **네 번 연속** 나왔고
**네 번 다 담당이 변이를 돌려 스스로 찾았다** — 검사를 쓰는 시점에는 안 보인다.

```
넓게 쓴 검사        "추론이 붙고 · 이유가 갈리고 · 기록에 남는다"
겨냥한 변이 하나가   그 셋을 한꺼번에 죽인다  →  셋 중 무엇이 깨졌는지 모른다
```

⚠️ **좁히는 방향은 정해져 있다** — **각 검사가 지는 것 하나**만 남긴다.
T-60에서 *"안 뜬다"* 를 **다른 검사의 관측값**과 비교하던 것을 **계약값**과 비교하게 바꾸자
비로소 둘이 갈렸다. **남의 결과를 기준으로 삼는 검사는 남과 함께 죽는다.**

⚠️ **변이가 여럿을 죽이는 것이 전부 결함은 아니다.** 기능을 통째로 없애는 변이는
그 기능 위에 선 검사를 다 죽이는 것이 **맞다**. 가르는 물음: *"이 검사들이 서로 다른 것을
센다는 증거가 다른 변이에 있는가."* 있으면 정상이고, 없으면 겹친 것이다.

### ⚠️ 그러나 좁히다 관측력을 버리는 자리가 있다 (T-63)

**겹침을 없애는 것이 목적이 되면, 겹친 조각이 무엇을 보고 있었는지 안 묻게 된다.**

```
검사 4   "켜진 줄을 눌러 setWatch(false) 가 불리고 · 줄이 꺼짐으로 바뀐다"
변이 V2  줄이 늘 "켜짐"  →  1 과 4 가 함께 죽는다      ← 겹쳤다
좁히려면  4 에서 "줄이 바뀐다" 를 뺀다                  ← ★ 여기서 멈춘다
```

⚠️ **그 조각이 T-63에서 실제 결함을 잡은 바로 그 조각이었다** — `setWatch` 호출만 세는 검사는
*"불렀는데 화면이 안 바뀌는"* 구현을 통과시킨다(T-55가 물린 모양). **호출은 관측이고
화면이 계약이다.**

**그래서 물음이 하나 더 붙는다:** *"이 조각을 빼면 무엇을 못 보게 되는가."*
못 보게 되는 것이 **다른 검사에도 없으면 겹침을 남긴다** — 겹친 채로 두고 §보고에 이유를 적는다.
**깨끗한 배터리보다 결함을 잡는 배터리가 낫다.**

### ★ 남의 값을 보는 것 자체는 결함이 아니다 — 자로 쓰는 것이 결함이다 (T-64)

**같은 문법이 정반대 두 일을 한다.**

```
자로 쓴다    off.display === t53Synced.display     주어는 "off 가 조용한가"
                                                  관계는 곁다리 → 남과 함께 죽는다
대상이다    p2.inferred === p1.inferred           주어는 "레벨로 안 가른다"
                                                  관계가 세는 것 → 계약값을 쓰면 오히려 좁아진다
```

**가르는 시험은 이름이다:** *"검사 이름에서 그 관계를 빼도 문장이 성립하는가."*

```
"네이티브가 없으면 아무 말도 안 한다"      ok 을 빼도 성립한다  →  자다.  계약값으로 바꾼다
"셋이 서로 다른 문구다"                   빼면 문장이 없다     →  주어다. 그대로 둔다
```

⚠️ **이름이 세는 것과 어긋나 있으면 이 시험이 안 먹는다.** T-53의 옛 이름은
*"'ok'와 화면에서 같고…"* 라고 **세지도 않는 관계를 주장**하고 있었다 —
그 이름으로 시험하면 *"주어다"* 라는 틀린 답이 나온다. **이름부터 맞춘 뒤에 시험한다.**

⚠️ **전수로 같은 결함이라 단정하지 않는다.** 좁히려고 일부러 남의 값만 본 자리가 실제로 있다
(주석이 그렇게 적혀 있다). **주석이 근거를 적고 있으면 변이를 돌리기 전엔 결함으로 세지 않는다.**

### ★ 혼자 죽을 수 없는 검사는 아무것도 안 센다

겹침을 재는 물음이 하나 더 있고, **이쪽이 더 조용하다.**

```
겹쳤다        변이 하나가 둘을 죽인다        →  어느 결함인지 못 읽는다
안 센다       이 검사만 죽이는 변이가 없다   →  ★ 있으나 마나다. 그런데 초록불은 켠다
```

**시험:** *"이 검사만 빨간불이 되게 하는 변이가 하나라도 있는가."* 없으면 그 검사는
**남이 이미 센 것을 다시 쓴 문장**이다 — 짝 검사들이 각각 계약값을 세고 있으면
*"둘이 화면에서 같다"* 는 그 둘에서 **연역된다.** 연역되는 것은 세지 않는다.

⚠️ **그런 검사는 지우지 말고 주석으로 낮춘다.** 문장이 담은 의도(*"이 둘은 같아야 한다"*)는
남길 값이 있지만, **초록불은 세는 것에만 준다** — 안 세는 초록이 `front`·`smoke` 숫자를
부풀리고, 이 리포는 **그 숫자로 진척을 보고한다.**

⚠️ **빼는 근거는 "못 찾았다"가 아니라 연역을 보이는 것이다.** 변이를 몇 개 돌려 본 것으로는
*"내가 안 만든 변이"* 와 *"만들 수 없는 변이"* 가 안 갈린다 — 앞은 있던 눈을 감기는 것이다.

```
짝이 초록  ⟹  그 값들이 못 박힌다        none = {none,none} · err = {error,none}
★ 이 그 값들만의 함수다                   display 끼리 같은가 · state 끼리 다른가
∴ 짝 ⟹ ★.   대우: ★ 이 죽으면 짝이 먼저 죽는다  →  혼자 죽는 세계가 없다
```

**이 꼴로 적히면 뺀다. 안 적히면 남긴다** — 짝이 그 값을 안 세고 있으면 전제가 성립하지 않고,
그때 그 검사는 실제로 혼자 죽는다(T-65에서 셋 중 하나가 그랬다).

### ★★ 일치 검사는 갈라짐만 잡는다 — 함께 틀리면 침묵한다 (T-71)

**"두 곳이 같은 것을 말하는가"를 재는 검사**(T-62 검사 4가 그것이다)는 **강력하다** —
실제로 T-71의 잘못된 전제를 잡았다(문구만 침묵하고 알람은 울리는 자리).

⚠️ **그런데 둘이 *같은 근원*에서 틀리면 그 검사는 초록이다.**

```
wakeLeadMin 의 갈래를 지운다
  → protectAxis 도 wakePoints 도 같은 함수를 부른다
  → 둘 다 틀린 값을 쓰고  ★ 여전히 일치한다  →  일치 검사는 안 죽는다
```

★ **그래서 일치 검사에는 *근원을 보는 짝*이 필요하다.** T-71의 검사 6(`wakeLeadMin`의
세 갈래가 살아 있는가)이 그 자리다 — **동작만 보는 검사는 이 결함을 영원히 못 잡는다.**

⚠️ **한 곳으로 모으는 것(T-62·T-69가 한 그 일)은 두 벌을 없애는 대신 이 눈먼 자리를 만든다.**
**모을 때마다 그 한 곳을 보는 스캐너를 함께 둔다.**

### ★ 티켓의 전제도 변이 대상이다 — 시킨 대로 하고 무엇이 죽는지 본다 (T-71)

**T-71의 §금지가 *"보호 일정은 이미 안다 쪽"* 이라고 근거 없이 단정했다.**
담당은 **먼저 묻지 않고 표대로 구현했고**, T-62 검사 4가 죽는 것을 본 뒤에 올렸다.

```
먼저 물었다면   "티켓이 틀린 것 같다"        ← 짐작
구현하고 올렸다  "T-62 #4 가 죽는다"          ★ 죽은 검사 이름 하나가 증거다
```

⚠️ **티켓이 §범위·§금지로 못 박은 것도 사실 주장이다** — 발행한 층이 코드를 다 안 보고 쓴다.
**시킨 대로 해서 무엇이 죽는지 보는 것이 그 주장을 재는 유일한 방법**이고,
그 결과는 **올리기 전에 손에 쥔 관측**이 된다(`AGENT-CHAIN` §7 — 한 칸 위로 올릴 때 무엇을 들고 가나).

### ★★ 집계표의 `0`은 "없었다"와 "안 봤다"를 구별하지 않는다 (T-73)

**함정 18이 판정표 위에 앉는 자리다.** T-73의 판정 자는 칸이 넷이었고 마지막 칸이 `0`이었다:

```
제스처 1 · 렌더 1 · 성능 1 · 구조 0
```

⚠️ **Cowork 는 그 `0`을 *"구조 문제는 없었다"* 로 읽었고, 담당이 잡았다** —
확인 절차에 넣은 항목(D1 가장자리 손넘김)의 **판정이 아예 안 온 것**이었다.
★★ 하필 그 칸은 §판정이 *"12월 결정에서 가장 크게 읽는다"* 고 적어 둔 칸이다.

**★ 그래서 집계표에는 값이 셋이다:**

```
0        ★ 봤고 없었다
미관측   ⚠️ 안 봤다 — 비용 0 이 아니다
해당없음    그 칸이 이 작업에 존재하지 않는다
```

⚠️ **셋을 한 칸에 `0`으로 적으면 표가 거짓말한다.** 그리고 이 거짓말은
**빨간불이 아니라 요약문으로 나온다** — 자를 읽는 층이 그것을 결론으로 옮긴다.

★ **이것은 §판정에 *"자에 없는 칸으로 들어온 비용은 0으로 세어진다"* 와 다른 실패다:**
저기는 **칸이 없어서**, 여기는 **칸은 있는데 채워지지 않아서** 0이 된다.
**앞은 티켓을 쓰는 층이, 뒤는 표를 읽는 층이 만든다.**

### ★★★ 비율은 자기 분모를 말하지 않는다 — `2/2`가 `2/5`다 (T-74)

**T-74 §①은 *"5건 중 몇 건에 `CATEGORIES`가 있나"* 를 물었다.**
담당이 내보내기 파일을 스캐너에 물려 **`CATEGORIES 2 / VEVENT 2` — 100%** 를 얻었다.
**참이다. 그런데 전수가 아니었다.**

```
물은 것   5건 중 몇 건인가
답한 것   ★ 이 파일이 담은 것 중 몇 건인가
```

⚠️ **그 파일은 내보내기 기간이 이틀이라 11-30짜리 두 건이 애초에 없었고,
그 둘이 하필 제목에 과목이 아예 없는 — `CATEGORIES`가 유일한 단서인 — 바로 그 둘이었다.**

★★★★ **그리고 이 거짓말이 `0건`보다 훨씬 오래 산다.**

```
0건          ⚠️ 의심이라도 산다
2/2 = 100%   ★★ 완벽해 보인다. 아무도 분모를 안 묻는다
```

(함정 17의 *"그럴듯한 수가 0건보다 오래 산다"* 의 극단이다 — **비율은 분자를 자랑하고 분모를 숨긴다.**)

★ **가른 것은 하나다: 표본의 *경계*를 수로 함께 내게 한 것.**
티켓이 *"`DTSTART` 최소·최대를 함께 적어라 — 그게 무엇을 못 봤는지 말해 준다"* 라고
못 박았고, 그 두 수가 없었으면 `2/2`가 그대로 결론이 됐다.

★★★ **그리고 그 규칙은 진단에도 걸린다 — 창은 끝이 둘인데, 보통 한쪽만 본다** (T-75).
**T-75 티켓은 *"7일 밖이라 안 보인다"* 로 진단했는데, 실측에서 가려진 넷 중 *절반이 과거*였다** —
`starts_at >= now` 라는 **아래 끝**이 그만큼을 가리고 있었고 **아무도 그쪽을 안 봤다.**
⚠️ **그중 하나는 *오늘* 마감인데 두 시간 지났다고 사라진 것이다.**
★ **범위로 무언가가 안 보인다고 진단할 때는 두 끝을 모두 적는다** — 한쪽만 적으면
**고친 뒤에도 나머지 절반이 조용히 남고, 그때는 "고쳤으니 이건 다른 결함"으로 읽힌다.**

⚠️ **그러므로 비율을 보고할 때는 분모가 *무엇의* 전체인지를 함께 적는다.**
**표본에서 나온 수는 표본의 경계와 짝으로만 뜻이 있다** — 기간·범위·필터 중
그 수를 만든 것을 같이 낸다. **짝이 없는 비율은 읽는 층에서 전수로 오독된다.**

### ★★★★ 반응이 만든 항은 그 반응의 분모가 아니다 — 지표가 자기를 부풀린다 (ADR-049 판정)

**앞의 규칙은 분모가 *좁아서* 거짓말한 경우다. 이번은 분모가 *자라서* 거짓말했다.**

```
accepted 58 · 96%          ★ 완벽해 보인다
그 발동 중 16건이 recheck   ⚠️ [알겠습니다] 가 arm() 으로 스스로 부른 발동이다
```

★★★ **수용이 발동을 낳고, 그 발동이 또 수용을 받아, 수용률의 분모를 키운다.**
**고르는 사람이 아무것도 안 바꿔도 수가 오른다** — 그 비율은 사용자가 아니라 **구조**를 재고 있다.

⚠️ **`2/2`와 달리 이쪽은 분모의 경계를 적어도 안 드러난다.** 기간도 필터도 맞는다.
★ **드러나는 것은 `cause`를 갈라 셀 때뿐이다** — *"이 항을 무엇이 만들었나"* 를 묻는 것이
분모를 묻는 것보다 한 겹 아래다.

★★ **그러므로 반응률·달성률처럼 *사람의 선택*을 재는 수는, 분모의 각 항이
그 선택과 독립인지를 먼저 가른다.** 선택이 만든 항은 **빼고 세거나, 따로 세거나, 둘 다 낸다.**
⚠️ **한 수로 합치면 그 수는 자기를 재게 되고, 그때도 화면에서는 그냥 높은 수다.**

### ★★★ 변이가 여럿을 죽였을 때 — 필연인지 표본 탓인지 **가르는 시험이 있다** (T-71 · T-77)

**변이 하나가 검사 여럿을 죽이면 두 가지 중 하나다. 그리고 대응이 정반대다.**

```
필연     변이가 만드는 상태가 정확히 그 검사가 금지하는 상태다
         → ★ 못 좁힌다. 좁히려면 검사가 눈이 멀어야 한다
표본 탓  ⚠️ 검사가 남의 명제를 **표본으로 업고 있다**
         → ★★ 좁힐 수 있고, 좁혀야 한다
```

★ **가르는 법은 하나다 — 표본을 바꿔 다시 돌린다. 안 죽으면 표본 탓이다.**

**T-71(필연):** `protect_prep_min` 무시 변이가 검사 2와 7을 함께 죽였다.
7이 세는 명제가 *"예약과 문구가 같은 기상을 말한다"* 인데 **그 변이가 만드는 상태가 바로 그 둘이
갈린 상태**다. **7이 안 죽으면 7이 눈먼 것이다** — 그래서 그대로 뒀다.

**T-77(표본 탓):** 게이트를 L3에 태우는 변이 `M1`이 검사 2·4·8을 함께 죽였다.
문구를 세는 4와 8이 **두 번째 표본으로 *"아침이 없는 L3 밤의 본문"* 을 쓰고 있었는데,
그 본문은 검사 2의 명제가 참일 때만 존재한다.** 게이트를 안 타는 표본으로 바꾸자
**변이 열이 전부 검사 하나씩만 죽였다.**

⚠️⚠️ **그리고 이것은 §판정의 *"검사 하나가 두 명제를 담으면 변이가 절반을 안 태운다"* 와
방향만 반대인 같은 병이다 — 검사와 명제가 1:1이 아니다:**

```
한 검사가 두 명제를 담는다    → ⚠️ 변이가 절반을 안 태운다      (T-76 ①)
한 검사가 남의 명제를 업는다  → ⚠️ 변이 하나가 여럿을 죽인다   (T-77)
```

★ **둘 다 표를 못 읽게 만든다.** 앞은 **못 잡은 결함을 0으로** 세고, 뒤는 **어느 검사가 무엇을
지키는지**를 가린다.

### ★★ 검사가 옛 구현을 **고정**할 수 있다 (T-77)

**`smoke.ts` T-60 검사 5가 *"`nextWake`가 `if (level == 2)` 안에 있다"* 를 세고 있었다.**
그런데 그 블록에는 **게이트(`return false`)와 재료(`wakeLine`)가 함께** 들어 있었고,
**재료가 거기 있는 것이 T-77이 고치려던 결함**이었다.

⚠️ **즉 검사가 결함을 지키는 자물쇠였다.** `CLAUDE.md` §기준선이 *"검사가 옛 동작을
검사하고 있으면 검사를 고치고 그 사실을 말한다"* 고 한 자리이고, **실제로 그 일이 났다.**

★★ **고친 방식이 중요하다 — 지우지 않고 명제를 쪼갰다:**

```
5    게이트가 가지 안에 있다    ← 여전히 지켜야 한다 (ADR-047 ②)
5b   ★ 재료는 가지 밖에 있다    ← 새 명제
```

⚠️ **한 줄이 둘을 함께 세고 있었기 때문에 결함이 그 안에 숨을 수 있었다.**
**구조를 세는 검사는 *무엇이 거기 있는가*가 아니라 *왜 거기 있어야 하는가*로 쪼갠다.**

**★★★ 경계 — 문서의 주장이 거짓이 됐다고 그것을 인용한 검사가 틀린 것은 아니다** (T-77 안전핀)

**`STATE.md`가 *"아침이 없는 밤에도 L3는 뜬다(front 검사 2가 지킨다)"* 라고 단언했고,
실측이 그 문장을 뒤집었다.** ⚠️ **그때 검사 2를 지우면 안 된다:**

```
검사가 지키는 것   "재료가 게이트 밖에 있다"   ★ 코드 모양. 참이고 계속 지켜야 한다
문서가 주장한 것   "그 밤에 실제로 뜬다"       ⚠️ 경로. 거기까지 못 온다
```

★★★ **위 규칙(검사가 옛 구현을 고정한다)은 *검사가 틀린 것을 지킬 때* 쓴다.**
**여기는 검사가 맞는 것을 지키는데 문서가 다른 사실을 그 검사에 업은 경우다.**
⚠️ **둘을 섞으면 멀쩡한 검사를 지우게 되고, 그 손해는 조용하다** — 지운 뒤에는
무엇이 안 지켜지는지 아무도 모른다.

★ **가르는 물음 하나: 그 검사가 세는 명제를 문장으로 적어 보라.**
**문서의 주장과 같은 문장이면 검사를 고치고, 다른 문장이면 문서를 고친다.**

### ★★★★ 검사는 죽을 때보다 **살아남을 때** 더 조용히 거짓말한다 (T-79)

**위 T-77은 검사가 *죽어서* 말한 경우다. 죽으면 반드시 읽는다. 그 반대가 있다.**

```
구현이 바뀌어 검사가 죽는다        ★ 빨간불. 읽는다. 고친다
구현이 바뀌어 검사가 ★ 늘 참이 된다  ⚠️ 초록이 하나 더 는다. 아무도 안 읽는다
```

**T-79가 필터를 앞단계(서버 큐)로 옮기자 T-56 검사 3의 `!(await t56Row(a4))` 조각이
*"답을 쓰기 전에 이미 큐에 없다"* 가 되어 **답과 무관하게 늘 참**이 됐다.**
★ **명제를 잃었는데 초록은 그대로다** — 그 줄을 지워도 수가 하나 주는 것 말고 아무 일이 없다.

⚠️ **그래서 *어느 단계에 거르는 조건을 넣는 변경*은 그 뒤를 세는 검사를 전부 다시 읽는다.**
**앞이 걸러 주면 뒤는 공짜로 통과하고, 공짜로 통과하는 검사는 다음에 진짜 결함이 와도 통과한다.**

★★★ **가르는 법은 변이다 — 그 조각이 *혼자서* 죽는 변이가 있는가.**
없으면 그 조각은 명제를 잃었다. ⚠️ **이 규칙은 같은 티켓의 §확인 절차에도 걸린다** —
T-79 초안의 *"'결과가 어땠나요?'는 여전히 뜬다"* 는 **배포 당일엔 참이고 이틀 뒤에 거짓**이었고,
**그 이틀을 픽스처가 안 만들어서 검사도 함께 초록이었다.**

★★★★ **저절로 참이 되는 길이 셋이고, 셋 다 한 번씩 났다.**
⚠️ **T-75가 둘을 더 냈는데, 이 규칙이 커밋된 *같은 날 · 티켓 하나 뒤*다** —
**변이 N7이 붙고도 아무것도 안 죽였다.** 규칙을 적어 두는 것과 그 자리를 피하는 것은 다르다:

```
①  앞단계가 걸러 준다      T-56 검사 3 — 필터가 서버로 가자 그 줄이 답과 무관해졌다
②  ★ 조건을 || 로 넓혔다    "열렸나"를 classList ‖ style.display 로 물었다
③  ★ 남의 DOM 을 셌다      "몇 줄"인지 셌는데 앞 블록이 남긴 줄 둘이 거기 있었다
```

⚠️ **②가 제일 흔하고 제일 안 보인다.** `style.display`는 **인라인이 빈 문자열이면 늘 참**이라
그 `||` 하나가 검사를 통째로 무르게 한다. ★★ **검사의 `||`는 관대함이 아니라 구멍이다** —
**계약이 하나면 조건도 하나여야 한다**(여기선 `classList.contains("on")`).
⚠️ **구현을 따라 넓히지 마라** — 화면이 두 길로 숨든 말든, **검사는 계약 하나만 본다.**

⚠️ **③은 함정 14가 스파이에서 물린 것과 같은 모양이다** — *"남의 한 벌을 자기 것으로 세고
초록이 된다."* **그런데 남는 것은 핸들러만이 아니라 DOM 이다.**
★ **막는 법도 같다 — 먼저 닫고(`closeAll()`), 자기 id 로 묻는다.**

★★★★ **T-80이 같은 날 저녁에 둘을 더 냈다. 이쪽 둘은 검사의 잘못이 아니다** —
**계약을 코드가 아니라 다른 것이 지고 있어서 변이가 닿을 자리가 없었다:**

```
④  ★★ 구조가 계약을 진다     "예정에 시각이 안 들어간다" — ⚠️ 스키마에 시각 칸이 없다
                              어떤 구현도 못 깨므로 변이가 죽일 것이 없다
⑤  ★★ 환경이 계약을 안 싣는다 "회색으로 보인다" — ⚠️ jsdom 은 외부 CSS 를 안 먹는다
                              화면이 아예 안 칠해지니 무엇을 넣어도 통과다
```

★★★ **④는 지우면 안 된다 — 스키마가 바뀌면 뜻이 돌아온다.**
**고치는 법은 계약을 *지고 있는 것*으로 검사를 다시 적는 것이다** — T-80은
*"구현이 시각을 안 넘긴다"* 를 **스키마 핀**(*"그 테이블에 시각 칸이 없다"*)으로 바꾸고
**그 사실을 검사 제목에 넣었다.** ★ 그러면 **구조가 바뀌는 날 그 검사가 죽어서 말한다.**

⚠️ **⑤는 함정 2의 사촌이다**(jsdom 은 `clientWidth`가 0). **화면을 보는 검사가 환경에서
안 서면, 보는 대신 *원문을 훑는다*** — 그리고 **그 훑기에 알려진 정답을 먹여 본다**(함정 17).
★ **"안 칠해졌다"와 "칠할 규칙이 없다"를 검사가 갈라야 한다.**

★★ **여섯째 — 픽스처가 계약을 진다** (T-84). 셀 검사 일곱이 전부 픽스처로 돌았는데,
**서버가 그 칸을 다른 이름으로 실어 보내면 일곱이 전부 초록인 채 폰에서만 안 합쳐진다**
(함정 15의 픽스처판 — **검사가 정한 이름을 구현이 안 쓴다**).
★ **막는 법은 짝 하나다: *실제 응답에 그 칸이 있는가*를 따로 묻는다.**
T-84가 그것을 넣었고, 변이 `M8`(서버가 칸을 안 싣는다)이 **1~7 초록인 채 그 하나만** 죽였다.

★★★★★ **여섯을 관통하는 물음은 하나다 — *이 검사의 계약을 지금 누가 지고 있는가.***
코드가 안 지고 있으면(앞단계·구조·환경·픽스처) **그 검사는 변이가 닿지 않는 자리에 있다.**

### ★★★★ 고치는 수단이 *같은 증상*을 만들면, 고친 것이 화면에서 안 보인다 (T-84)

**T-84가 고치려던 증상은 *"셀에 같은 것이 두 줄"* 이었다.**
짝을 알려면 `collected_items`를 타야 하는데, **`LEFT JOIN` 으로 타면 행이 불어난다** —
`task_id`에 UNIQUE가 없어 같은 task를 가리키는 행이 둘이면 `entries`가 두 줄이 된다.

```
원인 A (고치려던 것)   event 와 task 가 같은 칸에 둘    →  "두 줄"
원인 B (수단이 만든)   조인이 행을 불린다               →  ★ 똑같이 "두 줄"
```

⚠️⚠️ **증상이 같으면 원인이 바뀐 것을 아무도 못 본다.** 합치는 코드가 멀쩡해도 화면은 그대로이고,
**그때 읽히는 것은 *"새 원인이 생겼다"* 가 아니라 *"구현이 틀렸다"* 다** — 디버깅이 A만 계속 판다.

★ **T-84는 상관 서브쿼리로 피하고 검사 하나를 그 자리에 뒀다**(변이 `M9` = `LEFT JOIN` → 그 하나만 죽는다).
★★ **규칙: 고치는 수단이 같은 증상을 만들 수 있으면, 그 자리에 검사를 따로 둔다.**
**"고쳤는데 증상이 남는다"를 두 원인으로 갈라 주는 것은 그 검사 하나뿐이다.**

### ★★★ §확인 절차에 짝 없는 줄을 적으면 미완성이 조용히 닫힌다 (T-58 → T-82)

**T-58은 시간표를 조회 셋에 한꺼번에 얹고 §확인 절차에 이렇게 적었다:**

> *"□ 캘린더에서 다음 주를 봐도 수업이 있다"*

⚠️⚠️ **그 화면은 지어지지 않았다.** 서버는 달마다 `classes` 를 만들어 보냈고
**프런트는 한 곳에서도 안 읽었다.** 그 줄은 **체크될 수 없었는데 티켓은 닫혔고,
넉 달 뒤 T-82가 *"죽은 코드"* 로 만나서야 드러났다**(달마다 D1 질의 하나가 헛돌고 있었다).

★★★★ **사용자는 안 보이는 것을 *"결함"* 이 아니라 *"아직 안 됐나 보다"* 로 읽는다.**
그래서 **빠뜨린 절반은 빨간불을 안 내고 조용히 통과한다** — 위 §살아남는 검사의
사람 버전이다.

★ **막는 법 하나: §확인 절차의 각 줄은 §할 일의 어느 항목이 만드는지 짝이 있어야 한다.**
**짝이 없는 줄은 확인이 아니라 *의도*다.** 의도는 §이 티켓이 안 하는 것에 적는다.

### ★★ 훑기가 낸 "0건"은 스캐너가 살아 있다는 증거를 먼저 요구한다 (T-69 — 세 번째)

**검사 층은 이미 이 규칙을 안다** — T-59가 *"스캐너가 살아 있는가"* 짝 검사를 만들었고,
그 뒤로 스캐너마다 짝이 붙는다. ⚠️ **그런데 일회성 훑기·조사에는 아무도 그것을 대지 않았다.**

```
훑기가 "0건"     ──┬─  없다            ← 결과
                   └─  ★ 못 봤다        ← 스캐너가 헛돌았다. 화면에서 같다
```

**T-69에서 실제로 났다.** heredoc이 백슬래시를 삼켜 `\n`이 개행이 되고 정규식이 헛돌아
**0건**을 냈다. *"없다"* 로 보고될 뻔했고, **담당이 이미 물렸던 줄을 그 스캐너에 되먹여
보고서야** 드러났다.

★ **그러므로: 훑기가 0건이면 그 스캐너에 *물어야 하는 것 하나*를 먹여 본다.**
**안 물면 결과가 아니라 도구 고장이다.** 그것이 T-59의 짝 검사와 같은 물음이고,
**이 리포가 `sent 0`(T-54) · 요약 없음(T-67)에서 두 번 물린 것과 같은 모양**이다.

⚠️ **§보고에 *"없다"* 를 적을 때는 그 확인을 함께 적는다.** 안 적힌 0건은 읽는 쪽이 못 가른다.

### ★ 개수를 세는 검사는 이웃 변이와 함께 움직인다 (T-64 · T-67 · T-68 — 세 번)

**세 티켓이 같은 자리에서 좁혔다.** 검사가 *"몇 개인가"* 를 세면
**무엇이 늘고 줄었는지를 모른 채 합만** 알고, 그 합은 **옆 변이도 건드린다.**

```
합을 센다     본문에 n 이 몇 번 나오나 · 정수 쓰기가 몇 번인가 · 실패가 몇이나
              →  이 변이가 바꾼 것과 남이 바꾼 것을 구별하지 못한다  →  함께 죽는다
자리를 센다   2번째 발동의 문구 맨 앞에 그 수가 있나
              →  남이 무엇을 하든 이 자리 하나만 본다
```

⚠️ **`0인가`는 다르다** — T-66의 *"그 블록 안에 `--faint`가 하나도 없다"* 는 이웃 변이가
그 블록을 안 건드리므로 안 움직였다. **범위가 좁으면 합도 자리처럼 군다.**
가르는 물음은 *"몇 개냐 어디냐"* 가 아니라 **"이웃이 이 수를 바꿀 수 있는가"** 다.

### 두 시험은 충돌이 아니라 순서다

```
①  이름 시험      관계가 자인가 주어인가        →  자면 계약값으로 바꾼다      (T-64)
②  혼자 죽기 시험  주어라도 연역되지 않는가       →  연역이면 초록불을 안 준다   (T-65)
```

⚠️ **①만 대면 T-65의 두 검사가 *"주어다, 그대로 둬라"* 로 통과한다** — 관계를 빼면 문장이
남지 않으니 주어가 맞다. **그런데 주어인 것과 세는 것은 다른 물음이다.** ②까지 가야 갈린다.

### ⚠️ 변이가 검사 대신 러너를 죽이면 배터리가 거짓말을 한다

**T-55·T-56·T-58·T-60에서 네 번 났다.** 변이로 사라진 요소를 검사가 `null` 참조하면
러너가 통째로 죽고 **요약 줄을 잃는다.** 배터리는 그것을 *"아무도 안 죽었다"* 로 읽는다.

**요약이 없는 것과 0건은 다르다.** 배터리가 그 둘을 같은 칸에 찍으면
**변이 검증 자체가 무의미해진다** — 이 리포가 검사에서 열 번 넘게 물린 것과 같은 모양이
검사를 검사하는 층에서 반복되는 것이다.

**검사는 요소가 없으면 그 검사만 빨간불이 되게 쓴다.**

### ★ 환경이 러너를 죽여도 배터리는 똑같이 거짓말한다 — 가르는 법 (T-66)

**변이가 아니라 환경이 죽여도 화면은 같다.** 요약이 없고, 배터리는 *"안 죽었다"* 로 읽는다.
**변이가 무죄인지 유죄인지 먼저 갈라야 표를 채울 수 있다.**

```
①  깨끗한 트리에서 같은 실패가 나는가        git stash → 같은 검사
②  아무것도 못 바꾸는 변이에서도 나는가       안 타는 갈래에만 갇힌 편집
```

**둘 다 그렇다면 변이가 아니다.** T-66이 실제로 이 순서를 밟아 *"내 변경 탓이 아니다"* 를
**주장이 아니라 관측으로** 만들었다 — ②가 특히 강하다. 코드는 달라졌는데 동작이 같으므로
**"편집했다"와 "동작이 바뀌었다"를 가른다.**

⚠️ **그다음은 범위를 넓히지 말고 멈춘다.** 원인이 남의 티켓 땅이면 §미해결에 실마리를 적고
**못 잰 변이는 못 쟀다고 적는다.** 빈칸을 추측으로 채우면 그 표가 다음 티켓의 근거가 된다.

★ **그래도 그 시도는 값을 낸다** — T-66은 M5를 재려다 러너가 죽는 것을 보고
**검사 5가 던진 것을 문자열로 받도록 좁혔다.** 재지 못한 변이가 검사를 고친 것이다.

---

## 9. 한 사이클

```
사용자      "Me 탭 붙이자"
Cowork      설계 확인 → T-07 발행 → 사용자 승인
Claude Code 티켓 분해 → 락 → Antigravity에 지시
Antigravity 구현 → verify → 보고(숫자)
Claude Code 1차 검토 → 통과면 STATE·APP-BUILD 갱신 → commit
Cowork      설계 정합 검토 → 사용자에게 승인/재작업 요청
사용자      승인 → push · deploy · 폰 확인
```

막히면 그 층에서 멈추고 한 칸 위로 올린다. **아래층이 판단을 대신하지 않는다.**
