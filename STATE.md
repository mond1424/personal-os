# STATE — 최종 갱신 2026-08-17

## 저장소

> ### ⚠️ 아래 배포·마이그레이션 줄은 **이력이다. 지금의 사실이 아니다.**
>
> **규칙의 자리는 `CLAUDE.md` §사람이 하는 것의 상태 하나다**(2026-08-14 신설) —
> 배포·`--remote`·APK 설치는 사용자가 하고 이 층은 모른다. **하는 층과 적는 층이 다르면 반드시 낡는다.**
> 여기 적힌 것은 **쓰인 시점의 관측**이고, 읽을 때는 **다시 확인한다.**
> **네 번 물렸다** — "모드 하향은 라이브에 없다" · T-07의 "deploy 대기" · T-26·T-27의 "아직 배포 전" ·
> **T-33의 "아직 라이브가 아니다"가 한 시간 만에 거짓이 된 것**(2026-08-11 19:56 → 20:59 배포).
>
> **이 리포에만 있는 확인법**(명령 셋은 `CLAUDE.md`에 있다): 라이브 자산을 직접 집으면
> **어느 커밋이 올라갔는지까지** 말해 준다 — 그 티켓만 가진 문자열을 고른다.
> ```bash
> curl -s https://personal-os.mai-pos.workers.dev/app.js | grep -c "function handleBack"   # T-34
> ```

- repo: https://github.com/mond1424/personal-os
- branch: main
- 마지막 코드 변경: **T-39 `d5a6149`** (2026-08-20 · 서버 + Kotlin + smoke · **마이그레이션 없음**).
  그 앞이 **T-37 `d9a2f7a`** (같은 날 · `GuardVerify.kt`·`smoke.ts` — **Kotlin 상수**) ·
  **T-38 `d71d42b`** (같은 날 · **`0017_ai_reason.sql` 포함** · 서버 + Kotlin + smoke).
  - ⚠️ **셋 다 APK가 필요하고 T-38은 마이그레이션도 필요하다.** 한 번의 유선 연결에 묶는다.
    **T-39를 빼고 깔면 안 된다** — 그 구멍(판정이 flush를 앞지르면 유실)이 열린 채로 9월 데이터가 쌓인다.
    남은 것을 **여기서 단정하지 않는다** — 확인은 이 두 줄이 한다:
    ```powershell
    npx wrangler d1 migrations apply personal-os --remote --dry-run   # 0017이 남았나
    adb shell dumpsys package dev.mond1424.personalos | findstr versionCode
    ```
  - **웹 자산은 이 둘에 없다**(`public/` 무변경). 즉 `deploy`가 필요한 이유는 **서버 코드와
    0017**이지 화면이 아니다.
- 그 앞 코드 변경: **T-36 `4554c73`** (2026-08-14 18:39 KST · **`test/smoke.ts`만 — 소스 무변경**).
  그 앞이 **T-35 `7cd89e3`** (같은 날 17:46 · `public/app.js`·`index.html`·`front.mjs` — **웹만**).
  ⚠️ **T-35는 웹이라 배포가 필요하고, 배포 여부는 이 층이 모른다** — `deployments list`로 확인한다.
  그 앞이 T-34 `e469b42` (2026-08-11 20:53 · 웹+`@capacitor/app`) · T-33 `25a44c6` (19:56 · 웹만) ·
  T-32 `4889988` (18:15 · 서버만) · T-31 `de40a27` (17:27 · `0016` 포함) · T-30 `bb109ff` (12:57 · Kotlin만).
  - ✅ **2026-08-11 20:59:57 KST 배포 완료 — T-34까지 라이브** (2026-08-14 실측).
    `deployments list` 최신 `2026-08-11T11:59:57Z`가 T-34 커밋(20:53)보다 **뒤다.**
    **라이브 자산이 어느 커밋인지까지 말한다**: `/app.js`에 `function handleBack`·`backButton`
    (T-34) · `dataset.state`(T-33)가 **각각 있다.** 웹은 원격 URL 로드라 **APK 없이 이미 폰에 있다.**
  - ✅ 마이그레이션도 밀린 것이 없다 — `d1 migrations list --remote` → **"No migrations to apply!"**
    (`0016`이 마지막. T-33·T-34는 마이그레이션이 없다.)
  - ⚠️ **네 번째다.** 앞 세션이 19:56에 *"T-33은 아직 라이브가 아니다"*라고 적었고
    **한 시간 뒤 거짓이 됐다**(20:59 배포). 그때는 참이었다 — 그게 이 절 머리 경고가 말하는 바로 그것이다.
    이번엔 규칙이 일했다: **문서를 안 믿고 `deployments list`와 라이브 `/app.js`를 집었다.**
- 그 앞 코드 변경: **T-27 후속 `936547e`** (2026-08-10 12:40 KST · 가장자리 폭 0.2).
  그 앞이 T-27 `c3a8a83` · T-26 `25977c3` (같은 날) ·
  T-24+T-25 `05b50df` (2026-08-07) · T-23 `5ee0df6` · T-22 `2aa4237`.
  그 앞은 T-21 `8ddcbe9` · T-20 `3695c00` · T-08′ `7c602e5` · T-19 후속 `5687455` · T-19 `7d204cd` ·
  **T-10 `05d4914`**(0015) · T-18 `efec6ee` · T-17 `70d25d5` · T-16 `998faf9`.
  T-14 하네스 1단계는 `e9f2c0b`, T-13 릴리스 서명 결과 가드는 `4390699`, T-11은 `1df37a5`,
  T-12 날짜 독립 검사는 `e55b515`, 기존 T-09 두 커밋은 `6d0bb35`·`8139f7b`로 원격 이력 그대로 유지했다.
  - ✅ **2026-08-10 12:41 KST 배포 완료 — T-26·T-27(폭 0.2 포함)까지 라이브.**
    앞 세션이 "아직 배포 전"이라 적어 둔 것을 wrangler로 확인해 고쳤다. **사용자 말이 아니라 실측이다.**
    - `deployments list` 최신 `2026-08-10T03:41:30Z` · version `7c9bd903` (같은 날 12:32의 `8ddfdee5` 위에 한 번 더).
    - **라이브 자산이 어느 커밋인지까지 말한다**: `/app.js`가 `SWIPE_EDGE_RATIO = 0.2` →
      `936547e`(12:40:27 KST)가 들어 있다. `/style.css`도 `.cal-row{…border-top:1px solid var(--line)}`.
      **자산과 Worker 코드는 한 번의 `deploy`가 같은 version으로 올리므로 T-26의 `schedule()`도 같이 라이브다.**
    - `/api/health` → **401** (API_TOKEN이 걸려 있다 — 정상. Worker가 떠 있고 인증을 강제한다는 뜻).
    - `d1 migrations list --remote` → **"No migrations to apply"**. 새 마이그레이션 없음(0015가 마지막).
  - ✅ **2026-08-07 21:58 KST 배포 완료 — T-22·T-23·T-24·T-25까지 라이브.**
    (확인: `wrangler deployments list` 최신 `2026-08-07T12:58:43Z` · version `e74adb3a`. 마이그레이션 없음.)
  - ✅ **2026-08-06 04:40 KST 배포 완료** — `0015` 원격 적용 + T-21까지 라이브
    (확인: `d1 migrations list --remote` → "No migrations to apply" · `deployments list` 최신 `2026-08-05T19:40Z`).
  - ✅ **2026-08-03 폰 실측 완료** — T-04·T-05·T-09·T-11·T-12 닫힘. 기준선 smoke 237 / front 210 / 실패 0.
  - ✅ **0013까지 로컬·원격 모두 적용 + deploy 완료** (2026-07-29). 폰 APK도 갱신 완료.
  - ✅ 2026-07-30 21:55 배포 완료 — `normalizeIso`(UTC 정규화)·T-03·T-05까지 라이브.

## 에이전트 체인 (2026-07-30 도입)

**사용자 → Cowork → Claude Code → Codex CLI.** 문서 넷이 한 벌이다:

| 문서 | 무엇 | 주인 |
|---|---|---|
| `AGENT-CHAIN.md` | 층별 권한·파일 소유권·보고 형식 | Cowork |
| `AGENTS.md` | Codex CLI 진입 파일(CLAUDE.md를 원본으로 가리킨다) | Cowork |
| `OPERATIONS.md` | 사용자가 하는 일·붙여넣는 문장 | Cowork |
| `docs/tickets/*.md` | 지시와 보고가 같은 파일에 | Cowork 발행 · Claude Code 배정 |

이 층(Claude Code)이 지는 것: **`STATE.md`·`APP-BUILD.md`·`docs/api-surface.md`·`docs/schema-current.sql`·git의 유일한 편집자**,
`npm run verify`로 **숫자를 만드는 유일한 층**, 위임 금지 영역(트리거·마이그레이션·귀속일·Guard 발동 경로) 직접 구현,
Codex 티켓 분해·락·1차 검토. 설계 문서·`APP-PLAN`·`APP-ADR`은 읽기만 한다.

**락은 `APP-BUILD.md` 맨 위 한 줄.** 락을 쓰는 것은 Claude Code이고, 괄호 안 이름은 보유자다.

### 도입 시점에 발견된 규약 불일치 (Cowork 판단 대기)

1. **함정 번호가 갈라졌다** — `AGENTS.md`의 7번은 '전역 클래스명 충돌'인데 `CLAUDE.md`·`README0722.md`의 7번은
   'wait_extensions FK'다. 티켓이 "함정 7번"이라 쓰면 두 층이 서로 다른 것을 읽는다.
   '전역 클래스명 충돌'은 원래 11개 목록에 없고 이 문서의 '미해결' 절에만 있었다.
2. **push 권한** — `AGENT-CHAIN.md` §2·§9는 "`git push` 최종 승인은 사용자"인데,
   `CLAUDE.md` 세션 종료 규칙과 `OPERATIONS.md` §7은 Claude Code가 세션 끝에 push한다. 2:1로 후자가 맞아 보인다.
3. **티켓 파일 쓰기 권한** — `OPERATIONS.md` §2는 Codex에게 티켓의 '보고' 절을 채우라 하고
   `_TEMPLATE.md`도 "담당이 채운다"인데, `AGENT-CHAIN.md` §3 표는 "Codex는 읽기만"이다.
4. ~~**기준선 숫자가 위층 파일에 하드코딩**~~ — ✅ **해소됨 (2026-08-06).**
   `CLAUDE.md`에서 실수치를 빼고 "현재 기준선은 `STATE.md` §기준선에 있다"로 바꿨다.
   **숫자의 자리는 이 문서 §기준선 하나뿐이고**, 예시는 `smoke A → B` 형식으로 둔다.
   두 번 물린 뒤의 결론이다 — 167→183일 때 `CLAUDE.md`가 167에 멈췄고, 260/223일 때 다시 낡았다.
   숫자를 만드는 층이 못 고치는 파일에 숫자를 두면 **반드시** 낡는다.
5. `CLAUDE.md`의 "`docs/*`의 유일한 편집자"는 넓다 — `docs/tickets/*`는 Cowork 발행이다.

### T-01 — 체인 첫 티켓 (Education 폼, 2026-07-30)

Cowork 발행 → Claude Code 배정·분해 → Codex 구현 → Claude Code 1차 검토. **한 바퀴가 돌았다.**
결과물: `public/{api.js,index.html,app.js,style.css}` · `test/front.mjs`. **서버 무변경**(P1-b가 이미 다 해 뒀다).
`GET /api/lm/education/schema`의 `fields`로 폼을 조립한다 — 필드 목록 하드코딩 없음.

**잘 된 것**: 신규 클래스 전부 `.lm-education-*` 접두사(전역 충돌 4번째를 안 만들었다) ·
색은 CSS 변수 + 다크 짝 양쪽 · `itemType`까지 처리(분해 절이 전달됐다) · 범위 밖 파일 0개.

**체인이 실제로 잡은 것 두 종류.**

1. **검사가 목적을 못 지켰다** — 숫자(167→173)는 늘었는데 "폼이 스키마로 조립된다"는 검사가
   **하드코딩해도 통과하는 모양**이었고, 완료 조건 3번(필수 필드 프런트 차단)·1번 절반(빈 상태 문구)·
   삭제 경로가 미검사였다. 추가·수정도 `Api.lmCreate` 직접 호출이라 폼 경로가 검사에서 빠져 있었다.
   → 검사 6건을 16건으로 교체(front 183). **합격 기준을 분해 절에 미리 적어 뒀는데도 반영되지 않았다.**
   → **Cowork 판단(`AGENT-CHAIN.md` §8 보강)**: 구현자는 자기가 통과시킬 검사를 쓰므로 — 악의가 아니라
   같은 이해로 짜기 때문에 — 합격 기준은 **티켓이 아니라 검토에서 강제한다.** 미리 적는 것으로는 안 된다.
   검사 하나를 볼 때 묻는 것: **구현을 잘못했다면 이 검사가 빨간불이 되는가.**
2. **잠재 결함 2건** — ① `renderMe()`의 `Promise.all`에 `lmSchema`가 들어가 **Education 스키마가
   비활성화되면 Me 탭이 통째로 안 그려진다**(호출부 5곳 모두 await 없음 → unhandled rejection).
   레지스트리는 버전을 올리려고 둔 것이라 `active` 전환은 예정된 동작이다 → `.catch`로 격리.
   ② 항목 제목 추론이 `type==="string" && required`의 첫 번째를 잡는데 `status`도 `type:"string"`이라
   **0012의 properties 순서 덕에** 맞았다. v2에서 뒤집히면 제목이 `"enrolled"`가 된다 → enum 제외.

**Codex의 front 실행이 EPERM으로 죽어 수치를 못 냈다**(`xdg.config\.wrangler\logs` 쓰기 권한).
이 층에서는 재현되지 않았다 — 셸 환경 문제이고 코드와 무관하다. 숫자 없이 닫지 않고 막힌 것으로 올린 건 규약대로다.

**라벨 문제는 T-02로 닫혔다**(아래) — `lm_schema`에 `title`을 넣는 쪽으로 결정됐다.

### T-02 — 스키마가 필드 라벨을 준다 (0014, 2026-07-30)

**위임 금지 영역**(마이그레이션)이라 Claude Code가 직접 했다. T-01이 남긴 "폼 라벨이 영문 raw" 문제의 답.
프런트에 매핑을 두지 않은 근거는 §2.2 — 소비처 셋이 **같은 것**을 읽는다. 라벨만 프런트에 두면
스키마가 v2로 오를 때 새 필드만 영문으로 남고 **그 어긋남이 조용히 생긴다.**

- `0014` — `json_set`으로 세 섹션 body에 `title`만 얹는다. **`version` 불변**(라벨은 검증 의미가 아니다 →
  올리면 §5 거짓 stale). 전체 치환 대신 `json_set`을 쓴 이유는 원격 body가 갈라져 있어도 되돌리지 않기 위해서다
- `lib/schema.ts` — `SchemaNode.title` + `fieldsOf`가 `title` 실어 보냄. **폴백(`title ?? key`)을 서버에서 끝낸다** —
  소비처 셋이 각자 폴백을 짜면 그게 어긋나는 자리다. `validate`는 `title`을 보지 않는다(부분집합 안 넓힘)
- `app.js` — `educationFieldLabel(field)`가 `field.title`을 쓴다. 옛 배포가 `title` 없이 응답할 수 있어 프런트도 폴백 유지
- 라벨: 과목명·상태·학기·성적·학점·선수과목·메모 / 기간·연결 기간·지표·메모 / 요약
- **`status`의 enum 값 라벨(`completed`→'수료' 등)은 범위 밖.** 값 라벨은 `enum`과 표시명을 짝지어야 하고
  스키마 부분집합에 새 키워드가 붙는다 — 필요해지면 별도 티켓

검사는 **하드코딩과 구별되는 쪽**으로 넣었다: smoke는 `title` 없는 섹션을 넣어 `key` 폴백을 보고,
front는 `title`을 지워 라벨이 `key`로 **돌아가는 것**까지 본다.

### T-03 — Level 4 AI 검증 (서버측) · `buildCoreContext` (2026-07-30)

**위임 금지 영역**(Guard 발동 경로)이라 Claude Code 직접. **S3.1b가 닫혔다.**
`POST /api/guard/verify` — Level 3→4 격상만 검증한다(ADR-024). Level 1~3은 ADR-021 그대로 결정론.
**기기 배선은 T-04**(APK 재빌드 필요) — 서버만 먼저 끝내 폰 없이 배포한다.

- **어떤 경우에도 200.** 판정 불가는 `level:3`이지 500이 아니다 — 기기가 오류 분기를 타면 새벽에 터진다
- 통제 순서: **⑤킬 스위치 → ②캐시 → ③일일 상한 → 키 확인 → ④타임아웃 8초 → ①호출**
  - **캐시를 상한보다 먼저 본다.** 적중은 돈이 0이므로 상한이 찼다고 받은 판정을 버리면
    그 밤의 Level 4가 이유 없이 죽는다. 상한이 막아야 하는 것은 '새 호출'이다
  - 킬 스위치는 **항상 격상**(결정론 복귀). Level 3으로 떨구면 끄기가 벌이 된다
  - 캐시는 `guard_events.ai_verdict` 재사용 — 새 테이블 없음(원칙 1). **`'unavailable'`은 캐시하지 않는다**:
    판정이 아니라 "부를 수 없었다"는 기록이고, 재사용하면 네트워크가 돌아와도 그 밤 내내 Level 3에 묶인다
  - `callModel` 시그니처 불변 — 타임아웃은 호출부 `Promise.race`(분석 경로가 물린다)
- `lib/context.ts` `buildCoreContext` — **빈 섹션을 생략하지 않는다**(§6.2). 섹션 목록은 `lm_schema`에서 가져온다
- **⑥기록은 여기서 하지 않는다** — 기기가 발동을 올릴 때 `record()`가 `ai_used`·`ai_verdict`를 받는다(이미 지원).
  검증만 하고 발동하지 않은 밤의 **유령 행이 개입 이력을 오염**시키기 때문
  - ⚠️ **남는 구멍**: 검증 후 기기가 기록을 못 올리면(크래시) 그 호출이 상한에 안 세어진다.
    T-04가 `record()`에 `ai_used`·`ai_verdict`를 반드시 실어야 한다 — **T-04의 완료 조건에 넣어야 한다**

**검사가 내 버그를 하나 잡았다.** 캐시 조회를 `ORDER BY fired_at DESC`만으로 뒀더니 같은 분에 판정이
둘 들어온 밤에 어느 쪽이 나올지 정해지지 않았다(deny 검사가 approve를 받아 실패). 기기 재전송이 실제로
그 상황을 만든다 → `, id DESC`로 동점을 깬다(id는 당일 단조 증가).

**티켓 범위 밖을 한 줄 건드렸다 — `src/services/me.ts`.** 킬 스위치 `guard_ai_verify`를 쓸 경로가
아예 없었다: `putSetting`의 `RULES`가 화이트리스트라 등록되지 않은 키는 **404**다. 티켓이 이 통제를
요구하고 완료 조건 1번이 검사까지 요구하는데 범위에 `me.ts`가 없었다 — **티켓의 누락으로 판단**하고
`guard_ai_verify: 'on'|'off'` 한 줄을 넣었다. Cowork 확인 필요.

### T-06 — `npm run front`가 어느 셸에서든 자기 숫자를 낸다 (2026-07-30)

**증상은 셋이었는데 원인은 하나였다.** 티켓이 "추측으로 둘 다 고치려 하지 말고 재현해서 가른다"고 해서
측정부터 했다.

| 측정 | 결과 |
|---|---|
| 마이그레이션 단계(0014까지 14개) | **11.5초** — 300초는 '느림'이 아니라 '막힘'이다 |
| front 검사 본문 | **74.6초** — 안전망 180초에 한참 못 미친다 |
| 로그 경로를 못 쓰게 만들어 재현 | 이 셸에서는 **재현 안 됨**(권한이 있어 그냥 써졌다) |

**진짜 원인: `front.mjs`가 성공 경로에서 종료하지 않았다.** 실패 경로엔 `process.exit(1)`이 있는데
성공은 그냥 끝나 있었고, `pretendToBeVisual` jsdom **두 개**가 rAF 타이머를 계속 돌려 이벤트 루프가
비지 않는다 → 요약을 찍고도 프로세스가 살아 있었다.

그래서 `e2e.mjs`의 **안전망 SIGKILL(180초)이 유일한 종료 수단**이었고, 검사가 전부 통과해도
`npm run front`는 **exit 1**이었다. 옛 3분 15초 중 ~105초가 순수 hang이다.
**함정 8의 "끝의 ETIMEDOUT은 무해하다"가 이 결함의 흔적이었다** — 숫자를 눈으로 읽으면 되지만
종료 코드를 믿는 셸·에이전트에서는 매번 실패로 보인다. Codex가 숫자를 못 낸 이유로 가장 유력하다.

고친 것:
- `front.mjs` 성공 경로에 `process.exit(0)` — **범위 밖 한 줄**(§5로 메웠다, 아래).
  파이프 stdout은 비동기라 `write` 콜백에서 나간다 — 요약이 잘리면 숫자를 잃는다
- `e2e.mjs`가 **wrangler의 로그·설정·캐시를 임시 폴더로** 몬다(`WRANGLER_LOG_PATH`·`XDG_*`·
  `WRANGLER_SEND_METRICS=false`). T-01의 EPERM 경로(`%APPDATA%\xdg.config\.wrangler\logs`)는
  wrangler의 **기본** 전역 설정 위치다 — 격리 러너가 임시 D1을 쓰면서 로그만 홈에 남길 이유가 없다.
  `XDG_CONFIG_HOME`은 `~/.wrangler`가 있으면 밀리므로(legacy 폴백) 로그는 `WRANGLER_LOG_PATH`가 진다
- 마이그레이션 단계에 상한 120초 — **덮으려는 게 아니라 막힘이 스스로 이름을 말하게** 한다.
  없으면 T-05처럼 아무 말 없이 바깥 제한 시간까지 먹는다. 실측 11.5초라 오발동 여지가 없다
- 안전망 180 → 420초. 이제 순수 hang 방지이고, 실제 소요를 매 실행 찍는다(늘어나는 것이 보이게)

**결과: `npm run verify`가 exit 0이다 — 처음이다.** 숫자는 233/193 그대로(티켓 요구).

**2차 (같은 날) — Codex 셸은 여전히 막힌다. 이번엔 15분(Exit 124).**

비교가 깨끗해졌다. 같은 커밋·같은 디렉터리에서:

| 셸 | 결과 |
|---|---|
| cmd | 통과 193 · front 74.6초 · **exit 0** |
| Codex | **15분 초과(Exit 124)** · 출력이 마이그레이션 표에서 끊김 |

**결정적 사실: 내가 넣은 120초 마이그레이션 상한이 발동하지 않았다.** 마이그레이션 자식이 돌고 있었다면
"120초 안에 끝나지 않았다"가 떴어야 한다. 즉 Codex가 말한 "마이그레이션 단계 미완료"는 그 출력과 어긋난다.

그래서 **러너 안의 모든 대기에 상한을 채웠다** — 원인 추정 대신 다음 실패가 스스로 이름을 말하게.

- **헬스 대기의 `fetch`에 상한이 없었다.** 반복 120회 × 250ms로 "30초"처럼 보였지만, 서버가 포트만 열고
  응답을 안 주면 **그 한 번의 `fetch`가 영원히 걸린다** — 횟수로 감싼 상한은 상한이 아니다.
  `AbortSignal.timeout(2000)`으로 매 시도를 끊는다
- **`seedFixtures`도 상한이 없었다**(`seed.mjs:14`). `seed.mjs`를 고치지 않고 **호출부에서** 60초로 씌웠다
- **마이그레이션 직후에 러너가 자기 줄을 찍는다** — `[e2e] 마이그레이션 완료 N초`.
  표는 wrangler가 찍고 이 줄은 러너가 찍으므로, **표가 있고 이 줄이 없으면** 자식이 표를 찍고도
  끝나지 않은 것이고, 둘 다 있으면 막힌 곳은 그 뒤다. 이 한 줄이 다음 진단을 가른다
- **`XDG_CACHE_HOME`·`XDG_DATA_HOME`·`XDG_STATE_HOME`을 되돌렸다.** 관측된 실패는 설정 디렉터리
  쓰기(로그)뿐이었는데 캐시까지 매 실행 비우게 해 뒀다 — **근거 없이 넓힌 통제가 새 실패를 만든다.**
  `WRANGLER_LOG_PATH`·`XDG_CONFIG_HOME`·`WRANGLER_SEND_METRICS`만 남겼다

**이제 러너 안에 상한 없는 대기가 없다.** 그래도 Codex 셸이 **아무 메시지 없이** 15분을 돌면
원인은 리포 밖이다 — 가장 유력한 것은 `stdio: "inherit"`으로 나가는 stdout이 **소비되지 않는 파이프에서
막히는 것**이다(마이그레이션 표가 520줄이고, 출력이 정확히 거기서 끊겼다). 그 경우의 대처는
**파일로 리다이렉트**다: `npm run front > front.log 2>&1`. 확정되면 `AGENTS.md`에 넣도록 Cowork에 올린다.

**3차 (같은 날) — 15분 hang의 정체가 확정됐고, 원인이 한 단계로 좁혀졌다.**

2차 상한이 일하자 Codex 셸이 **30초에 이름을 달고** 끝났다:

```
[e2e] 마이그레이션 완료 12.7초
→ dev 서버가 30초 안에 /api/health에 응답하지 않았다
```

이로써 두 가지가 **확정**됐다:
1. **지난 15분 hang은 상한 없던 헬스 `fetch`였다.** 서버가 응답하지 않으니 그 한 번의 `fetch`가
   영원히 걸렸다. stdout 파이프 back-pressure 가설은 **기각** — 러너가 메시지를 정상 출력했다
2. **마이그레이션은 원인이 아니다**(12.7초). T-01·T-05의 "마이그레이션 단계" 진단은 셋 다 오진이었다

남은 단계는 하나 — **`wrangler dev`가 그 셸에서 30초 안에 뜨지 않는다.**
그런데 러너가 그 이유를 **버리고 있었다**: `stdio: "ignore"`라 wrangler가 찍은 것이 사라졌다.
"30초 안에 응답 없음"만 남고 원인은 안 남는 구조였다. 고친 것:

- dev 서버 출력을 **파이프로 받아 항상 비운다**(읽지 않는 파이프는 쓰는 쪽을 막는다).
  실패 보고에 그 출력을 붙인다 — 확인 결과 `wrangler 4.112.0` 배너·바인딩·`Starting local server...`가 잡힌다
- **앞 40줄 + 뒤 20줄**을 남긴다. 뒤쪽만 남기면 헬스 프로브의 접근 로그가 버퍼를 채워
  **기동 로그를 밀어낸다**(실제로 그랬다). "왜 안 떴나"의 답은 앞쪽에 있다
- dev 서버가 **일찍 죽으면 30초를 기다리지 않고** 종료 코드·시그널과 출력을 들고 즉시 끝낸다
- **실패하면 임시 폴더를 남긴다.** 지우면서 로그 경로를 알려 주는 건 아무 말도 안 하는 것과 같다 —
  실제로 3차 보고가 가리킨 `wrangler-logs`는 이미 삭제된 폴더였다

실패 경로는 사본(`e2e.probe.mjs`)으로 헬스 URL을 틀리게 만들어 **실제로 돌려서 확인**했다.

**4차 — 근본 원인 확정. 그리고 리포 안에서는 못 고친다(§7 위로 올림).**

3차 진단이 답을 잡아 왔다. Codex 셸의 `wrangler dev` 출력:

```
✘ [ERROR] EPERM: operation not permitted, mkdtemp
          'C:\dev\personal-os-worker\worker\.wrangler\tmp\dev-XXXXXX'
✘ [ERROR] The expression evaluated to a falsy value: (this.#tmpDir)
```

**`wrangler dev`가 리포 안 `.wrangler/tmp/`에 임시 디렉터리를 못 만든다.** 그리고 그 오류로 죽지 않고
**불구가 된 채 살아 있어서**(`#tmpDir`이 falsy) 헬스가 30초를 채운다 — "프로세스는 살아 있다"가 맞았다.

확인한 것:
- `.wrangler`·`.wrangler/tmp`의 **속성·ACL은 정상**이다(Authenticated Users에 Modify, ReadOnly 아님).
  이 셸에서는 같은 자리에 잘 만들어진다 — `tmp`는 내 실행이 만들어 놨다
- Codex는 리포 루트에는 쓸 수 있다(`front.log`가 실제로 써졌다). **`.wrangler/` 아래만 막힌다**
- ⇒ 파일시스템이 아니라 **Codex 샌드박스의 경로 정책**으로 본다

**리포 안에서 못 고치는 이유.** wrangler는 `.wrangler`를 `dirname(설정 파일)`에서 계산한다
(`getWranglerHiddenDirPath` ← `projectRoot = dirname(userConfigPath)`). 즉 그 디렉터리를 옮기려면
**`wrangler.toml` 자체를 임시 폴더에 복제**해야 하고, `main`·`migrations_dir`·`[assets] directory`
세 상대 경로를 절대 경로로 고쳐 써야 한다. 가능하지만 **검사가 진짜 설정을 안 보게 된다** —
나중에 바인딩·플래그가 추가되면 러너만 조용히 옛 설정으로 돈다. 테스트 하네스에서 그 대가는 크다.

**그래서 올린다: 고칠 곳은 Codex 샌드박스의 쓰기 허용 목록이다**(`<repo>/.wrangler/`).
한 줄이면 `wrangler dev`뿐 아니라 앞으로의 모든 로컬 wrangler 명령이 함께 풀리고, 유지 비용이 0이다.
T-06의 방향("환경에 기대지 않게")은 로그·상태까지는 통했지만 **`.wrangler/tmp`에서 끝난다.**

**곁에서 드러난 것**: `e2e.mjs`가 매 실행 `<repo>/.wrangler/tmp/bundle-*`를 남긴다(59개 쌓여 있었다).
러너 헤더의 "흔적 0"은 `--persist-to` 임시 DB에 대해서만 참이고, wrangler의 번들 임시 폴더는 리포에 남는다.
정리해 뒀다. 지우는 것은 wrangler 자신의 sweep이 하는 일이라 러너에 삭제를 넣지 않았다.

**5차 — Codex 자신의 A/B로 확정됐다. 권한 문제다.**

Codex 셸에서 같은 `mkdtemp`를 직접 돌린 결과:

```
기본 환경        → EPERM
추가 권한 환경   → 성공 (C:\...\.wrangler\tmp\dev-dwtlHS)
OS 임시 폴더     → 성공
node             → v24.14.1 · C:\Program Files\nodejs\node.exe  (이 셸과 동일)
```

**node가 같은 바이너리·같은 버전이고, 같은 연산이 환경 권한에 따라 갈린다.** 변수는 하나뿐이다 —
"권한 문제가 아니라 실행 환경 오류"라는 앞선 진단은 **Codex 자신의 실험으로 반증됐다.**

**고칠 곳(확정)**: Codex의 **기본 환경**에 `C:\dev\personal-os-worker\worker\.wrangler` **쓰기**를 허용한다.
추가 권한 환경에서만 되는 것으로는 부족하다 — `npm run front`가 기본 환경에서 돌기 때문이다.
(사용자의 첫 `--add-dir` 시도는 내가 자리표시자 `<repo>`를 그대로 준 탓에 절대 경로 오류가 났다.
그때 "작업공간이라 필요 없다"는 결론이 나왔는데, 이 A/B가 그것도 아니라고 말한다.)

**리포 쪽에서 더 할 일은 없다.** 러너는 이미 로그·상태를 임시 폴더로 몰고, 모든 대기에 상한을 두고,
실패 시 wrangler 출력과 임시 폴더를 남긴다. 남은 EPERM은 러너가 만들 수 있는 것이 아니다.

### T-06 — 배제된 원인 목록 (다시 파헤치지 않도록)

5차에 걸쳐 확인한 것. **아래는 전부 아니다:**

| 배제 | 근거 |
|---|---|
| 마이그레이션이 느리다 | 실측 11.5~12.7초. 상한 120초 미발동 |
| front 검사가 안전망(180초)에 닿았다 | 검사 본문은 74.6초 |
| stdout 파이프 back-pressure | 러너가 메시지를 정상 출력했다(2차 상한이 일한 뒤) |
| 잘못된 cwd (함정 9) | 리포 루트에서 실행해도 같다. 원격 마이그레이션도 정상이었다 |
| node 차이 | v24.14.1 · `C:\Program Files\nodejs\node.exe` — 두 셸이 **동일 바이너리** |
| `.wrangler` ACL·속성 | Authenticated Users에 Modify, ReadOnly 아님. 이 셸에서는 생성된다 |
| 오래된 `tmp` 잔여물 | 59개를 지운 뒤에도 같다 |
| 백신(알약·Defender) | **알약 프로세스가 하나도 안 돈다**(설치만 됨). Defender는 CFA·ASR 꺼짐, 차단 이벤트 없음 |

**남은 것은 Codex 자신의 실행 샌드박스 하나다.** 그런데 Codex의 자기 보고가 자기 A/B 결과와
어긋난다("이미 워크스페이스라 쓰기 가능" ↔ 기본 환경 EPERM). 메커니즘 추적은 **리포 밖이고 여기서 멈춘다.**

**당장의 해법은 이미 있다** — Codex가 `mkdtemp`에 성공한 그 **추가 권한 환경에서 `npm run front`를 돌리면 된다.**
193이 나오면 T-06의 완료 조건이 충족된다. 기본 환경을 고치는 것은 Codex 설정 쪽 별건으로 두고,
프로젝트를 막지 않는다.

### ✅ T-06 닫힘 (2026-07-31)

**Codex 셸에서 통과 193 · 실패 0 · front 검사 75.2초 · 정리 완료.** 완료 조건 충족.
권한을 적용한 환경에서 돌렸고 `.wrangler` 생성 오류는 재발하지 않았다.

기본 환경의 EPERM 메커니즘은 **미해결로 남긴다 — 리포 밖이고 프로젝트를 막지 않는다.**
재발하면 러너가 이제 어디서 막혔는지 이름을 말한다(위 배제 목록부터 보면 된다).

**이 티켓이 실제로 남긴 것**(원인은 리포 밖이었지만 빈손이 아니다):
- `npm run verify`가 **처음으로 exit 0** — 성공한 실행이 exit 1이던 것을 고쳤다.
  Codex가 세 번 숫자를 못 낸 가장 큰 이유였을 것이다
- 러너 안의 **상한 없는 대기가 사라졌다**(헬스 `fetch`·시드) — 15분 침묵이 30초 진단이 됐다
- 실패 시 **wrangler 출력과 임시 폴더를 남긴다** — 이번 원인을 잡은 것이 그 진단이다
- 오진 셋이 걷혔다: 마이그레이션 느림 · 파이프 막힘 · 안전망 부족

### T-07 — `ai_used`는 "모델을 불렀는가"다 (2026-07-31)

T-04에서 올린 구멍을 Cowork가 받아 발행했고, 그대로 고쳤다. **통제 ③이 지키는 것은 판정 수가
아니라 지출**이므로 세는 지점도 돈이 나가는 지점이어야 한다 — 요청이 나갔으면 1이다.
전엔 타임아웃·제공자 실패가 0이라 **8초 타임아웃이 반복되는 밤엔 상한이 사실상 없었다.**

가르는 선은 `ApiError.status`로 그었다. 키 확인이 `callModel` **안에도** 있어서(503) catch에서
그 경계를 되짚는다: 503이면 요청이 안 나갔으니 0, 그 밖(502·fetch 거절·타임아웃·파싱 실패)은 1.
키는 호출 앞에서 이미 걸러지므로 503이 catch까지 오는 건 그 사이 키가 지워진 경우뿐이다.

**검사가 옛 구현에서 빨간불이 되는지 직접 확인했다** — `stay3`를 옛 의미로 되돌려 돌리니
정확히 셋만 실패(234/3). 티켓이 "구현을 안 고쳐도 녹색인 검사는 이 티켓을 지키지 못한다"고
못 박았으므로 그 증명이 필요했다.

**검사 위치가 불변성에 걸렸다.** 처음엔 상한 검사 뒤에 두고 `cap-uuid-%` 행을 지워 상한을
비우려 했는데 `guard_events`는 **삭제가 트리거로 막혀 있다**(개입 이력 영구 보존). 그래서
T-07 블록을 상한을 채우기 **전으로** 옮겼다 — 설계가 검사 순서를 강제한 셈이다.

**타임아웃 분기만 검사에 없다.** `AI_TIMEOUT_MS`가 8초라 매 smoke에 8초가 붙는다. 같은
`stay3(..., 1)` 결정을 공유하지만 **타임아웃만 별도로 회귀할 여지는 남는다** — 8초를 감수하거나
상수를 주입 가능하게 바꾸는 것이 대안이고, 판단이 필요해 적어 둔다.

### T-04 — 기기가 Level 4 검증을 부른다 (2026-07-31)

**위임 금지**(Guard 발동 경로) — Claude Code 직접. **ADR-024의 루프가 닫혔다**:
T-03이 놓은 `/api/guard/verify`를 기기가 실제로 부르고, **승인일 때만 Level 4로 간다.**

```
fire(level=4) → fireLevel=3으로 즉시 발동(네트워크 0·지연 0)
             → 백그라운드 verify(6초)
                  승인   → 기록 level=4 + 떠 있는 화면을 Level 4로
                  그 외  → Level 3 그대로 (fail-closed)
```

- **격상은 살아 있는 화면 참조로** 한다(`WeakReference`). 배경에서 `startActivity`를 부르면
  ① 오버레이 권한이 없어 못 띄웠던 상황에도 없던 화면이 뜨고 ② Android 10+ 백그라운드 액티비티
  시작 제한에 걸린다. 화면이 없으면 **아무 일도 안 일어나는 것이 맞다**
- **기록은 3으로 넣고 승인되면 4로 고친다**(`amendFire`). 로컬 기록은 발동 즉시여야 하고(ADR-023)
  그 시점엔 판정이 없다. 6초 안이라 항목은 거의 항상 아직 큐에 있다
- `ai_used`는 **서버 값을 그대로** 싣는다 — 상한의 근거가 한 곳이어야 한다.
  `ai_verdict`: `ai`·`cache`→approve/deny · `cap`·`timeout`·`error`→`unavailable` · `off`→비움(결정론 복귀)
- **마찰 진입 후에는 격상하지 않는다.** 사유를 쓰는 중에 대기가 60→180초로 늘면 배신이다
- 기기 타임아웃 6초 < 서버 8초 — 서버가 8초를 다 쓰는 밤이면 격상을 기다릴 이유가 없다
- `overrideSilentAtL4`가 L4에서만 무음을 넘으므로 격상 시 플레이어를 다시 시작한다

**2026-08-03 폰 실측으로 닫혔다.** 확인 절차 전 항목과 `ai_used`·`ai_verdict` 전달까지 통과했다.
Kotlin 검사 러너는 없지만 `:app:compileReleaseKotlin`과 실제 발동 경로를 모두 확인했다.

⚠️ **서버 쪽 구멍 하나를 발견했다(범위 밖 — Cowork 판단).** `verifyLevel4`의 `stay3()`가
`timeout`에도 `ai_used: 0`을 돌려주는데, **타임아웃은 모델 호출이 이미 나간 뒤**일 수 있다.
돈은 나갔는데 일일 상한(통제 ③)에 안 세어진다 — 8초 타임아웃이 반복되는 밤이면 상한이 사실상 없다.
기기는 서버 값을 그대로 싣는 쪽이 맞고 서버 수정은 이 티켓의 금지 사항이라 손대지 않았다.

### T-05 — 보호 규칙 UI (2026-07-30)

Codex 구현 · Claude Code 검토. **경로 A(시각 예약)가 화면에서 걸린다** — 지금까지 콘솔로만 됐다.
설계 §6.1의 "09:00 시험 → 01:30 취침" 시나리오가 실사용에서 처음 가능해진다.
서버(`PUT /api/events/:id/protect`)는 이미 완성돼 있어 **smoke 무변경**.
**2026-08-03 폰에서 `G.sync()` → `G.listAlarms()`까지 통과해 닫혔다.**

**직접 할지 넘길지가 갈렸다.** 프런트가 `guard.ts`의 데드라인 역산식을 다시 구현하는 티켓이라
두 구현이 갈라지면 UI는 "01:30"이라 쓰고 알람은 다른 시각에 울린다 — 새벽 실패다.
넘긴 근거: **서버 식은 `smoke.ts:445`가 간격 450분으로 못 박아 뒀고**(바뀌면 빨간불),
`protect_from` 검증이 갈라져도 실패가 즉시 보인다. 남는 건 확정된 배선 → §4의 Codex 자리.

**티켓에 §범위가 없었다** — `AGENT-CHAIN.md` §5의 필수 항목이고 Codex에게는 유일한 경계선이다.
새 결정이 아니라 §쓸 API·§화면에서 나오는 목록이라 분해에서 채웠다(§3의 '분해').

분해에서 못 박은 함정 셋을 **전부 지켰다**: 종일 일정 09:00 폴백(`guard.ts:66`) ·
`protect_from` 정규식을 `events.ts:66`에서 글자 그대로 · 미리보기는 `protect_from`이 아니라 데드라인.

**Codex의 좋은 판단 하나** — 저장 시 `unchanged`(제목·날짜·시각 무변경이면 `PATCH` 건너뜀)를 뒀다.
없으면 **마감된 날의 일정에 보호를 걸 때 `trg_events_frozen_upd`가 409를 던진다.** 보호 규칙은
'계획'이라 마감된 날에도 붙어야 하고(§6.2), 서버가 `stSetProtect`를 분리한 성질이 프런트에서도 유지됐다.

**검토에서 고친 것 둘.**
1. **CSS 회귀 — 검사가 못 잡는 종류.** 날짜 팝업 일정 제목이 `span.en` → `button`으로 바뀌며
   `.evrow .en`의 `flex:1`·`14px`·`600`을 잃었다(전역 `button` 리셋이 `font:inherit`은 주므로 글꼴은 무사).
   굵기가 보통으로 내려가고 `flex:1`이 없어 `×`가 오른쪽으로 안 밀린다 → 되살렸다.
   **스타일을 보는 검사가 없어 폰에서 눈으로 볼 항목이었다.**
2. **검사 3건 추가 · 1건 강화**(190→193): 완료 조건 4번(잘못된 `protect_from` 차단)이 아예 미검사 ·
   **종일 09:00 폴백 미검사**(가장 큰 어긋남 지점인데 코드만 맞았다) → 미리보기가 **01:30**인지 본다.
   설계 §6.1 예시 그대로라 서버 `smoke.ts:445`와 **한 예시에 묶인다** ·
   데드라인 검사는 `08:00`만 봤는데 그러면 **`prep`을 빼먹은 식도 통과한다** → `07:00`도 함께 박았다.

**부수 효과**: 이 티켓이 **일정 제목·시각 편집 경로를 새로 열었다**(전엔 추가만). 보호를 기존 일정에
걸려면 그 일정을 열어야 하므로 불가피하다. `PATCH /api/events/:id`가 UI에서 처음 닿는다 —
마감된 날의 제목 수정은 409가 뜬다(정상).

### T-09 — Goals 디데이 + 하단 바 Me 분리 (2026-07-31)

Codex 구현 · 독립 검토 통과. **Me Reinforcement Plan Phase 1(P1-c)이 닫혔다.** 서버·마이그레이션은
바꾸지 않고 기존 Life Model·기간 API를 화면에 연결했다.

- Goals 목록·추가·수정·삭제. `data` 필드는 `lm_schema` 응답으로 조립하고, `period_id`만 자유 문자열 대신
  `Api.periods()` 선택으로 받는다. 목표명은 `lm_item.title`이라 스키마 `data` 필드 밖의 고정 입력이다.
- 디데이는 저장하지 않는다. `kind='constraint'`이고 `dday_label`이 있을 때만 `end_date - S.today.date`를
  렌더 시 계산한다. 미래 `D-N` · 당일 `D-DAY` · 과거 `D+N`; 일반 기간·빈 라벨은 표시하지 않는다.
- 하단 바는 Me 버튼에 구분선용 `.nav-me-tab`만 붙였다. 탭 전환·스와이프·`#nav-dot` 로직은 무변경.
- 검사는 스키마 필드 증감·기간 `end_date` 변동·일반 기간/빈 라벨·CRUD를 실제 경로로 확인한다.
  특히 `S.today.date='2001-01-15'` 센티널로 기기 날짜를 잘못 쓰는 구현이 통과하지 못하게 했다.
- T-09 단독 증가분은 front 193→210(17건). `docs/api-surface.md`는 기존 API 시그니처가 그대로라 재생성하지 않았다.
- **T-08은 ADR-019 충돌로 불합격.** UI와 검사 4건은 로컬 stash
  `T-08 mode UI (불합격, ADR-019 충돌)`에 보관하고 작업 트리에서 제거했다. 커밋·push 대상이 아니다.
- **폰 실측(2026-08-03)**: Goals 빈 상태·추가/목록, 일반 기간 연결 시 디데이 미표시, constraint 기간의
  실제 디데이, 하단 바 Me 구분선·탭 스와이프까지 확인해 닫혔다.

### T-11·T-12 — 실측·날짜 회귀 닫힘 (2026-08-03)

- **T-11** 수락 재확인·무음 존중은 `G.state()` 권한 넷이 `true`인 상태에서 확인 절차 전 항목을 통과했다.
- **T-12** 날짜 검사는 고정 `S.today.date`로 30일 달·윤년 2월을 각각 통과했고 검사 수는 front 210으로 유지됐다.

### T-13 — 릴리스 APK 서명 불일치 (2026-08-04)

`adb install -r`이 `INSTALL_FAILED_UPDATE_INCOMPATIBLE`로 막혀 앱을 삭제 후 재설치했고,
권한과 `GuardAlarmStore`·`GuardSettings`·`GuardActivityLog`가 초기화됐다.

- 진단 당시 release APK·설정된 keystore·재설치된 기기 앱의 SHA-256은 모두
  `C1:D8:B9:E6:48:33:E9:A4:60:47:81:28:A2:4C:A5:64:82:76:A9:63:85:2B:FD:2D:E0:46:88:19:11:9C:10:A6`로 같았다.
- 07-29에 `keystore password was incorrect`로 release 빌드가 실패한 사고가 있었다. 그 전후에
  디버그 키 APK가 설치됐을 가능성이 가장 설명력이 높지만, 삭제 전 APK 지문이 사라져 확정할 수 없다.
- `assembleRelease`는 이제 생성된 APK의 실제 인증서를 `apksigner`로 읽어 keystore 인증서와 비교하고,
  같을 때만 SHA-256 지문을 출력한 뒤 성공한다. keystore·alias는 바꾸지 않았다.
- 새 빌드에서 `adb install -r`이 삭제 없이 성공했다.

### T-10 — 모드 하향에 마찰을 붙였다 (0015, 2026-08-05)

**위임 금지**(마이그레이션 · Guard 발동 경로의 전제 조건) — Claude Code 직접.
ADR-019 부수 규칙 1·2가 정해 놓고 **구현되지 않은 채 남아 있던** 구멍이다:
`setMode`는 "존재하는 키인가 → 교체"가 전부였고, **새벽 01:30에 coach → secretary로 내리면
Guard가 통째로 조용해졌다.** 판정 기준은 만들지 않고 **ADR-027을 그대로 옮겼다.**

- **하향 = 강도 파라미터 다섯 중 하나라도 약해지는 것.** `STRENGTH_DIR`이 방향을 들고 있고
  **`risk_threshold`만 `-1`이다** — 문턱이라 높아지면 약함. `ai_daily_cap`(지출 통제 · ADR-024)과
  `sort`(표시 순서)는 판정에서 뺐다.
- **보호 구간 판정을 새로 짜지 않았다** — `protectingNow`가 `schedule()`을 그대로 부르고
  `[protect_from, start]`에 `t.now`가 드는지만 본다. 데드라인 역산식은 여전히 한 벌이다.
- 하향은 보호 중 **409**, 밖에서도 사유 없으면 **400**. **상향·동일은 그대로 자유**다(부수 규칙 1).
  대기 60초는 서버가 걸지 않는다(ADR-027 ③ — 서버는 60초가 흘렀는지 알 수 없다).
- `me_history` 기록은 **방향과 무관하게** 남긴다. ADR-027 ⑤가 '교체 + 기록'을 한 단계로 묶었고,
  교체는 상향에서도 일어나기 때문이다. 사유가 붙는 쪽만 하향이다.
- **프런트는 건드리지 않았다**(T-08′이 따로 받는다). front 223 무변경.

**덫을 변이로 확인했다.** 완료 조건 5번은 "구현을 잘못했다면 빨간불이 되는가"를 실제로 돌려서 봤다:

| 변이 | 결과 |
|---|---|
| `risk_threshold`를 `+1`로 (전부 "낮아지면 약함") | **5번 빨간불** — 260 → 250/10 |
| `ai_daily_cap: 1`을 판정에 추가 | **6번만 빨간불** — 260 → 259/1 |

첫 변이가 같은 블록의 뒷 검사들까지 무너뜨리면서 **무관한 AI 상한 검사 5건이 함께 빨간불**이 됐다
(활성 모드가 엉뚱한 곳에 멈춘 채 뒤 블록으로 넘어갔다). 정리를 **검사 결과와 무관하게** 되돌리도록
고쳐 실패를 이 블록 안에 가뒀다 — 원인이 흐려지면 다음 회귀 때 진단이 두 배로 든다.

**옛 동작을 검사하던 줄 하나를 고쳤다.** 기존 `(3) 모드` 블록의 `PUT {key:"secretary"}`는 이제
하향이라 400이 된다. 'Level 2 상한'을 보는 자리이므로 사유를 실어 통과시켰고, 마찰 자체는 새 `(3b)`가 본다.

**✅ 검토 합격 — 티켓 닫힘 (2026-08-05).** 검토 세션이 보고 수치를 믿지 않고 **변이를 직접 재현**했다
(250/10, 첫 실패가 5번). 함께 확인된 것: 역산 상수·식이 `schedule()` 안 한 곳뿐 ·
`docs/schema-current.sql`이 독립 재생성 결과와 **바이트 동일**(손질이 아니라 진짜 재덤프) ·
409·400이 `if (down)` 안에만 있어 상향 경로에 마찰이 하나도 없음 · `0015`가 smoke 스키마 목록에 있음.

**상향에도 기록을 남기는 판단은 Cowork가 승인했다** — ADR-027 §구현 명세에
"**⑤는 방향과 무관하게 실행된다**"가 명시됐다(하향만 남기면 이력이 '내림·내림'으로만 이어져
사이에 올린 것이 안 보인다). 구현은 개정문 그대로다 — `reason`만 하향에 붙는다.

⚠️ 문서 어긋남 하나(구현 결함 아님, Cowork 몫): ADR-027 의사코드는 `setMode(env, key, reason?)`인데
실제는 보호 구간 판정에 `t`가 필요해 `setMode(env, t, key, reason?)`다. `t`는 이 리포의 공통 인자다.

### T-17 — 침범 셀 흐리게 · 터치 피드백 (2026-08-05)

Codex 구현 · 검토 통과. 서로 무관한 시각 항목 둘. **프런트뿐이고 서버·마이그레이션 무변경.**

- 캘린더의 지난/다음 달 칸(`cal-dim-cell`)을 흐리게 하되 **`pointer-events`는 건드리지 않았다** —
  흐린 칸도 눌러서 그 날짜를 연다. 오버레이가 클릭을 먹으면 침범 셀이 죽은 칸이 된다.
- 누른 자리에 `press-feedback-on`. **가로로 움직이면 해제한다** — `:active`로 두면 스와이프
  중에도 눌린 채로 남아 "끌고 있는데 눌려 있다"가 된다. 검사 4번이 정확히 그것을 본다.
- 색은 `--press`·`--cal-dim` 변수 + 다크 짝 양쪽(함정 5). 제스처 상수(`AXIS_LOCK`·`FLICK_V`·
  `dragBlockUntil`)는 HEAD와 동일하다 — 시각 티켓이 제스처를 건드리면 회귀가 조용히 섞인다.
- **front 213→217**(4건). 검사 4번 변이(해제 제거 → `:active`와 같은 유지 동작)는 **216 · 실패 1**,
  유일한 실패가 `가로 이동 시 press 해제`였다.
- ⚠️ **시각 3항목은 폰 실측 대기**(배포는 08-06에 함께 됐다). 곁일로 `test/device.mjs`의 adb 데몬
  기동을 고쳤으나 그날은 폰이 `unauthorized`라 CDP 왕복까지는 못 갔다 — T-14 `a49600b`가 이어받았다.

### T-18 — Me 변경 이력 축약 · Guard 메모리 조회 (2026-08-05)

Codex 구현 · 검토 통과. `BACKLOG-0803.md` 7·12번. 목표 둘이 서로 무관하다.
**프런트뿐 — `src/`·`migrations/`·`public/api.js` diff가 전부 0이다.**

- **Me 변경 이력**을 최근 5건으로 접고, 펼치면 응답 전부를 그린다. 다 펼쳐져 화면이 길던 것.
- **Guard 메모리** — 개입 이력을 볼 화면이 없었다. 기존 `Api.guardEvents()` 조회만 쓰는
  **읽기 전용 목록**이다. 검토가 렌더 DOM의 버튼·입력·링크·폼·인라인 핸들러가 **0개**임을 확인했다 —
  개입 이력은 고칠 수 없다(§1.3)는 성질이 화면에서도 지켜져야 한다.
- **`fired_at`을 재계산하지 않는다** — 서버 문자열의 날짜·시각 자리만 자른다. `Date` 재파싱이
  들어가면 기기가 UTC로 보낸 시각이 또 한 번 흔들린다(§물린 것 — 기기가 UTC로 보낸다).
  `+14:00` 센티널에서도 01:30이 그대로 표시되는 것으로 확인했다.
- **front 217→223**(6건). 검사 4번 변이(`guardCauseLabel`을 알려진 전체 문자열 표 + 미등록값 빈칸으로)는
  **222 · 실패 1**, 유일한 실패가 `모르는 cause도 접두사 번역 또는 원문으로 남는다`였다.
- 신규 클래스 13개 전부 `hist-`·`gmem-` 접두사(전역 충돌 네 번째를 안 만들었다).

### T-19 — 서버가 하향 판정을 응답에 싣는다 (2026-08-05)

Codex 구현(조건부 위임) · 검토 통과. **T-08′이 여기서 막혀 있었다.**
화면은 PUT을 보내기 *전에* 하향인지 알아야 대기를 걸지 말지 정하는데, 서버 판정은 PUT 뒤에만 나왔다.
프런트가 다시 계산하는 것은 ADR-027 위반이므로 **서버가 조회 응답에 실어 준다.**

- `GET /api/guard/modes`에 `modes[].downgrade`(활성 모드 기준)와 `protecting`(보호 중이면
  `{title, start, until}`, 아니면 `null`)을 **조회 시 계산**해 싣는다. 파생은 저장하지 않는다(원칙 4).
- **사본이 늘지 않았다** — `modes()`가 기존 `isDowngrade`·`protectingNow`를 그대로 부른다.
  리포 전수에서 `STRENGTH_DIR`·`isDowngrade` 정의와 `[protect_from, start]` 비교는 `guard.ts` 한 곳뿐.
- `protecting.start`는 `schedule`의 `protect_from`(보호 진입), `until`은 실제 차단이 풀리는
  `start`(일정 시각)다. **`guard_modes`에 컬럼이 늘지 않았다** — 조회 응답에만 있다.
- **smoke 260→267**(7건). 검사 6번 변이(라우트가 요청 본문의 `downgrade`를 넘기고 `setMode`가 그걸
  서버 판정보다 우선)는 **266 · 실패 1**, 유일한 실패가 `요청의 downgrade=false를 믿지 않고 하향 차단`.
  **판정을 클라이언트가 위조할 수 없다는 것이 이 티켓의 핵심**이고 그 검사가 비어 있지 않다.
- **후속 `5687455`** — `protecting`의 시각을 로컬 ISO로 정규화(`normalizeIso`). 서버가 UTC 표기로
  내보내면 화면이 "8/7 00:00 해제"라고 읽는다(실제는 09:00). **smoke 267→268**(1건).
  이 커밋은 **T-08′의 §선행 조각으로 들어왔다** — 화면이 `until`을 그대로 쓰려면 먼저 필요했다.
  원장에서는 T-08′ 줄이 이 1건을 안고 있다(같은 것을 두 줄에 세지 않는다).

### T-08′ — 모드 화면: 왜 막혔는지가 보인다 (2026-08-05)

Codex 구현 · 검토 통과. T-10이 세운 서버 계약을 화면이 처음으로 안다.
전엔 폰에서 모드를 내리면 400·409가 **정체불명의 실패**로만 보였다.
(옛 T-08은 ADR-019 충돌로 불합격해 stash로 걷어냈다 — 이번은 그 자리를 다시 받은 것이다.)

- 하향이면 **사유 시트 + 60초 대기**를 먼저 열고, 대기가 끝나야 PUT을 보낸다.
  ADR-027 ③대로 **대기는 클라이언트가 센다** — 서버는 사유만 강제한다.
- **판정을 프런트가 하지 않는다** — `GET`이 준 `downgrade`·`protecting`만 쓴다(T-19가 실어 준 값).
  `mode.max_level`은 **표시용**이고 판정에 안 들어간다(검토가 확인).
- **상향 경로에 사유·대기가 하나도 없다** — `downgrade=false`면 `reason` 없이 즉시 PUT이고
  시트·타이머를 아예 열지 않는다(부수 규칙 1).
- **서버 문구를 삼키지 않는다** — 409는 보호 안내로, 400은 사유 안내로 **서버 메시지를 그대로** 띄운다.
  화면이 자기 문구를 지어내면 서버가 왜 막았는지가 사라진다.
- 데드라인 역산 사본이 늘지 않았다 — 해제 시각은 `protecting.until`의 자리를 줄여 표시할 뿐이다.
- **front 223→229**(6건). 검사 5번 변이(요청을 먼저 보내고 타이머만 표시)는 **228 · 실패 1**(검사 5만).
- 신규 클래스 전부 `.mode-` 접두사. ⚠️ **폰 실측 대기.**

### T-20 — 캘린더 전환이 끊긴다: 월 캐시 (2026-08-05)

Codex 구현 · 검토 통과. 기기 하네스 실측에서 출발한 첫 성능 티켓이다
(`Api.calendar` 3달치 왕복 990ms 콜드). **프런트뿐 — 서버·API·마이그레이션 무변경.**

- 월 키 **메모리 `Map` 캐시**(`calendarMonthCache`). 데이터는 ±2달(5개월)을 들고 있고
  **빠진 연속 구간만** 다시 요청한다. 스토리지를 쓰지 않는다 — `localStorage` 사용 0건.
- **쓰기 뒤에는 전체를 비운다**(`invalidateCalendarCache`). 캐시의 진짜 위험은 느린 것이 아니라
  **낡은 것**이다. 검토가 `public/` 전수로 호출부를 세어 **캐시를 안 비우는 쓰기 경로가 0곳**임을 확인했다
  (일정 추가·수정·`setProtect`·삭제 · 미루기 · 기간 CRUD · 마감).
  일정 저장·마감은 `try/finally`라 후속 요청이 실패해도 비운다.
- `dragBlockUntil` 200→**60ms**. 달을 넘긴 직후 탭이 씹히던 시간을 줄였다.
- **front 229→238**(9건). 무효화 제거 변이 둘(일정 추가·미루기)은 각각 **237 · 실패 1**로 해당 검사만 실패.
- `WEEKS_IN_GRID`(6)·`AXIS_LOCK`(20)·`FLICK_V`(0.5)·`calGen`은 무변경 — 함정 11(6주 고정)을 지켰다.

### T-21 — pane을 돌려 쓴다 (2026-08-06)

Codex 구현 · 검토 통과. **T-20의 진단이 틀렸다는 것을 실측이 보여 준 뒤의 후속**이다:
캐시가 적중해도 `renderCalendar`가 **570ms**였다(T-20 전 526ms). 왕복이 아니라 **조립**이 비쌌다.

- DOM을 **5-pane**으로 두고, 한 칸 넘길 때 기존 pane 넷을 그대로 돌려 쓴 뒤
  **가장자리 한 장만** 다시 조립한다(`app.js:1044~1053`). 같은 달 재렌더는 **0장**이다.
- **`calGen` 검사가 회전보다 먼저다** — `await` 뒤 `gen !== calGen`에서 반환한 다음에만
  `appendChild`/`insertBefore`가 실행된다. 버려질 조립이 pane 순서를 흔들면 **이후 대응이 전부 밀린다**
  (연속 스와이프에서 달과 화면이 어긋난다).
- **front 238→244**(6건). 조립 수는 `calendarPaneBuildCount`로 **직접 센다** — 눈으로 보는 대신
  숫자가 1인지 0인지 본다. 회전 인덱스 변이(오른쪽 4→3 · 왼쪽 0→1)는 각각 **243 · 실패 1**로
  **자기 방향 검사만** 실패했다(방향 사이를 전체 조립으로 격리해 반대쪽 오염을 막았다).
- 트랙 위치는 여전히 % `transform`, 손가락분만 px(함정 2). `WEEKS_IN_GRID`는 6 그대로.
- ⚠️ **폰 실측 대기 · 회전만의 비용은 아직 미측정**(§캘린더 회전 측정).

### T-22 — Guard 메모리를 일별로 접는다 (2026-08-06)

Codex 구현 · Claude Code 1차 검토 합격. **T-18의 줄 위에 한 층을 얹은 것**이지 대체가 아니다.
개입 하나가 한 줄이면 9~11월 실사용에서 한 달에 백 줄이 넘고, **스크롤로만 볼 수 있는 목록은 안 보게 된다** —
`guard_events`는 §6.5 자기 보정의 원재료라 사람이 실제로 들여다볼 수 있어야 값이 있다.

- 하루 요약(개입 수 · 반응별 개수)이 기본이고, 누르면 그날만 펼쳐진다. 최근 7일 + `[더 보기 (N일)]`.
- **덫은 날짜를 무엇으로 묶느냐였다.** `on_date`(서버가 기록 시점에 확정한 귀속일)로 묶는다 —
  `fired_at`의 앞 10자를 자르면 **01:30에 뜬 개입이 다음 날로 간다.** 하루 경계는 05:00이고
  Guard가 가장 많이 발동하는 시간대가 정확히 그 앞이라, 틀리면 **거의 전부가 하루씩 밀린다.**
  T-16이 memo에서 겪은 자리와 같고, 여기서는 서버가 이미 답을 주고 있어 **쓰기만 하면 됐다.**
- **프런트에 귀속일 계산이 없다** — 추가된 줄에 `new Date`·`getTime`·`slice(0,10)`·경계 상수가 0건이다.
  경계는 사용자 설정이라 프런트가 계산하기 시작하면 설정을 바꾼 날 조용히 갈라진다.
- 반응 표기는 T-18의 `guardReactionLabel` **한 벌**을 요약과 줄이 함께 쓴다. 요약에서 Override가
  사유 없이 접히는 것은 같은 함수의 정상 축약이다(사유는 펼친 줄에 있다).
- **front 244→249**(5건). `fired_at.slice(0,10)` 변이는 **248 · 실패 1**로 4번만 빨간불 —
  검토 세션이 직접 재현했다. 나머지 다섯은 변이에 반응하지 않는다(픽스처의 두 날짜가 같아서다).
- 새 클래스 전부 `.gday-` 접두사(구현 전 리포에 0회) · 색은 전부 변수 · `scrollIntoView` 0건.
- ⚠️ **폰 실측 대기**: 확인 절차 3번(새벽 개입이 전날에 묶임)은 **실제 `watch:bedtime` 데이터로만** 확인된다.

### T-23 — 요청당 `loadTime`은 한 번이다 (2026-08-07)

**위임 금지**(귀속일) — Claude Code 직접. 미들웨어가 만든 `TimeCtx`를 **안 넘기는 라우트가 둘** 있었고,
호출된 쪽이 스스로 `loadTime`을 다시 불렀다. **05:00 경계를 넘는 순간에만 갈라지고 아무 오류도 안 난다.**

라우트 **67개 전수**: `t`를 넘기던 39 / 안 넘기던 28(고친 뒤 기준). 시계를 다시 읽던 것은 둘이다.

- `guard.modes()` — 발행 티켓이 지목한 자리. `protectingNow`가 `t.now`로 보호 구간을 판정한다
- **`autoClose()` — 이쪽이 더 무겁다.** `t.d`로 '지난 날'을 고르고 `t.now`를 `daily`·`summaries.mech`에
  **쓴다.** 경계에서 갈라지면 **마감 기록이 잘못된 날에 남고 트리거가 그걸 동결한다** — 되돌릴 수 없다.
  cron에는 요청이 없으므로 `scheduled()`가 그 경계다: 이제 `loadTime`을 부르는 곳은 **진입 계층 둘뿐**
- 나머지 28개는 시계를 아예 안 읽는다 — `Date.now()`·인자 없는 `new Date()`·`datetime('now')` 전부 0

**검사는 응답이 아니라 소스의 호출부를 센다**(smoke `[11]`). 응답은 어느 쪽이든 글자까지 같기 때문이다.
`services/` 0건 + **진입 계층 2건(양성 대조)** + 훑은 범위, 그리고 주입한 `t`를 따르는지 보는 런타임 1건.

⚠️ **'0이다'를 세는 검사는 검사가 죽어도 0이 나온다.** 실제로 났다 — 주석 제거 정규식이
`app.use("/api/*")`의 `/*`에서 열려 18줄을 먹고 `index.ts`의 진짜 호출을 지웠는데 **0건 검사는 그대로 초록**이었고
양성 대조만 빨간불이었다. 판정을 줄 단위로 바꿔 고쳤다. **이 교훈은 `AGENT-CHAIN.md` §5에 규약으로 들어갔다.**

**남은 것(T-26으로 미룸)**: `guard.schedule()`이 `t`를 받고도 `Date.now()`로 한 번 더 읽는다.
귀속일이 아니라 '지난 발동 걸러내기' 필터라 어긋남이 밀리초뿐이지만 자리는 같은 계열이다.

### T-24 · T-25 — Guard 메모리 요일 · 캘린더 가장자리 (2026-08-07)

Codex 구현 · Claude Code 1차 검토 합격. **front 249 → 256.** 서버·CSS·마이그레이션 없음.

**T-25가 위험한 쪽이었다 — 두 `blocked`가 짝이다.** 캘린더가 화면을 덮어 탭 전환을 시작할 자리가
없던 문제를 좌우 각 10%를 탭 캐러셀에 내주는 것으로 풀었는데, `noSwipe`와 캘린더 `blocked` 중
**한쪽만 고치면 죽은 구역이 생기고 그건 폰에서만 드러난다.** 변이를 둘 다 태워 확인했다:

| 변이 | 결과 | 증상 |
|---|---|---|
| `noSwipe`만 | 254/2 | 탭도 달도 움직인다 — 서로 잡아챈다 |
| 캘린더 `blocked`만 | 254/2 | 아무것도 안 일어난다 |

**같은 두 줄이 서로 다른 이유로 빨간불**이고, `noSwipe`만 고친 쪽을 잡아내는 것은 탭이 아니라
**달 조건**이다(탭은 기대대로 바뀐다). 검사가 `tab()`과 `S.cal`을 함께 보지 않았으면 샜다.

**`clientWidth || 380` 폴백이 없으면 폭 0에서 화면 전체가 가장자리**가 되고, 그때
**오른쪽 가장자리 검사가 거짓으로 초록**이 된다(253/3 — 증상은 '가운데' 검사에서만 보인다).
폭 0 검사가 경계값 넷을 양쪽에서 눌러 그걸 막는다. 함정 3과 같은 종류다.

`AXIS_LOCK`·`FLICK_V`·`VEL_*`·`dragBlockUntil`·`setPointerCapture`는 **바뀐 줄에 하나도 없다** —
바뀐 것은 "누가 받는가"뿐이다. 두 `blocked`가 **같은 host(`.screens`)로** 가장자리를 재는 것도 확인했다.
서로 다른 사각형을 쟀으면 좌표가 어긋나는 폭만큼 '둘 다 막힌' 실틈이 생긴다.

**T-24**는 `md`→`dlabel`(요일 계산은 `dowIdx` 한 벌) + 회전 분기의 `cur` 토글 중복 제거.
공통 토글이 `if/else` 밖이고 조기 반환은 회전 분기보다 앞이라 세 경로를 다 덮는다 —
중복을 되살리면 255/1(`cur` 10 vs `CAL_PANE_COUNT` 5).
표기는 `8월 5일 수`로 **티켓 예시 `8/5 (수)`와 다르다** — `dlabel()`이 원래 그 모양이고,
예시에 맞추려면 요일 표기가 두 벌이 된다. 형식을 바꾸려면 `dlabel` 쪽 별건이다.

⚠️ **폰 실측 대기**: 가장자리 폭 10%는 시작값이고 **손가락으로만 정해진다**(T-25 확인 절차 4번).

### T-26 — `schedule()`도 넘겨받은 시계를 쓴다 (`25977c3`, 2026-08-10)

**위임 금지**(Guard 발동 경로) — Claude Code 직접. T-23이 범위 밖이라 보고만 하고 남긴 한 줄이다.
`schedule()`이 `t`를 받아 놓고 `Date.now()`를 다시 읽었다 → `Date.parse(t.now)`.
같은 함수의 `protectingNow`는 이미 그랬다 — **한 함수 안에 시계가 두 벌이면 다음에 또 "여기도 있었네"가 된다.**
`fires` 조립·데드라인 역산·`push` 조건 무변경. **smoke 272 → 279 · front 256 무변경.**

**⚠️ 티켓의 전제가 틀렸다 — 다음에 읽을 사람이 또 속지 않게 티켓 §금지도 고쳐 뒀다.**
"`src/` 전체에 `Date.now()`는 이 하나뿐"이라 했는데 **일곱 줄이었고 여섯이 남는다:**

| 자리 | 무엇 | 없앨 수 있나 |
|---|---|---|
| `lib/time.ts:90` | `loadTime(env, utcMs = Date.now())` 기본값 | **아니다** — 진입 계층이 요청당 한 번 읽는 **그 시계 자체**다 |
| `lib/ai.ts:122·125·127` | `testConnection` 응답 지연 `ms` | 아니다 — 시각이 아니라 **경과** 다 |
| `services/guard.ts:415·456` | `verifyLevel4` 모델 호출 소요 `ms` | 아니다 — 같은 이유 |

**`t.now`는 요청 *시작* 시각이라 경과 측정을 대체하지 못한다**(빼면 항상 0). 그래서 완료 조건
1번은 글자 그대로 만족될 수 없고, 의도대로 옮겨 적었다 — 없앨 것은 `Date.now()` 전부가 아니라
**"지금이 몇 시인가"로 읽는 자리**이고 그건 0이다. 남은 여섯은 **파일·줄 수 대장**으로 못 박았다.

**대장이 '0건'보다 나은 이유가 이 티켓의 핵심이다.** 기대값(`ai.ts 3 · time.ts 1 · guard.ts 2`)이
비어 있지 않으므로 **스캐너가 죽어 대장이 `[]`가 되면 그 자체로 빨간불**이다 — '0건'은 못 찾을 때
초록이고, T-23에서 실제로 그랬다. 양성 대조는 한 겹 더 뒀다: 스캐너에 **T-26이 지운 그 줄을
문자열로 직접 먹여** 잡히는지, 주석 두 모양·`Date.parse`는 안 잡는지 본다(대장과 독립).
대가는 **대장 숫자를 손으로 유지해야 한다**는 것 — `ai.ts`에 지연 측정이 하나 늘면 검사도 고친다.

**변이 셋**: 옛 `Date.now()` 275/4 · 부등호 뒤집기 275/4 · 스캐너 죽이기 276/3.
경계는 응답이 준 `deadline` 기준 ∓1분 **양쪽**이고, 각 줄이 "있어야 할 것"과 "없어야 할 것"을
함께 본다 — `−1분`은 L1이 남아서, `+1분`은 L3이 남아서 빨간불이다. **서로 다른 이유라
한쪽만 뒀으면 부등호 변이가 통과했다.** 날짜는 `addDays(D, 10)` 상대값(T-12).

✅ **2026-08-10 12:41 KST 배포됐다**(§저장소 — wrangler 확인).
⚠️ **폰 실측만 남았다**: 보호 일정의 알람 예약이 그대로 만들어지는지. **화면도 동작도 안 바뀐다** —
바뀌면 그게 회귀다(요청 소요 시간만큼의 밀리초 차이라 육안으로는 같아야 한다).

### ✅ 낮 실측 (2026-08-17) — 여섯 통과 · 둘은 창이 안 열렸다

**기록의 자리는 `APP-BUILD.md` §결정 기록 §낮 실측**이다(*"어느 조건에서 어떻게 됐다"*).
절차는 `docs/FIELD-TEST-DAY.md`. **T-30·T-31·T-32·T-33·T-34·T-35 ✅ 실측 완료.**

| 관측 | 값 · 뜻 |
|---|---|
| `intervene_sec` / `screen_on_sec` | **41 / 39** — 표식이 `ACTION_SCREEN_ON`보다 **2초 앞선다**(T-30이 `showOverLockScreen()` 앞에 뒀다. 설계대로) |
| Level 4 요청 | **격상 없음** — `13:57:40 L3 · unavailable · cap` |
| `usagePermission` | **이날 켰다** — 그 전 표본의 `top_apps`는 결측이다 |

**셋 다 그냥 통과가 아니라 각자 무언가를 말한다.**

1. **`intervene_sec > screen_on_sec`이 나온다.** 고장이 아니라 표식이 화면보다 앞서기 때문이고,
   ⚠️ `GuardActivityLog.kt:186`의 *"`screen_on_sec`의 부분집합"*이라는 주석은 **좁다** —
   두 항으로 비율을 내면 1을 넘고, 빼면 짧은 개입에서 음수가 된다. **9월이 읽을 때 걸린다.**
2. **`cap`이 이름을 갖고 남았다 — T-31의 첫 실물 성과다.** 이유가 없었으면 그 줄은
   `unavailable` 하나였고 *"낮인데 네트워크가 없었나"*로 오진했다. 관측을 늘린 값이 현금화됐다.
   대신 **T-28·T-29는 격상이 없어 검사할 창 자체가 없었다** — 통과도 실패도 아니고 밤으로 넘긴다.
   ⇒ 규칙 하나를 얻어 두 문서에 박았다: **밤 실측을 하는 날 낮에는 `testNotify({level:4})`를 쓰지 않는다**
   (상한은 귀속일 기준이고 경계가 06:00이라 **그 밤 01:30~05:00이 낮과 같은 귀속일**이다).
3. **감지 앱 항은 2026-08-17 낮이 경계다.** `top_apps`와 `foreground_app`이 같은 권한 하나에
   묶여 있어(`UsageProbe.hasPermission`) 그 전 표본의 빈 배열은 *"안 썼다"*가 아니라 *"볼 수 없었다"*다.
   **10월 가중치 유도에서 결측으로 다루지 않으면 거짓 전례가 된다** — 소급 복구 불가.

**새 backlog 하나**(`BACKLOG-0806.md`): `testNotify`의 알림 **제목이 요청 `level`을 쓴다**
(`GuardPlugin.kt:164`) — 발동은 `fireLevel`이라(`GuardNotifications.kt:151`) **Level 4 요청 →
제목엔 4, 화면·기록엔 3.** 실측에서 실제로 한 번 헷갈렸다. 응답도 발동 level을 안 준다.
**네이티브라 APK 재빌드가 필요해 다음 빌드에 묶는다** — 시험 도구의 표시 문제이고 발동·기록은 옳다.

### ✅ T-36 — 검사 날짜를 상대로 잡는다 (2026-08-14 · **최종 검토 합격 2026-08-17**)

**smoke 292/11 → 306/0 · front 291 무변경 · verify exit 0 · 소스 무변경**(`test/smoke.ts`만).

**11건이 한 원인이었다.** 일정 fixture 하나(`2026-08-15` + `-1d 00:00`)가 8/14에 보호 구간을 열자
`protecting=null` 검사가 깨지고, 이어서 **하향 차단(ADR-027)이 발동해 열 건이 딸려 죽었다.**
상대(`addDays(D, 4)`)로 바꾸자 열한 건이 한꺼번에 돌아왔다.

⚠️ **첫 시도가 다른 하나를 죽였다 — 피해야 할 시계가 둘이었다.** `D+3`으로 고치자 11건은
돌아왔는데 **T-23 검사가 죽었다**(305/1): 그 검사가 주입하는 시계(`gNow.start + 1분` = `D+2 09:01`)를
`D+3`의 보호 구간(`D+2 00:00~`)이 삼킨다. `D+4`가 실시각과 주입 시계를 **둘 다** 피한다.
**"넘겨받은 t를 따르는가"를 보는 검사가 내 fixture 선택이 그 t를 오염시킨 것을 잡았다.**

**고치는 것만으로는 재발을 못 막아 스캐너를 붙였다**(늘어난 셋): 코드 줄의 고정 날짜 0건 ·
**★스캐너가 살아 있는가**(합성 줄을 실제로 잡는다 — *"0건"*은 스캐너가 죽어도 0이다) ·
허용된 순수 함수 인자 **다섯 줄**(T-26의 자리 — 시간 블록이 사라져도 0건은 초록이다).
**허용되는 자리를 정의했다: 순수 함수에 넘기는 시각 인자**(`attributionDate`·`isoNow`·`mondayOf`) —
그것들은 시계를 안 읽어 달력이 못 따라잡고, **오히려 고정이어야** 30일 달·윤년을 검사한다(T-12).
⚠️ 합성 줄의 날짜는 이어 붙였다(`"20" + "26-08-15"`) — 그대로 적으면 **검사가 자기 자신을 잡는다.**

**전수**: `test/`·`src/`·`migrations/`·`public/` 전체에서 **서버 시계와 만나는 고정 날짜는
`smoke.ts`의 그 다섯뿐이었다.** `front.mjs`의 32건은 전부 목 데이터·순수 렌더 인자·센티널이고
(T-12가 그렇게 만들었다) `ai.ts`의 `2023-06-01`은 **API 버전 핀이라 고정이 맞다.**

**최종 검토 합격 (수정 없음) — 2026-08-17. 값은 날짜가 다르다는 데 있다.**
구현 8/14 · 검토 8/17. 원래 결함이 *"달력이 fixture를 따라잡았다"*이므로 **같은 날 재실행은
아무것도 증명하지 않는다** — 사흘 뒤에도 **306/0**인 것만이 증거다. fixture가 실제로 움직였다:
`gEvDate`가 08-18 → **08-21**, 보호 구간이 08-20 00:00~08-21 09:00으로 따라갔다.
⚠️ **`D+3`으로 뒀다면 그 구간이 08-17 00:00에 열려 이 검토 실행에서 11건이 다시 죽었다.**

**변이 셋을 검토가 직접 돌렸다** — 셋이 서로 다른 자리를 짚는다:
`const mutDate = "2026-09-01"` 한 줄을 심으면 **305/1**(줄 번호와 내용까지 말한다) ·
스캐너를 눈멀게 하면(`.filter(() => false)`) **305/1인데 죽는 것은 ★ 하나** —
**①은 초록으로 남는다**(*"0건은 스캐너가 죽어도 0이다"*가 실물로 확인됐다) ·
순수 함수 줄 하나를 지우면 **304/1**로 개수 검사가 `4`를 말한다.
**★가 없었으면 이 티켓은 아무것도 안 지키면서 영원히 초록인 검사를 남겼다.**

⚠️ **스캐너가 안 보는 자리 둘**(알고 남긴다): `smoke.ts` **자기 자신만** 읽는다(`front.mjs`는 안 본다 —
넓히면 32건의 예외 목록을 유지해야 하고 그 목록이 낡으면 스캐너가 거짓말을 한다) ·
`PURE_CLOCK` 면제가 **줄 단위**다(지금은 개수 검사가 다섯을 못 박아 막고 있다).

### ✅ T-35 — 세 번 밀린 일에 출구를 준다 (`7cd89e3`, 2026-08-14)

**최종 검토 합격 (수정 없음).** **front 283 → 291 · 실패 0 · 서버·마이그레이션·`style.css` 무변경.**
(당시 smoke 292/11은 **T-35와 무관**했고 **T-36이 306/0으로 되돌렸다**.)
**★이 먼저 멈췄고, ADR-036이 갈래를 정한 뒤 2차에서 구현했다.**

**출구는 '대기로 되돌리기'가 아니라 '멀리 미루기'다**(ADR-036) — `defer`의 2주 상한을
`defer_count ≥ CARRY_N`인 항목에서만 푼다. **서버·스키마·트리거는 한 줄도 안 바뀐다.**

```
① defer_count ≥ CARRY_N(3)   상수 한 자리 (app.js)
② 하루 한 번                 귀속일 하나를 localStorage에 — **띄우는 순간** 쓴다
③ 가장 많이 밀린 하나만       오늘 목록 + 재배정 대기를 함께 본다 · 동점은 id가 이른 것
```

**★ 상한 해제가 새지 않는 것이 이 티켓의 짝이다.** `far`가 실리는 자리는 팝업의
'멀리 미룬다' 하나뿐이고 `startPick`이 매번 `S.pick`을 새로 만든다.
**변이가 그 자리를 정확히 짚었다**: 상한을 어디서나 풀면(`max: null`) **288/3 — ⑤는 초록인 채
★와 기존 캘린더 검사 둘이 죽는다.** ⑤만 있었으면 *"모든 미루기에서 상한을 없앤 구현"*이 통과한다.
(기존 검사 둘이 함께 죽은 것도 소득이다 — **2주 상한은 이미 다른 검사가 지키고 있었다.**)

**"항상 안 뜬다"가 이 기능의 조용한 실패다.** `filter(() => false)` 변이는 **286/5**인데
**①(안 뜬다)은 초록**이다 — 안 뜨는 것이 기본 상태라서다. 짝인 ②가 잡는다.
⚠️ 그 변이가 처음엔 **러너를 죽였다**(⑤가 팝업을 전제로 `.click()` 후 `S.pick.far`를 읽어 TypeError).
`S.pick ? {...} : null`을 JSON으로 받아 **던지지 않고 빨간불이 되게** 고쳤다 — T-06이 없앤 실패 모양이다.

**부수 효과**: 시트로 만들어서 **T-34의 뒤로가기가 이 팝업을 닫는다**(모달이었으면 안 닫혔다).

#### 1차에서 멈춘 이유 — ADR-036의 재료

티켓이 *"대기로 되돌리는 경로가 있는지 먼저 확인하고, 없으면 멈추고 보고하라"*고
★로 걸어 뒀고, **그 장치가 일했다.** 확인 결과는 *"라우트가 없다"*보다 **한 겹 깊다.**

**되돌아오는 간선이 그래프에 없다.** 대기→예정은 `schedule`, 예정→예정은 `defer`,
예정→끝은 `complete`·`cancel`·`delete`. **예정→대기는 없다.** `scheduleTask`가 그것을
글자로 적어 뒀다(`services/tasks.ts:117`): *"이미 예정이 있는 task예요 — 이동은 미루기(defer)로"*.
예정을 지우는 SQL은 둘뿐이고(`db/index.ts:444`·`:451`) 호출부는 **삭제와 취소 둘뿐**이다.

**그리고 라우트를 더해도 안 된다 — "대기"의 정의가 막는다.**

```sql
is_waiting   NOT EXISTS (SELECT 1 FROM schedule_entries …)   -- 0008:37 · 하나도 없어야 한다
defer_count  MAX(COUNT(schedule_entries) - 1, 0)             -- 0008 · 바로 그 항목들의 수
```

⇒ **`defer_count ≥ 3`이면 항목이 넷 이상**이고, 대기로 만들려면 넷을 다 지워야 하는데
**지우는 순간 `defer_count`가 0이 된다** — 팝업을 띄운 조건 자체가 증발하고,
그 항목들이 곧 **Missed 기록**이다(1.2). ⇒ 게다가 **대부분 지울 수 없다**:
`trg_entries_frozen_del`(`0001:304`)이 마감된 날의 항목 삭제를 ABORT한다(함정 6).
**세 번 미뤘다는 건 그 날들이 마감됐다는 뜻이라, 지워야 할 것이 정확히 얼어 있는 것이다.**
열린 것만 지우면 `is_waiting`은 그대로 0 — **티켓 §완료 조건 5번이 통과 가능한 구현이 없다.**

**21일 시계도 결정이다.** `wait_anchor_at`은 생성 시각이라(`0001:81`) 그대로 두면 되돌아온 일이
**"대기 45일째"**로 들어가고(`WAIT_LIMIT = 21`), 옮기면 `trg_wait_ext_log`가
`wait_extensions`에 행을 남겨 **연장한 적 없는데 연장 이력이 생긴다.**

**⇒ ADR급이라 멈추고 올렸다**(`AGENT-CHAIN.md` §7). 갈래 셋을 올렸고 이 층의 견해는 B였다:
A `is_waiting` 정의 변경 · B 대기를 셋째 상태로 · C 선택지 교체.

**Cowork는 넷째를 골랐다 — ADR-036.** *"간선을 만들지 않는다."* 대기와 *"세 번 진 일"*은
**다른 상태**이고(대기는 부담이 0, 세 번 미룬 일은 이력이 있다) **스키마가 세 겹으로 저항한 것이 그 신호**다.
B는 *"21일 시계가 두 뜻을 갖는다"*로 기각됐고 **실사용이 요구할 때** 하기로 남았다.
**9월 1일 전에 원장 구조를 흔들지 않는다**는 것이 결정의 무게중심이다.

### ✅ T-34 — 뒤로가기가 맨 위 하나만 닫는다 (`e469b42`, 2026-08-11)

**최종 검토 합격 (수정 없음).** **front 277 → 283 · smoke 303 무변경 · `assembleRelease` 통과 ·
`cap sync` 성공.** `BACKLOG-0806` 5번(7월부터 열려 있던 것)을 닫았다.
**이번 APK에 묶였다** — 설치 전에 끝나서 다음 유선 연결까지 안 기다린다.

**웹에 그 이벤트를 받는 자리가 하나도 없었다** — `public/`에 `backButton`·`popstate` 0건이고
`@capacitor/app`도 설치돼 있지 않았다. 시트가 열려 있어도 뒤로가기를 누르면 앱이 그냥 닫혔다.

**판단과 리스너를 나눴다.** `handleBack()`이 순수 함수라 `front.mjs`가 직접 부른다 —
리스너 안에 넣었으면 jsdom엔 Capacitor가 없어 **검사가 통째로 0**이 됐다(T-33의 그 자리).
'맨 위' 판정은 새로 정하지 않고 **`syncOverlay`와 같은 기준(DOM 순서)**을 쓴다.

⚠️ **`exitApp()`이 없으면 사용자가 갇힌다.** Capacitor는 `backButton` 리스너를 달면
**기본 동작을 꺼 버린다** — 티켓의 *"기본 동작에 맡긴다"*를 글자 그대로(= 아무것도 안 함) 짜면
**앱이 안 닫힌다.** `handleBack()`이 `null`을 주면 `exitApp()`을 부른다.

**변이가 그 짝을 증명한다**: ④가 `null` 대신 `"tab"`을 주면 **282/1 — 딱 ⑤만 죽는다.**
①~④는 전부 초록이므로, **⑤가 없었으면 "앱을 영영 못 나가는 구현"이 통과한다.**

**`GuardAlertActivity`는 무변경이다(전수).** `git diff -- .../guard/`가 비어 있다.
그쪽의 뒤로가기 차단은 ADR-026의 Override 마찰이라 막는 것이 맞고, **둘은 다른 액티비티다.**
`android/`에서 바뀐 것은 `cap sync`가 만드는 생성 파일 둘뿐이다.
`MainActivity.java`의 `registerPlugin(GuardPlugin.class)`도 소스 등록이라 무사하다 —
sync 로그의 "Found 1 Capacitor plugin"은 **npm 패키지만 센 것**이다.

⚠️ **웹 검사가 못 보는 것 둘**: 리스너가 실제 하드웨어 키에 걸리는지 · 개입 화면에서 여전히
막히는지. §확인 절차의 마지막 두 줄이고 **반대 방향이라 한쪽만으로는 부족하다.**
⚠️ **모달은 대상이 아니다.** `handleBack`은 `.sheet`만 본다(티켓의 순서 그대로).
넣을지는 티켓이 정하지 않아 범위를 안 넓혔다.

**최종 검토가 확인한 것**(2026-08-11 · 수정 없음): '위'를 새로 정하지 않고 `syncOverlay`와 같은
DOM 순서를 쓴 것 · 닫기 경로가 `closeSheet`·`cancelPick`·`switchTab` 셋뿐인 것(`closeAll` 안 부름) ·
`guard/` 17개 파일 diff 0 · 리스너가 **`booted` 가드 안**에서 한 번만 걸리는 것(함정 4).
남은 것은 실측 두 줄뿐이고 **웹 검사가 볼 수 있는 종류가 아니다.**

**티켓이 틀렸고 담당이 그것을 찾았다 — 그 발견이 `AGENT-CHAIN.md` §5에 한 줄로 올라갔다.**
*"'아무것도 안 한다'가 '원래대로 둔다'와 같지 않을 수 있다."* 티켓의 *"기본 동작에 맡긴다"*는
Capacitor에서 성립하지 않는다(리스너를 다는 순간 기본 동작이 꺼진다) — 글자 그대로 짰으면
**§금지 2번이 금지한 바로 그것, 앱에 갇히는 구현**이 나왔다. **플랫폼 전제도 확인해야 하는 전제다.**

### ✅ T-33 — 루프의 시작점을 검사가 지킨다 (`25a44c6`, 2026-08-11)

**최종 검토 합격 (수정 하나).** **front 271 → 277 · smoke 303 무변경 · 웹만 — APK와 무관하다.**

**검토가 잡은 것 — 한 함수 안에서 상태와 화면이 갈렸다.** `send()`가 확정 직후 `set()`을 안 지나고
`display`만 꺼서 그 순간 `data-state="ask"`인데 화면은 안 보였다. **이 티켓이 없애려던 어긋남이
같은 함수 안에 있었다.** `set("none")`으로 고쳤다 — **검사는 못 잡는 종류다**(확정 후를 `await`로
건너뛴 뒤에 보므로 그 창을 지나 있다). 코드 읽기로만 나오는 자리다. front 277/0 그대로.
W4 전수가 `BACKLOG-0806`에 올린 것을 그대로 닫았다.

**`display:none`이 두 가지를 뜻하고 있었다** — *"물어볼 게 없다"*(정상)와 *"못 물어봤다"*(회귀).
화면에서 둘이 똑같이 보이고 **검사도 가를 수 없어서**, 카드가 안 떠도 버튼이 안 먹어도 271/0이었다.
`#td-guard`의 `data-state`에 `ask`·`none`·`error` 셋을 남긴다 — **DOM에만**(`S`에도 두면 갈라진다).

**화면 동작은 한 글자도 안 바뀐다.** `catch`가 화면을 안 막는 판단은 옳았고(Guard 없는 기기에서도
Today는 떠야 한다) **바뀐 것은 그 실패가 이름을 갖는다는 것뿐**이다.

**변이 둘이 서로 다른 자리를 짚어야 증명이 된다.** 조회가 항상 실패 **272/5**(①②③★는 죽고
**④는 산다** — 넷이 같은 것을 보는 게 아니다) · 상태를 안 가름 **275/2**(**④와 ★만** 죽는다 —
3만 사는 게 아니다). 한쪽만 돌렸으면 둘 다 통과하는 구현이 있었다.

⚠️ **첫 변이가 내 검사 하나를 떨어뜨렸다.** 버튼 확인을 `(() => { click(); return true; })()`로
썼는데 **떨어질 수가 없는 검사**였다 — 변이에서도 초록이었다. 뺐다(278 → 277).
**숫자를 하나 줄인 것이 맞다** — T-01의 "구현을 잘못했다면 이 검사가 빨간불이 되는가" 그대로다.
⚠️ **이 티켓이 지키는 것은 카드의 동작이지 `outcome`이 쌓이는지가 아니다.**
사람이 안 누르면 여전히 NULL이고 그건 검사가 볼 수 있는 것이 아니다 — 9~11월 데이터가 답한다.

### ✅ T-32 — 위험도 스냅샷에 서버 항을 얹는다 (`4889988`, 2026-08-11)

**최종 검토 합격 (수정 없음).** **smoke 293 → 303 · front 271 무변경 · 마이그레이션 없다** ·
`assembleRelease` 통과(주석만). W4 전수(아래)가 찾은 것을 같은 날 닫았다 — **9월 1일 마감 20일 전.**

**검토가 확인한 것**: `riskServerTerms`에 `t.now`·`t.d`·`Date.now()`가 **0건**(t는 offsetMin에만) ·
기기 여덟 키가 값까지 보존 · 서버 항이 최상위에 안 샘 · **`risk_score`를 읽어 분기하는 코드가
전수 0건**(쓰는 자리 하나, 읽는 자리 없음 — 구조적으로 게이트가 아니다) · §6.6 항 전수 충족.

**검토가 하나 더 적었다 — `score_trend`의 정의.** `last − avg`인데 **`avg`가 `last`를 포함한
창 평균**이라 폭이 구조적으로 줄어든다(창 7이면 실제 편차의 6/7). 틀린 값은 아니고 정의의 문제인데,
**10월에 이 정의를 모르고 계수를 읽으면 추세를 과소평가한다.** 지금 안 바꾼다(8월 표본과 갈라지고
어차피 10월에 정의째 다시 고른다) — 티켓 §검토에 적어 두는 것이 그때를 위한 조치다.

**`risk_snapshot`이 §6.6 항의 절반을 안 담고 있었다.** 담긴 것은 `현재 시각`·`foreground app` 둘뿐이고
`데드라인까지 남은 시간`·`최근 Log 활동`·`수면 추정`·`Feelings·Score 추세`가 통째로 빠져 있었다.
`risk_score`는 **항상 NULL**이었다 — 기기가 그 키를 안 보낸다.

**실수가 아니라 ADR-021의 부산물이다.** 계획을 쓸 때는 서버가 발동을 판단했으므로 그 자리에서
`logsRange`를 부를 수 있었다. ADR-021이 발동을 기기로 옮기면서 **스냅샷 생산도 같이 기기로 갔고**,
기기엔 `logs`·`feelings`·`daily`가 없어 서버 항이 사라졌다 — **그 누락이 어디에도 안 적혔다.**
ADR이 발동 위치를 옮길 때 *"그러면 스냅샷의 서버 출처 항은 어디서 오나"*를 아무도 안 물었다.

**★ `firedAt` 기준이다. `t.now`가 아니다.** `record()`는 오프라인 큐 경로도 탄다(ADR-023).
"지금"으로 조회하면 **새벽 발동의 스냅샷이 아침 값으로 채워지고**, 그 오염은 네트워크가 나쁜 밤에만
일어난다 — 즉 가장 읽고 싶은 밤에만. **T-31이 하마터면 저지를 뻔한 것과 같은 모양이다.**

**★ 이것이 규약이 됐다** — `AGENT-CHAIN.md` §5: *"판단 시점의 기록은 그 시점 이후를 담지 않는다.
기준과 상한은 다른 것이고 둘 다 필요하다."* 티켓은 **기준**만 말했고 상한은 담당이 찾았다.

**티켓이 지적하지 않은 것을 하나 더 잘랐다 — 발동 이후에 생긴 기록.** 큐가 늦게 오면 그 사이의
Log가 이미 DB에 있는데, 담으면 스냅샷이 **판단 시점에 없던 것을 안다.** `ts <= firedAt`으로 자른다.
티켓은 조회 *기준*만 적었는데 실제로는 *상한*도 필요했다 — 같은 병의 다른 얼굴이다.
(Feelings·Score는 원본에 시각이 없어 보장이 **날짜 단위**다. Log는 `ts`가 있어 초 단위로 잘랐다.)

**출처를 갈랐다.** 기기 항은 최상위 이름 그대로, 서버 항은 `server` 하나 아래. 섞으면 12월에
*"이 항은 누가 잰 것인가"*를 물을 수 없다. **더하고, 바꾸지 않는다**(T-31이 `ai_verdict`에서 택한 형태).
**스냅샷이 없으면 서버 항만으로 만들지 않는다** — 기기가 안 보낸 것과 서버가 못 찾은 것은 다른 사실이다.

**`risk_score`는 서버가 낸다 — 항 넷, 전부 임시값.** 발동이 끝난 뒤 `record()`에서 계산하므로
**구조적으로 게이트가 될 수 없고**, 그것이 ADR-021이 요구한 형태다. ②항이 T-30·T-31을 그대로 쓴다:
`screen_on_sec − intervene_sec`이라야 '사용자가 붙잡고 있던 시간'이 된다 — **그 둘이 없었으면
이 항은 Guard 자신을 점수로 세었을 것이다.** 가중치는 10월에 실제 스냅샷에서 유도한다.

**데드라인 역산을 두 벌 두지 않았다.** `schedule()` 안에 있던 것을 `protectAxis()`로 꺼내 양쪽이
같은 함수를 부른다. 검사가 `schedule()`의 `deadline`과 스냅샷의 `deadline_min`이 **분 단위로 정확히
일치**하는지 본다 — 갈라지면 즉시 빨간불이다.

⚠️ **`trg_guard_event_immutable`이 `risk_snapshot`·`risk_score`를 지킨다.** INSERT 경로에서만
채우므로 안 걸리지만, **나중에 사후 보정으로 채우려는 시도는 409가 된다** — 스냅샷은 발동 시점에
완성되어야 한다.
⚠️ **적용된 마이그레이션(0010)의 주석을 고쳤다.** D1은 파일명으로 추적하고 내용을 해시하지 않아
재적용이 없고 smoke는 처음부터 다시 적용하므로 영향은 없다. 그래도 사실이라 적어 둔다.
⚠️ **가중치가 옳은지는 검사할 것이 없다.** 숫자가 나오고 0~100 안이라는 것까지가 검사이고,
그 값의 옳고 그름은 10월에 유도하기 전엔 기준 자체가 없다.

### W4 세 항목 전수 확인 (2026-08-11) — `APP-BUILD.md` §4주 갱신

`APP-BUILD.md`가 **8/04에 멈춰** 있어 코드와 대조했다. ✅/⬜ 표기는 맞았지만 **S4.1의 `✅`가
오해를 부르고 있었다** — 위 T-32가 그것이다. 나머지 둘:

- **S4.2 outcome 확정 카드 ✅** — `app.js:455`부터 `POST /api/guard/events/:id/outcome`까지
  사슬이 끊긴 데 없고 서버 검사 3건이 있다. **그러나 프런트 검사가 0이고**, 실패 시 `catch`가
  `display:none`이라 **조용히 사라진다** — 카드를 통째로 지워도 front 271/0이다.
  `BACKLOG-0806`에 올렸다(§검사가 못 보는 것).
- **S4.3 알림함 ⬜** — `src/`·`public/`·`migrations/`·`test/`에서 `notif`로 **파일 0개**.
  테이블·라우트 셋·`api.js`·Today 배지·Me 토글·검사 전부 없다. 감축 순서 1순위.

**`APP-BUILD.md`에서 기준선 숫자를 뺐다.** `smoke 237 · front 210`이 박혀 있었는데 실제는 303/271이었다 —
숫자를 만드는 층이 못 고치는 파일에 숫자를 두면 반드시 낡는다(§규약 불일치 4번과 같은 사고, 세 번째다).

### ✅ T-31 — 9~11월이 읽을 것을 지금 늘린다 (`de40a27`, 2026-08-11)

**최종 검토 합격 (수정 없음) · 범위 밖 둘 승인.**
**smoke 283 → 293 · front 271 무변경 · `assembleRelease` 통과** · 마이그레이션 **0016**.
**순서가 강제된다**: `0016 --remote` → deploy → APK. 칼럼 없는 서버에 신버전 APK가 이유를 보내면
모르는 필드로 무시돼 **이유만 조용히 사라진다**(행은 산다). 반대 순서는 안전하다.

**검토가 확인한 것**: 캐시 `IN ('approve','deny')` 두 자리 무변경 · 양성 대조가 `approve`를 깔아
**감도를 갖는다**(담당이 자기 검사를 스스로 떨어뜨려 찾았다) · `screen_on_sec` 옆에 더하고 빼지 않음 ·
대장 셋 일치 · **함정 7**(0016이 `smoke.ts` 스키마 목록에 들어감) · `schema-current.sql` 재덤프.
**범위 밖 둘**(`GuardNotifications.kt`·`GuardEventQueue.kt`)은 승인 — 이유를 **만드는 자리**와
**싣는 자리**가 다른데 §범위가 앞만 적었다. 잇지 않으면 이유가 계산만 되고 아무 데도 안 남는다.

**★ 티켓의 전제가 틀렸고, 그대로 짰으면 기록이 사라졌다.** 티켓은 *"마이그레이션 없다 —
`ai_verdict`는 문자열이고 값만 늘어난다"*고 했는데 **닫힌 CHECK다**(`0010_guard.sql:41`).
`unavailable:timeout`을 넣으면 CHECK 위반 → `translateDbError`가 **400**(`index.ts:51`) →
`GuardEventQueue.flush()`가 400을 '재시도 무의미'로 보고 **큐에서 버린다**(`:142`).
**이유를 잃는 게 아니라 발동 행 자체가 사라진다** — 그것도 네트워크가 나쁜 밤에만,
즉 이 티켓이 관측하려는 바로 그 밤에만. **전례를 늘리려던 티켓이 전례를 지운다.**

**그래서 더하고, 빼지 않는 쪽으로 갔다**(사용자 결정). `ai_verdict`는 계속 `'unavailable'`이고
이유는 새 칼럼 `ai_unavailable_reason`(0016)에 놓는다 — `ADD COLUMN` 한 줄이라
**개입 이력 테이블을 재작성하지 않는다**(CHECK를 넓히는 쪽은 SQLite에서 테이블 전체 재작성이고
트리거 넷·인덱스 넷을 다시 세워야 한다). 티켓 §금지를 하나도 어기지 않는다 — 접두사를 버리기는커녕
값이 **글자 그대로** 그대로이고, 티켓이 걱정한 *"`=== "unavailable"` 비교가 조용히 어긋난다"*가
**아예 성립하지 않게 된다.** §②가 택한 논리를 ①에도 적용한 것이다.

**이유의 닫힌 목록**(대장은 `services/guard.ts`의 `UNAVAILABLE_REASONS` 하나 · 0016 CHECK와
`GuardVerify.kt`는 메아리이고 갈라지면 smoke가 빨간불):

```
timeout · dns · network · bad_response · no_base      기기가 못 닿았다
server_timeout · server_error · cap                    서버는 답했는데 판정이 아니었다
http_NNN                                               2xx 아닌 응답 (401과 503은 대응이 다르다)
```

**`timeout`과 `server_timeout`을 가른 것이 핵심이다** — 티켓의 네 가설 중 *"Doze가 끊었나"*와
*"서버가 늦었나"*가 바로 이 둘이고, 한 이름으로 세면 12월에 못 가른다.

**목록 밖은 버리되 행은 살린다.** 서버가 400을 던지면 CHECK에 걸리는 것과 결과가 같아진다 —
`flush()`가 발동 행을 버린다. 구버전 서버 + 신버전 APK가 실제로 그 자리다.

**②는 `snapshot()`에 `intervene_sec`을 더했고 `screen_on_sec`은 그대로다.** 빼서 저장하지 않는다
(파생 물화 금지 · 원칙 1) — 읽는 쪽이 뺀다. **개입 구간만 창 필터보다 앞에서 읽는다**:
개입은 창 밖에서 시작해 창 안까지 이어질 수 있고(소리는 3분에 멎지만 화면은 `KEEP_SCREEN_ON`),
**그게 T-30이 잡은 그 밤이다** — 뒤에 두면 가장 필요한 경우에만 못 가른다.

⚠️ **범위 밖 파일 둘을 건드렸다 — Cowork 확인 필요.** `GuardNotifications.kt`(`verify`→`attempt`) ·
`GuardEventQueue.kt`(`amendFire`에 이유 파라미터). 티켓 §범위가 `GuardVerify.kt`만 적었는데
**거기서는 기록에 닿을 수 없다** — 이유를 싣는 자리는 `GuardNotifications:246`이고 페이로드를
만드는 곳은 `amendFire`다. 안 이으면 이유가 계산만 되고 아무 데도 안 남는다(T-03 전례로 처리).
⚠️ **배포 순서가 강제된다**: `--remote` 마이그레이션 → `deploy` → APK. 칼럼 없는 서버에
신버전 APK가 보내면 키가 무시돼 이유가 그냥 사라진다. 반대 순서는 안전하다.
⚠️ **`network`가 뭉뚱그려진다** — `SocketTimeout`·`UnknownHost` 밖은 전부 여기다.
9~11월에 이 항이 크면 갈라야 하는데 그건 셋(0016·TS·Kotlin)을 함께 고치는 일이다.
**실측이 답하기 전에는 안 늘린다** — 안 쓰일 이름을 목록에 미리 남기지 않는다.

### ✅ T-30 — 감지가 Guard 자신의 개입 화면을 세지 않는다 (`bb109ff`, 2026-08-11)

**코드 검토 합격 (수정 없음, 2026-08-11) · §확인 절차 실측은 아직이다 — APK 설치 후 낮 2분.**
검토가 확인한 것: 원본에 거짓 없음(새 kind 둘) · 짝 둘 다 있음(`onCreate:94`·`onDestroy:284`) ·
`GuardWatch` 무변경(`intervene` 0건 · 임계·창·상한·`REFIRE_MS` 그대로) · 게이트 한 벌(`GuardWatch:60`).
**숫자가 이 티켓을 지키지 못한다** — Kotlin 러너가 없어 틀린 구현도 283/271이 나온다.
그래서 **§확인 절차 3번(닫은 뒤 다시 세어지는가)이 이 티켓의 유일한 변이 검사다.**
앞의 둘만 보면 *"아무것도 안 센다"*로 고쳐도 통과하고 그 고침은 감지를 통째로 죽인다.
검토가 하나 더 찾았다: 구간 플래그가 **`screen_on`을 남기는 세 자리를 전부 덮는다** —
보고는 전원 버튼 경로만 적었는데 `GuardService:119`의 서비스 재시작 부트스트랩도 함께 무시된다.

**위임 금지**(Guard 발동 경로) — Claude Code 직접. 8/11 밤 실측이 잡은 1번.
**smoke 283 · front 271 둘 다 무변경**(Kotlin만 바뀌었다) · `assembleRelease` 통과.

**고친 것은 데이터의 거짓이다.** T-28이 프롬프트의 거짓을 지웠고 여기는 같은 종류다 —
기록에는 *"새벽 3시에 20분째 화면을 보고 있었다"*고 남는데 실제로는 자고 있었다.

```
개입 화면이 FLAG_TURN_SCREEN_ON으로 화면을 켠다
  → GuardService가 ACTION_SCREEN_ON을 받아 screen_on 표본을 남긴다
  → FLAG_KEEP_SCREEN_ON이라 screen_off가 영영 안 온다
  → 20분 뒤 continuousScreenOnMin이 임계를 넘어 감지가 스스로를 발동시킨다
```

**원본에는 사실을 적고 계산이 가른다.** `screen_off`를 대신 남기는 안은 기각 —
화면은 실제로 켜져 있으므로 그건 표본에 거짓을 적는 것이고 이 티켓이 고치는 잘못 그 자체다.
대신 `GuardAlertActivity`가 뜰 때·닫힐 때 `intervene_on`/`intervene_off`를 남기고
`continuousScreenOnMin`이 그 구간을 뺀다.

**짝이 둘인 것이 핵심이다.** 뜰 때만 끊으면 `KEEP_SCREEN_ON`으로 켜진 화면에는
`ACTION_SCREEN_ON`이 **다시 오지 않으므로** `onAt`이 0에 붙박여 **진짜 사용을 영영 못 센다** —
감지가 통째로 죽는다. 닫을 때 그 시각부터 다시 세는 것이 그것을 막는다.

**순서 경합이 실재했고 나쁜 쪽이 유력했다.** 화면 켜기는 윈도 부착 시점이라
`ACTION_SCREEN_ON`이 `onCreate`보다 **뒤에** 온다 — 표식만 남기고 계산이 순진하면
`intervene_on`(0) 다음 `screen_on`이 다시 세기 시작한다. 그래서 계산에 **구간 플래그**를 뒀고
표식 순서에 의존하지 않는다. 그 플래그가 **전원 버튼 경로도 함께 맞춘다**(개입 중 껐다 켠
`screen_on`은 무시되고, 닫는 시점이 시작점이 된다 — 그때는 사용자가 실제로 반응한 것이다).

**규칙은 손대지 않았다** — `GuardWatch`의 창·임계·상한·`REFIRE_MS` 무변경.
규칙이 옳고 **규칙이 읽는 값**이 틀렸다. 개입 중 평가를 막는 별도 게이트도 안 뒀다:
시작점이 0이면 `usedMin`이 0이라 `evalInner`에서 이미 걸린다(게이트를 두 벌 두지 않는다).

⚠️ **Kotlin 러너가 없어 숫자로 보일 것이 없다 — 감추지 않는다.** 실검사는 티켓 §확인 절차이고
**낮에 2분**이면 답이 나온다(밤을 기다리지 않는다). **APK 설치가 선행**이다.
⚠️ **`onDestroy`가 안 불리면**(강제 종료) `intervening`이 참으로 남아 감지가 0을 준다.
**다음 개입이 정상으로 닫히면 회복**되고 링 버퍼에서도 결국 밀려난다. 시간 상한은
§금지의 "게이트 두 벌"에 걸리고 경로 B는 보조 입력이라(ADR-018·021) **알고 남겼다.**
⚠️ **범위 밖 관찰**: 티켓이 `risk_snapshot.screen_on_min`이 "부풀려진다"고 했는데
**그 값은 부풀려진 것이 아니다** — 개입 중에도 화면은 실제로 켜져 있었고 필드명 그대로다.
문제는 나중에 둘을 **가를 수 없다**는 것이고, 이제 `intervene_*` 표본이 있어 `recent()`로는
가른다. `snapshot()` 요약에 항을 더하는 것은 `risk_snapshot` JSON 모양을 바꾸는 일이라
(9~11월 전례가 그걸 읽는다) 하지 않았다 — Cowork 판단.

### ✅ T-29 — 화면이 Level 4를 읽고 날짜를 튼다 (`cdaf1fd`, 2026-08-10)

**최종 검토 합격 (수정 하나).** **ADR-035의 루프가 닫혔다.** T-28이 낸 `Guard.level4State()`를 화면이 읽고,
**Level 4 구간에는 오늘 날짜가 붙지 않는다.** **front 257 → 271 · smoke 282 → 283.**
Kotlin·서버 라우트·마이그레이션 무변경. 프롬프트는 이제 **사실**을 말한다(T-28이 거짓을 지운 자리).

**막지 않는다.** 적는 것도 대기에 담는 것도 내일 이후를 고르는 것도 그대로다 —
바뀌는 것은 **어느 날에 놓이는가** 하나다. 자리마다 성격이 다르다:

| 자리 | Level 4 중 | 왜 |
|---|---|---|
| `addTaskOn` | **내일로 돌린다** | 날짜가 생성과 함께 붙어 되돌릴 것이 없다 |
| `assignDate` 비-defer | **안 붙이고 다시 고르게** | 사용자가 고른 날이라 말없이 옮기지 않는다 |
| `dfx-ok` | **안 붙인다 · 시트는 남긴다** | 막고 닫으면 사유를 다시 쓰게 된다 |
| 대기 생성 | **손대지 않는다** | 날짜가 없어 오늘의 부담이 아니다(ADR-035 ①) |

**여는 자리에는 하나도 안 걸었다** — `assignDate`의 defer 분기와 `startPick` 호출부 넷은
피커·시트를 **열 뿐**이다. 붙는 자리는 확인 버튼(`dfx-ok`)이고, 시트가 떠 있는 동안
구간이 시작될 수 있다.

**안내와 판정을 나눴다.** `startPick`이 한 번 물어 `S.level4`에 담고 `pickMinMax`가 읽어
오늘 칸을 흐린다(안내). 붙는 자리 셋은 확정 직전에 **다시 묻는다**(판정).
`S.level4`는 항상 false로 먼저 두고 답이 오면 좁힌다 — **안내가 관대하고 판정이 엄하다.**
03:29에 열고 03:31에 격상되면 dim이 낡는데 그 경우가 판정에서 걸리고, 막힌 자리가
그 즉시 안내를 좁힌다. 반대로 만들면 조용히 새는 구멍이 된다.

**★ 변이 D가 이 티켓의 핵심이다.** `askLevel4`를 항상 true로 만들면 **267/4**인데,
빨간불은 **짝 셋과 fail-open뿐이고 "Level 4면 안 붙는다" 쪽은 전부 초록**이다.
짝이 없었으면 T-29가 통과하고 **낮에 오늘 칸이 통째로 막힌 앱**이 배포된다.
게이트를 하나씩 지운 변이는 268/3 · 270/1 · 268/3으로 **겹침 0**이었다.

**모르면 걸지 않는다.** 플러그인이 없으면(브라우저 PWA · 구버전 APK) 게이트가 안 걸린다.
fail-closed로 "고치지" 않는다 — ADR-024와 방향이 같다: **모르면 덜 개입한다.**
발동하지도 않은 기기에서 날짜가 튀면 그것이 §6.3의 도구 이탈이다.

**`until`은 화면에 안 넘긴다.** 카운트다운은 기다리라는 초대이고, 발동이 이어지면 창이
갱신되므로 그 시각은 애초에 약속이 아니다. 검사가 토스트에 `HH:MM` 꼴이 없는 것까지 본다.

**검토가 잡은 것 — 프롬프트가 ADR이 구별한 것을 도로 합쳤다.** 초안이 *"**새 일에** 오늘
날짜가 붙지 않는다"*라고 썼는데 **구현은 둘 다 막는다.** ADR-035 ①이 "새 일"과 "있던 부담의
이동(미루기)"을 명시적으로 갈라 놓고 둘 다 걸린다고 정했는데 프롬프트가 한쪽만 적었다 —
**T-28이 '없는 대가'를 지웠는데 T-29가 '절반짜리 대가'를 적을 뻔했다.** 같은 종류다.
고친 뒤 smoke 검사가 양쪽을 다 센다(초안대로 되돌리면 282/1).

**주석 하나의 근거가 틀렸다(행동은 그대로).** `dfx-ok`에서 시트를 안 닫는 근거를
"사유를 다시 쓰게 된다"로 적었는데 성립하지 않는다 — `to`를 못 바꾸니 결국 취소해야 하고
`openDeferSheet`가 사유를 비운다. **남는 이유는 맥락이다**: 무엇을 어디로 옮기려 했는지가
토스트 한 줄만 남기고 사라지지 않는 것. 코드는 안 바꿨다.

**비대칭 하나를 알고 남겼다(`BACKLOG-0806`).** 같은 "오늘로는 안 된다"인데 `assignDate`는
1단계(피커가 떠 있고 오늘만 흐려짐), `dfx-ok`는 3단계(취소→피커→다시 고름)다.
**새벽에 미루기가 오늘로 막히는 빈도를 몰라서** 지금 정하지 않는다 — 드물면 맥락 유지가 낫고
잦으면 단계 수가 이긴다. 밤 실측 뒤에 정한다.

⚠️ **배포 순서: 웹 먼저 · APK 나중이어도 안전하다.** 웹이 원격 로드라 순서가 뒤집혀도
`Guard.level4State`가 없으면 오늘이 그냥 붙는다. **지금 폰이 정확히 그 상태다**(APK 미설치) —
결함이 아니라 fail-open의 정의이고, 검사 ⑤가 보는 것이 그 상태다.
⚠️ **밤 실측 하나로 셋이 확인된다** — Doze 통과 · Level 4 기록(T-28) · 날짜 게이트(T-29).
**낮 확인이 더 중요하다**: 낮에 잘못 걸리면 그게 회귀다.

### ✅ T-28 — 기기가 Level 4 상태를 노출한다 · 프롬프트에서 거짓을 지운다 (`087dad1`, 2026-08-10)

**위임 금지**(Guard 발동 경로) — Claude Code 직접. **최종 검토 합격.** **smoke 279 → 282 · front 257 무변경.**
`assembleRelease` 통과(`[signing] SHA-256=C1:D8:…:10:A6` — T-13 기록과 일치). **APK는 설치 안 했다** — T-29 뒤에 한 번.

**고친 것은 거짓말이다.** `VERIFY_SYSTEM`이 Level 4를 *"신규 작업 차단까지"*라고 적어 뒀는데
**그 강제력은 구현이 없었다.** 모델이 없는 대가를 저울에 올려 판정했고, 그렇게 쌓인
`guard_events`가 §6.5가 읽을 전례다. **거짓을 지우는 것과 사실을 적는 것을 나눴다** —
지금 새 문구를 적으면 거짓말을 다른 거짓말로 바꾸는 것이다(사실은 T-29).

**격상 자리는 하나였고 킬 스위치도 같은 자리를 지난다.** `GuardVerify.upgrades`
(`approved && level >= 4`)로 전부 수렴하는데, 킬 스위치가 `off`면 서버가
`level:4 · approved:true · source:"off"`로 답하므로 결정론 복귀 경로도 참이 된다.

**`GuardLevel4.kt`를 새로 뒀다** — `GuardNotifications`가 쓰고 `GuardPlugin`이 읽는데
둘 중 하나가 소유하면 다른 하나가 남의 내부를 알게 된다. prefs 파일은 기존 `"guard"`,
키는 `level4_at` 하나, 쓰는 곳·읽는 곳이 같은 상수를 본다.
**`GuardWatch.REFIRE_MS`와 갈랐다** — 값이 같고 뜻이 다르다(감지 재발동 간격 vs Level 4 발동 주기).
한 상수로 묶으면 한쪽을 조정할 때 다른 쪽이 말없이 끌려간다.

**화면 수명으로 판정하지 않는다**(ADR-035 ③) — `live` 참조는 `finishWith()`가 지우므로
정상 경로로 화면을 나오면 이미 `null`이고, 앱에서 작업을 추가하는 것은 그 뒤다.
기준은 **격상이 승인됐다**는 사실이고, 창 30분은 Level 4의 30분 간격과 맞아 다음 발동이 이어받는다.

**검토가 ADR을 고쳤다 — 구현이 맞았다.** ADR-035 ③은 "마지막 **발동** + 30분"이었는데
구현은 "마지막 **격상 승인** + 30분"이다. 승인은 검증 왕복만큼 발동보다 뒤라서 창이
다음 발동을 몇 초 **넘겨서** 닫힌다 — **겹치는 쪽으로 어긋난다.** 발동 시각으로 재면 반대로
**다음 발동 직전에 잠깐 열리는 틈**이 생기고, 그 틈은 새벽에 한 번 열려서 재현되지 않는다.
기록 자리가 `upgrades == true` 안이었던 것이 이 성질을 자동으로 줬다 — ADR 문구를 그대로
따랐다면 `fire()` 초입에서 재야 했고 그쪽이 틀린 자리였다. **ADR을 실측에 맞춰 고쳤다.**

**주석 한 자리를 옮겼다.** 프롬프트의 내력을 적는 주석에는 "막는다"가 들어가는데,
그것을 `VERIFY_SYSTEM` **배열 안**에 두면 검사가 프롬프트로 읽는다. 배열 밖으로 뺐다 —
스캐너에 주석 제거를 시키는 대신 **볼 범위를 좁혔다**(T-23이 18줄을 먹은 그 종류를 피했다).

⚠️ **Kotlin에 검사가 없다 — 감추지 않는다.** ①②의 근거는 코드 읽기와 빌드 통과까지다.
**실검사는 밤 실측이다** — 그 밤에 Doze 통과와 Level 4 기록이 **같이** 확인된다(격상 없이는 둘 다 못 잰다).
⚠️ **서버는 배포 대기** — 프롬프트 문구만 바뀌었고 동작은 무변경이다. **T-29는 T-28이 닫힌 뒤.**

### T-27 — 가장자리 폭 0.4 · 캘린더 가로 구분 (`c3a8a83`, 2026-08-10)

Codex 구현 · Claude Code 1차 검토 **합격**. **front 256 → 257.** 서버·마이그레이션 없음.
**§보고의 기준선도 이 층이 채웠다** — `verify`로 숫자를 만드는 층은 하나이고, Codex가 이번엔
verify를 못 돌렸다. 지금까지 Codex 보고에 숫자가 들어온 것이 예외였다(`AGENT-CHAIN.md` §3).

**1부 — `SWIPE_EDGE_RATIO` 0.1 → 0.4 → **0.2**.** T-25의 10%가 좁았고(사용자 판단),
40%는 이번엔 **가운데가 답답했다** — 확인 절차 2번의 답이 그렇게 왔다. **현재 값은 0.2다**
(가장자리 각 20% · 가운데 60%). `app.js` diff는 **정확히 한 줄**이고 두 번 다 그랬다 —
`AXIS_LOCK`·`FLICK_V`·`VEL_*`·`dragBlockUntil`은 무변경.

**이 티켓의 요점은 폭이 아니라 검사가 폭을 따라가느냐였고, 0.4 → 0.2가 그 답을 실물로 받았다.**
T-25 검사가 `+190`·`+10`·`+370`·`37/39/341/343`을 박아 뒀는데, 폭만 바꾸고 검사를 두면
**경계값이 옛 폭을 가리킨 채 초록**이 된다. 전부 `ev("SWIPE_EDGE_RATIO")`에서 유도하도록
다시 썼다 — **검사가 자기 사본을 두지 않고 구현이 쓰는 그 변수를 읽는다.**
검토에서 0.25로 돌려 본 이식성이 **실제 조정에서 값을 했다: 0.4 → 0.2에 검사를 하나도
손대지 않았고 verify가 그대로 통과했다**(front 257/0 · smoke 279). 박혀 있었으면 경계값
여덟 개를 매번 손으로 고쳐야 했다.

검사가 **가운데 좌표가 정말 가운데 구간 안인지 스스로 단언**하는 두 줄을 넣은 것이 좋았다.
없으면 가운데를 눌렀는데 사실 가장자리였을 때 **"달이 안 넘어감"이 정상 동작이 되어 거짓 초록**이다.

`|| 380` 폴백 제거 변이는 **254/3**이고, **오른쪽 가장자리가 거짓으로 초록인 것도 T-25 그대로**다
(폭 0이면 전 구간이 가장자리라 '탭이 바뀐다'가 우연히 맞는다). 재작성이 감도를 잃지 않았다.

**2부 — 행 구분: 3px 배경색 띠 → 1px 선.**

| | 옛 | 새 |
|---|---|---|
| 행 총높이 | 99px | **99px (불변 — 함정 11)** |
| 셀 안쪽 | 96px | **98px (늘었다)** |
| 가른 것 | `border-top:3px solid var(--card)` | `border-top:1px solid var(--line)` |

`*{box-sizing:border-box}`라 경계가 99px **안에** 든다. `.cells`는 `inset:0`이라 padding box를
따라가므로 셀 내용 공간은 줄지 않고 늘었다. `--line`은 다크 짝 둘 다 있다(함정 5).

**행 구분 검사는 CSS 텍스트 매칭이다** — 규칙의 존재는 잡지만 **뒤 규칙의 덮어쓰기는 못 잡는다.**
**Cowork 판단: jsdom의 상한이고 후퇴가 아니다. 덮어쓰기는 하네스 2단계(픽셀 비교)의 몫이고 BACKLOG에 있다.**
T-25 검토가 "스타일을 보는 검사가 없다"고 적은 자리를 처음 메웠다.

✅ **확인 절차 2번은 답이 왔다 — 40%는 가운데가 답답했다. 0.2로 내렸고 그것까지 배포됐다**
(2026-08-10 12:41 · 라이브 `/app.js`가 `SWIPE_EDGE_RATIO = 0.2`).
⚠️ **3번은 폰 실측 대기**: **주와 주가 갈리는가**(3px 띠 → 1px 선의 교환).
가장자리 폭은 앞으로도 값 하나로 조정한다 — 검사가 비율을 읽으므로 매번 verify만 다시 돌리면 된다.

## ★ 진행 중 — 8월 Guard v1 (APP-PLAN)

**목표: 8/31까지 Guard 탑재 Android 앱, 9/1 실사용 시작.**
계획은 `APP-PLAN.md`, 결정 근거는 `APP-ADR.md`, 진행 상태는 `APP-BUILD.md`, 개발 절차는 `GUARD-DEV-LOOP.md`.

> **(2026-08-10) 계획 문서 갱신 — 주체성 축소 편입.** `BRIEF-AGENCY-0810` 근거로 APP-PLAN에
> **Phase 7 입력 이관**(캘린더 양방향 동기화 — 창=열린 날 · W5 읽기, 진입 조건 "Guard 루프 닫힘"은 이미 충족)과
> **Phase 8 감각 계층**(HC → 공지 → 메신저, 9월 1주 착수)을 신설, 9~11월 병렬 우선순위 교체(감각 계층 > 위젯 —
> 위젯·FCM·인증은 10월). APP-ADR에 **ADR-028~034** 추가(+목록 표에 누락됐던 027 행 보충).
> 캘린더 마이그레이션은 **0016_cal_sync 예약**. **코드 무변경.**
> ※ 이 갱신은 사용자 직접 지시로 Cowork 층이 문서 편집·git까지 처리했다(에이전트 체인 예외 1회).

> **S2.6 반영 절차 (2026-07-30)** — 마이그레이션 **없음**. 순서: `npm run verify` → `npm run deploy`(cron의 `ignored` 확정) → **APK 재빌드·설치**(네이티브 변경: `GuardWatch.kt`·`GuardService`·`GuardPlugin`) → GUARD-DEV-LOOP §6으로 감지 발동 확인.

완성의 정의 — 시험 일정 하나를 등록하면 전날 00:00에 보호 모드가 걸리고, 01:30(=일정시각−준비−수면 역산)에
알람 소리로 잠금화면을 점유하는 알림이 뜨고, 해제에 사유 + Level별 대기가 들고, 전부 `guard_events`에 남는다.

### 1주차 (7/29~8/4) — 폰이 허용하는가만 실측. 규칙 코드 0줄.

| 단계 | 상태 |
|---|---|
| S1.1 Capacitor 골격·권한·서명 | ✅ 실기기 6항목 통과 |
| S1.2 알림 채널·FSI·개입 화면·소리/진동 정책 | ✅ 실기기 통과 (일반/진동/무음 3모드) |
| S1.3 알람 예약 + 재부팅 재등록 | 🔄 **① 낮 3분(앱 완전종료) ✅ · ② 재부팅 복구 ✅** · ③ 밤 실측 두 밤(8/11·8/18) — **충전 없이 정시 ✅ · 깊은 Doze는 아직 표본이 없다** |
| S1.4 포그라운드 서비스 + UsageStats | ✅ |
| S1.5 게이트 화면 + 권한 배너 | ⬜ |

### 2주차 (8/5~8/11) — 기록 구조 + 감지. **서버는 먼저 끝냈다**

| 단계 | 상태 |
|---|---|
| S2.1 `guard_events` 확장 (마이그레이션 0010) | ✅ |
| S2.2 `events` 보호 필드 + 서비스·라우트 | ✅ |
| S2.3 기기측 예약 (`GuardSync.kt`) | ✅ |
| S2.4 로컬 우선 기록 (ADR-023, 마이그레이션 0011) | ✅ |
| S2.5 감지 수집 | ✅ |
| **S2.6 감지 기반 발동** (ADR-025) | ✅ **계획에 없던 추가 단계** |

**S2.6 — 감지가 방아쇠가 됐다(ADR-025).** 발동이 `protect_from` 일정에만 걸려 있으면 루프가 몇 주에 한 번 돌고,
그러면 9~11월에 전례가 쌓이지 않는다 — ADR-014가 말한 실사용 기간의 목적 자체가 무너진다.
- **경로 B 신설**: `GuardWatch.evaluate()`가 `GuardService` 60초 폴링에 얹혀 결정론 규칙을 평가한다.
  취침 창 안 · 화면 켜짐 · **연속** 사용 ≥ N분 → Level 2(1회) → 이후 30분마다 Level 3, 하룻밤 상한 M회.
  기본값 00:30~06:00 / 20분 / 5회 (`GuardSettings`, 기기 저장 — 새벽에 서버가 안 붙어도 판단이 선다).
- **경로 A(시각 예약)와 독립**이다. B가 죽어도 A는 예약이 시스템에 있어 그대로 발동한다 — ADR-018이 지키려던 견고성 유지.
- **루프의 닫는 쪽**: 반응 없이 지나간 발동을 30분 cron이 `ignored`로 확정한다(`guard.finalizeIgnored`).
  유예 **36시간** — 기기가 오프라인이면 발동과 반응을 함께 늦게 올리고 재동기화는 하루 한 번이라,
  먼저 박으면 트리거가 진짜 반응을 막고 소급 복구가 불가능하다.
- 확인용 브리지: `G.watchStatus()` / `G.evaluateWatch()` / `G.setWatch({...})` / `G.resetWatchNight()` — 절차는 GUARD-DEV-LOOP §6.
- **`screen_on`을 서비스 시작 시 직접 기록한다.** 화면 on/off는 런타임 등록만 받아, 서비스가 시작된 시점에
  이미 켜져 있으면 아무도 기록해 주지 않는다 → 연속 시간이 다음 off/on 주기까지 0. 재부팅 직후, 즉 하필 밤에 드러난다.

**S2.3** — 기기가 `/api/guard/schedule`을 받아 `fires[]`를 `setAlarmClock`으로 전부 예약한다. 멱등(서버발 예약만 갈아엎음, 테스트 알람은 보존). **재동기화는 `경계 + 10분`** — 경계가 사용자 설정이라 서버가 응답에 `boundary`를 실어 주고 기기가 그걸 따른다(하드코딩했다가 06:00 설정에서 경계 이전에 도는 버그를 잡음). 앱을 안 열어도 하루 1회 갱신.

**S2.4** — 발동 순서를 뒤집었다: **① 로컬 기록 → ② 개입 화면 → ③ 알림 → (온라인이면) 밀어 올리기.** 발동 경로에 서버 왕복이 있으면 새벽에 기록이 통째로 사라지는데, 개입이 실패하는 밤은 대개 상황이 정상이 아닌 밤이라 하필 그날 데이터가 빈다.
- 재시도 멱등을 위해 기기가 `client_id`(UUID)를 만든다. 서버가 UNIQUE로 들고 있어(0011) 응답만 유실된 재전송이 두 행이 되지 않는다.
- `record()`가 upsert처럼 동작 — 발동만 / 발동+반응 동시(오프라인) / 반응 후행, 셋을 한 엔드포인트로 받는다.
- 큐는 400·409를 버리고 넘어간다. 안 그러면 형식 오류 하나로 큐가 영원히 막힌다.

**0010이 한 것** — `guard_events` 재작성(`reaction`에 `ignored` 추가가 CHECK 변경이라 ALTER 불가) + `risk_snapshot`·`mode`·`source`·`foreground_app`·`ai_*` / `events`에 보호 4필드 / `guard_modes`(ADR-019)·`watch_apps`(ADR-022) 선반영 — **3주차·4주차의 마이그레이션 부담을 없앴다.**

**불변성은 '통짜 금지'가 아니라 '한 번만 채울 수 있다'** — 발동 시 행을 만들고 반응·분류·결과는 나중에 온다. 트리거가 `NULL → 값`만 허용한다. 이래야 "발동했지만 아무 반응이 없었다"(= `ignored`)도 행으로 남는다.

**데드라인은 저장하지 않는다** — `guard.schedule`이 조회 시 역산한다(원칙 4). `일정시각 − 준비(90) − 수면(360)` → 09:00 시험이면 01:30(설계 §6.1 예시와 일치).

**①의 통과가 1주차 게이트의 본체다** — 앱을 완전히 종료한 상태에서 시스템이 스스로 깨워 개입 화면을 띄웠다.
8월 계획이 서 있는 가정이 증명됐다.

### 미커밋 (APP-2) — Guard 네이티브 모듈

```
android/app/src/main/java/dev/mond1424/personalos/guard/
  GuardNotifications.kt  채널 4종·발동(알림 ∥ 개입화면 독립)
  GuardAlertActivity.kt  FSI 대상 화면·뒤로가기 차단·소리 주인
  GuardAlarmPlayer.kt    소리·진동 재생(벨소리 모드 반영)
  GuardSettings.kt       sound/vibration/overrideSilentAtL4 + 정책
  GuardAlarms.kt         setAlarmClock 예약·취소·복구 + 예약 원본 저장소
  AlarmReceiver.kt       실제 발동 지점(시스템이 앱을 깨움)
  BootReceiver.kt        재부팅·앱 업데이트 후 복구
  GuardPlugin.kt         웹 브리지
+ res/layout/activity_guard_alert.xml · res/drawable/ic_guard.xml
+ AndroidManifest(권한·Activity·Receiver 2종) · styles.xml(Theme.GuardAlert)
+ build.gradle(Kotlin 2.1.0) · MainActivity.java(registerPlugin)
+ capacitor.config.ts(webContentsDebuggingEnabled) · test/e2e.mjs(CI=true)
+ public/app.js(syncOverlay — 팝업 겹침 깊이)
```

### 1주차에 물린 것 (전부 해결)

- **Android 14+ `setOngoing(true)` 무력화** — 앱이 만드는 '못 지우는 알림'은 없다. 마찰을 화면 + 재발동 주기로 이전
- **targetSdk 35+ 예측형 뒤로가기** — `onBackPressed()` 미호출. `OnBackInvokedDispatcher` 필요
- **`VIBRATE` 권한 미선언** — 진동이 예외 없이 조용히 무시됨
- **채널 설정 불변** — 소리를 설정으로 끄려면 채널이 아니라 화면이 소리의 주인이어야 한다. `guard_high_v1` 폐기 → 조용한 채널 + 폴백 채널
- **디버그/릴리스 서명 혼용** — 삭제 후 재설치를 강제해 권한·예약 원본이 매번 초기화. **릴리스 빌드만 쓴다**(`webContentsDebuggingEnabled: true`)
- **wrangler 4.1x 마이그레이션 프롬프트** — `e2e.mjs`에 `CI=true`(capacitor 설치 시 락파일 갱신 여파)

### 3·4주차 — 개입과 루프. **완성의 정의는 이미 충족됐다**

| 단계 | 상태 |
|---|---|
| S3.1 데드라인 역산 + Level 1~4 결정론 발동 | ✅ (S2.3에 흡수 — 서버가 `fires[]`로 펼치고 기기가 예약) |
| S3.2 Override 마찰 | ✅ |
| S4.1 `risk_snapshot` 수집 | ✅ (감지가 붙어 `screen_on_sec`·`unlocks`·`top_apps`까지) |
| S4.2 outcome 확정 카드 | ✅ **루프가 닫혔다** |
| S4.4 PC 스키마 자리 | ✅ (0010 `watch_apps`) |
| S3.1b Level 4 AI 검증 (ADR-024) | ⬜ |
| S3.3 Level 4 신규 작업 차단 | ⬜ |
| S3.4 모드 UI | ⬜ |
| S4.3 알림함 | ⬜ |
| S1.5 게이트 화면 + 권한 배너 | ⬜ |

**루프 전체가 돈다**: `감지·데드라인 역산 → 발동(FSI+알람) → 반응(마찰) → 로컬 기록 → 서버 → outcome 확정`

**S3.2 — Override 마찰.** 마찰을 처음부터 보여주지 않는다([알겠습니다] / [그래도 계속하기]를 나란히 두면 대등한 선택지로 읽힌다). 대기는 사유 입력과 **동시에** 흐른다(순차면 짜증이 된다). 배수를 서버에서 못 받으면 1.0 — 통신 실패가 마찰을 없애면 그게 우회로다.
- **사유 길이 하한(20자)은 폐기했다.** 실사용에서 마찰이 아니라 강제로 읽혔다. §6.3은 "비용을 치르게 한다"이지 "분량을 채우게 한다"가 아니다. 비어 있지만 않으면 통과.
- 마찰 화면 진입 즉시 소리·진동을 끈다. 울리는 채로는 사유를 쓸 수 없다.

**S4.2 — outcome.** 한 번에 하나만 묻는다. 여러 개를 늘어놓으면 대충 눌러 치우고, 그렇게 들어온 outcome은 **없는 것보다 나쁘다**(보정을 틀린 방향으로 끈다). `style.css`의 미사용 `.guardbar`가 설계 §6을 위해 미리 있던 자리였고 그대로 썼다.

### 물린 것 — front가 잡은 실제 결함 (2026-07-29)

"짧은 가로 이동은 무시(임계값)"가 실패했다. **플레이크가 아니라 검사의 결함이었다.**
- `loadGuardOutcome` 추가로 이벤트 루프가 조금 느려지자 `dt`가 `VEL_MIN_DT`(16)를 넘겨, 40px 이동이 `2.5 px/ms`로 계산돼 `FLICK_V`(0.5)를 넘었다.
- **앱 코드는 맞다** — 16ms에 40px는 실제로 빠른 손짓이다. 검사가 이벤트를 동기로 쏘면서 "속도 0"을 가정한 것이 틀렸다. **시간을 안 흘리면서 시간의 함수를 검사했다.**
- 조치: `swipe`에 실제 간격(120ms)을 넣었다. `VEL_WIN(90) < 120 < VEL_STALE(130)`이라 구간별 속도가 재지고, 루프가 밀리면 속도가 더 낮아져 **지연이 통과 방향으로만 작용**한다. 검사 이름도 "짧고 **느린** 가로 이동"으로 정정.

### 병행 트랙 — Me Reinforcement Plan (Phase 1)

계획서 `me-reinforcement-plan.md`. **§9 오픈 이슈는 닫혔다** — 안드로이드 앱이 기존 Worker + D1을 그대로 쓴다(Capacitor 원격 로드, 네이티브는 Guard 전용 계층뿐). 백엔드 재작성 없음.

Guard v1이 1순위라는 건 안 바뀐다. Phase 1을 셋으로 쪼개 **UI를 맨 뒤에** 뒀다 — 밀리면 뒤쪽만 잘라낸다.

| 조각 | 내용 | 상태 |
|---|---|---|
| **P1-a** | 마이그레이션 0012 — analysis 앵커 4컬럼+backfill · 기간 `kind`/`dday_label` · `lm_item`(version 트리거) · `lm_schema` 레지스트리 | ✅ |
| **P1-b** | 경량 스키마 검증기 · `lm_item` CRUD API · Me→Overview 이관 · analysis가 앵커·`source_versions` 실제 기록 | ✅ |
| **P1-c** | 하단 바 (4탭/Me) 분리·애니메이션 · Education 폼 · Goals 디데이 표시 | ✅ **T-01·T-09 완료** |

**P1-a를 8월로 당긴 이유** — 계획서 §2.3: `source_versions`는 생성 시점의 입력 스냅샷이라 나중에 만들 수 없다. 9~11월 analysis가 앵커 없이 쌓이면 그 구간은 영영 stale 판정(§5) 밖이다. `guard_events.risk_snapshot`과 같은 논리(ADR-020).

**`buildCoreContext()`는 아직 만들지 않았다.** 현재 Guard에는 AI 판단부가 없다(ADR-021로 발동이 결정론). 유일한 소비처가 S3.1b(Level 4 AI 검증, 3주차)이므로 그때 함께 짠다 — 지금 만들면 소비처 없는 껍데기다.

### 다음 마이그레이션 번호

`0015_me_history_reason`이 최신(T-10, 2026-08-05). **알림 아웃박스=0016 · 인증(9월)=0017.**
추가 시 `test/smoke.ts`의 하드코딩 스키마 목록에도 파일명을 넣는다.

## raw 링크 (Chat이 직접 읽는 주소)
- 설계문서(권위) https://raw.githubusercontent.com/mond1424/personal-os/main/personal-agent-design_v0.9.md
- APP-PLAN      https://raw.githubusercontent.com/mond1424/personal-os/main/APP-PLAN.md
- APP-ADR       https://raw.githubusercontent.com/mond1424/personal-os/main/APP-ADR.md
- APP-BUILD     https://raw.githubusercontent.com/mond1424/personal-os/main/APP-BUILD.md
- GUARD-DEV-LOOP https://raw.githubusercontent.com/mond1424/personal-os/main/GUARD-DEV-LOOP.md
- CLAUDE.md      https://raw.githubusercontent.com/mond1424/personal-os/main/CLAUDE.md
- README0722     https://raw.githubusercontent.com/mond1424/personal-os/main/README0722.md
- 사용설명서0722 https://raw.githubusercontent.com/mond1424/personal-os/main/사용설명서0722.md
- REFACTOR-PLAN  https://raw.githubusercontent.com/mond1424/personal-os/main/REFACTOR-PLAN.md
- WORK-PLAN-0726 https://raw.githubusercontent.com/mond1424/personal-os/main/WORK-PLAN-0726.md
- BRIEF-AGENCY-0810 https://raw.githubusercontent.com/mond1424/personal-os/main/BRIEF-AGENCY-0810.md
- STATE.md       https://raw.githubusercontent.com/mond1424/personal-os/main/STATE.md
- api-surface    https://raw.githubusercontent.com/mond1424/personal-os/main/docs/api-surface.md
- schema         https://raw.githubusercontent.com/mond1424/personal-os/main/docs/schema-current.sql
- 라우터         https://raw.githubusercontent.com/mond1424/personal-os/main/src/index.ts
- types          https://raw.githubusercontent.com/mond1424/personal-os/main/src/types.ts
- db/index.ts    https://raw.githubusercontent.com/mond1424/personal-os/main/src/db/index.ts
- daily.ts       https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/daily.ts
- tasks.ts       https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/tasks.ts
- periods.ts     https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/periods.ts
- events.ts      https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/events.ts
- memos.ts       https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/memos.ts
- me.ts          https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/me.ts
- analysis.ts    https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/analysis.ts
- guard.ts       https://raw.githubusercontent.com/mond1424/personal-os/main/src/services/guard.ts
- scheduled.ts   https://raw.githubusercontent.com/mond1424/personal-os/main/src/scheduled.ts
- lib/time.ts    https://raw.githubusercontent.com/mond1424/personal-os/main/src/lib/time.ts
- lib/id.ts      https://raw.githubusercontent.com/mond1424/personal-os/main/src/lib/id.ts
- lib/ai.ts      https://raw.githubusercontent.com/mond1424/personal-os/main/src/lib/ai.ts
- app.js         https://raw.githubusercontent.com/mond1424/personal-os/main/public/app.js
- api.js         https://raw.githubusercontent.com/mond1424/personal-os/main/public/api.js
- index.html     https://raw.githubusercontent.com/mond1424/personal-os/main/public/index.html
- style.css      https://raw.githubusercontent.com/mond1424/personal-os/main/public/style.css

## 기준선
typecheck 통과 / **smoke 321** / **front 291** / 실패 0 / verify exit 0
**2026-08-20 (T-38 → T-37 → T-39).** 착수 전 재측정에서도 **306/291**이었다 —
8/17에서 사흘이 더 지났는데 같다. **날짜 의존 결함에서는 그것만이 증거다**(T-36의 상대화가 버틴다).

**여기가 기준선 숫자의 유일한 자리다.** `CLAUDE.md`·`AGENTS.md`·`_TEMPLATE.md`에서 실수치를 뺐다 —
숫자를 만드는 층이 못 고치는 파일에 숫자가 박혀 있어 두 번 낡았다(아래 §규약 불일치 4번).
아래 괄호는 **원장**이다. 한 줄이 한 티켓이고, **앞 숫자는 반드시 앞 줄의 뒤 숫자와 이어져야 한다** —
끊기면 244가 어떻게 244가 됐는지 알 수 없다(2026-08-06 보수에서 실제로 세 칸이 비어 있었다).

**숫자는 자기 티켓만 있는 트리에서 잰다.** T-26을 잴 때 T-27의 미커밋 변경이 트리에 있었고
front가 **257**로 나왔다 — T-26은 `public/`·`front.mjs`를 건드리지도 않았는데. 셋을 stash하고
다시 재니 256이었다(smoke 279는 같았다). **오염된 트리에서 잰 숫자가 원장에 들어가면 그 줄은
거짓이고, 다음 티켓의 '앞 숫자'가 어긋난다.** 병렬 발행은 Cowork가 `AGENT-CHAIN.md` §3로 막았다.

(T-39 판정이 flush를 앞질러도 살아남는다: smoke **317 → 321** · front 291 무변경 · **마이그레이션 없음** —
 T-37이 넓힌 창(6초 → 최악 16초)을 닫고 T-38이 만든 칸을 실제로 채운다. **두 곳이 함께 막혀 있었다**:
 `amendFire`가 큐에 없으면 `?: return`으로 버렸고, `record()`의 dup 경로는 `reaction`만 채웠다 —
 **기기를 고쳐도 서버가 안 받는다.** `stAmendGuardAi`가 `COALESCE`로 `NULL → 값`만 채운다.
 ⚠️ **`COALESCE`가 유일한 방어선이다** — `trg_guard_event_immutable`은 `ai_*` 넷을 **아예 안 본다**
 (0010은 `reaction`·`override_*`·`outcome`만 지킨다). `ai_used`만 `MAX`로 `0 → 1`(지출은 안 되돌려진다).
 **변이 셋**: 서버 dup 되돌리기 **320/1**(1만) · **★기기 fallback 되돌리기 320/1 — 3만**(1·2는 초록.
 **이 티켓의 판정**이고 T-38에서 물린 자리다. 실패 줄이 `block=1079자`를 찍어 **슬라이스가
 살아 있었다**는 것까지 말한다) · 스캐너를 눈멀게 **319/2**(3도 함께 죽는다 — T-37 둘째와 같은 이유).
 **별건으로 올렸다 — 아래 §미해결.**)
(T-37 중첩 타임아웃 순서: smoke **313 → 317** · front 291 무변경 — `GuardVerify.kt`의 `TIMEOUT_MS`
 하나를 `CONNECT_TIMEOUT_MS 4_000` · `READ_TIMEOUT_MS 12_000`으로 갈랐다. **서버 8초 무변경.**
 늘어난 넷은 **두 언어에 걸친 부등호를 지키는 스캐너**다: ①read > 서버 · ②★합성 소스에서 세 값을
 뽑는다 · ③두 상수를 각각 찾았다 · connect < read(한 상수로 되돌리면 죽는다).
 **변이 셋**: `READ 6_000` **316/1**(①만 · `read=6000 server=8000`을 찍는다) ·
 스캐너를 눈멀게 **314/3**(③이 `server=null`로 이름을 댄다) · connect=read **316/1**.
 ⚠️ **둘째가 티켓의 기대와 다르다** — 티켓은 *"①은 초록으로 남는다"*였는데 ①도 죽는다.
 §할 일 ②가 경고한 *"파싱 실패를 0으로 채우면 참이 된다"*를 피해 `null`을 그대로 뒀기 때문이다.
 **못 한 것**: `server_timeout`을 실물에서 한 번 보는 것(§할 일 ③) — 서버 값을 100ms로 낮춘 채
 **배포**해야 하고 배포는 사용자가 한다. 절차를 티켓 §확인 절차에 더할 것 A로 옮겼다.)
(T-38 모델이 왜 거부했는지: smoke **306 → 313** · front 291 무변경 · **0017 `ai_reason TEXT`** —
 서버는 늘 `reason`을 보냈고 `GuardVerify`는 파싱까지 했는데 `amendFire`가 안 날라 버려졌다.
 운반과 저장만 잇는다(`VERIFY_SYSTEM`·판정 무변경). `ai_unavailable_reason`과 **반대편**이다 —
 저쪽은 판정이 없을 때(닫힌 목록·기계가 센다), 이쪽은 있을 때(자유 문자열·사람이 읽는다).
 ⚠️ **검사가 넷이 아니라 일곱이다. 티켓의 변이 예상이 어긋나서다** — 티켓은 *"`amendFire`에서
 다시 버리면 1·2가 죽는다"*였는데 **1·2는 산다**(서버로 직접 POST하므로). 즉 티켓이 정한 넷만으로는
 **이 티켓이 고치는 결함이 재발해도 아무것도 안 죽는다.** 끊기는 자리를 직접 보는 검사와 짝을 넣었고,
 스캐너는 **주석을 걷어내고** 본다(안 그러면 `// aiReason = ...`이 그대로 통과한다).
 **변이 셋**: 기기가 다시 버린다 **312/1**(배선 검사만 · `notif=false`로 어느 쪽인지 말한다) ·
 판정 없을 때도 싣는다 **312/1** · 서버가 다시 버린다 **310/3**(부재를 주장하는 3·4는 산다).
 길이는 **거부하지 않고 자른다(500자)** — 400은 `flush()`가 발동 행을 버리는 경로다(0016).)
(T-36 검사 날짜를 상대로: smoke **292/11 → 306/0** · front 291 무변경 · **소스 무변경** —
 11건이 **한 원인**이었다(일정 하나가 보호 구간을 열었고, 하향 차단이 열을 딸려 죽였다).
 늘어난 셋은 **검사가 자기 소스를 훑는 스캐너**다: 고정 날짜 0건 · **★스캐너가 살아 있는가**(합성 줄을
 실제로 잡는다 — '0건'은 스캐너가 죽어도 0이다) · 허용된 순수 함수 인자 **다섯 줄**(T-26의 자리).
 ⚠️ **첫 시도가 다른 하나를 죽였다(305/1)** — 피해야 할 시계가 둘이었다: 실시각과 **T-23이 주입하는
 시계**(`gNow.start+1분`). `D+3`은 후자를 삼킨다 → `D+4`. **그 검사가 내 fixture 선택을 잡았다.**
 합성 줄의 날짜는 이어 붙였다(`"20"+"26-08-15"`) — 그대로 적으면 **검사가 자기 자신을 잡는다.**)
(T-35 세 번 밀린 일의 출구: front 283→291 · **서버 무변경**(당시 smoke는 날짜 의존으로 292/11 —
 T-35와 무관했고 **T-36이 306/0으로 되돌렸다**) — 조건 미달·조건 충족(짝)·하루 한 번+날짜 바뀜(짝)·최다 하나·동점 id·
 **선택지 개수 셋**·⑤ 2주 밖 허용·**★ 평소 미루기는 D+14 그대로** 8건.
 **변이 넷**: 상한 어디서나 해제 **288/3**(⑤는 초록 — ★와 기존 캘린더 둘이 죽는다. **이게 ★의 자리다**) ·
 ② 무시 290/1 · 정렬 제거 289/2(④와 동점이 함께) · 항상 안 뜬다 **286/5**(**①은 초록** — 안 뜨는 것이
 기본이라 짝인 ②가 잡는다). 마지막 변이가 처음엔 **러너를 죽여** 검사를 방어적으로 고쳤다.)
(T-34 뒤로가기가 맨 위 하나만: front 277→283 · smoke 303 무변경 — 시트 하나·겹친 둘(맨 위부터)·
 둘째도 닫힘·피커는 원래 탭·다른 탭은 Today·**Today에서 null** 6건. `@capacitor/app` 추가.
 **변이 둘**: ④가 `null` 대신 `"tab"`(=항상 뭔가를 닫는다) **282/1 — 딱 ⑤만 죽는다**
 (①~④만 있었으면 "앱을 영영 못 나가는 구현"이 통과한다) · 겹쳐도 `closeAll` **281/2**(짝 둘이 함께).
 ②는 **연 순서와 반대로 열어** DOM 순서를 따르는지 본다 — 같은 결과가 나오는 시나리오면
 무엇을 검사하는지 알 수 없다. `guard/` 17개 파일 diff 0 · `cap sync` 후 `assembleRelease` 통과.)
(T-33 outcome 카드를 검사가 지킨다: front 271→277 · smoke 303 무변경 — 대기 있으면 뜸(날짜·제목) ·
 이어 묻기 · 버튼 둘이 다른 값 · none · error · **none/error 짝** 6건. 웹만이라 APK와 무관.
 **변이 둘이 서로 다른 자리를 짚어야 증명이 된다**: 조회가 항상 실패 **272/5**(①②③★ 죽고 ④는 산다 —
 넷이 같은 것을 보는 게 아니다) · 상태를 안 가름(`catch`도 none) **275/2**(④와 ★만 죽는다 —
 3만 사는 게 아니다). ⚠️ 첫 변이가 **내 검사 하나를 떨어뜨렸다** — `return true`라 떨어질 수
 없는 검사였고 변이에서도 초록이었다. 뺐다(278→277). 숫자를 하나 줄인 것이 맞다.)
(T-32 위험도 스냅샷에 서버 항: smoke 293→303 · front 271 무변경 — firedAt 기준·양성 대조·수면 추정·
 기기 항 보존·출처 분리·risk_score·스냅샷 없을 때 NULL·보호 구간 전제·데드라인 일치·protecting 10건.
 **변이 넷 — ①만 죽는 것이 이 티켓의 증명이다**: 조회를 `t.now`/`t.d`로 되돌리면 **302/1**이고
 양성 대조는 초록이다(둘이 같은 것을 보고 있었다면 함께 죽는다). 서버가 기기 항 덮어쓰기 **301/2**
 (내가 안 쓴 기존 검사 `risk_snapshot이 JSON으로 보존됨`도 함께 깨졌다) · 평면에 섞기 4건 ·
 risk_score 안 내기 302/1. 마이그레이션 없다 — `risk_score`·`risk_snapshot` 둘 다 CHECK가 없음을
 `docs/schema-current.sql`에서 **확인하고** 적었다(T-31에서 이 문장이 틀렸다).)
(T-31 9~11월이 읽을 것을 늘린다: smoke 283→293 · front 271 무변경 — 이유가 남는지·캐시 음성/양성·
 목록 밖이어도 행 생존·http 세 자리·approve엔 이유 없음·대장 셋 일치·http 모양·snapshot 짝 둘 10건.
 **첫 변이가 내 검사를 떨어뜨렸다**: `deny` 위에 `unavailable`을 얹는 모양으로 짰더니 `IN`을 지워도
 293/0이었다 — `guard.ts:392`가 `=== "approve"`만 보므로 캐시가 `unavailable`을 집어도 **결과가
 `deny`와 글자 그대로 같다.** `approve`를 깔고 얹는 모양으로 고쳐 감도를 만들었다.
 변이 다섯 **겹침 0**: 캐시 IN 제거 292/1 · Kotlin 이유 갈라짐 292/1 · `screen_on_sec` 제거 292/1 ·
 목록 밖에 400 던지기 **291/2** · 0016 스캐너 죽이기 292/1. 마지막이 T-26의 교훈 자리다 —
 기대값이 비어 있지 않아 스캐너가 죽으면 그 자체로 빨간불이다.
 마이그레이션 **0016 추가** — 로컬·원격 적용 완료(2026-08-11). 프런트 무변경.)
(T-30 감지가 Guard 자신을 세지 않는다: smoke 283 · front 271 **둘 다 무변경** — Kotlin만 바뀌었고
 이 층에 Kotlin 러너가 없다. **숫자로 보일 것이 없다는 사실을 그대로 적는다** — 근거는 코드 읽기와
 `assembleRelease` 통과까지이고 실검사는 티켓 §확인 절차(낮 2분)다. 서버·프런트·마이그레이션 없음.)
(T-29 Level 4면 오늘이 아니라 내일로: front 257→271 · smoke 282→283 — 게이트 셋 각각 켠 쪽·끈 쪽 ·
 dim 안내 · 이유 문구 · 대기 통과 · fail-open · 웹에 창 길이 없음 14건 + 프롬프트가 사실을 말하는지 1건.
 **변이 D가 이 티켓의 핵심이다**: `askLevel4`를 항상 true로 만들면 267/4인데 **빨간불은 짝 셋과
 fail-open뿐이고 "Level 4면 안 붙는다" 쪽은 전부 초록**이다 — 짝이 없었으면 낮에 오늘 칸이
 통째로 막힌 앱이 배포된다. 게이트 셋을 하나씩 지운 변이는 268/3 · 270/1 · 268/3으로 **겹침 0**.
 검사 자리를 **마감 앞으로** 옮겨야 했다 — 뒤에 두니 트리거가 쓰기를 409로 막아 "게이트는
 통과했는데 목록에 없다"가 됐다. 게이트와 무관한 증상이었다.)
(T-28 Level 4 상태 노출 · 프롬프트 정정: smoke 279→282 — VERIFY_SYSTEM 추출 양성 대조 ·
 옛 문구를 잡는 양성 대조 · '차단' 없음 3건. front 257 무변경 · 마이그레이션 없음.
 **양성 대조가 없었으면 T-28이 스캐너 죽은 채로 통과한다**: 정규식을 죽이면 `vs=""`이 되어
 검사 1('차단이 없다')이 **그대로 초록**이고 대조 둘만 빨간불이다(280/2). 옛 프롬프트 변이는 281/1.
 Kotlin은 검사가 없다 — `assembleRelease` 통과와 코드 읽기까지이고 **실검사는 밤 실측**이다.)
(T-27 가장자리 폭 · 캘린더 가로 구분: front 256→257 — 행 구분 1건.
 **폭은 0.1→0.4→0.2로 두 번 움직였고 검사는 그대로다**(현재 0.2 — 실측에서 40%가 답답했다).
 조정마다 verify를 다시 돌려 257/0을 확인했다. **제스처 검사 여섯은
 다시 쓴 것이지 늘린 것이 아니다** — 같은 여섯이 이제 `ev("SWIPE_EDGE_RATIO")`에서 좌표를 얻는다.
 10% 시절 좌표(`+190`·`+10`·`+370`·`37/39/341/343`)가 **0개**다. smoke 279 무변경 · 서버 없음.
 **이식성을 실제로 돌렸다**: 폭을 0.25로 바꿔 front 257/0 — §완료 조건이 "1·2가 전부"라 한 자리다.
 `|| 380` 폴백 제거 변이는 254/3이고 **오른쪽 가장자리가 거짓으로 초록인 것도 T-25 그대로**
 (폭 0이면 전 구간이 가장자리라 '탭이 바뀐다'가 우연히 맞는다) — 재작성이 감도를 잃지 않았다.
 검사가 **가운데 좌표가 가운데 구간 안인지 스스로 단언한다** — 아니면 '달이 안 넘어감'이 정상이 되어
 거짓 초록이 된다. 행 구분은 `border-top:3px var(--card)` → `1px var(--line)`, `height:99px` 불변
 (`box-sizing:border-box`라 경계가 안에 든다) · 셀 안쪽 96→**98**.)
(T-26 schedule()도 넘겨받은 시계를: smoke 272→279 · **front 256 무변경(기여 0)** — 스캐너 양성 대조·`Date.now()` 대장·
 services 잔여 2건이 경과 측정·보호규칙 전제·실시각 10건·데드라인 ∓1분 7건. 서버만 바뀌었다.
 **"src에 `Date.now()` 0"은 티켓의 전제가 틀려 그대로 짤 수 없었다** — 일곱 줄이었고 여섯이 남는다
 (`loadTime` 기본값 1 · 경과 측정 5). 없앨 것은 '지금이 몇 시인가'로 읽는 자리이고 그건 0이다.
 그래서 **파일·줄 수 대장**(`ai.ts 3 · time.ts 1 · guard.ts 2`)으로 못 박았다 — 기대값이 비어 있지
 않으므로 **스캐너가 죽어 대장이 `[]`가 되면 그 자체로 빨간불**이다. '0건'은 못 찾을 때 초록이다.
 변이 셋: 옛 `Date.now()` 275/4 · 부등호 뒤집기 275/4(`−1분`은 L1이 남아서 · `+1분`은 L3이 남아서
 — **서로 다른 이유**라 한쪽만 뒀으면 통과했다) · 스캐너 죽이기 276/3.)
(T-25 캘린더 가장자리 → 탭 전환: front 250→256 — 가운데 달 넘김·좌 가장자리·우 가장자리·세로 무시·
 input 예외·폭 0 폴백 6건. **두 blocked가 짝이라 변이를 둘 다 태웠다**: noSwipe만 고침 254/2(탭도 달도 움직임) ·
 캘린더 blocked만 254/2(아무것도 안 일어남) — 같은 두 줄이 서로 다른 이유로 빨간불.
 `clientWidth||380` 폴백 제거는 253/3이고 그때 **오른쪽 가장자리 검사가 거짓으로 초록**이다.
 AXIS_LOCK·FLICK_V·VEL_*·dragBlockUntil 무변경. 서버·CSS 없음.)
(T-24 Guard 메모리 요일·cur 중복: front 249→250 — `md`→`dlabel`로 그룹에 요일, 줄의 날짜는 유지 1건.
 회전 분기의 `cur` 토글 중복 제거는 T-21 회전 검사에 얹었다(중복을 되살리면 255/1 · cur 10 vs 5).
 표기는 `8월 5일 수` — 티켓 예시 `8/5 (수)`와 다르지만 `dlabel()`이 원래 그 모양이고,
 형식을 맞추려면 요일 표기가 두 벌이 된다. smoke 무변경.)
(T-23 요청당 loadTime 한 번: smoke 268→272 — services/ 호출 0 · 진입 계층 2건(양성 대조) · 훑은 범위 ·
 주입한 t를 따르는지 4건. **양성 대조가 실제로 일했다**: 주석 제거 정규식이 `"/api/*"`에서 열려
 18줄을 먹고 진짜 호출을 지웠는데 '0건'은 그대로 초록이었다. 변이(modes 안 재호출)는 269/3.
 `loadTime`을 부르는 곳은 이제 진입 계층 둘뿐 — `index.ts` 미들웨어 · `scheduled()`. front 249 무변경.)
(T-22 Guard 메모리 일별 접기: front 244→249 — on_date 묶기·8일 이상 7일+남은 일수·7일 이하 더보기 없음·
 05:00 경계 전 개입이 전날에 묶임·펼치면 T-18 줄 그대로 5건. `fired_at.slice(0,10)` 변이는 248/1로 4번만 실패.
 서버·마이그레이션 없음. smoke 268 무변경.)
(T-08′ 모드 화면: front 223→229 — 하향 사유 시트·60초 대기·상향 즉시 PUT·409/400 서버 문구 6건.
 요청을 먼저 보내는 변이는 228/1로 검사 5번만 실패. smoke 267→268은 §선행 조각(`5687455`) 몫.)
(T-18 이력 축약·Guard 메모리: front 217→223 — 최근 5건 접기·펼치기·읽기 전용 목록·cause 폴백 6건.
 전체 문자열 표 변이는 222/1. 서버 무변경.)
(T-17 침범 셀·터치 피드백: front 213→217 — 흐린 칸도 열림·press 부여/해제·가로 이동 해제 4건.
 해제 제거 변이는 216/1. 서버 무변경.)
(T-21 캘린더 pane 재사용: DOM 5-pane·한 칸 이동 시 1-pane만 조립·같은 달 재렌더 0-pane.
 front 238→244 — 5-pane·1-pane 조립·좌우 3회 대응·캐시 무효화 full rebuild·가운데 pane 6건.
 오른쪽 4→3·왼쪽 0→1 변이는 각각 243/1로 자기 방향 검사만 실패. 서버·마이그레이션 없음.)
(T-20 캘린더 전환: 월 키 메모리 캐시·데이터 5개월·쓰기 뒤 전체 무효화·dragBlockUntil 60.
 front 229→238 — 캐시 요청 3건, 일정 추가·수정·삭제·미루기·마감 무효화 5건, 60ms 1건.
 일정 추가·미루기 무효화 제거 변이는 각각 237/1로 해당 검사만 실패. 서버·마이그레이션 없음.)
(T-19 모드 판정 응답: smoke 260→267 — modes[].downgrade·활성 false·보호 구간 값/null·
 risk_threshold 방향·요청 판정 위조 차단·guard_modes 무변경 7건. front 무변경, 마이그레이션 없음.)
(T-10 모드 하향 마찰: smoke 244→260 — 하향 판정(risk_threshold·ai_daily_cap)·보호 구간 409·사유 400·
 `me_history` 사유·상향 자유 16건. front 무변경 — 프런트는 T-08′이 받는다.)
(T-16 memo 작성 시점 구분: smoke 241→244 — 05:00 경계 판정·나중 memo·ts 정렬 3건 / front 210→213 — 추가 날짜·그날 작성·빈 상태 3건. 마이그레이션 없음.)
(T-15 취소 상태 판정: smoke 237→241 — Today Todo·예정·재배정 제외와 취소 해제 재등장 4건. front 무변경.)
(T-12 날짜 검사: `S.today.date` 고정 주입으로 30일 달·윤년 2월을 각각 재검증. 검사 수는 유지.)
(T-09 Goals 디데이·Me 분리: front 193→210 — 스키마 동적 폼·CRUD·디데이 계산/숨김·귀속일 센티널 17건.)
(T-08 불합격 UI·검사 4건을 stash로 제거해 작업 트리 기준 front 214→210.)
(T-07 `ai_used` 의미: smoke 233→237 — 호출 후 실패 3건이 1로 세는지 + 키 없음은 여전히 0.
 front는 무변경이나 **말일 검사 하나를 고쳤다** — 아래 T-07 참조.)
(T-05 보호 규칙 UI: front 185→190(Codex) →193(검토 보강 3건). smoke 무변경 — 서버는 이미 완성돼 있었다.)
(T-03 Level 4 AI 검증: smoke 216→233 — 통제 6겹 11건 + `buildCoreContext` 6건. front 무변경(서버만).)
(T-02 스키마 라벨: smoke 213→216 — title 실림·검증 불변·key 폴백 3건 / front 183→185 — 라벨이 title·title 지우면 key 2건.
 T-01 Education 폼: front 167→183 — Codex가 낸 6건을 16건으로 보강. 아래 'T-01' 참조.)
(S2.6 `ignored` 확정: 207→211 — 유예 넘김·cron 보고·유예 안쪽 NULL 유지·재실행 멱등 4건.
 UTC 시각 정규화: 211→213 — 귀속일·저장 표기 2건.)

### 물린 것 — 기기가 UTC로 보낸다 (2026-07-30, 실기기 덤프에서 발견)

`GuardEventQueue.nowIso()`가 `2026-07-29T20:09:15Z`(UTC)로 보내는데, `attributionOfIso`와
`fired_at` 문자열 비교는 **표기된 시각 자리를 로컬로 읽는다.**
- 밤 발동(00:30~06:00 KST = UTC 15:30~21:00)은 경계 위라 안 드러났다. **낮 발동에서 물린다** —
  UTC 00~06시(= KST 09~15시)가 경계(06:00) 아래로 떨어져 **전날로 귀속**된다.
- `finalizeIgnored`의 36시간 유예도 같은 이유로 실효 27시간이 됐다.
- 조치: `lib/time.ts`에 `normalizeIso` — 오프셋이 붙은 표기만 로컬 표기로 변환(오프셋 없으면 이미 로컬로 본다).
  `record()`의 `fired_at`·`applyReaction()`의 `reacted_at`이 통과한다.
- **기기가 아니라 서버에서 흡수했다.** APK 재빌드가 필요 없고, 9월 PC 에이전트가 같은 실수를 해도 한 번 더 안 물린다.
- 기존 14행은 `Z` 표기로 남아 있다(귀속일은 우연히 전부 맞았다). 소급 수정하지 않는다 — 개입 이력은 고치지 않는다.
(S3.2에서 사유 길이 하한을 걷어내며 빈 사유 검사 1건 추가.)
(0010 Guard 서버: 154→180 — 보호 규칙·데드라인 역산·모드·발동 기록·불변성·watch_apps 26건.
 0011 client_id 멱등: 180→184 — 재전송·발동+반응 동시·반응 후행 4건.
 0012 Life Model: 184→206 — 스키마 레지스트리·검증기·CRUD·version 트리거·Me 이관 22건.)
(WORK-PLAN-0726: smoke 148→154 취소 사유 6건, front 164→167 취소 사유 3건.
 그 앞 S1 분석 depth·S3′ 마감 유도 포함 누적치 — smoke 145→154, front 157→167.)

## 캘린더 회전 측정 (T-21 후속 · 아직 안 쟀다)

T-21이 조립을 5→1 pane으로 줄였지만 **회전만의 비용**은 미측정이다.
`renderCalendar` 전체에는 회전이 아닌 것이 훨씬 많다 — 제목·`weeksOf`×5·`calendarDataFor`·
5개월 인덱스 조립·`capRx`의 강제 리플로우·`trackSet`·범례·기간 목록. **그래서 빼기로 가른다.**
코드에 이미 그 짝이 있다: `panesAlreadyCurrent`(`app.js:1041`)가 참이면 pane을 **0장** 조립하고
나머지는 전부 똑같이 한다.

```
회전 비용 = renderCalendar(±1)  −  renderCalendar(0)      ← 같은 달 · 캐시 warm
             (pane 1장 조립)        (pane 0장 조립)
```

`app.js`는 classic script라(`index.html:511`) 전역이 콘솔에서 이름 그대로 잡힌다.
**캘린더 탭을 연 상태**로 붙여넣는다(T-14 하네스의 CDP 콘솔도 같다):

```js
await (async () => {
  if ($("#phone").dataset.tab !== "cal") return "캘린더 탭을 먼저 열어라";
  const track = $("#cal-track");
  if (track.children.length !== CAL_PANE_COUNT) return "5-pane이 아니다 — 회전 분기를 안 탄다";
  const frame = () => new Promise((r) => requestAnimationFrame(() => requestAnimationFrame(r)));
  const once = async (dir) => {                 // calGo의 finish()가 하는 상태 전이를 그대로
    if (dir) { S.cal = addMonth(S.cal.y, S.cal.m, dir); calGen++; }
    const b0 = calendarPaneBuildCount, t0 = performance.now();
    await renderCalendar(dir);
    track.offsetHeight;                         // 파싱만 재지 않게 레이아웃까지 밀어 넣는다
    return { ms: performance.now() - t0, panes: calendarPaneBuildCount - b0 };
  };
  for (let i = 0; i < 6; i++) await once(i % 2 ? -1 : 1);   // 월 캐시·JIT warm-up
  const rot = [], idle = [];
  for (let i = 0; i < 20; i++) {
    rot.push(await once(i % 2 ? -1 : 1));       // ±1 진동 — 네트워크가 안 섞이게
    idle.push(await once(0));
    await frame();
  }
  const med = (a) => a.map((x) => x.ms).sort((x, y) => x - y)[a.length >> 1];
  return {
    rotate_ms: +med(rot).toFixed(2),
    noop_ms: +med(idle).toFixed(2),
    rotation_only_ms: +(med(rot) - med(idle)).toFixed(2),
    panes_rot: [...new Set(rot.map((r) => r.panes))],    // [1] 이어야 한다
    panes_noop: [...new Set(idle.map((r) => r.panes))],  // [0] 이어야 한다
  };
})()
```

**숫자보다 `panes_rot`·`panes_noop`를 먼저 본다.** `[1]`·`[0]`이 아니면 **잰 것이 회전이 아니다.**

읽을 때의 단서 넷:
- **`±1` 진동**이라 달이 캐시 범위를 벗어나지 않는다. 한 방향으로 계속 넘기면 매번 새 달을 받아
  **네트워크가 회전 비용으로 잘못 잡힌다.**
- `track.offsetHeight` 읽기가 없으면 `innerHTML` **파싱까지만** 재고 레이아웃이 빠진다.
- `capRx`의 리플로우·범례 재조립은 **양쪽에 다 있어 빼기에서 상쇄된다** — 그게 성립 근거다.
- `calGo`를 우회하므로 `calBusy`가 안 걸린다. 도는 동안 진짜 손가락이 닿으면 오염된다.
  26회가 끝나면 `S.cal`은 원래 달로 돌아온다(+1/−1 짝수).

## 마이그레이션
최신: **`0017_ai_reason`** (T-38 · 2026-08-20) — **`--local` 적용 완료.**
**`--remote`·deploy 여부는 이 층이 모른다**(CLAUDE.md §사람이 하는 것의 상태) —
확인은 `npx wrangler d1 migrations apply personal-os --remote --dry-run`.
- `0017_ai_reason` (T-38) — `guard_events`에 `ai_reason TEXT`. **CHECK도 NOT NULL도 없다.**
  위 `ai_unavailable_reason`(0016)과 **반대편**이다: 저쪽은 판정이 **없을 때**(닫힌 목록 —
  12월에 기계가 센다), 이쪽은 판정이 **있을 때**(자유 문자열 — 사람이 읽는다). **합치지 않는다** —
  자유 문자열이 섞이면 `IN`·`GLOB`으로 세던 쪽이 뜻을 잃는다.
  `ai_verdict`의 CHECK는 **또 안 건드렸다**(0016과 같은 이유 — 400은 발동 행이 사라지는 경로다).
  길이 상한도 SQL이 아니라 서버가 진다(`record()`가 500자로 **자른다** — CHECK 위반은 400이다).
  `test/smoke.ts`의 하드코딩 스키마 목록에 등록 완료 · `docs/schema-current.sql` 재덤프 완료.
  - ⚠️ **순서는 0016과 같다: `--remote` → `deploy` → APK.** 칼럼 없는 서버에 신버전 APK가
    보내면 그 키는 무시돼 **사유가 그냥 사라진다**(발동 행은 무사하다). 반대 순서는 안전하다.
- `0016_guard_unavailable_reason` — ✅ **로컬·원격 적용 + deploy 완료** (2026-08-11)
(확인: `d1 migrations list --remote` → "No migrations to apply!" · `deployments list` 최신 `2026-08-11T10:15:41Z`)
- `0016_guard_unavailable_reason` (T-31) — `guard_events`에 `ai_unavailable_reason TEXT` + 닫힌 목록 CHECK.
  **`ai_verdict`는 안 건드린다.** 티켓 초안은 값을 `'unavailable:timeout'`으로 넓히자고 했는데
  `0010`의 CHECK가 세 값만 받으므로 **위반 → 400 → 기기 `flush()`가 발동 행을 버린다** —
  이유가 아니라 **기록이 사라지고**, 그것도 네트워크가 나쁜 밤에만이다. 그래서 **더하고, 빼지 않는다.**
  `ALTER TABLE ... ADD COLUMN` 하나뿐이라 **테이블 재작성이 없다**(CHECK를 넓히는 쪽은 SQLite에서
  전체 재작성이고 트리거 넷·인덱스 넷을 다시 세워야 한다 — 영구 보존 원장을 통째로 옮기는 일이다).
  `guard_events`엔 트리거가 있지만 `ADD COLUMN`은 트리거·뷰를 건드리지 않는다.
  `test/smoke.ts`의 하드코딩 스키마 목록에 등록 완료 · `docs/schema-current.sql` 재덤프 완료.
  - ⚠️ **순서가 강제된다: `--remote` → `deploy` → APK.** 칼럼 없는 서버에 신버전 APK가 보내면
    그 키는 무시돼 **이유가 그냥 사라진다**(발동 행은 무사하다). 반대 순서는 안전하다.
- `0015_me_history_reason` — ✅ **로컬·원격 적용 + deploy 완료** (원격·배포 2026-08-06 04:40 KST)
- `0015_me_history_reason` (T-10) — `me_history`에 `reason TEXT` (NULL 허용). Guard 모드 하향의 사유가 앉는 자리다(ADR-027).
  **새 테이블을 만들지 않는다** — 만들면 §6.5 자기 보정이 읽을 곳이 둘로 갈린다. `ALTER TABLE ... ADD COLUMN` 하나뿐이라
  트리거·뷰를 건드리지 않고, `me_history`엔 트리거가 없어 0013의 "로컬 통과, 원격 실패" 경로에 해당하지 않는다.
  `test/smoke.ts`의 하드코딩 스키마 목록에 등록 완료.
  - ✅ **원격 적용 + deploy 완료 (2026-08-06 04:40 KST).** 첫 `--remote` 시도가 Cloudflare API **7403**로
    막혔으나 계정 ID·`database_id`·`d1 write` 스코프가 전부 일치했고 같은 토큰으로 `d1 list`·
    원격 `SELECT`가 통과해 **일시적 인증 실패**로 판단했다 — 재시도로 풀렸다. 같은 증상이 또 나면 재시도부터.
- `0014_schema_titles` — ✅ **로컬·원격 적용 + deploy 완료** (2026-07-30 21:55)
- `0014_schema_titles` (T-02) — `lm_schema.body`의 각 필드에 `title`(표시 라벨). **`version`은 올리지 않는다** —
  라벨은 검증 의미를 바꾸지 않고, 올리면 기존 `lm_item` 전부에 §5 거짓 stale 신호가 나간다.
  `json_set`으로 지정 경로에만 얹는다(전체 치환은 원격 body가 갈라져 있으면 조용히 되돌린다). 재실행 멱등.
  `lm_schema`엔 트리거가 없고(0012의 유일한 트리거는 `trg_lm_item_version`) 대상 3행은 0012가 INSERT한 것이라
  모든 환경에 존재한다 — **0013의 "로컬 통과, 원격 실패"가 걸리는 경로가 아니다.** 로컬 적용 후 body 3건 육안 확인.
- `0013_analysis_backfill` — ✅ **로컬·원격 적용 + deploy 완료** (적용 07-29 · deploy 07-30 21:55)
- `0013_analysis_backfill` — 기존 analysis에 날짜 앵커 backfill. **트리거를 내렸다 원문 그대로 복원**한다(아래 사고 참조)
- `0010_guard` — Guard v1: guard_events 재작성(`reaction`에 `ignored` 추가가 CHECK 변경이라 ALTER 불가) · `events` 보호 4필드 · `guard_modes`(ADR-019) · `watch_apps`(ADR-022)
- `0011_guard_sync` — `guard_events.client_id` + 부분 UNIQUE 인덱스(NULL 제외). 로컬 우선 기록의 재시도 멱등 키(ADR-023)
- `test/smoke.ts`의 하드코딩 스키마 목록에 둘 다 등록 완료.
- `0012_life_model` — analysis 앵커(anchor_type/anchor_id/model_tier/source_versions) + 기존 행 backfill · `periods.kind`/`dday_label` · `lm_item`(version 트리거) · `lm_schema` 레지스트리(overview·goals·education v1)
- **다음 번호는 위 §다음 마이그레이션 번호 하나만 본다** — 여기 적어 뒀던 `0014`·`0015`는 이미 쓰였다(T-02·T-10). 번호를 두 곳에 적으면 갈라진다.

### ⚠️ 물린 것 — 로컬 통과, 원격 실패 (2026-07-29)

0012에 `UPDATE analyses ... backfill`을 넣었더니 **로컬은 통과하고 원격만 터졌다**(`SQLITE_CONSTRAINT_TRIGGER`).

- 원인: `trg_analyses_no_upd`가 analyses의 모든 UPDATE를 막는다(설계 §5.4 영구 보존).
- **로컬 `analyses`가 0행이라 UPDATE가 no-op이 되어 트리거가 깨어나지 않았다.** 실제 분석이 쌓인 원격에서만 발화.
- 원격은 전부 롤백됐고(`d1_migrations` 최신이 0011로 남음) 부분 적용은 없었다.
- 조치: backfill을 `0013`으로 분리하고 `DROP TRIGGER → UPDATE → CREATE TRIGGER`(원문 그대로)로 감쌌다. 불변성은 완화하지 않았다.

**교훈 — 트리거가 걸린 테이블에 backfill을 넣을 때는 로컬 검증이 원격을 보장하지 못한다.**
트리거 발화가 데이터 유무에 갈리기 때문이다. 인메모리 sqlite에 **행을 넣은 상태**로 재현해서 확인한다.

직전: `0009_cancel_reason` (…0007_defer_reason · 0008_cancel_task · 0009_cancel_reason)
- **0009_cancel_reason**: `tasks`에 `cancel_reason`(자유 텍스트)·`cancelled_by`(`'user'`/`'guard'`) 추가 + 뷰 재생성(`v_task_stats`에 두 컬럼 노출, 0008의 `state` CASE·`is_waiting`·`v_period_achievement` 조건은 그대로 보존). 트리거 `trg_task_cancel_excl` 미접촉. smoke 스키마 목록에도 등록.
  - ✅ **로컬·원격 적용 완료.** (2026-08-11 실측으로 정정 — 이 줄이 **"원격 미적용 · 사용자 실행 필요"로 3주 가까이 멈춰** 있었다.
    `d1 migrations list --remote`가 "No migrations to apply!"라 0009는 진작 올라가 있었다. **이 절 머리의 규칙이 막으려는 것이 정확히 이것이다.**)
- **0008_cancel_task**: `tasks`에 `cancelled_at`/`cancelled_on` + 트리거 `trg_task_cancel_excl`(완료·취소 상호배제) + 뷰 재생성(`v_task_stats.state` 추가, `v_period_achievement`·`is_waiting` 취소 제외).
  - ✅ 로컬·원격 모두 적용 완료 + deploy 완료 (2026-07-26 확인).
- ✅ 0007까지는 로컬·원격 모두 적용 완료 (2026-07-23 확인).
- `0007_defer_reason`: `schedule_entries`에 `defer_reason TEXT` 추가(미루기 사유). **WORK-PLAN의 `task_entries` 표기는 오기** — 실제 테이블은 `schedule_entries`(예정 항목·rate가 있는 곳).

## 이번 세션 (2026-07-26) — WORK-PLAN-0726 S1~S4 구현 완료
산출물은 지시 문서 **`WORK-PLAN-0726.md`(리포 루트, rev.2) 그대로** — 설계 결정은 직전 세션에 이미 확정했고, 이번 세션에 단계별로 구현·검증했다. plan의 "단계 단위로 진행·정지" 원칙대로 S1→S2→S3′→S4 순서, 각 단계 후 typecheck+smoke+front 확인.
- **[S1 분석 출력량]** `analysis.ts`에 `DEPTH` 상수(`normal`/`detailed`/`deep`, 기본 `detailed`) + `create(…, depth?)`. 잘못된 값·누락은 400 아니라 `detailed` fallback. 선택값은 `context_meta.depth`에 보존(신규 컬럼 없음). `#anal-depth` 세그(`.wsegs`/`.wseg` 재사용) + 펼친 분석 카드에 분량 라벨(추가 보강, 사용자 지시).
  - **plan에 없던 보정**: `boot()`·`goInbox()`가 `$$(".wseg")`로 **전역**을 훑고 있어, depth 세그를 works 뷰 전환과 분리하려면 선택자를 `#scr-works .wseg`/`#anal-depth .wseg`로 좁혀야 했다(아래 전역 클래스명 충돌 항목 참조).
- **[S2 공용 모달]** `.modal`/`.mbox` grid→flex(S2-a) + `#cf-text` `overflow-wrap:anywhere`(S2-b). **다만 사용자가 실제로 겪던 증상은 `#confirm`이 아니라 취소 후 뜨는 노란 toast였다** — `toast(msg,"warn")`이 붙이는 무접두사 `warn`이 전역 15px 원형 배지 규칙(`style.css:79`)과 충돌해 27px로 접혀 있었다. `.toast.warn`→`.toast.t-warn`(app.js·style.css)으로 별도 수정. S2-a 자체는 Chrome 390px·데스크톱 양쪽에서 무해·유효함을 측정으로 확인.
- **[S3′ 마감 시 상태 서술 유도]** `askClose(kind)` — `feelings_text`가 빈 날에만 확인 박스에 `#cf-feel` textarea. `closeDay`보다 먼저 저장(순서 뒤집히면 트리거 409). 백엔드 무변경.
- **[S4 취소 사유]** `0009_cancel_reason`(로컬만 적용) + `cancelTask(env,t,id,reason?)`(500자 제한, append-only) + 취소 확인 박스 `#cf-reason` + 상세 시트 사유 표시(해제 상태에선 미노출).
  - **plan에 없던 보정**: 라우터가 `body(c)`(본문 없으면 400)를 그대로 쓰면 무본문 `POST /cancel`이 깨진다 — smoke가 6건 실패로 잡음. 선택적 본문 헬퍼 `bodyOpt` 추가로 해결.
- 세션 종료 문서화: `docs/api-surface.md`(analyses `depth`, cancel `reason`/`cancel_reason` 응답) · `docs/schema-current.sql`(0009 헤더·tasks 컬럼·view) 재생성.
- 검증 누적: typecheck 통과 · smoke 145→148(S1)→154(S4) · front 157→162(S1)→164(S3′)→167(S4) · 실패 0. **로컬 마이그레이션만(0009). 원격 적용·deploy는 사용자.**

## 직전 세션 (2026-07-23) — CANCEL-0723 취소 상태 도입 (마이그레이션 0008)
- **제3의 종결 '취소'** — 삭제가 1.3 불변성(마감/Guard 참조)에 막히는 일을 기록 보존한 채 목록에서 내린다. 삭제는 분리 유지, 409에서 취소 안내.
- **0008**: `tasks.cancelled_at`/`cancelled_on` + 트리거(완료·취소 상호배제). status enum 확장 안 함(CHECK 2개·테이블 재작성 회피). **상태의 유일한 진실 = `v_task_stats.state`**(`not_finished`/`finished`/`cancelled`). `v_period_achievement`·`is_waiting` 취소 제외(달성률 오염·21일 시계 방지).
- **db**: `stCancelTask`/`stUncancelTask`/`stDeleteOpenEntries`(열린 날 예정만 — `NOT EXISTS(closed)`로 미래 예정 포함, 마감 항목은 트리거 ABORT 회피 위해 제외). `worksDeferring`/`worksByPeriod` status→state, `worksDone` UNION으로 취소행(`on_date`/`kind`), `calEntries`·`classifyAt`에 표시용 `is_cancelled`(분류 로직 불변).
- **services/tasks**: `status==='finished'` 판정을 전부 `state`로. defer/schedule/extend/complete에 취소 가드(409). **`deferTask`는 취소 확인을 예정 조회보다 먼저** — 취소 시 열린 예정이 없어 entry 404가 먼저 터지던 것 수정(smoke가 잡음). 신규 `cancelTask`(kept_dates)·`uncancelTask`(예정 복구 없이 대기 복귀). `deleteTask` 409에 `suggest:"cancel"`.
- **types/index**: `ApiError`에 `suggest?` → onError가 `{error, suggest}`. `TaskStats`에 state/cancelled_at/cancelled_on.
- **프런트**: 상세 시트 `[취소]`·`[취소 해제]` 신설, `[삭제]`(=deleteTask)를 '삭제'로 개명(전엔 '취소' 표기라 충돌). 취소된 task는 완료·미루기·연장·취소 숨기고 '취소 해제'+'취소됨' 배지. 삭제 409(suggest) → '대신 취소하기' 원탭. done 세그에 취소 행(흐림+취소선). cancel 확인문구에 kept_dates.
- 검증: typecheck 통과 · smoke 129→145 · front 151→157 · 실패 0.

## 직전 세션 (2026-07-23) — CAL-PLAN-0723 캘린더 셀 개선 (마이그레이션 없음)
- **1단계** `public/style.css` — 시각 지정 일정 앞 점(`.ev.evt.timed::before`) 제거. 시각 있는/종일 일정의 제목 시작 위치 일치, 제목 1~2자 더 노출. `.timed` 클래스 부여는 향후 훅으로 유지(app.js). 시각은 날짜 팝업에서 '종일/14:30'으로 이미 명확.
- **2단계(진단만·코드 무변경)** 7/24 '포르쉐 바이브 티켓' 미표시 = **분기 B**로 확정. `deferred_to` 없는 살아있는 할 일 2건인데 셀은 할 일을 '항상 1줄 대표'로 압축 → 포르쉐는 대표(created_at 첫)가 아니라 `+1`에만 접힘. 조회 계층 정상·버그 아님 → 3단계 동적 예산으로 해소.
- **선행 A** `public/{app.js,style.css}` — `+n` 배지 잘림(실버그) 수정. 배지가 제목과 같은 `.ev`(overflow:hidden;ellipsis) 안이라 제목이 넘치면 배지까지 잘렸다. 제목을 `.etxt`로 감싸 말줄임 분리, `.ev.tsum` flex화. `.etxt`는 자체 포맷팅 문맥이라 부모 취소선이 안 번져 `text-decoration:inherit`로 완료·이동 취소선 회귀 방지. **처음 `.tt`로 썼다가 전역 `.tt`(14.5px 시트 제목)와 충돌해 셀 글자가 커진 것 발견 → `.etxt`로 개명(`2fad8ad`).**
- **3단계** 셀 memo 노출 + 종류별 보더 인코딩 + 동적 공간 예산 + `.dr` 마커 축소:
  - 3-a `src/db/index.ts` 신규 `calMemos`(날짜별 대표 1건[가장 이른 ts]+개수). `calendar()`(daily.ts) 응답에 **`memos` 추가**. `calDiaryDates`에서 **memo 조건 제거** → `.dr` 마커 = 마감·점수·감정·로그만.
  - 3-b `public/app.js` 셀 공간 예산을 **동적 재배분**(상수 `CELL_MAX_LINES=4, CELL_EV_MAX=2, CELL_TK_MAX=2`): 일정(최대2+초과 '일정 +N')→할 일 1줄→memo 1줄→남으면 할 일 2번째 줄. memo 자리를 먼저 비워 할 일 확장이 memo를 굶기지 않게. 할 일 여러 줄은 살아있는 항목(미완료·미이동) created_at 순 우선, `+n`은 마지막 표시 줄에. memo 줄은 보더 없이 `.etxt`+`+n`.
  - 3-c `public/style.css` `.ev.memo`(border-left transparent로 폭 유지·글자 시작 정렬, `--faint` 500). 3-d 팝업=전문/셀=대표+n 관계 주석.
  - `test/smoke.ts` — memo→diary 마커 검사를 **memo→`memos` 줄 검사**로 교체(마커 축소 반영, 개수 불변).
  - **회귀 잡음 1건**: 셀 memo 변수를 `mm`으로 뒀다가 `rowHtml(row, mm)`의 '월' 파라미터를 셰도잉 → TDZ ReferenceError로 renderCalendar 전체가 던져 셀 0개. **front가 잡아냄** → `mo`로 개명해 해소.
- 검증: typecheck 통과 · smoke 129(무변경, 검사 1건 재타겟) · front 151(무회귀) · 실패 0. **마이그레이션·스키마 무변경.**

## 직전 세션 (2026-07-23) — 실사용 피드백 4건 (UX, 프런트+문서만)
- **[#1 캘린더 기간]** `public/app.js` renderCalendar — `#p-list`를 범례와 동일한 이번 달 겹침 필터(`start_date≤curTo && end_date≥curFrom`)로 축소. `#p-cnt`도 이번 달 기준. 빈 문구 "이번 달엔 기간이 없어요". 목록·편집(`openPeriod`)·다른 달(달 넘기면 재표시)은 유지 — 전체 나열만 제거.
- **[#2 me 직접입력]** `public/{app.js,index.html}` — 고정 5필드를 값 없어도 항상 노출("아직 없음 — 눌러서 입력해요")로 직접입력 진입 명확화 + 필드별 가이드(`ME_GUIDE`: 시트 상시설명 `#me-guide` + textarea placeholder 예시). 5필드가 다 보여 무의미해진 '+ 필드 추가'(`me-add`)·`addMeField` 제거. **백엔드·마이그레이션 무변경**(`putMeField`는 이미 `/^[a-z_]{1,40}$/` 임의 필드 허용). 설계 §3(고정 필드 프레임)과 정합.
- **[#3 캘린더 모션]** `public/{app.js,index.html,style.css}` — 둥근 카드 chrome(border·radius·bg)과 요일줄(`CAL_WKDAYS`)을 고정 프레임 `.calbox`→각 `.calpane`(월 카드)로 이전. 이제 **카드가 통째로 슬라이드**(고정 창 안 grid만 미끄러지던 '흉내' 해소). ‹/› 화살표도 무모션 즉시교체→`calGo` 슬라이드로 통일. 색은 기존 변수만(다크 자동, 함정 5). 슬라이드 자체는 원래 진짜 3-pane 트랙 — 구조 재설계가 아니라 카드 단위로 격상. front line 216(cal-next)이 이제 `calGo` 경로를 타지만 translateX 문자열 동일→검사 통과.
- **[#4 대기행 폭]** `public/index.html` — `#today-wait` `width:65%`→`fit-content;min-width:60%;max-width:90%`, `#tw-text` `nowrap`+`ellipsis`+`min-width:0`. 내용따라 60~90% 유동·항상 한 줄(초과 시 …).
- **문서**: `CLAUDE.md` 기준선 문구 `smoke124/front145`→`129/151`로 정정(직전 세션에 STATE는 이미 129/151, CLAUDE만 지연됨. stash로 변경 전 코드도 151 확인).
- 검증: typecheck 통과 · smoke 129(무변경) · front 151(무회귀) · 실패 0. **사용자 `deploy` + 폰 실사용 정상 확인 완료.**

## 직전 세션 (2026-07-23) — WORK-PLAN-0723 (1~3단계 완료)
- **1단계 완료** (항목 1·2·3·5, 프런트 표시/모션만, 백엔드 무변경):
  - [#1] `public/style.css` — 다크모드 `.wseg.on` 선택색 오버라이드 2곳 추가(미디어쿼리+data-theme). "이월 중" 세그가 hotN 명시도에 밀려 선택 시 배경이 안 바뀌던 것 수정.
  - [#2] `public/style.css` — `.screens`에 `touch-action:pan-y` 추가. 탭 가로 스와이프가 네이티브 스크롤에 먹혀 무효화되던 것. **폰 실측 대기**.
  - [#3] `public/app.js` — 경계 스트레치 진폭↑(`STRETCH_MAX 48→90`, `STRETCH_K 0.3→0.42`)+스냅백 전용 곡선 분리(`STRETCH_BACK_MS 460`/`cubic-bezier(.22,1,.36,1)`, 탭 전환 `TRACK_MS` 미접촉). **폰 실측 후 미세조정**.
  - [#5] `public/index.html` — `#today-wait` 인라인 `width:100%→65%`+`margin-right:auto`(좌측 고정). 대기 행을 시각적으로 하위로.
  - 검증: typecheck 통과 · smoke 124 · front 145 · 실패 0 (무회귀).
- **2단계 완료** (완료율 화면 제거 + 미루기 사유 `defer_reason`, migration 0007):
  - 2-a 완료율 **화면만 제거**(DB 컬럼·완료 로직·`rate=100` 완료 신호는 유지): `app.js` 리스트 pct 배지 2곳·날짜 시트 tag/모노칸 제거·치환. `rbar`/`rateSet`/`rateOf`/`setRate`/`stSetRate` 등 함수·라우터는 그대로.
  - 2-b `migrations/0007_defer_reason.sql`(`schedule_entries.defer_reason`) + `db.stSetDeferReason` + `tasks.deferTask(reason?)` + 라우터/`api.js` `defer(reason)` + 미루기 시트를 완료율 바→사유 `textarea`(#dfx-reason)로 교체. 사유는 도착지(새 예정) 항목에 저장.
  - **2단계 보강**(사용자 지시): task 상세 시트(`#tk-rates`)의 완료율도 제거 — 헤더 `완료율`→`상태`, 본문 `{n}%`/문구를 상태(완료/대기/예정)로, SCHEDULE 이력의 `완료율 {n}%` 제거, 완료 버튼 `완료 100%`→`완료`, 완료 토스트 `완료 100%`→`완료`, 날짜 시트 '할 일' 부제 `완료율·미루기…`→`예정·미루기 이력`. 이제 리스트·날짜 시트·미루기 시트·상세 시트 **어디에도 % 없음**. `rbar`/`rateSet`/`setRate`/DB·완료(rate=100) 로직은 유지(B-1 재사용 대비).
  - `defer_reason` 분석 화면 노출은 향후.
  - 검증: typecheck 통과 · smoke 124→127 · front 145 · 실패 0.
- **3단계 완료** (memo 통합 — 어느 날짜에든 + 날짜 시트 통합 입력 폼, **신규 마이그레이션 없음**):
  - 3-a `memos.ts addMemo`: daily 없으면 404 대신 `stOpenDaily`(기존 INSERT…ON CONFLICT DO NOTHING)로 open daily ensure 후 memo 붙임. → 과거·오늘·미래 어디든 memo.
  - 3-a `db.calDiaryDates`: 캘린더 `.dr` 마커가 **빈 daily**를 오인하지 않게, `status=closed` 또는 score/feelings/logs/memos가 실제로 있는 날만 반환하도록 변경(빈 자동 daily 제외, memo 있는 날은 포함).
  - 3-b `app.js openDay`: 흩어진 3개 추가 UI(일정 버튼·`#day-add` 할일·`#memo-input` memo)를 **통합 추가영역** `addZoneHtml(k,relation,closed)` + `setAddMode`로 합침. 세그 `[일정|할 일|memo]`, relation별 가용(past=일정·memo / today·future=셋 다). 일정은 기존 `openEventSheet`(시각 드럼·마감 경고) 재사용, 할일·memo는 인라인(`addTaskOn`/`sendMemo` 그대로). memo 표시는 전 relation으로 확장.
  - 검증: typecheck 통과 · smoke 127→129 · front 145→151 · 실패 0.

## 최근 세션에서 바뀐 것 (UX 개선 A-1~A-6)
- A-1 [#3] `public/style.css` — 다크모드 캘린더 색: 다른 달 날짜 `var(--faint)`, 일요일 헤더 다크 오버라이드
- A-2 [#7] `src/lib/ai.ts` — Gemini 모델 `-latest` 별칭(gemini-2.5-* 404 회피, 요청 로직 불변)
- A-3 [#5 Phase1] `public/{app.js,style.css}`·`test/front.mjs` — 완료율 인라인 막대 제거→읽기전용, 편집은 미루기 시트만
- A-4 [#2] `public/{app.js,style.css}`·`test/front.mjs` — 캘린더 달 간격(CAL_GAP=20)+터치 씹힘 완화(dragBlockUntil 200·전환 후 즉시 재중심화·calGen 가드)
- A-5 [#1] `public/app.js` — 스와이프 인접탭 프리렌더(드럼 느낌)+민감도 하향(AXIS_LOCK20·축비1.9·RATIO0.35·FLICK0.5)
- A-6 [#4] `public/app.js` — 경계 스트레치(러버밴드 대체, `bindEdgeStretch` 격리·off 가능)
- **기준선 smoke 124 · front 147→145**(A-3에서 인라인 완료율 검사를 미루기 시트 재탭 검사로 이동·통합). 매 커밋 전 검증, 실패 0

## 미해결 / 다음 할 것

> **배포 여부는 STATE가 아니라 `wrangler deployments list`로 확인한다.
> 이 문서는 배포보다 늦게 쓰인다.** 배포는 사용자가 하고 이 문서는 그다음 세션에 갱신되므로,
> 여기 적힌 "배포 전"은 **쓰인 시점의 사실**이지 지금의 사실이 아니다.
> **세 번 물렸다** — "모드 하향은 라이브에 없다" · T-07의 "deploy 대기"가 일주일 넘게 남은 것 ·
> **T-26·T-27의 "아직 배포 전"(2026-08-10)**. 세 번째로 안 것: 이 규칙을 **§저장소 머리에도** 걸어야 한다 —
> 거짓말이 사는 자리는 여기가 아니라 거기다. 걸었다.

> **열린 목록의 주인은 `docs/tickets/BACKLOG-0806.md`다** (2026-08-07 교체).
> `BACKLOG-0803.md`(실사용 12건)는 **닫혔다** — 다섯이 티켓(T-15·T-16·T-17·T-18·T-20)으로 나갔고,
> 10번(취소된 할 일이 Today에)은 T-15 이후 **재현이 안 돼 접었다.** 8/3 파일은 이력으로 남긴다.
> 아래 목록은 그 backlog가 다루지 않는 **이 층의 기술 부채·미실측**이다. 새 항목은 backlog로 간다.

- ✅ ~~smoke가 292/11 — 달력이 fixture를 따라잡았다~~ → **T-36으로 닫혔다 (306/0).**
  고정 날짜는 이제 **검사가 자기 소스를 훑어** 막는다(`CLAUDE.md` 함정 12).
  허용되는 자리는 **순수 함수에 넘기는 시각 인자**뿐이고, 그것도 개수로 센다.

- ✅ ~~낮 실측이 한 파일로 모였다 — `docs/FIELD-TEST-DAY.md`~~ → **2026-08-17에 돌았다.**
  **여섯이 통과**(T-30·T-31·T-32·T-33·T-34·T-35)했고 기록은 `APP-BUILD.md` §결정 기록 §낮 실측이다.
  위 §낮 실측 (2026-08-17)에 요약이 있다.

  🌙 **T-28·T-29만 남았다 — 밤 실측 대기.** Level 4가 상한(`cap`)에 막혀 **격상이 없었고,
  격상이 없으면 `level4State()`가 `false`라 검사할 창 자체가 없다.** 통과도 실패도 아니다.
  ⚠️ **밤 실측을 하는 날 낮에는 `testNotify({level:4})`를 쓰지 않는다** — 상한이 귀속일 기준이라
  **그 밤 01:30~05:00이 낮과 같은 귀속일**이다(경계 06:00). `FIELD-TEST-NIGHT.md` ★에 박아 뒀다.

  ⚠️ **T-34의 짝 하나가 보고에 없다** — ③ 마지막 줄(*"개입 화면에서 뒤로가기 → 아무 일도 없다"*).
  Level 3 개입 화면은 떴으므로 낮에도 볼 수 있는 항이다. 안 봤으면 밤에 함께 본다.

- 📋 **T-28 범위 — 날짜가 붙는 자리는 프런트 셋이다**(ADR-035 ①. 2026-08-10 코드 확인).

  | 자리 | 무엇 | Level 4 중 |
  |---|---|---|
  | `app.js:628` `addTaskOn` | `Api.createTask({title, date:k})` — 캘린더 날짜 팝업 | 오늘이면 내일로 |
  | `app.js:1143` `assignDate` | `Api.schedule(id, k)` — **고른 날짜가 실제로 붙는 유일한 지점** | 오늘이면 내일로 |
  | `app.js:873` 미루기 시트 | `Api.defer(id, from, to)` | 오늘이 **선택 가능하다** — 판단 필요 |

  ⚠️ **`startPick` 호출부(`1248`·`1336`·`1440`)는 자리가 아니다 — 피커를 여는 것뿐이다.**
  셋 다 `assignDate`(1143) 하나로 모인다. 준비 메모 초안이 이 셋을 자리로 적었는데,
  그대로 티켓에 넣었으면 **구현자가 피커 여는 곳 셋을 고치고 정작 날짜가 붙는 `assignDate`를 놓친다.**
  `AGENT-CHAIN` §5가 방금 ADR에 건 규칙("자리를 말할 때는 그 자리를 읽고 쓴다")이 여기에도 걸린다.

  **`1429`(빠른 추가 → 대기)는 손대지 않는다** — 날짜가 없어 오늘의 부담이 아니다(ADR-035 ①).

  **미루기(`873`)가 미결이다.** `pickMinMax()`(1099)가 defer의 하한을
  `p.from >= D ? D+1 : D`로 잡아서, **이미 지난 날의 일을 미루면 오늘이 고를 수 있는 날이 된다.**
  ADR-035는 "새 작업"을 말하는데 미루기는 새 작업이 아니라 **있던 부담의 이동**이다 —
  같은 규칙을 걸지 Cowork 판단이 필요하다.

  서버 쪽 대응은 `POST /api/tasks`(date 있을 때) · `POST /api/tasks/:id/schedule` · `.../defer` 셋이지만
  **ADR-035 ②가 "서버는 막지 않는다"로 정했으므로 게이트는 프런트에만 건다.**

### 🌙 8/11 밤 실측이 찾아낸 것 셋 (기록은 `APP-BUILD.md` §밤 실측 기록)

**충전기를 꽂은 채 자서 Doze는 못 쟀다** — 그 밤의 과녁은 그대로 남았다(아래 🔴).
그래도 예약 일곱이 전부 정시에 발동하고 기록까지 도달했고, **그 과정에서 셋이 드러났다.**

- ✅ **1. 감지가 Guard 자신의 개입 화면을 사용자 활동으로 센다 — T-30으로 고쳤고 2026-08-17 낮에 실측했다.**

  ```
  Level 3 FSI가 화면을 켠다 → screen_on → continuousScreenOnMin 누적
                            → 20분 뒤 watch:bedtime 발동
  ```

  8/11 밤 `watch:bedtime`이 02:20·02:50·03:20·03:50 넷 — **자는 중이었다.**
  02:00 L3가 켠 화면을 감지가 "20분째 화면을 보고 있다"로 읽은 것이다.
  **8/8·8/9 기록에도 같은 모양이 있다.**

  **매일 밤 가짜 전례가 쌓인다.** 9~11월이 §6.5 자기 보정의 전례를 쌓는 기간인데
  (ADR-014) 그 전례에 **Guard가 스스로 만든 활동**이 섞이면, 12월에 그것을 읽을 때
  "이 사람은 새벽에 화면을 오래 본다"가 된다 — 소급해서 못 가른다.
  경로 A(시각)와 경로 B(감지)가 독립이라는 ADR-025의 전제도 여기서 깨진다.

  **T-30이 고쳤다** — `GuardAlertActivity`가 뜰 때·닫힐 때 표본에 `intervene_on/off`를 남기고
  `continuousScreenOnMin`이 그 구간을 뺀다. **원본에는 사실을 적고 계산이 가른다** —
  `screen_off`를 대신 남기는 것은 표본에 거짓을 적는 것이라 안 했다.
  **짝이 둘인 것이 핵심**이다: 뜰 때만 끊으면 `KEEP_SCREEN_ON`으로 켜진 화면에
  `ACTION_SCREEN_ON`이 다시 안 와서 **진짜 사용을 영영 못 센다.**
  ⚠️ **실검사는 §확인 절차다(낮에 2분)** — 이 층에 Kotlin 러너가 없다.
  **2026-08-17 낮에 돌았다**: `intervene_sec 41 · screen_on_sec 39` — 표식이 화면보다 **2초 앞선다.**
  순서가 반대였으면 개입 중에도 연속이 쌓였고, 그게 바로 이 항목이 말하는 그 밤이다.

- 🔴 **2. 새벽에 Level 4 격상이 승인되지 않는다.**

  02:30·03:00·03:30·04:00 **넷 연속 `unavailable`**(= 판정을 못 받았다).
  04:30·05:00은 `deny`였으니 그때는 모델에 닿았다.

  **충전 중이었다** — 네트워크 조건이 가장 좋은 밤인데도 그랬다. Wi-Fi 절전·
  Doze 이전의 유휴 네트워크 제한·제공자 지연 중 무엇인지 **모른다.**

  ADR-024의 fail-closed는 옳지만 **그 비용이 처음 숫자로 나왔다: 그 밤 Level 4는 0건.**
  → **원인을 모르므로 ADR을 고치기 전에 관측부터.** `guard_events`에 이미
  `ai_used`·`ai_verdict`가 있으니 무엇이 더 필요한지부터 정한다.

  **(2026-08-20) 전수를 세니 이 항목이 둘로 갈라졌다** — ADR-038 §맥락. 판정 22건:
  `deny` 11 · `unavailable/timeout` 3 · `unavailable/(이유없음)` 6 · `unavailable/cap` 2 · **`approve` 0.**

  ```
  못 받은 쪽  →  T-37  기기 6초 < 서버 8초라 server_timeout 이 관측 불가였다 (닫힘 · 실물 확인 대기)
  받은 쪽     →  T-38  deny 열한 번의 사유가 어디에도 없다 → 0017 ai_reason (닫힘 · 실물 확인 대기)
  ```

  ⚠️ **`deny` 열한 번은 네트워크 문제가 아니다** — 모델이 닿았고 판단했고 거부했다(둘은 `ai_used=1`).
  **이 항목의 제목("승인되지 않는다")이 두 원인을 한 이름으로 덮고 있었다.**
  둘 다 이제 관측이 열리고, **무엇을 고칠지는 9~11월 데이터가 정한다**(둘 다 판정 로직 무변경).

- 🟡 **3. event 캐시가 안 먹는 것 같다 — `ai_daily_cap`이 새는 자리일 수 있다.**

  04:30에 `deny` 판정이 있었는데 **05:00이 `ai_used:1`**이다. `guard.ts:389`가
  *"② event당 1회 캐시 — 가장 중요한 통제"*라고 적은 것과 어긋난다.

  **코드를 읽어 가를 지점을 좁혔다**(`db.guardAiVerdictFor`):

  ```sql
  WHERE on_date = ? AND event_id = ? AND ai_verdict IN ('approve','deny')
  ```

  캐시 키가 **`on_date` + `event_id`** 둘이다. 그런데 —
  - **`on_date`는 기기가 보낸 `fired_at`으로 계산**되고(`record()`), 조회의 `onDate`는
    **서버 요청 시각의 `t.d`**다. 하루 경계를 넘는 발동에서 **둘이 갈린다.**
  - `ai_verdict`는 **`amendFire`가 나중에 채운다.** 04:30 판정이 아직 서버에
    안 올라갔으면(오프라인 큐) 05:00 조회는 못 본다 — 2번과 같은 밤이라 유력하다.

  **가르는 법**: 두 행의 `on_date`를 본다. 다르면 경계 문제(그러면
  `guardAiCallsOn`도 같은 키라 **일일 상한도 한밤에 리셋된다**), 같으면 기록 도달 순서다.

  ⚠️ **경계 시각을 여기에 적지 않는다** — 설정값이고, `GET /api/guard/schedule`의
  `boundary`가 준다(`CLAUDE.md` §아키텍처 원칙).

  **(2026-08-20) 둘째 갈래의 절반이 T-39로 닫혔다.** *"`ai_verdict`가 서버에 안 올라갔으면
  조회가 못 본다"* — 그 원인 하나가 `amendFire`의 `?: return`이었다. 판정이 `flush()`보다
  늦으면 **큐에 그 행이 없어 조용히 버려졌다.** 이제 판정만 담은 항목이 따로 간다.
  **남은 갈래는 `on_date` 경계다** — 그건 아직 안 봤다. 두 행의 `on_date`를 보는 것이 여전히 가르는 법이다.

- 🟡 **뒤늦은 격상은 `level`에 기록되지 않는다 (T-39가 확인하고 남긴 별건).**

  `flush()`가 판정보다 빨랐던 밤에는 `ai_verdict='approve'`가 실려도 **`level`은 3에 남는다.**
  `trg_guard_event_immutable`의 WHEN 절이 `OLD.level != NEW.level`을 **조건 없이** 막기 때문이다
  (`0010_guard.sql:89` — `reaction`·`outcome`처럼 `IS NOT NULL AND`로 감싼 사후 확정 필드가 아니다).

  **동작은 안 바뀐다** — Level 4 구간은 기기의 `GuardLevel4.note()`가 정한다(ADR-035 ③).
  **기록만 덜 남는다**: 12월에 `level=4`를 세면 그 밤들이 빠진다.
  **복구 가능한 종류다** — `ai_verdict='approve' AND level=3`으로 셀 수 있다.

  푸는 방법은 트리거의 그 줄을 **한 방향으로만** 여는 것인데(`OLD.level < NEW.level` 허용),
  트리거 수정은 SQLite에서 **테이블 재작성**이고 `guard_events`는 영구 보존 원장이다
  (0016이 같은 이유로 CHECK를 안 넓혔다). **값어치와 비용은 Cowork 판단.**

- 🔴 **밤 실측이 W5(8/26) 게이트다 — 두 밤 잤고(8/11·8/18) 깊은 Doze는 아직 표본이 없다.**
  `APP-PLAN` W5의 "Guard 루프가 닫혔다"에 **밤 발동이 포함된다**(2026-08-10 명시).
  낮 확인은 화면이 켜져 있어 전부 통과하지만, Guard가 일하는 시간은 **새벽이고 그때 폰은 Doze**다 —
  그리고 이 실패는 **아무 오류도 내지 않는다.** 알림이 안 뜨는 것으로만 드러나고 그때는 그 밤이 지났다.

  **8/11에 무효가 된 이유는 하나뿐이다: 충전기를 꽂았다.** 충전 중이면 Doze에 안 들어가므로
  그 밤은 이 질문에 아무 말도 하지 않는다. **절차 ④의 네 줄이 전부 그 한 조건을 지키려는 것이다** —
  충전기 · 화면 · 손 · USB. 다음 밤에는 거기부터 본다(`docs/FIELD-TEST-NIGHT.md`).

  **8/18 밤이 절반을 얻었다**(`APP-BUILD.md` §밤 실측). 01:50 이후 셋(02:00·02:30·03:00)이
  **충전 없이 정시**에 울렸고 `cancelSynced`·`outcome=failure`까지 실물에서 돌았다.
  ⚠️ **과녁이던 01:30은 이번에도 Doze가 아니다** — 콘솔을 유지하려고 USB를 꽂아 둔 채 지났다.
  **두 밤 다 같은 이유로 놓쳤고**, 무선 adb가 그 맞바꿈을 없앤다(절차 ④ · 이 기기에서 미검증).
  남은 것 둘: **01:30을 진짜 Doze에서** · **깊은 Doze**(30분 간격이 매번 화면을 켜 타이머를
  리셋한다 — 발동 하나만 멀리 둔 밤이 따로 필요하다).

  **그 밤이 헛되지는 않았다**: 예약 일곱이 정시에 발동하고 기록까지 도달해
  **알람→발동→화면→기록 경로가 완주함을 처음 확인했다.** 남은 질문은 Doze 하나로 좁혀졌다.
  위 셋(감지 자기참조 · 격상 승인 0 · 캐시)은 **Doze와 무관하게 틀려 있던 것**이라
  다음 밤에도 그대로 나온다 — 먼저 고치면 Doze 결과에 섞이지 않는다.

- ⚠️ **T-08′·T-20·T-21·T-22·T-23·T-24·T-25 폰 실측 미실시.** 화면·체감이 결과물인 것들이다.
  T-08′은 하향 시 사유 시트와 60초 대기·409 문구, T-20·T-21은 달 넘김이 실제로 덜 끊기는지,
  **T-22는 새벽 개입이 그 전날에 묶여 있는지**(`watch:bedtime` 실데이터로만 확인된다),
  T-24는 요일 표기, **T-25는 가장자리 폭 10%가 쓸 만한지 — 손가락으로만 정해지는 값이다.**
  T-23은 화면이 안 바뀌는 것이 정상이다(바뀌면 그게 회귀).
  **넷 다 2026-08-07 21:58 KST에 배포됐다** — 실측을 막는 것은 이제 없다.
- 📋 **T-26 — `guard.schedule()`의 `Date.now()`.** T-23이 `loadTime` 재호출은 없앴지만 이 한 줄이 남았다:
  `t`를 받고도 시계를 다시 읽는다. 귀속일이 아니라 '지난 발동 걸러내기' 필터라 어긋남이 밀리초뿐이라
  **Cowork가 T-26으로 미뤘다**(Guard 발동 경로라 신중히). `protectingNow`는 이미 `Date.parse(t.now)`를 쓴다.
- 📋 **접힌 날이 안 보이는지는 front가 못 본다** (T-22 검토). `.gday-section:not([hidden])`로
  **속성**만 세고 실제 표시는 안 본다. 지금 CSS는 맞지만(`.gday-section`에 author `display` 규칙이 없어
  UA의 `[hidden]{display:none}`가 먹는다) 누가 `display:flex`류를 넣으면 **검사는 초록인 채 폰에서
  열 줄이 다 보인다.** T-05에서 한 번 물린 "검사가 못 잡는 CSS 회귀"와 같은 종류다.
- 📋 **캘린더 회전 비용을 아직 안 쟀다.** T-21이 조립을 5→1 pane으로 줄였지만 **회전만의 비용**은
  미측정이다. 표현식은 아래 §캘린더 회전 측정에 있다 — 폰 콘솔(T-14 하네스 CDP)에 붙여넣으면 나온다.
- ✅ **`renderCalendar`의 `cur` 토글 중복 — T-24로 닫혔다 (2026-08-07).** 회전 분기 안의 것을 지웠다.
  공통 토글이 `if/else` **밖**이라 회전·전체 조립·같은 달 세 경로를 다 덮고, 조기 반환 둘은
  회전 분기보다 앞이라 회전하고 토글을 건너뛰는 경로가 없다. 되살리면 front 255/1(`cur` 10 vs 5).
  **회전 측정의 잡음이 하나 걷혔다** — 아래 §캘린더 회전 측정은 여전히 미실시다.
- ⚠️ **Guard 기기 자동화 — T-14 하네스 1단계 완료 · 2단계(시나리오)는 미착수.**
  adb+CDP로 앱 콘솔 JS 실행과 실패 진단까지 가능해졌다. GuardWatch·GuardVerify·GuardRecheck의
  시간 계산·상한·창 판정을 자동 단언하는 시나리오는 아직 없다.
- ✅ **`ai_used` 의미 — T-07로 닫혔다 (2026-07-31).** 요청이 나갔으면 1. 이후 배포에 실려 라이브다.
  (이 줄에 **"deploy 대기"가 일주일 넘게 남아 있었다** — 이 절 머리의 배포 확인 규칙이 막으려는 것이 그것이다.)
- ⚠️ **`front.mjs`의 말일 검사가 31일이 있는 달의 1~30일에만 통과하고 있었다** — T-07 중 발견·수정.
  오늘(7/31)이 처음 걸린 날이다. 7월에 시작한 프로젝트라 아직 30일까지인 달·2월을 안 지났다.
  **같은 종류가 더 있을 수 있다** — 날짜를 문자열로 조립하는 검사는 달·윤년에서 조용히 깨진다.
  (`front.mjs`의 `${D0.slice(0,8)}NN` 꼴을 한 번 훑을 것.)
- 📋 **제안(Cowork 판단) — `README0722.md`를 소유권 표에 넣는다.**
  `AGENT-CHAIN.md` §3 표에 없는 파일이라 오늘 함정 8 문구 정정에서 아무도 안 건드렸다.
  **제안: Claude Code 소유.** 근거는 `STATE.md`와 같다 — 이 파일은 "지금 무엇이 참인가"(구현 현황·함정)를
  적는 곳이고, **그 사실을 만드는 층이 유지해야 낡지 않는다.** 오늘 두 번 물린 실패가 정확히 그것이다
  (`CLAUDE.md` 기준선이 두 세대 낡았던 것 · 함정 8 문구가 셋 다 틀린 채 남았던 것).
  설계 판단이 들어가는 파일이 아니므로 Cowork가 쥘 이유는 없다.
  - **함께 정해야 할 것 — 함정 번호의 원본.** 지금 목록이 `README0722.md`·`CLAUDE.md`·`AGENTS.md`
    셋에 복제돼 있고 **번호가 이미 갈라졌다**(T-01에서 지적: AGENTS의 7번은 '전역 클래스명',
    README·CLAUDE의 7번은 'wait_extensions FK'). 티켓이 "함정 7번"이라 쓰면 두 층이 다른 것을 읽는다.
    → **번호의 원본은 `README0722.md`**로 못 박고, `CLAUDE.md`·`AGENTS.md`는 요약하되 **같은 번호를 쓴다.**
    (복제 자체는 남긴다 — `CLAUDE.md`는 매 세션 실리는 파일이라 함정을 인라인으로 두는 값이 크다.)
- ✅ **T-06 닫힘 (2026-07-31)** — Codex 셸에서 193 · 실패 0 확인.
  기본 환경의 `.wrangler` EPERM 메커니즘은 미해결로 남긴다(리포 밖, 프로젝트를 막지 않는다).
- ⚠️ **`CLAUDE.md` 함정 8 · `AGENTS.md` 검사 절 · `README0722.md` 함정 8이 이제 틀렸다** —
  "끝의 `spawnSync ETIMEDOUT`은 무해하다"는 문구. T-06이 원인을 없앴으므로 **이제 그 메시지가 뜨면
  진짜 hang이다.** 셋 다 이 층 소유가 아니라 고치지 않았다 — Cowork가 한 번에 갱신할 것.
- ⚠️ **T-02 스키마 라벨 — 원격 적용·deploy 완료, 폰 실측 미실시 (2026-07-30).**
  T-01과 함께 확인. 원격 환경이라 보류.
- ⚠️ **T-01 Education 섹션 — deploy 완료, 폰 실측 미실시 (2026-07-30).**
  티켓 §확인 절차 5단계, 특히 **status 3색 다크모드 가독**. 원격 환경이라 보류.
  → T-02의 라벨 확인과 **함께 본다**. 같은 화면을 두 티켓이 건드렸으므로, 실측에서 문제가 나오면
  **어느 변경이 원인인지 가리는 것이 먼저다**(T-01 = 목록·배지·시트 / T-02 = 라벨 문자열만).
- ✅ **`0014` 원격 적용 + `deploy` 완료 (2026-07-30 21:55).** 확인 방법: `d1 migrations list --remote`가
  "No migrations to apply", `deployments list`의 최신이 21:55 — 마지막 코드 커밋(T-05, 19:32) 이후다.
  이 배포가 **`normalizeIso`(UTC 정규화) · T-03 `/api/guard/verify` · T-05 보호 규칙 UI**를 함께 올렸다.
  → **밀린 배포는 없다.** 남은 것은 폰 실측뿐.
- ✅ **마이그레이션 0006·0007·0008 원격 적용 + 코드 `deploy` 완료**(0008은 2026-07-26 확인). 라이브 = 최신(0009 제외).
- ~~마이그레이션 0009 원격 적용 대기~~ — **낡은 항목이라 지웠다.** 0013까지 원격 적용 완료(2026-07-29,
  위 '마이그레이션' 절 기준). 지금 대기 중인 것은 `0014`뿐이다.
- **철회**: T-06 2차에 "Codex는 항상 파일로 리다이렉트" 규약을 `AGENTS.md`에 넣자고 올렸으나,
  3차에서 **stdout back-pressure 가설이 기각**됐다(원인은 `.wrangler/tmp` EPERM). 그 제안은 취소한다 —
  리다이렉트는 로그를 받기 편한 수단일 뿐 규약에 넣을 근거가 없다.
- **폰 실측 대기 (WORK-PLAN-0726 결과물)**: S1 분석 세 가지 분량(보통/자세히/매우 자세히)의 실제 출력 길이 차이 · S2 노란 toast가 정상 폭으로 펼쳐지는지 · S3′ 마감 확인 박스 유도 문구 · S4 취소 사유 입력·표시. 코드는 검증 완료(typecheck·smoke·front)이나 실제 렌더는 폰 확인 필요.
- **`#cf-no` 라벨 충돌**: 공용 확인 모달의 부정 버튼 라벨이 "취소"(=닫기)라 0008의 새 '취소' 기능과 혼동된다. 공용 모달이라 문구를 바꾸면 모든 확인 박스가 흔들리므로 **별도 항목으로 보류**.
- **★ 전역 클래스명 충돌 — 세 번째다. 새 컴포넌트에 짧고 일반적인 클래스명을 쓰기 전 반드시 `grep`으로 기존 사용처를 확인한다.**
  - `.tt` (CAL-PLAN, 2026-07-23) — 캘린더 셀 제목에 썼다가 전역 `.tt`(14.5px 시트 제목)와 충돌해 셀 글자가 커졌다 → `.etxt`로 개명.
  - `.wseg` (S1, 2026-07-26) — 분석 depth 세그에 재사용했더니 `boot()`·`goInbox()`의 **전역 `$$(".wseg")`** 가 works 뷰까지 훑었다 → 선택자를 `#scr-works .wseg`/`#anal-depth .wseg`로 한정.
  - `.warn` (toast, 2026-07-26) — `toast(msg,"warn")`이 붙이는 무접두사 `warn`이 `style.css:79`의 15×15 원형 경고 배지 규칙(`width:15px;inline-grid;border-radius:50%`)을 뒤집어써 노란 알림이 **27px로 접혔다** → 종류 클래스에 `t-` 접두사(`.toast.t-warn`).
  - 위험군: `ok`·`err`·`warn`·`info`·`tt`·`wseg`·`on`·`cur` 같은 2~5자 일반명. **CSS 규칙과 JS 선택자 양쪽을 본다** — `.tt`·`.warn`은 CSS 충돌, `.wseg`는 JS 선택자 충돌이었다.
- **event 취소(0010) 재검토 트리거**: 캘린더 셀에서 취소된 일정에 취소선이 안 그어지는 게 실사용에서 거슬릴 때. 그전까지는 memo로 대체(아래 '설계 정책').
- **폰 실측 후 미세조정**(코드 주석에도 표시): 스와이프 민감도 상수(AXIS_LOCK·축비·TRACK_RATIO·FLICK_V) · 캘린더 gap(20px) · 경계 스트레치 on/off(boot의 `bindEdgeStretch()`) · 다크모드 색(다른달·일요일) · 세로선 농도
- **다음 세션 구현 대기 (B, 미착수)**: B-1[#5 Phase2] 미완료 전환/수동 마감 시 완료율 입력 · B-2[#6] light task 플래그(신호 오염 금지) · B-3[#8] 튜토리얼 상세화(step3 전 필수) · B-4[#4] 러버밴드 원안 보류 기록 → REFACTOR-PLAN "재구상/보류" 정리 예정
- 최종 정리(리포 밖 상위 `Pos/`): 스캐폴딩 중복·대용량 백업

## 설계 정책 (2026-07-26 확정)
- **상태 서술(`feelings_text`) = 기록 — 과거 수정 불가.** 7/25 분석이 지적한 '상태 서술 공백'을 고치려고 날짜 시트에 입력 경로를 여는 안(S3)을 검토했으나 **폐기**했다. `src/scheduled.ts`의 autoClose가 30분마다 `openDatesBefore(env, t.d)`로 오늘 이전의 열린 날을 전부 auto 마감하므로, **'과거의 열린 날'은 최대 30분만 존재한다.** 즉 과거 입력을 열어도 거의 항상 마감 가드에 걸려 실효가 없다. memo와 달리 `feelings_text`는 PUT 덮어쓰기라 성격도 append-only가 아니다.
  - → **정책**: 상태 서술은 그날 안에만 쓴다. 마감 후에는 어떤 경로로도 수정·추가되지 않는다.
  - → 공백 문제는 **입력 경로가 아니라 수집 시점 문제**로 재정의한다. 해법 (a) `closeDay(kind:"manual")`에서 비어 있으면 한 줄 유도, (b) auto 마감된 날은 memo로 사후 보완 — memo는 마감된 날에 붙는 유일한 통로이고 `analysis.ts:124`가 이미 분석 컨텍스트에 넣고 있으므로 정성 채널은 이미 존재한다.
- **취소 사유는 append-only.** `cancel_reason`은 취소 시점에 한 번 쓰고, 취소 상태인 동안 수정하지 않는다. 취소 해제 시에도 NULL로 지우지 않고 남긴다(다음 취소가 덮어쓴다). Guard가 '어떤 이유로 취소했는가' 패턴을 읽을 수 있어야 하기 때문이다. `cancelled_by`(`'user'`/`'guard'`)를 함께 넣는다 — 현재는 항상 `'user'`지만 Guard 개입 4단계가 오면 필수이고 지금 넣는 비용은 0이다. 진짜 이력이 필요해지면 `wait_extensions`(`trg_wait_ext_no_del`/`no_upd`로 불변 강제)와 동형의 `task_cancellations` 테이블로 승격한다. **Guard 스켈레톤 전까지는 컬럼으로 시작한다.**
- **event 취소(0010) 보류 — memo로 대체.** "잘못 만든 미래 일정 = 삭제 / 실제로 취소된 과거 일정 = 취소"라는 의미 구분 자체는 옳다. 그런데 **삭제 쪽은 이미 구현돼 있다** — `trg_events_frozen_del`이 마감된 날 삭제를 막고 열린 날·미래는 자유다. 남는 '과거 취소'는 `trg_events_frozen_upd`가 막는다 — 마감된 날의 event는 UPDATE 자체가 ABORT되므로 `cancelled_at`을 쓰는 것도 불가능하다. 하려면 **불변성 트리거에 구멍을 뚫어야** 하는데, 이는 `0005_delete_scope`(wait_extensions 삭제 잠금)와 같은 종류의 위험한 변경이다. 그리고 "7/30 MT 우천 취소"는 그날 memo로 이미 기록되고 분석이 읽는다(스키마·트리거 변경 0). 따라서 **0010은 하지 않는다.** memo로 안 되는 것은 하나뿐 — 캘린더 셀에서 취소된 일정에 취소선이 안 그어진다. 그게 실사용에서 거슬릴 때가 0010을 재검토할 시점이다.

## 설계와 어긋난 지점
- **완료율 100%** — 지난 세션에 "인라인 100%=즉시 완료"로 이탈했으나, **A-3(#5 Phase1)에서 인라인 막대를 제거하며 폐기 → 완료는 완료 버튼 전용으로 설계 §1.4 재정합**(이제 설계와 일치). 완료율 편집은 미루기 시트에서만.
- **events 마감일 추가** — 마감된 날에도 일정 추가 허용(1.3 "과거엔 추가만 가능"과 정합, 경고문 표시). 설계 위반 아님, 명시적 결정.
- **완료율 화면 제거(2단계+보강, 2026-07-23)** — 완료율 개념을 **화면에서 전면 제거**(리스트·날짜 시트·미루기 시트·**task 상세 시트**). DB `rate` 컬럼·`completeTask`의 `rate=100` 완료 신호·`setRate`/`rbar` 경로는 **물리적으로 유지**(되돌리기 쉽게, 완료 로직 안전, B-1 재사용 대비). 물리적 소거는 향후 별도.
- **미루기 사유 도착지 보존** — 사유(`defer_reason`)는 원 항목이 아니라 **도착지(새 예정) 항목**에 남긴다. 마감된 날의 원 항목은 트리거가 수정을 막으므로, 열린 날/재배정 두 갈래 모두 균일하게 도착지에 붙여 보존.
- **memo 개념 확장(3단계, 2026-07-23)** — 설계 §1.3 "memo = 마감 후 유일한 추가 통로"를 **"memo = 어느 날짜에든 붙는 짧은 노트(마감된 날은 여전히 불변)"**로 확장. daily 없으면 자동으로 빈 open daily를 만들어 붙인다(마감된 날의 불변은 트리거가 계속 강제). 빈 daily가 캘린더 '기록 있는 날' 마커로 오인되지 않도록 `calDiaryDates`를 내용 기준으로 조정. **문서 v1.0 갱신은 사용자 지시로 연기 중이나, 이 확장은 명시적 결정으로 여기 기록.**
  - **(2026-07-23 CAL-PLAN 3단계 갱신)** memo는 이제 캘린더 **셀 본문에 직접** 노출되므로 `calDiaryDates`의 `.dr` 마커 조건에서 **memo를 다시 제외**했다. 마커 = '마감·점수·감정·로그'만 의미(선명해짐). 빈 daily 오인 방지 취지는 그대로 유지.
- **취소 상태 도입(0008, 2026-07-23)** — 설계 §1.4의 종결은 완료·미루기 둘뿐이었으나, 삭제가 1.3 불변성에 막히는 일에 **제3의 종결 '취소'**를 추가했다. `status` enum이 아니라 `cancelled_at`/`cancelled_on` 컬럼으로 저장하고(CHECK 2개·테이블 재작성 회피), **상태 판정은 `v_task_stats.state`(`not_finished`/`finished`/`cancelled`) 하나로 통일**했다. `status='not_finished'` + `cancelled_at IS NOT NULL` = 취소라는 물리적 사실은 schema-current.sql의 tasks 주석에만 남기고, 코드는 `state`만 읽는다. 취소는 **열린 날 예정만 비우고 마감된 날 항목은 보존**(defer의 두 갈래와 동형), 해제 시 예정은 복구되지 않고 대기로 돌아간다. 삭제는 분리 유지, 409에서 취소를 안내. `cancel_reason`은 도입하지 않음 → **2026-07-26 S4에서 `0009_cancel_reason`으로 도입 결정**(위 '설계 정책' 참조). 프런트에서 기존 '삭제=취소 라벨'을 '삭제'로 바로잡아 새 '취소'와 분리.
