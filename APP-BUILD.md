# APP-BUILD — Guard v1 실행 체크포인트

[APP-PLAN.md](./APP-PLAN.md)의 8월 계획을 실행 단위로 쪼갠 것. **이 문서가 진행 상태의 단일 진실이다.**

---

## 재개 방법

대화가 끊기거나 새 세션에서 이어갈 때 **"continue"** 만 입력한다.

```
"continue"
  → 이 문서의 '현재 위치'를 읽는다
  → 다음 단계의 산출물·검증 방법을 제안한다
  → 승인을 받는다
  → 실행하고 산출물을 outputs에 만든다
  → 이 문서의 체크포인트를 갱신한다
```

**승인 없이 다음 단계로 넘어가지 않는다.** 각 단계 끝에서 멈춘다.

---

## 락 — 지금 누가 무엇을 들고 있나

```
WIP: (없음)
```

> 최근 이력(새 것부터) — 전부 커밋 직전 해제:
> **T-13** 릴리스 서명 결과 가드 (codex, 08-04) · **T-11** 수락 재확인·무음 존중 (codex, 08-03) ·
> **T-09** Goals 디데이·Me 분리 (codex, 07-31) · **T-07** `ai_used` 의미 (claude-code, 07-31) · **T-04** L4 검증 기기 배선 (claude-code, 07-31) ·
> **T-06** front 환경 (claude-code, 07-30) · **T-05** 보호 규칙 UI (codex, 07-30) ·
> **T-03** L4 검증 서버 (claude-code, 07-30) · **T-02** 스키마 라벨 (claude-code, 07-30) ·
> **T-01** Education 폼 (codex, 07-30)

> 형식: `WIP: T-07 (codex, 07-30 14:20) — public/app.js, public/index.html`
> 비어 있으면 아무도 안 들고 있다는 뜻. **같은 리포에 실행 에이전트를 둘 동시에 붙이지 않는다.**
> 규약은 `AGENT-CHAIN.md`.

---

## 현재 위치

> **완성의 정의 ✅ 충족** — 보호 모드 · 데드라인 역산 발동 · FSI 잠금화면 점유 · 사유+대기 마찰 · guard_events 기록 · outcome 확정
> **S1.1·S1.2·S1.4 ✅ / S2.1~S2.6 ✅ / S3.1·S3.2 ✅ / S4.1·S4.2·S4.4 ✅ / P1-a·P1-b·P1-c ✅**
> **S1.3 🔄** ①낮3분 ✅ ②재부팅 ✅ / **③밤 03:00 미실시**
> **남은 것(완성의 정의 밖)**: S3.3(L4 작업 차단) · S3.4(모드 UI) · S4.3(알림함) · S1.5(권한 배너)
> **2026-08-03 실측 완료** — T-04·T-05·T-09·T-11·T-12 닫힘. 기준선 **237/210/0**.
> **S3.1b ✅** L4 AI 검증 — 서버측(T-03) + **기기 배선·실측(T-04)**. **ADR-024의 루프가 닫혔다.**
> **T-01 ✅** P1-c의 Education 폼(Codex 구현 · Claude Code 검토) · **T-02 ✅** 스키마 라벨(0014)
> **T-09 ✅** P1-c의 Goals 스키마 폼·귀속일 디데이·하단 바 Me 분리 — 폰 실측 완료
> **T-05 ✅** 보호 규칙 UI — **경로 A(시각)가 화면에서 알람까지 이어짐을 실측**
> **T-13 ✅** release APK 실제 인증서 = keystore 인증서 가드 · 빌드별 SHA-256 출력 · `adb install -r` 성공
> **기준선은 여기 적지 않는다** — 숫자의 자리는 `STATE.md` §기준선 하나뿐이다.
> (여기 `smoke 237 · front 210`이 박혀 있었는데 실제로는 303/271이었다. 숫자를 만드는 층이
> 못 고치는 파일에 숫자를 두면 반드시 낡는다 — `STATE.md` §규약 불일치 4번과 같은 사고다.)
> **T-33 ✅ · T-34 ✅** 최종 검토 합격 — outcome 카드 상태 셋 · 뒤로가기가 맨 위 하나만.
> 둘 다 **2026-08-11 20:59 KST 배포로 라이브**(2026-08-14 실측 — `deployments list` + 라이브 `/app.js`).
> **낮 실측은 `docs/FIELD-TEST-DAY.md`로 모였다** — APK 설치분 다섯(T-28·T-29·T-30·T-31·T-34)
> + 웹 둘(T-32·T-33). 결과는 아래 §결정 기록에 *"어느 조건에서 어떻게 됐다"*로 적는다.
> **T-35 ✅** 세 번 밀린 일의 출구 — **ADR-036**(예정→대기 간선은 만들지 않는다).
> `defer`의 2주 상한을 `defer_count ≥ 3`인 항목에서만 푼다. **서버·스키마·트리거 무변경.**
> **T-36 🟡** 검사 날짜를 상대로 — smoke **292/11 → 306/0**, `verify` exit 0 복구. 검토 대기.
> 고정 날짜는 이제 **검사가 자기 소스를 훑어** 막는다(`CLAUDE.md` 함정 12).
> 최종 갱신: **2026-08-14** — T-33·T-34·T-35 검토 합격 · 낮 실측 문서 · T-36 (마감 8/31)

---

## 진행표

범례 — ⬜ 대기 · 🔄 진행 · ✅ 완료 · ⏸️ 보류

### 1주 — 폰이 허용하는가만 실측

| 단계 | 내용 | 상태 |
|---|---|---|
| **S1.1** | Capacitor 골격 + 권한 선언 + 서명 | ✅ 실기기 6항목 통과 · T-13 결과 기준 서명 가드 |
| **S1.2** | 알림 채널 + FSI + 개입 화면 + 소리/진동 정책 | ✅ 일반·진동·무음 3모드 통과 |
| **S1.3** | 알람 예약 + 재부팅 재등록 | 🔄 ① 낮 3분(앱 완전종료) ✅ · ② 재부팅 복구 ✅ · **③ 밤 03:00 미실시** |
| **S1.4** | 포그라운드 서비스 + UsageStats | ✅ |
| **S1.5** | 게이트 화면 + 권한 배너 | ⬜ |

**①의 통과가 게이트의 본체다** — 앱을 완전히 종료한 상태에서 시스템이 스스로 깨워 개입 화면을 띄웠다. 8월 계획이 서 있는 가정이 증명됐다.

### 2주 — 기록 구조 + 감지

| 단계 | 내용 | 상태 |
|---|---|---|
| **S2.1** | `guard_events` 확장 (`0010_guard`) | ✅ |
| **S2.2** | `events` 보호 필드 + 서비스·라우트 | ✅ |
| **S2.3** | 보호 일정 pull + 기기측 예약 (경계+10분 재동기화) | ✅ |
| **S2.4** | 로컬 우선 기록 (`0011_guard_sync` client_id) | ✅ |
| **S2.5** | 감지 수집 (전면 앱 · 화면 on/off) | ✅ |
| **S2.6** | **감지 기반 발동** (취침 창 규칙 · `ignored` 확정) — ADR-025 | ✅ 추가된 단계 |

**S2.6은 원래 계획에 없었다.** 발동이 보호 일정에만 걸려 있으면 루프가 몇 주에 한 번 돌고,
그러면 9~11월에 전례가 쌓이지 않는다 — 실사용 기간의 목적 자체가 무너진다.
경로 B(감지)를 시각 경로와 독립으로 추가하고, 반응 없는 발동을 cron이 `ignored`로 닫는다.

### 3주 — 개입

| 단계 | 내용 | 상태 |
|---|---|---|
| **S3.1** | 데드라인 역산 + Level 1~4 결정론 발동 | ✅ S2.3 흡수 · T-05 시각 경로 실측 |
| **S3.2** | Override 마찰 (사유 + 대기) | ✅ T-11 수락 재확인·무음 존중 실측 |
| **S3.1b** | Level 4 AI 검증 게이트 (ADR-024) | ✅ 서버(T-03) + 기기·실측(T-04) + `ai_used` 의미(T-07) |
| **S3.3** | Level 4 신규 작업 차단 (`#stale` 패턴) | ⬜ |
| **S3.4** | 모드 2종 UI + 하향 금지 | ⬜ |

### 4주 — 루프 닫기

| 단계 | 내용 | 상태 |
|---|---|---|
| **S4.1** | `risk_snapshot` 수집 | ✅ **T-32로 항이 다 찼다** — 그전까지는 절반이었다(아래) |
| **S4.2** | outcome 확정 카드 | ✅ **루프가 닫혔다** — 서버 3건 + **T-33으로 프런트 6건**(아래) |
| **S4.4** | PC 스키마 자리 (`watch_apps`) | ✅ |
| **S4.3** | 알림함 | ⬜ **한 줄도 없다** (2026-08-11 전수 확인) |

**W4 세 항목 전수 확인 (2026-08-11).** 이 표가 8/04에 멈춰 있어 코드와 대조했다.

**S4.1 — `✅`가 오해를 부르고 있었다.** 수집은 켜져 있었지만 **계획이 요구한 항의 절반이 없었다.**
APP-PLAN §위험도의 표가 요구한 여섯 중 담긴 것은 `현재 시각`·`foreground app` 둘뿐이었고,
`데드라인까지 남은 시간`·`최근 Log 활동`·`수면 추정`·`Feelings·Score 추세`가 통째로 빠져 있었다.
`risk_score`는 **항상 `NULL`**이었다 — 기기가 그 키를 안 보냈다.

**빠진 것은 전부 출처가 서버였고, 원인은 실수가 아니라 ADR-021의 부산물이다.**
계획을 쓸 때는 서버가 발동을 판단했으므로 그 자리에서 `logsRange`를 부를 수 있었다.
ADR-021이 발동을 기기로 옮기면서 스냅샷 생산도 같이 기기로 갔고, 기기엔 `logs`·`feelings`·`daily`가
없어 서버 항이 사라졌다 — **그 누락이 어디에도 안 적혔다.** `T-32`가 `record()`에서 메웠다
(기기 항은 최상위 그대로, 서버 항은 `server` 아래, 전부 `firedAt` 기준).

**S4.2 — 검사 구멍은 `T-33`으로 닫혔다.** 전수 당시 `#td-guard`·`loadGuardOutcome`·`guardPending`이
`test/` 전체에 **0건**이었고(`front.mjs`의 `outcome`은 전부 Guard 메모리 — 다른 화면),
실패 시 `catch`가 `display:none`이라 **조용히 사라졌다** — 카드가 안 떠도 271/0이었다.
`T-33`이 `data-state`로 `ask`·`none`·`error`를 갈라 프런트 검사 6건을 붙였다(front 271 → 277).
**화면 동작은 무변경** — `catch`가 화면을 안 막는 판단은 옳았고, 그 실패가 이름을 갖게 됐을 뿐이다.

**S4.3 — 없다.** `src/`·`public/`·`migrations/`·`test/` 전체에서 `notif`를 대소문자 무시로 훑어
**파일 0개**. 테이블·라우트 셋·`api.js` 메서드·Today 배지·Me 규칙 토글·검사 전부 없다.
계획도 미착수로 안다(`APP-PLAN.md:511`이 번호를 비워 뒀다). **감축 순서 1순위**라 계획상 가장 먼저 버릴 것이다.

### 병행 — Me Reinforcement Plan Phase 1

| 조각 | 내용 | 상태 |
|---|---|---|
| **P1-a** | `0012_life_model` — analysis 앵커 · 기간 constraint · `lm_item` · 스키마 레지스트리 | ✅ |
| **P1-b** | 검증기 · CRUD · Me→Overview 이관 · analysis가 앵커 기록 | ✅ |
| **P1-c** | 하단 바 (4탭/Me) 분리 · Education 폼 · Goals 디데이 | ✅ T-01·T-09 · 폰 실측 완료 |

### 감축 순서

일정이 밀리면 이 순서로 줄인다: 알림함 → 모드 UI → 위험도 항 개수.
**줄이지 않는 것**: `guard_events` 기록 구조 · outcome 연결 · 감지 수집. 데이터는 소급해서 만들 수 없다.

---

## 단계별 공통 절차

각 단계는 이렇게 끝난다.

```
1. 산출물을 outputs에 만든다
2. 리포에 넣을 위치를 명시한다 (신규 파일 / 기존 파일의 어느 지점)
3. 검증 방법을 준다 (typecheck · smoke · front · 실기기 확인)
4. 이 문서의 체크포인트를 갱신한다
5. 멈춘다 — 다음 단계는 승인 후
```

**서버 코드가 들어가는 단계**(S2.1·S2.2·S4.x)는 기존 검증을 통과해야 한다 — 기준선은 위 '현재 위치'의 숫자를 본다(여기에 적으면 낡는다).

**네이티브 코드가 들어가는 단계**(S1.x·S2.3~5·S3.1~2)는 자동 검증이 없다. 실기기 확인이 유일한 검증이므로 매 단계 산출물에 **무엇을 눌러서 무엇을 확인하는지**를 함께 적는다.

---

## 결정 기록 (실행 중 확정된 것)

실행하다 보면 계획에 없던 선택이 나온다. 골자가 바뀌면 APP-ADR로, 세부는 여기에.

| 날짜 | 단계 | 결정 | 근거 |
|---|---|---|---|
| 07-28 | S1.1 | 웹은 **원격 URL 로드**(`server.url`) | 8월에 프런트를 자주 고친다. deploy만으로 앱에 반영 → APK 재빌드 없음. 오프라인 화면은 포기하되 Guard 발동은 네이티브라 무관 |
| 07-28 | S1.1 | `appId = dev.mond1424.personalos` | 영구. 바꾸면 다른 앱이 되고 keystore 연결이 끊긴다 |
| 07-28 | S1.1 | keystore는 `~/keys`, 비밀번호는 **환경변수만** | 리포에 커밋될 경로를 아예 만들지 않는다 |
| 07-28 | S1.1 | 서명 환경변수 없으면 **빌드 중단** | 없으면 gradle이 조용히 디버그 키로 서명하고, 그 APK는 릴리스 키로 업데이트 불가. 조용한 실패를 막는다 |
| 07-28 | S1.1 | `minifyEnabled false` | 난독화가 리시버·서비스 클래스명을 바꾸면 매니페스트 참조가 깨지는데, 그 실패는 새벽 알람에서만 드러난다 |
| 07-28 | S1.1 | 8월치 권한을 **한 번에 선언** | 매니페스트를 매주 고치지 않기 위해. 선언만으로는 아무 일도 안 일어난다 |
| 07-28 | S1.1 | 개발 환경 = **Windows / cmd**. 절차를 Windows용으로 재작성 | `setx`·`gradlew.bat`·Android Studio 번들 JDK 경로 |
| 07-28 | S1.1 | 리포를 `C:\dev\`로 옮기기 **권장** | 기존 경로에 한글(`새 폴더`)·공백·깊은 중첩이 겹침. Android 빌드는 MAX_PATH·비ASCII에 취약하다 (Worker만 빌드할 땐 드러나지 않던 문제) |
| 07-29 | S1.1 | 서명 자격증명을 `android/keystore.properties` 우선 + 환경변수 폴백 | Windows `setx`가 `& ^ % !`를 깨뜨린다. properties 파일은 cmd를 안 거친다 |
| 07-29 | — | `e2e.mjs`에 `CI=true` | wrangler 4.1x부터 `--local` 마이그레이션에도 프롬프트가 붙어 검사가 멈췄다. capacitor 설치 시 락파일 갱신으로 wrangler가 올라간 여파 |
| 07-29 | — | **기준선 정정 — smoke 154 · front 167** (145/157 아님) | 대화 초기에 raw로 받은 STATE가 7/23 스냅샷이었다. 로컬 STATE.md가 진실. 마이그레이션도 0009까지 있어 계획서 번호를 0010~0012로 밀었다 |
| 07-29 | S1.2 | 알림 채널 ID에 `_v1` 접미사 | 채널 설정은 생성 뒤 코드로 못 바꾼다. 소리·중요도를 고치려면 ID를 올리는 수밖에 없다 |
| 07-29 | S1.2 | 방해금지는 **`USAGE_ALARM`으로만** 통과 (setBypassDnd 안 씀) | 알림 정책 접근 권한이 따로 필요하고, 알람 카테고리가 방해금지의 '알람 허용'(기본 켬)을 이미 탄다. 취침 모드에 막히면 새벽 게이트에서 드러난다 |
| 07-29 | S1.2 | Kotlin 툴체인 도입 (Capacitor 템플릿은 Java 전용) | Guard 네이티브 코드 전부가 Kotlin이고, ADR-017 전환 시 그대로 살아남는다. 여유 있는 1주차에 깔아 둔다 |
| 07-29 | S1.2 | `MainActivity`는 Java로 두고 한 줄만 추가 | Capacitor 생성 파일이라 변경을 최소화. Kotlin 클래스는 Java에서 그대로 호출된다 |
| 07-29 | S1.2 | **"밀어서 못 지우는 알림"을 포기.** 마찰은 **화면 + 재발동 주기**가 진다 | Android 14부터 `setOngoing(true)`이 무력화됐다. 앱이 만드는 못 지우는 알림은 존재하지 않는다(시스템·기기정책만). 실기기 sdk 36에서 확인. **설계 §6.3의 마찰을 알림이 아니라 `GuardAlertActivity`와 Level 4 재발동(30분)이 지도록 재배치** |
| 07-29 | S1.2 | 알림 dismiss를 **`reaction='ignored'` 신호로 기록**(S2.4에서) | 못 막는다면 대신 센다. 밀어서 지운 것 자체가 Guard Memory의 데이터다 |
| 07-29 | S1.2 | 뒤로가기 차단은 `OnBackInvokedDispatcher`로 | targetSdk 36 → Android 15부터 예측형 뒤로가기가 기본 활성이라 `onBackPressed()`가 호출되지 않는다. 구버전용으로 둘 다 둔다 |
| 07-29 | S1.2 | `testNotify`에 `delayMs` | FSI는 **발동 시점에** 화면이 잠겨 있어야 Activity를 띄운다. 콘솔 실행 후 잠그면 이미 늦다 |
| 07-29 | S1.2 | **'알람 앱 위장' 편법 불필요** — FSI·USE_EXACT_ALARM은 이미 부여됨 | Android 14의 FSI 제한은 **Play Store가 설치 시 회수**하는 방식이다. 사이드로드는 그 경로를 안 탄다. 실기기 `state()`가 `fullScreenIntent: true` 반환 |
| 07-29 | S1.2 | **소리의 주인을 알림 채널 → 개입 화면으로 이전** | 채널로는 요구 셋을 못 맞춘다: 생성 후 변경 불가(설정 토글 불가) · 알람 카테고리라 벨소리 모드 무시 · 소리가 POST_NOTIFICATIONS에 묶임. `guard_high_v1` 폐기, 조용한 채널 + 폴백 채널로 분리 |
| 07-29 | S1.2 | 벨소리 모드 존중 — 무음=화면만 / 진동=진동만 / 일반=설정대로 | 사용자 요구. **대가: 무음 모드가 0마찰 우회로가 된다**(§6.3의 '비용 없는 Override'). `overrideSilentAtL4`로 Level 4만 뚫는 절충을 남김 |
| 07-29 | S1.2 | 알림 권한 없어도 **개입 화면은 뜬다** | 원래 `fire()`가 `!canPost`에서 조기 반환해, 오버레이가 멀쩡해도 Guard가 통째로 침묵했다. 두 경로를 독립시킴 |
| 07-29 | S3.1b | **Level 4만 AI 검증**, 실패 시 Level 3 강등 (ADR-024) | Level 4 오발동이 이탈을 부른다. 실패 방향을 강등으로 잡아 ADR-021의 '개입이 사라지지 않는다'를 유지 |
| 07-29 | S1.3 | **릴리스 빌드 하나만 쓴다.** `npx cap run android`(디버그) 금지 | 서명이 달라 덮어쓰기가 안 되고 Android가 삭제 후 재설치를 강제한다 → 권한·`GuardAlarmStore`가 매번 초기화. 재부팅 복구 테스트 실패의 원인. `webContentsDebuggingEnabled: true`로 릴리스도 `chrome://inspect` 가능 |
| 07-29 | S3.2 | **사유 길이 하한(20자) 폐기** — 비어 있지만 않으면 통과 | 실사용에서 마찰이 아니라 **강제**로 읽혔다. §6.3이 원하는 건 "비용을 치르게 한다"이지 "분량을 채우게 한다"가 아니다. 마찰은 대기 시간이 지고, 사유는 §6.5의 데이터로서 존재만 하면 된다 |
| 07-29 | S3.2 | **[그래도 계속하기] 진입 즉시 소리·진동 정지** | 울리는 채로는 사유를 쓸 수 없다 — 그건 마찰이 아니라 소음이다. 우회로도 아니다: 화면은 남고, 반응이 기록되기 전까진 끝난 게 아니며, Level 4는 30분 뒤 다시 온다 |
| 07-29 | S2.6 | 연속 사용은 **누적이 아니라 마지막 `screen_off` 이후**로 센다 | 누적으로 세면 하루 종일 폰을 쓰는 날엔 취침 창 진입 즉시 임계를 넘는다. 규칙이 가르려는 것은 "잠깐 확인"과 "붙잡고 있음"이고, 그 차이는 연속성에만 있다 |
| 07-29 | S2.6 | 서비스 시작 시 화면이 켜져 있으면 **`screen_on`을 직접 기록** | 화면 on/off는 런타임 등록만 받으므로 서비스가 시작된 시점의 상태는 아무도 기록해 주지 않는다. 그러면 연속 시간이 다음 off/on 주기까지 0에 머물러 감지 발동이 침묵하는데, 그게 재부팅 직후 — 하필 밤에 드러난다 |
| 07-29 | S2.6 | **`ignored` 확정 유예 = 36시간** (즉시 아님) | 기기가 오프라인이면 발동과 반응을 함께 늦게 올리고(ADR-023) 재동기화는 하루 한 번이다. 서버가 먼저 박으면 트리거가 진짜 반응을 막고 소급 복구가 불가능하다. 늦게 닫히는 비용은 거의 없다 — 집계는 10월이다 |
| 07-30 | S2.6 | 기기가 보낸 UTC 시각을 **서버에서 정규화**(`normalizeIso`) — 기기를 안 고친다 | 실기기 덤프에서 `fired_at: "...Z"` 발견. 귀속일 계산이 표기된 시각 자리를 로컬로 읽어 KST 09~15시 발동이 전날로 귀속된다. 서버에서 흡수하면 APK 재빌드가 없고, 9월 PC 에이전트가 같은 실수를 해도 한 번 더 안 물린다 |
| 07-29 | S2.6 | `finalizeIgnored` 실패를 **삼킨다**(`.catch`) | 자동 마감과 같은 cron에 얹혀 있다. Guard 확정이 던지면 그날의 자동 마감이 통째로 멈추는데, 둘은 서로 무관한 일이다 |
| 07-29 | S1.2 | **깨어 있는 화면 개입 = `SYSTEM_ALERT_WINDOW`** (오버레이를 8월로 당김) | FSI는 화면이 잠겼을 때만 Activity를 띄우는 게 계약이다. 깨어 있는 화면을 덮으려면 백그라운드 액티비티 시작이 필요하고, Android 10+는 이를 막되 '다른 앱 위에 표시' 보유 시 예외를 준다. `fire()`가 Level 3+에서 알림과 **별개로** `startActivity`. **ADR-018의 오버레이 시점(9월)이 앞당겨짐** — 다만 '화면 일부 가림 UI'가 아니라 '항상 전체 화면 개입'이라 비용은 20줄 |
| 08-04 | T-13 | release 서명 가드를 **자격증명 존재가 아니라 생성 APK의 실제 인증서 결과**로 판정 | APK·keystore·설치 앱 SHA-256을 먼저 비교해 현재 셋이 같음을 확인했다. `assembleRelease`가 `apksigner` 결과와 keystore 인증서를 비교하고 지문을 매번 출력한다. 불일치면 설치 전에 빌드를 실패시킨다 |

---

## 밤 실측 기록 (`docs/FIELD-TEST-NIGHT.md` §기록)

**"확인했다"가 아니라 "어느 조건에서 어떻게 됐다"를 적는다.** 9월에 문제가 나면 여기와 비교한다.

> **낮에 되는 것은 `docs/FIELD-TEST-DAY.md`다**(2026-08-11 신설). 둘은 짝이다 —
> **밤에만 되는 것**(Doze · `unavailable`의 이유)은 저쪽, **낮에 되는 것**은 이쪽.
> 낮 실측 결과도 이 절에 적는다.

### 2026-08-11 밤 — **충전기 연결. Doze 미성립이라 이 밤의 과녁은 못 쟀다**

```
조건    충전기 연결 · USB 연결 상태로 취침 → Doze에 안 들어갔다
예약    일곱 — 02:00 L3 · 02:30~05:00 L4 여섯
결과    일곱 전부 정시 발동 · guard_events 기록까지 도달
```

**잰 것**: 알람 → 발동 → 화면 → 로컬 기록 → 서버 동기화까지 **경로가 완주한다.**
7월 말 Guard v1이 선 뒤 이 경로 전체가 한 번에 돈 것은 처음이다.

**못 잰 것**: **Doze.** 충전 중이면 Doze에 들어가지 않으므로 이 밤은 "알람이 Doze를
뚫는가"에 대해 **아무 말도 하지 않는다.** `FIELD-TEST-NIGHT.md` ④의 첫 줄
("충전기를 꽂지 않는다 — 충전 중이면 Doze가 안 걸린다. 시험이 무의미해진다")이
가리키던 그 상황이다. **W5(8/26) 게이트는 열리지 않았다 — 다시 자야 한다.**

| 관측 | 값 |
|---|---|
| Level 4 승인 | **0건** |
| — `unavailable` | 넷 (02:30 · 03:00 · 03:30 · 04:00) |
| — `deny` | 둘 (04:30 · 05:00) |
| `watch:bedtime` | 넷 (02:20 · 02:50 · 03:20 · 03:50) — **자는 중이었다** |

**세 발견이 나왔고 전부 `STATE.md` §미해결에 있다.** 요약:

1. **감지가 Guard 자신의 개입 화면을 사용자 활동으로 센다** — Level 3 FSI가 화면을 켜고,
   그 `screen_on`이 `continuousScreenOnMin`에 쌓여 20분 뒤 `watch:bedtime`이 뜬다.
   8/8·8/9 기록에도 같은 모양이다. **매일 밤 가짜 전례가 쌓인다** → 다음 티켓
2. **새벽에 Level 4 격상이 승인되지 않는다** — 충전 중(= 네트워크 조건이 가장 좋은 밤)에도
   넷 연속 `unavailable`. ADR-024 fail-closed의 **실제 비용**이 처음 숫자로 나왔다
3. **event 캐시가 안 먹는 것 같다** — 04:30에 판정이 있었는데 05:00이 `ai_used:1`이다.
   `ai_daily_cap`(통제 ③)이 새는 자리일 수 있다 → 확인 필요

**이 밤이 헛되지 않은 이유**: Doze는 못 쟀지만 **Doze가 아닌 조건에서도 셋이 틀려 있었다.**
충전기를 뽑았어도 이 셋은 그대로 나왔을 것이고, 그랬으면 Doze 결과에 섞여 원인이 흐려졌다.

---

## 막힌 것 / 미해결

| 발견 | 단계 | 상태 |
|---|---|---|
| 리포 경로에 한글·공백 (`Desktop\새 폴더\Pos\...`) | S1.1 | ✅ `C:\dev\`로 이동 완료 |
| **상시 서비스가 반복 스와이프에서 안 살아난다** | S1.4 | **고치지 않는다.** 알람으로 되살아난 서비스는 앱 task에 속하지 않아 두 번째 스와이프에서 `onTaskRemoved`가 불리지 않는다. 뚫으려면 제조사별 대응이 되고, 잃는 것은 감지 표본과 상시 표시뿐이다 — 알람은 스와이프를 견디고(S1.3 ①) 감지는 보조 입력(ADR-018·021). 앱을 열면 항상 복구된다 |
| 밤 03:00 실측 (S1.3 ③) | S1.3 | 미실시. ①(앱 완전종료 3분)·②(재부팅 복구)가 통과해 핵심 가정은 증명됨. 실사용 진입 전 한 번 |
