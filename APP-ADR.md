# APP-ADR — 앱화 설계 결정 기록

Personal OS 앱화([APP-PLAN.md](./APP-PLAN.md))에서 내린 결정과 그 근거를 보존한다.
APP-PLAN은 **무엇을 만드는가**, 이 문서는 **왜 그렇게 정했고 무엇을 기각했는가**를 담는다.

각 기록은 `맥락 · 결정 · 근거 · 기각한 대안 · 감수하는 비용 · 재검토 트리거` 형식.
**재검토 트리거**가 발생하면 해당 ADR을 다시 연다. 그 전에는 결정을 되묻지 않는다.

## 상태(Status) 규약

| Status | 의미 | 읽는 법 |
|---|---|---|
| `Proposed` | 논의 중 | 아직 따르지 않는다 |
| `Accepted` | 현행 | **이 결정을 따른다** |
| `Superseded` | 다른 ADR로 대체됨 | 이력으로만 읽는다. 현행은 대체 ADR |
| `Deprecated` | 대체 없이 폐기 | 전제가 사라졌다. 따를 것 없음 |

**대체 규칙**

1. **기존 ADR의 본문을 고치지 않는다.** 상태 줄만 바꾸고 `Superseded by`를 단다 — 그때 무엇을 왜 그렇게 판단했는지가 보존되어야 대체 결정을 평가할 수 있다
2. 새 ADR에 `Supersedes ADR-0NN`을 달고, **맥락**에 "무엇이 바뀌어서 뒤집는가"를 쓴다
3. **번호를 재사용하지 않는다.** 폐기된 번호도 비워 둔다
4. 부분 수정은 대체가 아니다 — 결정의 골자가 바뀔 때만 새 ADR을 만든다. 세부 조정은 APP-PLAN에서 처리한다

```
## ADR-003. …
**Status** Superseded by [ADR-021](#adr-021) · **Phase** 0 · 2026-07-26

## ADR-021. …
**Status** Accepted · **Supersedes** [ADR-003](#adr-003) · **Phase** 0 · 2027-01-14
```

## 목록

| # | 제목 | Status | Phase |
|---|---|---|---|
| [001](#adr-001) | 인증을 principal / credential / session 3계층으로 분리 | `Accepted` | 0 |
| [002](#adr-002) | 웹은 쿠키 세션, 네이티브는 Bearer — 전달 방식 이원화 | `Accepted` | 0 |
| [003](#adr-003) | profile 레이어는 새 테이블이 아니라 `settings` 확장으로 | `Accepted` | 0 |
| [004](#adr-004) | 오프라인은 읽기 전용 — 쓰기 큐를 구현하지 않는다 | `Accepted` | 1·2 |
| [005](#adr-005) | 오프라인 데이터는 Service Worker가 아니라 app.js가 담당 | `Accepted` | 1·2 |
| [006](#adr-006) | 앱 셸 자산은 network-first — 번들러를 도입하지 않는다 | `Accepted` | 1 |
| [007](#adr-007) | Android 전달 경로 = Capacitor + FCM (Web Push 기각) | `Accepted` | 4 |
| [008](#adr-008) | Capacitor 확인을 Phase 0.5로 앞당긴다 | `Superseded` → [014](#adr-014) | 0.5 |
| [009](#adr-009) | 알림은 규칙 평가와 발송을 분리한다 (아웃박스) | `Accepted` | 3 |
| [010](#adr-010) | 위젯 순서는 가치 순서가 아니라 복잡도 순서 | `Accepted` ⚠️ 전제 변동 | 5 |
| [011](#adr-011) | 시각·날짜는 언제나 서버 귀속일을 따른다 | `Accepted` | 전 Phase |
| [012](#adr-012) | 위젯 쓰기는 기존 엔드포인트를 재사용한다 | `Accepted` | 5 |
| [013](#adr-013) | Guard를 마지막 Phase로 옮긴다 | `Superseded` → [014](#adr-014) | 6 |
| [014](#adr-014) | Guard v1을 최우선 경로로 — 8월 마감이 순서를 정한다 | `Accepted` | 전 Phase |
| [015](#adr-015) | Local-first 아키텍처 전환 | `Proposed` | — |
| [016](#adr-016) | Worker의 장기 역할 = AI 프록시 + 백업 | `Proposed` | — |
| [017](#adr-017) | Kotlin + Compose 전환 — 12월 결정 | `Proposed` | — |
| [018](#adr-018) | 개입 상한을 올린다 — 사용 감지 + 화면 가림 (설계 §6.4 갱신) | `Accepted` | 6 |
| [019](#adr-019) | Guard 모드 = 규칙이 아니라 파라미터 프로파일 | `Accepted` | 6 |
| [020](#adr-020) | 적응형 3요소는 v1 필수 — 줄이되 빼지 않는다 | `Accepted` | 6 |
| [021](#adr-021) | 발동은 기기가 결정론으로 — FCM·위험도·AI를 발동 경로에서 뺀다 | `Accepted` | 6 |
| [022](#adr-022) | PC 감지는 9월 이후, 스키마 자리는 8월에 | `Proposed` | 6 |
| [023](#adr-023) | `guard_events`는 로컬 우선 기록 — 오프라인 쓰기 금지의 유일한 예외 | `Accepted` | 6 |
| [024](#adr-024) | Level 4만 AI 검증을 거친다 — 실패 시 Level 3으로 강등 | `Accepted` | 6 |
| [025](#adr-025) | 감지도 발동시킨다 — 시각 경로와 **독립**으로 | `Accepted` · [018](#adr-018) 일부 대체 | 6 |
| [026](#adr-026) | 이탈 경로에는 전부 대가가 있다 — 막지는 않는다 | `Accepted` · [019](#adr-019) 보강 | 6 |
| [027](#adr-027) | 모드 하향이란 무엇인가 — 단일 컬럼이 아니라 파라미터 벡터 | `Accepted` · [019](#adr-019) 구체화 | 6 |
| [028](#adr-028) | 입력의 무게중심을 시스템으로 — 주체성 축소 원칙 | `Accepted` | 전 Phase |
| [029](#adr-029) | 캘린더 양방향 동기화 — 동기화 창은 열린 날만 | `Accepted` | 7 |
| [030](#adr-030) | 보호 규칙은 자동 부착이 아니라 자동 제안 | `Accepted` | 7 |
| [031](#adr-031) | 기록 경로를 OS 표면으로 — share-target·알림 액션 | `Accepted` | 7 |
| [032](#adr-032) | 건강 데이터는 Health Connect에서 로컬로 읽는다 | `Accepted` | 8 |
| [033](#adr-033) | 학교 공지는 Worker가 수집한다 — fetch·diff·키워드 제안 | `Superseded` | 8 |
| [034](#adr-034) | 메신저는 알림 캡처로 읽는다 — 기기에서 필터, 제안만 | `Accepted` | 8 |
| [035](#adr-035) | Level 4의 "신규 작업 차단"은 막는 것이 아니라 내일로 돌리는 것 | `Accepted` | 6 |
| [036](#adr-036) | 예정 → 대기 간선은 만들지 않는다. 출구는 "멀리 미루기"다 | `Accepted` | 1·6 |
| [037](#adr-037) | 학교 일정은 Moodle 캘린더를 토큰으로 읽는다 | `Accepted` | 8 |
| [038](#adr-038) | 중첩 타임아웃은 바깥이 안보다 길다 | `Accepted` | 6 |
| [039](#adr-039) | 밤 실측은 두 종류다 — 실사용 조건과 알람 계층을 섞지 않는다 | `Accepted` | 6 |
| [040](#adr-040) | 마감은 빈 칸을 채우는 일이 아니라 확인하는 일이다 | `Accepted` | 7 |
| [041](#adr-041) | 첫 위젯은 데이터를 그리지 않는다 — 버튼 하나로 시작한다 | `Accepted` | 5 |
| [042](#adr-042) | 미루기는 마지막 출구가 아니다 — 강요된 미루기는 신호를 오염시킨다 | `Accepted` | 5 |
| [043](#adr-043) | 두 번째 위젯은 홈에서 찍는다 — 점수는 요약과 짝으로만 온다 | `Accepted` | 5 |

⚠️ = 결정은 유효하나 전제가 달라져 재검토 대상. ADR-010은 위젯의 목적이 "빈번한 기록"으로 확정되면서 복잡도 순서와 목적 순서가 갈렸다 — 9월에 판단한다.

---

<a id="adr-001"></a>
## ADR-001. 인증을 principal / credential / session 3계층으로 분리

**Status** `Accepted` · **Phase** 0 · 2026-07-26

### 맥락
기존 인증은 `env.API_TOKEN` 단일 시크릿을 `localStorage`에 넣고 Bearer로 보내는 방식이다. 앱화에서 새로 붙는 세 표면이 모두 이 방식과 맞지 않는다.

- Service Worker — `localStorage`는 동기 API라 SW 컨텍스트에서 접근 불가
- FCM 구독 — 구독을 매달 주체가 없다. "토큰을 아는 사람"만 있고 "누구"가 없다
- Android 위젯 — 네이티브 Kotlin이라 `localStorage` 자체가 존재하지 않는다

부가적으로 시크릿이 단일값이라 기기별 발급·개별 폐기가 불가능하고, XSS 한 번이면 전권이 넘어간다. 앱화는 노출면(위젯·알림·네이티브 브리지)을 늘리는 방향이므로 이 상태로 진행하면 위험이 함께 커진다.

### 결정
`principals`(누가) / `credentials`(무엇으로) / `sessions`(어떻게 전달) 세 테이블로 분리한다.
지금은 principal이 admin 1행뿐이고 로그인 UI를 만들지 않는다. **계층만 먼저 세운다.**

### 근거
- 사용자가 장기적으로 로그인과 Admin/User Role 도입을 계획하고 있다. 나중에 로그인을 붙이는 일이 `credentials.kind='password'` 한 종류 추가로 끝나야 한다
- `scopes` 컬럼이 있으면 위젯 토큰에 `widget:read task:write`만 주고 분석·설정 API는 막을 수 있다. 단일 `full` 토큰으로는 불가능
- 자격증명을 DB로 빼면 폰 분실 시 해당 기기 토큰만 폐기하면 된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| `env.API_TOKENS`를 쉼표 구분 다중값으로 | 발급·폐기가 배포 행위가 된다. 스코프·만료·라벨을 붙일 자리가 없다 |
| 위젯 붙일 때(Phase 5) 한꺼번에 처리 | Phase 1의 SW부터 막힌다. SW가 API를 못 부르면 Phase 2 설계가 통째로 달라진다 |
| 지금 완전한 멀티 유저(데이터 테이블에 `principal_id`) | 단일 사용자 앱을 유지하기로 했다. 지금 필요 없는 스키마 변경 |

### 감수하는 비용
진짜 데이터 격리는 여전히 불가능하다. `tasks`·`daily`·`events`에 `principal_id`가 없기 때문이다.
ADR-001이 보장하는 범위는 **인가 경계의 단일화 · 자격증명의 복수화와 회전 · Role/profile 자리 확보**까지다. 데이터 소유권 마이그레이션은 별건으로 남는다.

### 재검토 트리거
실제로 두 번째 사용자가 생길 때. 그 시점에 데이터 테이블의 `principal_id` 마이그레이션을 별도 ADR로 연다.

---

<a id="adr-002"></a>
## ADR-002. 웹은 쿠키 세션, 네이티브는 Bearer — 전달 방식 이원화

**Status** `Accepted` · **Phase** 0 · 2026-07-26

### 맥락
ADR-001로 자격증명 저장소를 정했다면, 다음 질문은 요청마다 그것을 **어떻게 실어 보내는가**다. 웹과 네이티브의 제약이 다르다.

### 결정
표면별로 전달 방식을 나누고, 서버는 `resolveAuth()` 하나에서 순서대로 시도한다.

| 표면 | 방식 |
|---|---|
| 웹 · PWA · Service Worker | `Cookie: pos_sess` (HttpOnly; Secure; SameSite=Lax) |
| Android 위젯 · 네이티브 | `Authorization: Bearer <기기 토큰>` |
| 부트스트랩 · 로컬 개발 | `Authorization: Bearer <env.API_TOKEN>` |

### 근거
- **쿠키가 SW 문제를 그냥 없앤다.** `credentials:'include'` 한 줄이면 SW의 `fetch`에도 자동으로 붙는다. 토큰을 IndexedDB에 넣고 SW에 전달하는 배관이 통째로 사라진다
- HttpOnly라 JS가 읽을 수 없다. XSS로 자격증명을 빼갈 수 없다 — `localStorage` 방식 대비 실질적 개선
- 위젯은 쿠키 자를 쓰지 않는다. 네이티브에는 Bearer + `EncryptedSharedPreferences`가 자연스럽다
- 부트스트랩 경로를 남기는 이유는 **0009 적용 도중 자기 앱에서 잠기지 않기 위해서**다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 전 표면 Bearer 통일 | 웹에서 토큰을 JS가 읽을 수 있는 곳에 둬야 한다. SW 전달 배관도 별도로 필요 |
| 전 표면 쿠키 통일 | 위젯의 네이티브 HTTP 호출에 쿠키 자를 붙이는 건 불필요한 복잡도 |

### 감수하는 비용
서버가 두 가지 인증 경로를 유지한다. `resolveAuth()` 한 함수로 격리해 라우터 이하로는 차이가 보이지 않게 한다.

### 재검토 트리거
`SameSite=Lax`가 문제 되는 크로스 오리진 시나리오가 생길 때 (현재 계획에는 없음).

---

<a id="adr-003"></a>
## ADR-003. profile 레이어는 새 테이블이 아니라 `settings` 확장으로

**Status** `Accepted` · **Phase** 0 · 2026-07-26
> 결정 내용 자체가 "지금 구현하지 않는다"이다. 상태는 `Accepted`가 맞다.

### 맥락
`role`은 권한이지만, AI 모델 선택 · 시간대 · 귀속일 경계 · 테마 · 기본 Feelings 필드는 권한이 아니라 취향·환경이다. session이나 credential에 속할 성질이 아니므로 언젠가 profile 레이어가 필요해진다.

### 결정
**지금 만들지 않는다.** 이 자리는 이미 `settings` 테이블이 차지하고 있고, 이행 경로는 컬럼 하나다.

```
settings (전역 싱글턴)  →  settings + principal_id (DEFAULT '00000000-001')
```

대신 두 가지를 지금부터 지킨다.

1. `settings`에 새 키를 추가할 때 **"사용자 것인가 인스턴스 것인가"** 를 판정해 둔다
2. **귀속일 경계는 예외**로 표시해 둔다

### 근거
- `me.getSettings`/`putSetting`은 RULES로 키별 형식을 검증하며 이미 정확히 프로필 노릇을 하고 있다. 단일 사용자라 싱글턴일 뿐이다. 새 테이블은 같은 것을 두 벌 만드는 일
- (1)이 필요한 이유: provider API 키는 마스킹되는 비밀이고 사용자별로 복제할 성질이 아니다 → **인스턴스**. 모델 선택·테마·시간대 → **프로필**. 지금 판정을 안 해두면 나중에 쪼갤 때 키가 섞이고, 그때는 어느 쪽이었는지 판단할 근거가 사라진다
- (2)가 필요한 이유: `lib/time.ts`의 `loadTime(env)`는 요청마다 불리고 **cron `autoClose`도 이것을 쓴다.** 경계가 principal별이 되면 cron이 principal을 순회해야 한다. 다른 프로필 필드는 컬럼 추가로 끝나지만 이것만은 실행기 변경이다 — 멀티 유저를 실제로 할 때 가장 먼저 걸릴 항목

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 지금 `profiles` 테이블 신설 | 단일 사용자에서 `settings`와 완전히 중복. 어느 키가 어디 있는지 매번 판단해야 한다 |
| 프로필 개념을 아예 문서화하지 않음 | `settings` 키가 쌓인 뒤에는 사용자/인스턴스 판정 근거가 사라진다 |

### 감수하는 비용
`settings` 키의 성격 판정이 문서와 관례에 의존한다(스키마가 강제하지 않는다). 키 추가 시 이 ADR을 참조하는 규율이 필요하다.

### 재검토 트리거
두 번째 principal이 생기거나, 프로필 성격 키가 20개를 넘어 `settings`가 관리하기 어려워질 때.

---

<a id="adr-004"></a>
## ADR-004. 오프라인은 읽기 전용 — 쓰기 큐를 구현하지 않는다

**Status** `Accepted` · **Phase** 1·2 · 2026-07-26

### 맥락
PWA의 표준 레퍼토리에는 Background Sync로 오프라인 쓰기를 큐잉했다가 복귀 시 전송하는 패턴이 있다. 원래 계획에도 "오프라인에서 마지막 데이터 조회"가 있었으나 쓰기 정책은 명시되지 않았다.

### 결정
**오프라인 쓰기를 구현하지 않는다.** 오프라인일 때 프런트는 쓰기 UI를 비활성화한다(로그 입력·완료·미루기·마감).

### 근거
이 도메인에는 큐잉과 정면으로 충돌하는 규칙이 둘 있다.

1. **귀속일 경계 05:00** — 시각이 날짜를 결정한다. 04:50에 큐잉한 로그가 05:10에 전송되면 **다른 날짜에 귀속**된다. 사용자는 어제 기록한 줄 알지만 오늘에 붙는다
2. **마감된 날의 불변성** — 30분 cron `autoClose`가 열린 과거 날짜를 마감한다. 큐잉된 쓰기가 도착했을 때 그 날이 이미 마감돼 있으면 트리거가 ABORT하고 **409로 조용히 유실**된다

즉 오프라인 쓰기는 이 앱에서 "나중에 반영됨"이 아니라 **"틀린 날짜에 반영되거나 사라짐"** 이다. 부분적으로만 동작하는 기능은 신뢰를 깎는다.

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 큐에 클라이언트 타임스탬프를 실어 서버가 귀속을 재계산 | 마감 문제는 남는다. 마감된 날에 쓰려면 트리거를 우회해야 하는데, 그 불변성이 이 앱 설계의 핵심 |
| 큐잉하되 충돌 시 사용자에게 물어보기 | 오프라인 편의를 위한 기능이 복귀 시 갈등 해소 UI를 요구한다. 비용이 편익을 넘는다 |
| 쓰기를 시도했다가 실패 시 토스트 | 눌렸다 실패하는 것보다 처음부터 잠긴 편이 낫다 |

### 감수하는 비용
지하철·비행기에서 기록을 남길 수 없다. 로그는 시각이 의미를 갖는 데이터라 "나중에 몰아 쓰기"가 원래 잘 맞지 않는다는 점으로 상쇄한다.

### 재검토 트리거
귀속일 경계 개념이 사라지거나, 마감 후 수정 불변성이 완화될 때. 둘 다 설계 §1.3·§1.4의 근간이므로 사실상 발생하지 않는다.

---

<a id="adr-005"></a>
## ADR-005. 오프라인 데이터는 Service Worker가 아니라 app.js가 담당

**Status** `Accepted` · **Phase** 1·2 · 2026-07-26

### 맥락
PWA 관례상 오프라인 캐시는 SW의 `fetch` 핸들러에서 API 응답을 가로채 처리한다. 그런데 최종 목표에 Capacitor 래핑이 있고, **Android WebView의 Service Worker는 앱 재시작 시 등록이 유지되지 않을 수 있다.** 오프라인 읽기를 SW에 두면 wrapper 안에서 조용히 죽는다.

### 결정
책임을 나눈다.

| 계층 | 책임 |
|---|---|
| **Service Worker** | 앱 셸(HTML·JS·CSS·아이콘) 캐시. `/api/*`는 **network-only로 통과** |
| **app.js + IndexedDB** | API 응답 스냅샷 저장·조회. `api.js`의 `_req`를 감싸는 얇은 계층 |

### 근거
- app.js는 **어느 컨테이너에서든 실행된다.** 브라우저·설치된 PWA·WebView 어디서나 같은 코드가 돈다. SW가 죽어도 오프라인 읽기는 살아남는다
- 덕분에 ADR-008의 스파이크 결과(SW가 WebView에서 유지되는가)가 **셸 전략에만 영향을 준다.** 데이터 경로는 결과와 무관하게 고정된다 — 미확정 변수의 영향 범위를 좁힌 것
- SW에서 인증된 API 요청을 캐시하려면 쿠키·응답 검증·무효화를 SW 안에서 다뤄야 한다. app.js에서는 이미 `_req`가 그 자리에 있다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| SW에서 API 캐시(표준 관례) | WebView에서 신뢰할 수 없다. 두 컨테이너에 서로 다른 오프라인 동작이 생긴다 |
| 양쪽 모두 구현 | 무효화 지점이 두 곳이 된다. 완료한 task가 한쪽 캐시에만 남는 종류의 버그 |

### 감수하는 비용
SW가 API를 캐시할 때 얻는 "네트워크 요청 자체를 스킵하는" 이점을 포기한다. 스냅샷 조회는 `fetch` 실패 이후에 일어나므로 오프라인 판정에 한 박자가 든다. `navigator.onLine` 선판정으로 완화한다.

### 재검토 트리거
Capacitor를 최종적으로 쓰지 않기로 하고 PWA 단독으로 갈 때. 그때는 SW 캐시가 더 단순하다.

---

<a id="adr-006"></a>
## ADR-006. 앱 셸 자산은 network-first — 번들러를 도입하지 않는다

**Status** `Accepted` · **Phase** 1 · 2026-07-26

### 맥락
이 리포에는 빌드 단계가 없다. `public/`을 Cloudflare `[assets]`로 직접 서빙하고 `<script src="app.js">`로 로드한다. 파일명에 콘텐츠 해시가 없으므로 SW가 `app.js`를 cache-first로 잡으면 배포해도 구버전이 계속 붙는다.

### 결정
- HTML·JS·CSS는 **network-first**(실패 시 캐시 폴백), 아이콘·manifest만 cache-first
- `sw.js`에 `APP_VERSION` 상수를 두고 **배포마다 수동으로 올린다**. 배포 체크리스트에 항목 추가
- **번들러를 도입하지 않는다**

### 근거
- 구 프런트 + 신 API 조합은 조용히 깨진다. 화면은 뜨는데 특정 동작만 실패하는 종류라 진단이 어렵다. **셸 자산은 신선도가 속도보다 중요하다**
- 무빌드 구조는 이 프로젝트의 강점이다. 파일을 열면 그게 곧 실행되는 코드고, raw 링크로 바로 읽힌다(CLAUDE.md의 참조 규약이 이 구조에 의존한다). 캐시 무효화 하나 때문에 빌드 파이프라인·소스맵·의존성을 들이는 건 교환비가 나쁘다
- network-first는 오프라인에서 캐시로 폴백되므로 ADR-004의 요구(셸이 뜬다)를 그대로 만족한다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 번들러 도입 후 해시 파일명 + cache-first | 무빌드 구조를 포기하는 대가가 크다. 측정된 성능 문제도 아직 없다(APP-PLAN §2-b) |
| 쿼리스트링 버전(`app.js?v=3`) | index.html도 캐시 대상이라 같은 문제가 한 단계 올라갈 뿐 |
| SW에서 `skipWaiting()` 즉시 적용 | 사용 중에 코드가 바뀐다. 대기시키고 토스트로 알리는 편이 안전 |

### 감수하는 비용
`APP_VERSION` 갱신이 수동이라 잊을 수 있다. 배포 체크리스트(APP-PLAN 부록 A)에 넣어 완화한다. network-first라 온라인에서는 매번 요청이 나가지만, 에셋이 4개뿐이고 304로 끝난다.

### 재검토 트리거
에셋 총량이 커져 §2-b 측정에서 병목으로 확인될 때.

---

<a id="adr-007"></a>
## ADR-007. Android 전달 경로 = Capacitor + FCM (Web Push 기각)

**Status** `Accepted` · **Phase** 4 · 2026-07-26

### 맥락
목표에 홈 위젯 3종과 알림이 함께 있다. 원래 계획은 Phase 3에서 Web Push를 구현하고 Phase 4에서 Capacitor로 감싸는 순서였다.

### 결정
**Capacitor + FCM 단일 경로.** Web Push는 구현하지 않는다.

### 근거
- **홈 위젯은 PWA로 불가능하다.** 위젯이 목표에 있는 한 네이티브 래퍼는 선택이 아니라 전제다
- **Android WebView는 Web Push API를 지원하지 않는다.** 원래 순서대로면 Phase 3에서 만든 푸시가 Phase 4에서 전부 죽고 FCM으로 다시 짜야 한다 — 같은 일을 두 번 하는 경로
- Workers에서 Web Push를 하려면 VAPID ES256 서명과 aes128gcm 페이로드 암호화를 직접 구현해야 한다(`web-push` npm은 Node 전용). 버릴 코드에 들이기엔 비싸다
- 개인용이므로 Play Store 심사가 필요 없다. `assembleRelease` + 사이드로드로 끝난다 — 네이티브 경로의 가장 큰 비용이 빠진다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| PWA 유지 + Web Push, 위젯 포기 | 작업량은 절반 이하지만 홈 위젯이 영원히 없다. 목표에 명시된 항목 |
| Web Push 먼저 만들고 나중에 FCM 추가(원안) | WebView 미지원으로 재작업이 확정적 |
| TWA(Trusted Web Activity) | 위젯 문제는 동일하게 남는다. 네이티브 코드를 넣을 자리가 Capacitor보다 좁다 |

### 감수하는 비용
- 데스크톱 브라우저·iOS에서는 알림을 받을 수 없다. `push_subscriptions.transport` 컬럼(`'webpush'|'fcm'`)을 미리 두어 나중에 붙일 자리만 확보한다 — 컬럼 하나라 비용이 0
- Firebase 의존이 생긴다
- 서명 키 관리 책임이 생긴다. **이 계획에서 유일하게 복구 불가능한 실수**이므로 APP-PLAN Phase 4 함정에 명시

### 재검토 트리거
위젯 요구가 철회될 때. 또는 WebView가 Web Push를 지원하게 될 때.

---

<a id="adr-008"></a>
## ADR-008. Capacitor 확인을 Phase 0.5로 앞당긴다

**Status** `Superseded by` [ADR-014](#adr-014) · **Phase** 0.5 · 2026-07-26 → 2026-07-28
> 스파이크의 목적은 "Phase 1·2 설계 입력을 먼저 얻는 것"이었다. ADR-014가 Phase 1·2를 전환 결정 이후로 보류시키면서 그 목적이 사라졌다. 스파이크 자체는 남지만 별도 게이트가 아니라 Phase 4의 첫 단계(W0)로 흡수된다.

### 맥락
Android 앱은 확정된 목표다(ADR-007). 그렇다면 남은 미확정 변수는 **wrapper 안에서 SW와 쿠키가 실제로 어떻게 동작하는가**인데, 이 답이 Phase 1·2의 설계를 좌우한다.

| 답 | 셸 전략 |
|---|---|
| SW가 재시작 후에도 유지 | `server.url` 원격 로드, SW가 양쪽 셸 담당 |
| SW 등록이 소실 | 에셋을 APK에 번들, SW는 브라우저 전용 |

### 결정
Phase 0 직후에 **반나절짜리 버리는 스파이크**를 넣는다. 확인 항목은 4개(앱 구동·safe area / SW 등록 / 재시작 후 유지 / 쿠키 유지)뿐이고, FCM·위젯·서명·아이콘은 건드리지 않는다. APK와 프로젝트는 버린다.

### 근거
- 이 질문의 답을 Phase 4에서 발견하면 **Phase 1·2를 다시 쓴다.** 미확정 변수는 그 변수에 의존하는 설계보다 먼저 해소하는 게 싸다
- 스파이크 비용이 반나절로 작다. `cap add android` + 기존 배포 URL 지정 + 폰 설치가 전부다
- ADR-005 덕분에 스파이크 결과의 영향 범위가 이미 좁다(셸에만 영향, 데이터 경로는 무관). 그래도 셸 전략은 확정해야 Phase 1을 쓸 수 있다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Phase 4에서 확인(원안) | 답을 알아야 할 시점보다 세 단계 늦다 |
| 문서만 읽고 결정 | WebView SW 동작은 기기·버전 편차가 있다. 자기 폰에서의 실측이 유일한 근거 |
| 스파이크 없이 항상 APK 번들(보수적 선택) | 웹 배포만으로 앱이 갱신되는 이점을 근거 없이 포기한다 |

### 감수하는 비용
반나절과, 버려질 Capacitor 프로젝트 하나.

### 재검토 트리거
없음. 결과가 나오면 APP-PLAN Phase 0.5 결과표에 기록하고 종료한다.

---

<a id="adr-009"></a>
## ADR-009. 알림은 규칙 평가와 발송을 분리한다 (아웃박스)

**Status** `Accepted` · **Phase** 3 · 2026-07-26

### 맥락
알림 기능은 "언제 무엇을 알릴지 판단하는 부분"과 "실제로 폰에 띄우는 부분"으로 나뉜다. 원래 계획은 둘을 한꺼번에 Phase 3에서 만드는 것이었다.

### 결정
`notifications` 테이블(아웃박스)을 두고 cron이 규칙을 평가해 행을 적재한다. **발송은 Phase 4로 분리한다.**
Phase 3에서는 적재된 알림을 **앱 내 알림함**으로만 보여준다.

### 근거
- **발송하기 전에 규칙 품질을 볼 수 있다.** 임계값이 잘못된 규칙을 자기 폰에 며칠 발송해보고 고치는 것보다, 앱 안에서 2주 쌓아놓고 보는 편이 낫다. 알림 스팸은 되돌리기 어려운 종류의 실패다
- 알림함은 발송이 붙은 뒤에도 남는다 — 놓친 알림을 되돌아보는 화면이 된다. 버리는 코드가 아니다
- Guard(ADR-013)가 같은 아웃박스에 `rule_key='guard:*'`로 적재하면 발송 경로를 공유한다
- `UNIQUE (rule_key, on_date)`로 30분 cron의 멱등성이 스키마 수준에서 보장된다 — `autoClose`와 동일한 패턴

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 규칙 평가 즉시 발송 | 규칙 검증 기회가 없다. 잘못된 임계값을 실제 알림으로 배운다 |
| 규칙을 클라이언트에서 평가 | 앱이 닫혀 있으면 평가되지 않는다. 알림의 목적과 어긋난다 |
| 새 cron 추가 | 기존 `*/30 * * * *`에 태우면 충분하다. 최대 30분 지연은 개인 앱에 무해 |

### 감수하는 비용
알림이 실제로 폰에 도달하기까지 Phase 4를 기다려야 한다. 그 사이 2주 실사용에서 규칙 임계값과 Daily 위젯 필요 여부를 얻으므로 대기 시간이 비어 있지 않다.

### 재검토 트리거
없음. 아웃박스는 발송 방식과 무관하게 유지된다.

---

<a id="adr-010"></a>
## ADR-010. 위젯 순서는 가치 순서가 아니라 복잡도 순서

**Status** `Accepted` · **Phase** 5 · 2026-07-26

### 맥락
위젯 3종의 제작 순서를 정해야 한다. 사용 가치로만 보면 Tasks가 가장 높다(오늘 할 일을 홈에서 바로 본다).

### 결정
**Calendar → Tasks → Daily** 순으로 만든다. Daily만 Phase 3의 2주 관찰 결과가 "필요"일 때 진행한다.

### 근거
- **첫 위젯이 일회성 인프라 비용을 전부 떠안는다** — AppWidgetProvider 등록 · 기기 토큰 발급 UI · `/api/widget/*` 규격 · 다크모드 색 · 갱신 스케줄. 이 비용은 어느 위젯이 먼저 오든 동일하다
- 그렇다면 **인프라를 디버깅할 때 도메인 복잡도가 섞이지 않는 쪽**이 먼저다. Calendar는 읽기 전용이라 쓰기 경로·409·`state` 의미가 없다. 42개 셀을 위젯 XML에 정적으로 두면 `RemoteViewsService` 어댑터도 필요 없다
- Tasks는 Calendar가 인프라를 깔아둔 뒤에는 "리스트 + 쓰기 한 줄"로 줄어든다
- Daily만 미검증 가정 위에 있다 — "홈에서 빠르게 기록할 것이다"가 참인지 모른다. 게다가 RemoteViews에 텍스트 입력 위젯이 없어 우회(알림 RemoteInput)가 필요하다. 가정이 확인되기 전에 우회 비용을 치를 이유가 없다
- Calendar·Tasks가 보여줄 데이터(일정·오늘 할 일·미룬 일·대기)는 **알림 규칙 품질과 무관하게 이미 매일 존재한다.** 그래서 Phase 3의 2주와 병렬로 진행할 수 있다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Tasks 먼저(가치 순서) | 인프라 버그와 도메인 버그가 섞인다. 첫 위젯에서 다룰 문제가 많아진다 |
| 3종 동시(원안) | 인프라가 안 잡힌 상태에서 표면 3개가 동시에 실패한다 |
| 3종 모두 2주 후 | Calendar·Tasks는 관찰 대상이 아니다. 대기할 이유가 없다 |

### 감수하는 비용
가장 유용한 위젯(Tasks)이 두 번째로 밀린다. Calendar가 인프라를 깔아둔 뒤라 간격은 짧다.

### 재검토 트리거
Calendar 위젯 제작 중 RemoteViews 제약이 예상보다 크게 걸릴 때(42셀 정적 레이아웃이 통하지 않을 때). 그 경우 Tasks를 먼저 놓는 편이 낫다.

---

<a id="adr-011"></a>
## ADR-011. 시각·날짜는 언제나 서버 귀속일을 따른다

**Status** `Accepted` · **Phase** 전 Phase · 2026-07-26

### 맥락
Personal OS의 "오늘"은 자정이 아니라 **경계 05:00 기준 귀속일**이다(`lib/time.ts`의 `attributionDate`). 새벽 3시에 기기 달력은 27일이지만 Personal OS는 26일이다. 앱화로 새로 생기는 표면(오프라인 스냅샷·알림·위젯)은 모두 날짜를 표시하거나 기록한다.

### 결정
모든 신규 표면은 **서버가 준 `d`를 그대로 사용한다.** 클라이언트·네이티브에서 날짜를 재계산하지 않는다.

- 위젯 API 응답 3종 모두에 `d` 포함
- `notifications.on_date`는 `TimeCtx.d`
- 오프라인 스냅샷은 `server_date`를 함께 저장하고 **찍힌 날짜 그대로** 표시

### 근거
- 경계 로직이 클라이언트에 복제되면 진실이 둘이 된다. 경계 시각은 설정값이라 바뀔 수 있고, 그때 복제본이 조용히 어긋난다
- 새벽 시간대에만 나타나는 버그는 재현이 어렵다. 위젯처럼 30분마다 갱신되는 표면에서 특히 그렇다
- 오프라인 스냅샷은 **과거의 사실**이다. "언제 찍은 것"이 "지금 며칠"보다 정확한 표현이다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 클라이언트가 경계 시각을 받아 계산 | 진실이 둘이 된다. 계산 시점 차이로 어긋날 수 있다 |
| 위젯이 기기 날짜 사용 | 새벽 0~5시에 항상 틀린다 |

### 감수하는 비용
위젯이 오프라인이면 날짜조차 표시할 수 없다. 마지막 성공 응답의 `d`를 그대로 두고 "마지막 갱신" 시각을 함께 보여주는 것으로 처리한다.

### 재검토 트리거
귀속일 경계 개념이 제거될 때. 설계 §1의 근간이므로 사실상 발생하지 않는다.

---

<a id="adr-012"></a>
## ADR-012. 위젯 쓰기는 기존 엔드포인트를 재사용한다

**Status** `Accepted` · **Phase** 5 · 2026-07-26

### 맥락
Tasks 위젯의 체크 완료를 위해 `/api/widget/*` 아래에 전용 쓰기 엔드포인트를 만드는 선택지가 있었다. 읽기는 이미 전용 API로 분리하기로 했으므로(페이로드 크기 때문에) 쓰기도 대칭으로 두는 게 자연스러워 보인다.

### 결정
**읽기만 분리하고, 쓰기는 기존 엔드포인트를 그대로 쓴다.** 체크 완료 = `POST /api/tasks/:id/complete`.

### 근거
- 읽기를 분리한 이유는 **페이로드 크기**다(`assembleToday`는 8종 조인, 위젯은 2KB 이하가 필요). 쓰기에는 그 이유가 없다 — 요청도 응답도 이미 작다
- `completeTask`에는 도메인 규칙이 들어 있다: live 항목 rate 100, 마감된 날은 건드리지 않음, 취소된 task 409 가드. 위젯용으로 복제하면 **규칙이 두 곳에 살고 언젠가 어긋난다**
- 409 `{suggest:"cancel"}` 응답 규약도 그대로 재사용된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| `POST /api/widget/tasks/:id/complete` 신설 | 도메인 규칙 중복. 읽기 분리와 달리 얻는 게 없다 |
| 위젯에서 쓰기를 아예 막음 | "체크하여 바로 완료"가 Tasks 위젯의 핵심 가치 |

### 감수하는 비용
위젯이 앱과 같은 409·트리거 오류를 만난다. 오히려 의도한 바다 — 위젯이 도메인 규칙을 우회할 길이 없다. 다만 위젯 쪽에서 **409를 반드시 처리**해야 한다(토스트 없이 목록 새로고침). APP-PLAN 5-2에 명시.

### 재검토 트리거
위젯 쓰기 지연이 사용자가 체감할 만큼 커질 때(현재는 단발 호출이라 해당 없음).

---

<a id="adr-013"></a>
## ADR-013. Guard를 마지막 Phase로 옮긴다

**Status** `Superseded by` [ADR-014](#adr-014) · **Phase** 6 · 2026-07-26 → 2026-07-28
> 이 ADR의 재검토 트리거는 "Guard가 앱화보다 시급해질 때"였고, 8월 마감이 확인되면서 정확히 발생했다. 다만 논거의 절반("Guard는 알림 인프라의 소비자다")은 ADR-014에 그대로 승계된다 — 순서가 뒤집혔을 뿐 의존 관계는 같다.

### 맥락
원래 로드맵은 Guard(구현 3) 다음에 앱화였다. 순서를 뒤집는 판단이 이 계획의 출발점이다.

### 결정
Guard를 **마지막 Phase**로 옮긴다.

### 근거
- **Guard는 알림 인프라의 소비자다.** `public/index.html` 설정 화면에 이미 이렇게 적혀 있다 — "Guard 규칙 평가는 서버(Cron)가 주기 실행하고 Web Push로 도달해요 — 앱이 닫혀 있어도. (구현 3)". 규칙 평가기·구독 저장·발송 경로가 없으면 Guard는 시작조차 할 수 없다
- 즉 이 순서 변경은 "Guard를 미루는 것"이 아니라 **"Guard의 하부구조를 먼저 짓는 것"** 이다. Phase 3이 Guard의 절반을 미리 만든다
- Guard를 먼저 만들면 이후 앱 구조에 맞춰 다시 수정할 가능성이 높다. 반대 방향의 재작업은 발생하지 않는다
- Guard에는 별도 선행 조건도 있다 — **B-3 튜토리얼 상세화**가 STATE에 "step3 전 필수"로 기록되어 있다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Guard 먼저(원래 로드맵) | 발송 경로가 없어 완성할 수 없다. 만들어도 앱 구조에 맞춰 재작업 |
| Guard와 알림을 한 Phase에서 | Guard는 "규칙 위반 감지", 알림은 "일정 리마인더"로 도메인이 다르다. 섞으면 규칙 품질 검증(ADR-009)의 관찰 대상이 흐려진다 |

### 감수하는 비용
Guard가 상당히 뒤로 밀린다. Phase 3에서 아웃박스·규칙 실행기·구독이 완성되므로, Guard 차례에는 `services/guard.ts`에 `evaluate()`를 추가하고 `rule_key='guard:*'`로 적재하는 작업만 남는다.

### 재검토 트리거
Guard가 앱화보다 시급해질 때. 그 경우에도 Phase 3까지는 선행되어야 한다.

---

<a id="adr-014"></a>
## ADR-014. Guard v1을 최우선 경로로 — 8월 마감이 순서를 정한다

**Status** `Accepted` · **Supersedes** [ADR-008](#adr-008) · [ADR-013](#adr-013) · **Phase** 전 Phase · 2026-07-28

### 맥락
사용자가 목표를 명시했다 — **8월 안에 Guard v1 완성, 9~11월(2학기) 실사용.** 그리고 우선순위를 1순위 Guard(알림·통제), 2순위 위젯(빈번한 기록)으로 정했다.

기존 계획(ADR-013)은 Guard를 마지막 Phase에 뒀고, 그 앞에 인증·PWA·오프라인·성능·위젯 16개 세션이 있었다. **4주 안에 도달 불가능하다.**

ADR-013의 논거 자체는 틀리지 않았다 — Guard는 알림 인프라의 소비자다. 틀린 것은 **그 앞에 놓인 것들 대부분이 Guard와 무관하다**는 사실을 순서에 반영하지 않은 것이다. Guard가 실제로 요구하는 것은 규칙 실행기와 도달 경로뿐이다.

### 결정
**Guard v1 크리티컬 패스만 8월에 남기고 나머지를 전부 뒤로 보낸다.**

```
8월:  Capacitor+FCM(도달) → 규칙 엔진 → 보호 규칙·Override → outcome 연결
9~11월: 실사용 + Guard 규칙 반복. 병렬로 위젯·인증·Compose 스파이크
12월: 네이티브 전환 결정
보류: PWA·오프라인·성능
```

**Phase 번호는 정체성이고 순서가 아니다**로 규약을 바꾼다. 번호 재배치 대신 APP-PLAN §3이 실행 순서를 갖는다.

### 근거
- **되돌릴 수 없는 것을 먼저 한다.** 2학기는 한 번뿐이고 아키텍처 작업은 언제든 할 수 있다. 놓치면 다음 기회는 3월이다
- **설계 §9가 "미확정 1~3의 설계 시점 = 구현 3 직전, 실사용 데이터가 쌓인 뒤"라고 이미 정해 뒀다.** 보호 규칙 문법·Override 마찰 수위·Level 4 실행 형태·outcome 판정·위험도 계산식이 전부 미확정이고, 이것이 Guard v1의 실제 작업량이다. 코드가 아니라 규칙 설계가 병목이므로 **가장 오래 걸리는 일을 가장 먼저 시작해야 한다**
- 인증(Phase 0)은 FCM 구독 1행을 기존 `API_TOKEN` 뒤에 두면 되므로 크리티컬 패스가 아니다. 위젯이 붙을 때 필요해진다
- PWA·오프라인은 네이티브 전환 시 폐기 가능성이 있다(ADR-017). 전환 결정 전에 짓는 것은 투기다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 기존 순서 유지, Guard를 9월로 | 2학기 전체를 실사용 없이 보낸다. 목표 자체를 포기하는 것 |
| Guard와 앱 기반을 병행 | 4주에 둘 다는 안 된다. 병행하면 규칙 설계 시간이 먼저 잘린다 |
| Guard v1을 웹에서만(네이티브 없이) | 설계 §6.4의 개입 상한이 "앱 내 마찰 + 알림"인데, 웹 알림으로는 Level 3~4의 도달률이 부족하다. FSI가 없으면 새벽에 무시된다 |

### 감수하는 비용
- PWA·오프라인이 사실상 무기한 보류된다. 오프라인에서 앱을 못 쓰는 상태가 길어진다
- Capacitor 프로젝트와 FCM 배관 일부가 네이티브 전환 시 버려진다. 다만 **알림 채널·FSI·권한 유도 Kotlin 모듈은 살아남고**, 서버 발송기도 클라이언트와 무관하게 유지된다
- 인증이 미뤄져 `localStorage` 평문 토큰 상태가 길어진다. 단일 사용자·개인 기기라 감수 가능

### 재검토 트리거
8월 W2 종료 시점에 도달 경로가 완성되지 않았을 때. 그 경우 Guard v1의 범위를 더 줄인다(알림함까지만, 개입은 9월).

---

<a id="adr-015"></a>
## ADR-015. Local-first 아키텍처 전환

**Status** `Proposed` · **Phase** — · 2026-07-28
> 결정하지 않았다. 12월 전환 결정(ADR-017)과 함께 판단한다. 이 기록은 분석의 보존이 목적이다.

### 맥락
현재는 `App → Worker → D1`로 서버가 진실의 원본이다. 프로젝트의 목표가 "캘린더+일정관리"에서 "Android에서 항상 함께하는 Personal OS"로 이동하면서, 기기 내 저장 + 필요 시에만 서버 구조가 더 적합한지 검토가 필요해졌다.

### 결정(안)
`Android App → Local SQLite → (AI·백업 필요 시) Worker`

### 근거
- **ADR-004(오프라인 읽기 전용)의 제약이 사라진다.** 그 제약은 서버가 진실이라서 생긴 것이다. Local-first에서는 오프라인 쓰기가 자연스럽다 — "오프라인"이라는 상태 자체가 없어진다
- **이미 SQLite다.** D1 → Android SQLite는 스키마와 트리거가 거의 그대로 간다. 불변성을 API가 아니라 **트리거에 둔 원래 설계**(설계 §8)가 여기서 크게 유리하다
- **위젯의 실패 모드가 통째로 없어진다.** 로컬 DB를 읽으면 401·빈 카드·ETag·토큰 만료가 전부 사라진다
- **Guard가 네트워크 없이 발동한다.** APP-PLAN Phase 3의 `AlarmManager` 예약이 이미 이 방향의 첫 조각이다. 30분 cron 해상도 문제도 해소된다
- 지연이 사라진다 — 모든 상호작용이 현재는 왕복이다

### 기각/보류한 대안
| 대안 | 평가 |
|---|---|
| 현행 유지(서버 진실) | 멀티 디바이스가 공짜. 다만 지금은 단일 기기라 이득이 없다 |
| 양방향 동기화(CRDT 등) | 단일 사용자·단일 기기에 과잉. 충돌 해소 규칙을 짓는 비용이 편익을 넘는다 |
| WebView 안에서 local-first(SQLite-wasm) | 플랫폼과 싸우는 구조. 위젯·백그라운드가 여전히 네이티브를 요구한다 |

### 감수하는 비용
- **백업이 새 요구사항이 된다.** 개인 저널이라 기기 분실 = 되돌릴 수 없는 소실. ADR-016과 짝으로만 성립한다
- 05:00 경계 자동 마감이 `WorkManager`로 가면 Doze·제조사 배터리 정책에 노출된다. `autoClose`가 이미 멱등이라 앱 실행 시 보정으로 상당히 완화되지만, 서버 cron보다 신뢰도가 낮아지는 건 사실이다
- **ADR-004를 대체하게 된다.** 채택되면 ADR-004는 `Superseded`

### 재검토 트리거
12월 전환 결정. 또는 9~11월 실사용에서 오프라인 발동 실패·위젯 응답성 문제가 반복될 때.

---

<a id="adr-016"></a>
## ADR-016. Worker의 장기 역할 = AI 프록시 + 백업

**Status** `Proposed` · **Phase** — · 2026-07-28

### 맥락
Local-first(ADR-015)로 가면 Worker가 진실의 원본이 아니게 된다. 그러면 Worker를 없앨 수 있는가? Android에서 Claude/OpenAI API를 직접 호출하면 되지 않는가?

### 결정(안)
**Worker를 유지하되 역할을 셋으로 좁힌다: AI 프록시 · 백업 저장소 · (나중에) 멀티 디바이스 조정자.**

### 근거
- **AI 프록시가 필요한 이유는 키다.** 앱에 API 키를 넣으면 추출 가능하다. 본인 키라도 요금은 본인이 낸다. 설계 §8이 이미 "키는 서버(설정)에 저장하되 조회 API는 값을 돌려주지 않는다"고 정했고, 그 원칙은 local-first에서도 유효하다
- **백업이 필요한 이유는 되돌릴 수 없음이다.** Log·Feelings·Guard Memory는 재생성 불가능한 원본이다(설계 원칙 4). 기기 분실이 전부의 소실이 되면 안 된다
- **D1 스키마를 그대로 재사용한다.** 소스 오브 트루스만 바뀌고 테이블은 같다 — 백업이 곧 현재 스키마의 사본이라 별도 설계가 거의 없다
- 멀티 디바이스는 지금 필요 없지만, 백업 경로가 있으면 그 위에 얹힌다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Worker 폐기, 앱에서 AI 직접 호출 | 키 노출. 그리고 백업 대상이 사라진다 |
| Worker 유지 + 서버가 계속 진실 | ADR-015의 이점을 전부 포기 |
| 백업을 Google Drive 등 외부로 | 스키마 사본이 아니라 파일 덤프가 된다. 복원·부분 조회가 어렵다 |

### 감수하는 비용
백업 주기·충돌 처리를 새로 설계해야 한다. 단일 기기에서는 "기기 → 서버 단방향 푸시"로 단순화 가능하다.

### 재검토 트리거
ADR-015가 기각되면 이 ADR도 함께 무의미해진다(현행 구조에서 Worker는 이미 전부를 한다).

---

<a id="adr-017"></a>
## ADR-017. Kotlin + Compose 전환 — 12월 결정

**Status** `Proposed` · **Phase** — · 2026-07-28

### 맥락
목표 기능 목록(Widget · Guard · 적극적 Notification · Background · Lock Screen · Biometric · Share · Quick Settings · 장기적으로 Launcher)이 wrapper의 영역을 넘어섰다. 사용자는 Claude를 활용하면 코드 변환 자체는 1주일 내외로 가능하고, 오래 걸리는 것은 UI/UX와 Guard 검증이라고 본다.

### 결정(안)
**12월에 결정한다. 그 전에 Compose 스파이크(캘린더 1화면)로 전환 비용을 실측한다.**

### 근거 — "1주일" 추정의 평가
맞는 부분: 스키마·트리거는 SQLite→SQLite라 거의 복사고, services는 순수 함수 번역이라 정말 빠르다. 통상적인 "네이티브 재작성은 오래 걸린다"는 전제가 달라졌다는 판단은 타당하다.

**틀린 부분: 코드는 번역되지만 튜닝은 번역되지 않는다.**
STATE에 남은 상수들 — `AXIS_LOCK 20 · 축비 1.9 · TRACK_RATIO 0.35 · FLICK 0.5 · CAL_GAP 20 · STRETCH_MAX 90 · STRETCH_K 0.42 · STRETCH_BACK_MS 460` — 이 숫자들이 제품이다. Compose는 제스처 시스템이 완전히 달라(`pointerInput`/`Animatable`/`nestedScroll`) 값이 넘어가지 않고 경험적으로 다시 유도해야 한다. 게다가 STATE의 "폰 실측 후 미세조정"이 아직 미완이다 — 웹에서도 끝나지 않은 튜닝이다.

**그러나 "UI/UX와 Guard 검증이 더 오래 걸린다"는 관점 자체는 옳다.** 그리고 그것이 정확히 지금 전환하면 안 되는 이유다 — 오래 걸리는 그 일을 8월에 시작해야 9월에 쓸 수 있다.

**타이밍이 핵심이다.** Kotlin 전환과 Guard v1은 같은 4주를 놓고 경쟁한다. 그리고 9~11월 실사용은 Compose 재설계의 **입력**이 된다. 매일 실제로 쓰는 화면이 무엇인지 알고 UI를 짜는 것과 모르고 짜는 것은 다른 일이다.

### 검증 방법 (스파이크)
캘린더 화면 하나만 Compose로 만든다. 가장 어렵기 때문에 고른다 — 배경 밴드 경계선 모델, 셀 동적 공간 예산, 3-pane 슬라이드, 날짜 팝업.

| 결과 | 해석 |
|---|---|
| 2~3일에 동작 | 1주일 추정이 신뢰할 만하다. 전환 진행 |
| 1주 이상 | 추정이 4~6배 틀렸다. 재검토 |

측정 대상은 코드 작성이 아니라 **튜닝 재현**이다.

### 전환 방식 (전환한다면)
| 층 | 방식 | 이유 |
|---|---|---|
| 스키마 + 트리거 | **이식** | SQLite → SQLite, 거의 복사 |
| smoke 145건 | **먼저 이식** | 도메인 규칙의 실행 가능한 명세. 번역 검증이 자동화된다 |
| services 도메인 규칙 | **번역** | `deferTask` 순서, `completeTask`의 rate 100, cancel의 열린 날 처리는 하드-원 지식이다. 재설계하면 이미 고친 버그를 다시 만든다 |
| UI | **재설계** | 웹 관용구(sheet·backdrop·track slide)를 Compose에서 흉내내면 최악이다. 단 정보 구조(5탭·캘린더 셀 내용)는 유지 — 실사용으로 얻은 것 |

즉 **아래는 번역, 위는 재설계.**

### 지금 해 둘 리팩터링 — 거의 없다
라우터 얇게 / services에 도메인 규칙 / `db/index.ts`에 SQL 전부 / 불변성은 트리거 — 이미 `Service ↔ Repository ↔ Database`다. `(env, t, ...)` 시그니처는 사실상 repository 주입이라 Kotlin으로 거의 1:1로 간다.

분리되지 않은 것은 `app.js`(fetch+상태+렌더 혼재) 하나인데, **Compose로 갈 거면 app.js 구조 정리는 버릴 코드를 정리하는 일이다. 하지 않는다.**

### 감수하는 비용
결정을 12월까지 미루므로, 그 사이에 만드는 Capacitor·위젯 코드 일부가 버려진다. 다만 네이티브 Kotlin 모듈(알림 채널·FSI·위젯 Provider)은 전환해도 살아남는다 — **Capacitor는 전환의 장애물이 아니라 다리다.**

### 재검토 트리거
12월. 또는 9~11월에 네이티브 모듈이 웹 코드보다 커지는 시점이 먼저 오면 그때.

---

<a id="adr-018"></a>
## ADR-018. 개입 상한을 올린다 — 사용 감지 + 화면 가림

**Status** `Accepted` · **Phase** 6 · 2026-07-28
> ⚠️ **설계문서 §6.4의 갱신이 필요하다.** v0.9는 상한을 "앱 내 마찰 + 알림"으로 명시했다. 이 ADR이 그것을 넘는다.

### 맥락
설계 v0.9 §6.4는 이렇게 적었다.

> PWA는 다른 앱 종료·PC 전원 제어 불가. **소프트웨어 개입 상한 = 앱 내 마찰 + 알림.**

이 상한의 근거는 **PWA의 제약**이었다. 네이티브(ADR-007)로 가면서 전제가 사라졌다. 그리고 §6.1 Level 4("적극적 개입")와 §6.2 사전 서약의 취지를 보면, 앱 내 마찰만으로는 실패 사례 #1 같은 상황 — 다른 데 몰입해 있어 앱을 열지도 않는 상황 — 에 도달하지 못한다.

### 결정
개입 수단을 네 단계로 확장한다.

| 단계 | 수단 | 권한 | 시점 |
|---|---|---|---|
| 감지 | `UsageStatsManager.queryEvents` | `PACKAGE_USAGE_STATS` | W3 |
| 알림 | FSI + 알람 채널 | `USE_FULL_SCREEN_INTENT` | W1 |
| 앱 내 차단 | 신규 작업 차단 | 없음 | W3 |
| **화면 가림** | `SYSTEM_ALERT_WINDOW` 오버레이 | 오버레이 권한 | W5 |

**감지를 개입보다 먼저 넣는다.**

### 근거
- §6.1 Level 3의 예시("현재 01:30. 지금 계속하면 예상 수면 4시간 이하")는 **사용자가 무엇을 하고 있는지 알아야** 성립한다. 감지 없이는 Level 3 판단의 입력이 절반이다
- **감지가 개입보다 값어치가 크다.** "새벽 1시에 무엇을 하고 있었는가"는 위험도 계산의 가장 강한 항이 되고, `guard_events.risk_snapshot`에 쌓여 자기 보정의 원재료가 된다. 오버레이 없이도 Guard가 똑똑해진다
- 감지는 싸다(권한 1개 + 폴링). 오버레이는 비싸다(포그라운드 서비스 + 배터리 + 제조사 정책). 싼 것부터
- 개인용 사이드로드라 Play 정책 심사가 없다. Android 13+ 제한된 설정은 앱 정보에서 1회 해제

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| §6.4 유지 (앱 내 마찰 + 알림만) | 몰입 중에는 앱을 열지 않는다. 실패 사례 #1의 상황에 도달하지 못한다 |
| AccessibilityService로 실시간 감지 | UsageStats보다 정확하지만 권한이 과도하고 오용 위험이 크다. 폴링으로 충분하다 |
| 앱 강제 종료 · Device Admin | §6.3의 "Override는 금지가 아니라 마찰" 원칙 위반. 절대 잠금은 도구 이탈로 귀결 |

### 감수하는 비용
- **포그라운드 서비스 + 상시 알림 + 배터리.** 배터리 최적화 예외를 요청해야 제조사 정책에서 안 죽는다
- **오버레이는 완전하지 않다.** 다른 앱이 전체화면이면 가려질 수 있다(게임·동영상). §6.7대로 목표는 완벽한 차단이 아니라 실패 확률의 감소다
- 일정이 4주 → 5주가 된다. W5가 9월 첫 주로 넘친다
- **설계문서 §6.4를 v1.0에서 다시 써야 한다.** 이 ADR이 그 근거다

### 재검토 트리거
오버레이가 도구 이탈을 부를 때(§6.3). 9~11월 실사용에서 모드를 secretary로 내리는 빈도가 높으면 그 신호다.

---

<a id="adr-019"></a>
## ADR-019. Guard 모드 = 규칙이 아니라 파라미터 프로파일

**Status** `Accepted` · **Phase** 6 · 2026-07-28

### 맥락
상황에 따라 Guard의 강도를 바꾸고 싶다는 요구가 나왔다 — coach(적극 개입) / secretary(알려주고 기록). 나중에 모드를 더 추가할 수 있어야 한다.

### 결정
**규칙 집합은 하나고, 모드는 그 강도를 스케일링하는 파라미터 행이다.**
`guard_modes(key, max_level, risk_threshold, friction_mult, use_fsi, use_overlay, ai_daily_cap)`.
모드 추가 = 행 하나.

부수 규칙 셋:
1. **보호 모드 중 모드 하향 금지.** 상향은 자유
2. 하향에도 Override와 같은 마찰(사유 + 대기)
3. `guard_events.mode`를 기록하고 **자기 보정 집계를 모드로 분리**

### 근거
- 모드마다 규칙을 따로 두면 규칙 수정이 모드 수만큼 늘어난다. 파라미터 프로파일이면 규칙은 한 벌
- **부수 규칙 1이 핵심이다.** 모드 전환은 Override의 완벽한 우회로다 — 새벽에 coach → secretary로 내리면 마찰이 전부 사라진다. 설계 §6.2의 사전 서약("미리 정한 나가 규칙을 만들고 집행은 자동으로")이 그대로 무너진다. **하향을 막지 않으면 모드 기능이 Guard를 무력화한다**
- 잦은 하향 시도 자체가 신호다. 기록하면 자기 보정의 입력이 된다
- 부수 규칙 3: 같은 규칙이 모드에 따라 다른 결과를 낸다. coach에서 학습한 것과 secretary에서 학습한 것을 섞으면 보정이 오염된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 모드별 규칙 집합 분리 | 규칙 수정 비용이 모드 수배. 모드 추가가 비싸진다 |
| 모드 없이 단일 강도 | 시험 기간과 방학의 적정 강도가 다르다 |
| 모드 전환 자유 | Override 우회로가 되어 Guard 전체가 무력해진다 |

### 감수하는 비용
모드 하향이 막히면 "정말 필요할 때 못 내리는" 상황이 생긴다. 마찰을 통과하면 내릴 수 있게 두어 완화하되, 보호 모드 중에는 예외 없이 막는다 — 그 구간이 바로 사전 서약이 지켜야 할 구간이다.

### 재검토 트리거
모드가 4종을 넘어 파라미터로 표현이 안 될 때. 또는 하향 금지가 실사용에서 심각한 마찰을 만들 때.

---

<a id="adr-020"></a>
## ADR-020. 적응형 3요소는 v1 필수 — 줄이되 빼지 않는다

**Status** `Accepted` · **Phase** 6 · 2026-07-28

### 맥락
8월 4주라는 제약 때문에 v1 범위 축소를 검토하면서, 위험도 계산식(§6.6) · AI 판단(§8) · 자기 보정(§6.5)을 v2로 미루는 안이 나왔다. "전례가 0이라 자기 보정은 작동하지 않는다"는 것이 근거였다.

### 결정
**세 요소 모두 v1에 넣는다.** 대신 정교함을 줄인다 — 위험도는 항 개수를 적게, AI는 호출 위치를 좁게(Level 3~4 후보만 + 하루 상한), 자기 보정은 로직 발화를 전례 N건 이상으로.

### 근거
- **설계 §6.5·§6.7이 Guard를 적응형으로 정의한다.** "규칙만 적용하는 시스템이 아니라 과거 개입의 결과를 기억하는 시스템", "실패를 줄이는 방향으로 스스로 보정되는 시스템". 세 요소를 빼면 남는 것은 알림 앱이지 Guard가 아니다
- **"작동하는 것"과 "지어두는 것"을 혼동한 것이 원래 판단의 오류였다.** 자기 보정 *로직*은 전례 없이 발화하지 않는 게 맞다. 하지만 보정이 읽을 **기록 구조와 집계 경로**는 v1에 있어야 한다. 없으면 9~11월 데이터가 안 쌓이고, 12월에 뒤늦게 넣으면 **학기 전체가 낭비된다. 데이터는 소급해서 만들 수 없다**
- 위험도 계산식이 없으면 `guard_events.risk_snapshot`에 남길 것이 없다. 사후에 "이 규칙이 과도했나"를 평가할 재료가 사라진다 — 즉 **위험도는 자기 보정의 전제 조건**이다
- AI 판단은 §8이 이미 `model_high`를 "이후 Guard 판단"으로 예약해 뒀다. 설계에 있는 것을 빼는 것은 범위 축소가 아니라 설계 이탈이다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 세 요소를 v2로 (원래 안) | 9~11월 데이터가 안 쌓인다. 학기를 낭비한다 |
| 자기 보정만 v2로 | 기록 구조가 없으면 같은 문제. 로직만 미루는 것이 정답 |
| 위험도만 v1, AI는 v2 | AI 없이도 굴러가지만, 호출 위치를 좁히면 비용이 작아 미룰 이유가 없다 |

### 감수하는 비용
- 일정이 늘어난다. W2에 위험도, W4에 보정 골격이 들어간다
- 초기 위험도 가중치는 근거 없는 값이다. `settings`에 두어 조정 가능하게 하는 것으로 대응 — **조정 가능한 구조가 초기값보다 중요하다**
- AI 호출 비용이 발생한다. 1단계 결정론적 필터 + 하루 상한으로 통제

### 재검토 트리거
없음. 일정 압박이 와도 이 셋은 줄이는 대상이지 빼는 대상이 아니다 — APP-PLAN §5의 감축 순서에 명시했다.

> **적용 형태는 [ADR-021](#adr-021)에서 구체화됐다.** 위험도와 AI는 v1에 남되 **발동 경로 밖**에 놓인다. 이 ADR의 골자("기록 구조는 v1 필수, 정교함은 줄인다")는 그대로다 — 대체가 아니라 실행 형태의 확정이다.

---

<a id="adr-021"></a>
## ADR-021. 발동은 기기가 결정론으로 — FCM·위험도·AI를 발동 경로에서 뺀다

**Status** `Accepted` · **Phase** 6 · 2026-07-28

### 맥락
8월 계획의 첫 주가 "FCM 도달"이었다. 그런데 v1의 완성 조건을 한 문장으로 적어 보니 발동 조건이 전부 **시각 기반**이거나 **기기 로컬 이벤트**였다.

> 시험 일정을 등록하면 전날 00:00에 보호 모드가 걸리고, 01:30에 알람 소리로 잠금화면을 점유하는 알림이 뜨고, 해제하려면 사유 20자 + 60초 대기가 필요하고, 그 전부가 `guard_events`에 남는다.

**서버만 아는 발동 조건이 하나도 없다.** 보호 시각은 `events`에서 역산되고, foreground app은 기기가 안다. 그렇다면 발동 경로에 네트워크가 왜 들어가는가.

같은 질문을 위험도 점수와 AI 판단에도 적용할 수 있다 — 이 둘이 발동을 결정하면 발동이 **임의 가중치**와 **서드파티 가용성**에 걸린다.

### 결정
**발동 경로에는 결정론적 시각 규칙만 둔다.**

| 요소 | 발동 경로 | v1에서의 위치 |
|---|---|---|
| 알람 예약·발동 | ✅ 기기 (`setAlarmClock`) | 크리티컬 패스 |
| 보호 규칙 역산 | ✅ 결정론 (일정 시각 − 준비 − 수면) | 크리티컬 패스 |
| FCM | ❌ | **9월** — 위젯 갱신용 |
| 위험도 점수 | ❌ | `risk_snapshot`에 **기록만** |
| AI 판단 | ❌ | 문구 생성·사유 분류 (사후) |
| UsageStats 감지 | ❌ | **보조 입력** — `risk_snapshot` 강화 |

서버는 **하루 1회 보호 일정 pull**만 제공한다. 기기가 받아서 알람을 전부 예약하고, 발동한다.

### 근거
- **발동은 단일 실패점이 적을수록 좋다.** 새벽 1시 30분은 재시도가 없는 순간이다. 네트워크·서드파티 모델·임의 임계 중 하나라도 발동 경로에 있으면 그날 개입이 통째로 사라진다
- **Gemini `-latest` 404 전례**(STATE A-2)가 실제로 있었다. 모델 별칭 하나가 바뀌어 호출이 죽었다. 그런 일이 개입 순간에 일어나면 안 된다
- **임의 가중치의 오발동이 가장 비싼 실패다.** §6.3이 경고한 도구 이탈로 이어지고, 이탈하면 데이터도 안 쌓여 **가중치를 유도할 기회 자체가 사라진다.** 순서를 뒤집는다 — 안전한 규칙으로 먼저 발동시켜 스냅샷을 모으고, 10월에 실제 데이터에서 가중치를 유도한다
- **감지를 발동에서 빼면 제조사 정책 리스크가 사라진다.** UsageStats가 삼성 절전에서 죽어도 Guard는 그대로 돌아간다. 감지는 있으면 좋은 것이지 없으면 안 되는 것이 아니다
- 부수 효과로 **Guard가 완전히 오프라인에서 작동한다** — ADR-015(local-first) 방향과 일치한다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| FCM으로 발동 (원안) | 서버만 아는 발동 조건이 없다. 네트워크를 크리티컬 패스에 넣는 순수 손실 |
| 위험도 임계로 발동 | 근거 없는 가중치 → 오발동 → 도구 이탈 → 데이터 소실. 자기 파괴 루프 |
| AI가 Level을 결정 | 발동이 서드파티 가용성에 걸린다. 새벽에 404면 개입이 없다 |
| 위험도·AI를 v1에서 완전 삭제 | 기록은 소급 불가. ADR-020 위반 |
| 감지를 발동 조건에 포함 | 제조사 배터리 정책이 발동을 죽일 수 있다 |

### 감수하는 비용
- 발동이 **단순하다.** "데드라인 지났으니 Level 3"뿐이라 문맥을 못 읽는다. 실제로 자고 있어도 뜬다 — 9월 실사용에서 조정할 첫 항목
- ADR-007(Capacitor + FCM)의 FCM 부분이 8월에서 빠진다. **폐기가 아니라 시점 분리** — 위젯에는 여전히 필요하다
- 알림 문구가 캐시 폴백이면 개인화가 약하다. 보호 모드 진입 시 미리 생성해 완화

### 재검토 트리거
10월. 실제 `risk_snapshot`에서 가중치를 유도할 만큼 데이터가 쌓이면, 위험도를 발동 경로에 넣을지 판단한다.

---

<a id="adr-025"></a>
## ADR-025. 감지도 발동시킨다 — 시각 경로와 독립으로

**Status** `Accepted` · **Supersedes** [ADR-018](#adr-018)의 "감지는 방아쇠가 아니다" 부분 · **Phase** 6 · 2026-07-29

### 맥락
ADR-018은 감지를 **보조 입력**으로 못박았다. 근거는 견고성이었다 — 보호 규칙이 시각으로 예측되므로 UsageStats가 제조사 정책에 막혀도 Guard는 돈다.

그 논거는 지금도 맞다. 그런데 **따지지 않은 것이 있었다: 루프가 얼마나 자주 도는가.**

현재 발동은 `protect_from`이 붙은 일정이 있을 때만 일어난다. 그런 일정은 드물고(시험·마감), 붙이는 UI도 아직 없다. 결과적으로:

- Guard가 **몇 주에 한 번** 발동한다
- `guard_events`가 거의 비어 있다
- 9~11월 실사용의 목적이 "전례를 쌓는 것"인데(ADR-014), **쌓일 게 없다**
- 자기 보정(§6.5)은 전례를 요구하므로 12월에도 발화하지 못한다

ADR-020에서 "데이터는 소급해서 만들 수 없다"를 근거로 기록 구조를 v1에 넣었는데, **기록할 사건 자체가 없으면 그 구조가 빈 채로 남는다.** 같은 논리가 여기서도 적용된다.

### 결정
**감지 기반 발동을 추가한다. 시각 기반 발동과 완전히 독립된 경로로.**

```
경로 A (시각)  보호 규칙 → 데드라인 역산 → setAlarmClock 예약 → 기기 발동
경로 B (감지)  상시 서비스 폴링 → 결정론 규칙 평가 → 기기 발동      ← 신설
```

두 경로는 서로를 모른다. B가 죽어도 A는 예약이 시스템에 있으므로 그대로 발동한다 — **ADR-018이 지키려던 견고성이 유지된다.**

**B의 규칙도 결정론이다**(ADR-021 유지). 위험도 점수나 AI가 아니라 시각·연속 사용 시간 같은 관찰값의 임계 비교다.

v1 규칙 하나로 시작한다 — **취침 창 안에서 계속 쓰고 있음**:

| | |
|---|---|
| 조건 | 현재 시각이 취침 창 안 · 화면 켜짐 · 연속 사용 ≥ N분 |
| 발동 | Level 2(1회) → 이후 계속되면 30분마다 Level 3 |
| 상한 | 하룻밤 최대 M회 |

실패 사례 #1이 정확히 이 상황이다 — 시험 전날 밤, 몰입, 새벽. 설계 §6.6도 "최근 수면 패턴(Log 시각에서 추정)"을 판단 입력으로 들었다.

**루프는 닫히는 쪽도 필요하다.** 수락·Override는 기기가 올려 주지만, 아무 반응 없이 지나간 발동은 누구도 `reaction`을 쓰지 않아 영원히 NULL로 남는다. 그러면 §6.5가 **가장 중요한 실패 사례인 "무시된 개입"을 아예 못 본다.** 30분 cron이 유예를 넘긴 무반응 발동을 `ignored`로 확정한다.

유예는 **36시간**이다. 짧게 두면 안 되는 이유: 기기가 오프라인이면 발동과 반응을 **함께** 나중에 올리고(ADR-023), 재동기화는 하루 한 번(경계+10분)이라 최악의 지연이 24시간에 가깝다. 서버가 먼저 `ignored`를 박으면 트리거가 진짜 반응을 막고 그 손실은 소급 복구가 불가능하다 — 개입 이력은 고칠 수 없기 때문이다. 늦게 닫히는 비용은 거의 없다. 집계는 10월이고 그때 값이 맞으면 된다.

### 근거
- **이 규칙은 매일 발동할 수 있다.** 보호 일정이 없어도 "새벽에 계속 쓰고 있다"는 것 자체가 §6.7의 "장기 목표와 단기 행동이 충돌하는 순간"이다
- 루프가 매일 돌면 9~11월에 전례가 쌓이고, 그게 12월 자기 보정의 입력이 된다. **이것이 실사용 기간의 목적 자체다**
- 독립 경로라 ADR-018의 견고성 논거가 깨지지 않는다. 감지가 죽으면 B만 멈춘다
- 결정론이라 ADR-021도 유지된다. 임계값은 실사용으로 조정하고, 가중치 유도는 여전히 10월이다
- 부수 효과: 감지 권한을 실제로 쓰는 이유가 생긴다. 지금은 표본만 모으고 아무것도 안 하니 배터리를 쓰는 근거가 약했다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 보호 규칙 UI만 먼저 만들기 | 발동 빈도가 여전히 일정 개수에 묶인다. 시험이 매일 있지 않다 |
| 감지 발동을 시각 경로에 합치기 | A가 B의 실패에 물린다. ADR-018이 막으려던 것 |
| 위험도 점수로 발동 | ADR-021 위반. 근거 없는 가중치의 오발동은 도구 이탈을 부른다 |
| 그대로 두고 데이터가 적은 채로 진행 | 학기가 지나가고 12월에도 보정이 발화하지 않는다. 실사용의 목적을 포기하는 것 |

### 감수하는 비용
- **오발동 위험이 커진다.** 매일 발동하는 규칙이라 임계가 잘못되면 매일 성가시다 — §6.3의 도구 이탈이 가장 현실적인 실패다. 그래서 하룻밤 상한과 Level 2 시작을 둔다
- 취침 창·임계는 사람마다 다르다. 설정으로 빼고 기본값은 조정 대상으로 본다
- 배터리를 실제로 쓰기 시작한다(폴링 + 발동)

### 재검토 트리거
실사용 2주. 하룻밤 발동이 3회를 넘거나, 모드를 secretary로 내리는 일이 반복되면 임계가 과한 것이다 — 그때 조인다.

---

<a id="adr-022"></a>
## ADR-022. PC 감지는 9월 이후, 스키마 자리는 8월에

**Status** `Proposed` · **Phase** 6 · 2026-07-28

### 맥락
실패 사례 #1의 몰입은 **PC 앞에서** 일어났다. 사용자는 PC와 폰 양쪽에서 몰입하는데, 폰만 감지하면 가장 중요한 상황을 놓친다.

동시에 PC 에이전트는 8월 범위 밖이다 — 별도 실행 파일·자동 시작·업데이트 경로가 필요하다.

### 결정(안)
**PC 에이전트는 9월 이후. 단 스키마가 폰 전용으로 굳지 않게 자리만 8월에 만든다.**

```
guard_events:  source TEXT ('android'|'pc') · foreground_app TEXT
POST /api/guard/activity  { source, device_id, app, at }
watch_apps(source, identifier)
```

PC 에이전트의 형태는 경량으로 — **1분마다 실행 중 프로세스가 감시목록에 있는지만 보낸다.**

### 근거
- 스키마에 `source`를 넣는 비용은 컬럼 하나다. 나중에 넣으면 8~11월 데이터가 전부 출처 불명이 된다
- 라우트를 공용으로 두면 PC 에이전트가 붙을 때 서버 변경이 없다
- 경량 형태를 고른 이유: 창 제목·입력 감지까지 가면 사생활 데이터가 과하게 쌓이고 OS별 구현이 갈린다. **감시목록 대조**면 충분하다
- 폰 감지와 같은 취급을 받는다 — **보조 입력**이지 발동 조건이 아니다(ADR-021). PC 에이전트가 꺼져 있어도 Guard는 돌아간다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| PC 에이전트를 8월에 | 8월이 5주로도 빠듯하다. 완성의 정의에 없다 |
| 스키마도 9월에 | 8~11월 데이터의 출처가 불명이 된다. 컬럼 하나 값이 학기 데이터보다 싸다 |
| 전체 활동 로깅(창 제목·입력) | 사생활 데이터 과잉, OS별 구현 분기. 감시목록 대조로 충분 |

### 감수하는 비용
8~11월 동안 PC 몰입은 감지되지 않는다. 다만 **개입 자체는 도달한다** — 보호 규칙이 시각 기반이라 PC를 보고 있어도 폰에서 알람이 울린다(ADR-018의 "FSI가 하중을 진다").

### 재검토 트리거
9월 실사용에서 PC 몰입 중 개입이 반복적으로 실패할 때.

---

<a id="adr-023"></a>
## ADR-023. `guard_events`는 로컬 우선 기록 — 오프라인 쓰기 금지의 유일한 예외

**Status** `Accepted` · **Phase** 6 · 2026-07-28

### 맥락
ADR-021로 Guard 발동이 완전히 기기 로컬이 됐다. 그런데 기록은 여전히 D1이다.

새벽에 네트워크가 없거나 서버에 못 붙으면 **발동은 했는데 기록이 안 된다.** 완성의 정의 마지막 절("그 전부가 `guard_events`에 남는다")이 깨지고, 더 나쁘게는 **가장 중요한 순간의 데이터가 빠진다** — 개입이 실패하는 밤은 대개 상황이 정상이 아닌 밤이다.

ADR-004는 오프라인 쓰기를 금지했다. 그 근거는 ① 귀속일 경계 05:00에서 날짜가 밀린다 ② 마감 트리거가 ABORT한다 였다.

### 결정
**`guard_events`만 예외로 둔다.** 기기에 즉시 기록하고 온라인 시 밀어올린다.

### 근거
- ADR-004의 두 근거가 **`guard_events`에는 해당하지 않는다.** ① 발동 시각은 벽시계이지 귀속일이 아니다(보호 모드 판정 자체가 벽시계 기준) ② 마감 트리거는 `daily`·`tasks`에 걸린 것이고 `guard_events`는 대상이 아니다
- **append-only라 충돌 해소가 필요 없다.** 설계 §9 #5가 "Log·memo는 append-only라 비교적 안전"이라 한 것과 같은 구조다. 미확정으로 남은 것은 "마감·미루기 같은 **상태 변경**의 동기화"였지 append가 아니다
- 발동이 네트워크와 무관한데 기록만 네트워크에 걸리면 **설계가 절반만 오프라인**이다. 가장 필요한 순간에 깨진다
- local-first(ADR-015)의 두 번째 조각이 된다. 나중에 전환할 때 이 경로가 선례가 된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 서버 직접 쓰기 (현행) | 새벽 오프라인에 가장 중요한 데이터가 사라진다 |
| 오프라인이면 발동 안 함 | 본말전도. 발동이 목적이고 기록은 부산물이다 |
| 오프라인 쓰기를 전면 허용 | ADR-004의 근거가 다른 테이블에는 여전히 유효하다. 예외는 최소로 |

### 감수하는 비용
- 기기와 서버에 같은 테이블이 두 벌 생긴다. 단일 방향(기기 → 서버)이라 동기화 로직은 단순하다
- 밀어올리기 실패 시 재시도 큐가 필요하다. `id`를 기기에서 생성하고 `INSERT OR IGNORE`로 멱등하게 처리한다
- **`outcome` 사후 확정은 서버 쪽에서 일어난다.** 로컬 행이 이미 올라간 뒤라 순서 문제는 없지만, 밀어올리기 전에 outcome을 매기려 하면 어긋난다 — outcome 카드는 온라인일 때만 뜨게 한다

### 재검토 트리거
ADR-015(local-first)가 채택되면 이 ADR은 예외가 아니라 기본이 된다. 그때 통합한다.

---

<a id="adr-024"></a>
## ADR-024. Level 4만 AI 검증을 거친다 — 실패 시 Level 3으로 강등

**Status** `Accepted` · **Refines** [ADR-021](#adr-021) · **Phase** 6 (3주차) · 2026-07-29

> ADR-021을 대체하지 않는다. **Level 1~3은 021 그대로** 결정론·네트워크 무관이다.
> 이 ADR은 Level 4 **격상**에만 조건을 붙인다.

### 맥락
Level 4는 개입 중 가장 비싸다 — 화면 점유 + 신규 작업 차단 + (설정에 따라) 무음 모드 무시. 오발동 한 번의 비용이 다른 Level과 다르다. 설계 §6.3이 경고한 도구 이탈이 바로 여기서 일어난다.

그런데 ADR-021은 AI를 발동 경로에서 뺐다. 근거는 "발동이 서드파티 가용성에 걸리면 안 된다"(STATE의 Gemini `-latest` 404 전례). 두 요구가 부딪힌다.

### 결정
**Level 3 → 4 격상에만 `model_high` 검증을 요구한다. AI를 못 부르면 Level 3에 머문다.**

```
데드라인 +30m 도달
   → 결정론 규칙이 "Level 4 후보" 판정          (021 그대로, 네트워크 불필요)
   → model_high에 검증 요청 (타임아웃 8초)
        승인    → Level 4 발동
        거부    → Level 3 유지
        실패·타임아웃·상한 초과 → **Level 3 유지**
```

### 근거
- **실패 방향이 안전하다.** AI가 죽어도 개입이 사라지지 않는다 — Level 3이 이미 화면 점유와 알람 소리를 한다. 잃는 것은 격상뿐이고, ADR-021이 지키려던 "새벽에 재시도가 없다"는 성질이 유지된다
- **오발동 비용이 Level마다 다르다.** Level 3의 오발동은 성가심이고 Level 4의 오발동은 이탈이다. 검증 비용을 치를 가치가 Level 4에만 있다
- 설계 §8이 `model_high`를 "이후 Guard 판단"으로 예약해 뒀다 — 설계에 있는 자리를 쓰는 것이다
- Level 4는 하루 몇 번 안 되므로 호출량이 작다. Level 1~3에 붙였다면 감당이 안 됐다

### 지출 통제 — 권한을 엄격히 제한한다

실제 돈이 나가므로 상한을 여러 겹 둔다. 하나가 뚫려도 다음이 막는다.

| # | 통제 | 값 |
|---|---|---|
| 1 | **호출 지점 제한** | Level 3→4 격상뿐. 다른 어떤 경로도 `model_high`를 부르지 않는다 |
| 2 | **event당 1회 캐시** | 같은 데드라인에 대한 판정은 재사용. Level 4는 30분마다 재발동하므로 **이게 없으면 하룻밤에 10회 이상** |
| 3 | **일일 상한** | `guard_modes.ai_daily_cap` (기본 5). 초과 시 즉시 Level 3 |
| 4 | **타임아웃 8초** | 넘으면 Level 3. 새벽에 기다리지 않는다 |
| 5 | **킬 스위치** | 설정에서 끄면 Level 4가 결정론으로 돌아간다(= 항상 격상). 비용이 걱정되면 여기서 끈다 |
| 6 | **기록** | 매 호출을 `guard_events.ai_used` + 토큰·지연에 남긴다. 월말에 실제 비용을 본다 |

**②가 가장 중요하다.** 캐시가 없으면 밤새 반복 호출이 나가고, 그건 지출 사고다.

### 평가 순서 — 표의 번호순이 아니다

```
⑤킬 스위치 → ②캐시 → ③일일 상한 → 키 → ④타임아웃 → ①호출
```

**캐시가 상한보다 먼저다.** 적중은 돈이 0이므로, 상한이 찼다고 이미 받은 판정을 버리면
그 밤의 Level 4가 이유 없이 죽는다. 상한이 막아야 하는 것은 **새 호출**이지 판정의 재사용이 아니다.

**킬 스위치는 Level 4를 준다**(결정론 복귀). Level 3으로 떨구면 "끄면 Guard가 약해진다"가 되어
끄는 것이 벌이 된다 — 비용이 걱정될 때 쓰라고 둔 스위치가 쓰이지 않게 된다.

**`unavailable`은 캐시하지 않는다.** `ai_verdict`의 세 값 중 `approve`·`deny`만 재사용한다.
`unavailable`은 판정이 아니라 "부를 수 없었다"는 기록이고, 재사용하면 네트워크가 돌아온 뒤에도
그 밤 내내 Level 3에 묶인다.

### ⑥ 기록은 검증 시점이 아니라 발동 시점에

검증 결과를 그 자리에서 `guard_events`에 쓰지 않는다 — **검증만 하고 발동하지 않은 밤의 유령 행이
개입 이력을 오염**시킨다. `record()`가 `ai_used`·`ai_verdict`를 입력으로 받으므로 기기가 발동을
올릴 때 함께 남는다.

대가: 검증 후 기기가 기록을 못 올리면(크래시) 그 호출이 일일 상한에 안 세어진다.
**기기는 `record()`에 `ai_used`·`ai_verdict`를 반드시 싣는다** — 이것이 상한의 전제다.

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| AI 실패 시 Level 4 발동(fail-open) | ADR-021이 막으려던 것과 반대. 검증을 요구한 이유 자체가 무의미해진다 |
| AI 실패 시 아무것도 안 함 | 개입이 사라진다. 가장 나쁜 실패 |
| Level 3에도 AI 검증 | 호출량이 수십 배. 비용·지연 모두 감당 안 되고, Level 3 오발동은 이탈을 부르지 않는다 |
| 결정론 규칙만 정교하게 | 정교한 규칙은 실사용 데이터가 있어야 만든다. 그게 없는 게 8월의 조건 |

### 감수하는 비용
- Level 4가 네트워크에 의존하게 된다. 오프라인이면 영원히 Level 3이다 — 받아들인다. Guard의 핵심 도달은 Level 3이 진다
- 판정 지연 최대 8초. 데드라인 +30분 시점이라 8초는 무의미한 차이다
- 실제 지출 발생. 위 6겹으로 통제하고 월말에 실측한다

### 재검토 트리거
10월. `risk_snapshot`이 쌓여 결정론 규칙으로 Level 4를 판정할 수 있게 되면, AI 검증을 뺄지 판단한다.

---

<a id="adr-026"></a>
## ADR-026. 이탈 경로에는 전부 대가가 있다 — 다만 막지는 않는다

**Status** `Accepted` · **Refines** [ADR-019](#adr-019) · **Phase** 6 · 2026-08-01

### 맥락

실사용에서 사용자가 찾았다. **Level 2 이상 개입에서 `[알겠습니다]`를 누르면 화면이 닫히고 그대로 폰을 계속 쓸 수 있다.**

코드상 의도된 동작이다 — `[알겠습니다]`는 `accepted`를 기록하고 끝난다. 수락에 비용을 매기지 않는 것 자체는 옳다. **"네 말이 맞다"는 사람에게 60초를 물리면 아무도 수락하지 않고 전부 Override로 간다.**

문제는 다른 데 있다. **수락이 지켜졌는지 아무도 보지 않는다.**

```
정직한 경로   [그래도 계속하기] → 사유 + 60초
빠른 경로     [알겠습니다] → 계속 사용 → 0초
```

`[알겠습니다]`가 사실상 **'화면 없애기' 버튼**이다. 설계 §6.3이 Override에 비용을 매겼는데, 비용 없는 Override가 그 옆에 있으면 마찰은 장식이 된다.

같은 날 T-08 검토에서 **모드 하향**도 같은 성질임이 드러났고(ADR-019 부수 규칙 위반), 무음 모드 역시 오래전부터 같은 자리에 있었다. **개별 사고가 아니라 종류다.** 하나씩 고치면 다음 것이 또 나온다.

### 결정

**Guard에서 빠져나가는 모든 경로에 대가가 있다. 그러나 어느 경로도 막지 않는다.**

두 문장이 함께 있어야 한다. 대가가 없으면 마찰이 장식이 되고, 막으면 급할 때 도구를 떠난다.

#### 대가의 세 형태

| | 무엇 | 왜 |
|---|---|---|
| **사유** | 한 문장. 길이 하한 없음 | §6.5의 입력. 비어 있지만 않으면 된다 |
| **대기** | Level별 초. `friction_mult` 배수 | 충동과 결정 사이에 시간을 넣는다 |
| **재확인** | 수락이 지켜졌는지 다시 본다 | **이 ADR이 새로 넣는 것** |

#### 이탈 경로 전수 (2026-08-01 기준)

| 경로 | 지금 | 결정 |
|---|---|---|
| `[그래도 계속하기]` | 사유 + 대기 | **기준선. 그대로** |
| **`[알겠습니다]` 후 계속 사용** | 없음 | **재확인을 붙인다** |
| **무음 모드** | `overrideSilentAtL4`로 뚫음 | **뚫지 않는다. 설정을 폐기한다** |
| 모드 하향 | UI 미구현 | ADR-019 그대로 — **서버가 강제**한다 |
| 알림 밀어서 지우기 | 36시간 뒤 `ignored` | 그대로 |
| 화면 끄기·홈 버튼 | 소리 계속 | 그대로 |

### 결정 1 — 수락에는 재확인이 붙는다

`accepted` 후 **5분**이 지났는데 화면이 켜진 채 사용 중이면, **같은 Level로 한 번 더 발동**한다.

```
Level N 발동 → [알겠습니다] → 5분 → 아직 쓰고 있다 → Level N 재발동
```

- **하룻밤 재확인 상한 2회.** 그 뒤에는 그날 다시 묻지 않는다 — 오발동이 매일 반복되면 도구를 떠난다(§6.3)
- 재확인도 **하나의 발동**이다. `guard_events`에 `cause = "recheck:accepted"`로 새 행이 남는다.
  새 컬럼도, 앞선 행과의 FK도 두지 않는다 — 시간 근접으로 충분하고, 필요해지면 그때 잇는다
- 화면이 꺼져 있으면 재확인하지 않는다. **잤다는 뜻이고, 그게 수락이 지켜진 것이다**

**이것이 무음 구멍도 함께 막는다.** 무음에서 화면만 없애고 계속 쓰는 것이 지금의 우회로인데, 재확인이 붙으면 그 경로에도 대가가 생긴다.

#### 왜 수락 자체에 마찰을 붙이지 않는가

붙이면 수락이 벌이 된다. 그러면 사용자는 수락 대신 Override를 고르고, **Guard가 받는 신호가 오염된다** — "수락했다"는 기록이 사라지고 전부 "무시하고 계속했다"가 된다. §6.5는 그 구분으로 배운다.

재확인은 벌이 아니라 **확인**이다. 지켰으면 아무 일도 일어나지 않는다.

### 결정 2 — 무음 모드를 뚫지 않는다. `overrideSilentAtL4`를 폐기한다

**무음은 사용자의 명시적 선택이고, 앱이 그것을 뒤집지 않는다.**

이 설정은 "무음이 0마찰 우회로가 된다"는 우려로 남겨 뒀다. 그 우려는 **재확인이 해소한다** — 소리 없이 화면만 없애도 5분 뒤에 다시 온다.

그리고 원래 논리가 틀렸다. **소리는 도달 수단이지 마찰이 아니다.** 마찰은 사유와 대기가 진다. 소리를 키워 얻는 것은 마찰이 아니라 **불쾌함**이고, 무음으로 둘 때는 대개 이유가 있다(수업·회의·타인 앞). 그 자리에서 알람이 울리면 §6.3의 도구 이탈이 **그날 즉시** 일어난다.

무음일 때 개입은 **화면이 전부 진다.** 화면은 뜨고, 없애려면 수락이나 Override를 눌러야 하고, 수락에는 재확인이 붙는다.

### 결정 3 — 어느 경로도 막지 않는다

**급한 일은 언제든 생긴다.** 사유를 적고 대기를 견디면 **항상** 빠져나갈 수 있다.

- Override에 상한을 두지 않는다. 하룻밤 다섯 번이든 열 번이든 사유+대기를 치르면 통과한다
- Level 4에서도 마찬가지다. 대기가 180초로 길 뿐 막히지 않는다
- 유일한 예외는 **모드 하향 중 보호 구간**이다(ADR-019 부수 규칙 1). 그 구간은 사전 서약이 지켜야 할 구간이고, **개별 개입을 빠져나가는 것과 규칙 자체를 끄는 것은 다르다** — 앞은 열려 있고 뒤가 닫혀 있다

이 구분이 이 ADR의 핵심이다:

> **개입에서 빠져나가는 것은 언제든 가능하다. 대가를 치르면 된다.**
> **개입이 오지 않게 만드는 것은 보호 구간 중에는 불가능하다.**

### 근거

- §6.3은 "Override는 금지가 아니라 마찰"이다. 마찰이 **한 경로에만** 있으면 다른 경로가 그 마찰을 무의미하게 만든다 — 사용자는 항상 싼 경로를 찾는다. 그게 나쁜 게 아니라 사람이 그렇다
- 재확인은 **막지 않으면서 대가를 만드는** 유일한 형태다. 지킨 사람에게는 비용이 0이다
- 무음 존중은 §6.3의 도구 이탈 방지와 정합한다. 원래 `overrideSilentAtL4`가 그것과 부딪히는 채로 남아 있었다
- 이탈 경로를 **전수로 적어 두면** 다음에 새 경로가 생길 때 이 표에 추가할지부터 묻게 된다. 개별 사고로 다루면 매번 처음부터 논의한다

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| `[알겠습니다]`에도 사유·대기 | 수락이 벌이 된다. 사용자가 Override로 몰리고 §6.5의 신호가 오염된다 |
| Override 횟수 상한 | 급한 일이 있을 때 도구가 사람을 막는다. §6.3의 이탈이 그 밤에 확정된다 |
| 기기 잠금 (접근성 권한) | Guard는 잠그는 도구가 아니다. 권한 비용도 크고 되돌리기 어렵다 |
| 무음에서 소리를 강제 | 타인 앞에서 울리면 그날 앱을 지운다. 얻는 것은 마찰이 아니라 불쾌함 |
| 재확인을 30분 뒤로 | 그 사이에 이미 자거나 이미 몰입한다. 5분이 "지금 끄는가"를 묻는 창이다 |
| 재확인 무제한 | 오발동이 반복되면 도구를 떠난다. ADR-025의 밤당 상한과 같은 취지 |

### 감수하는 비용

- **재확인이 성가실 수 있다.** 정말 자려고 준비하는 5분에 한 번 더 온다. 상한 2회와 "화면 꺼져 있으면 안 함"으로 완화하되, 실사용 2주 뒤 임계를 조정한다
- `overrideSilentAtL4`를 쓰던 상황(무음인데 반드시 깨야 하는 밤)이 사라진다. 그런 밤은 **알람 앱의 일**이지 Guard의 일이 아니다
- 재확인 행이 `guard_events`를 늘린다. 밤당 최대 2행이라 감당된다

### 구현 명세

**마이그레이션 없음.** `guard_events`는 그대로 쓴다.

```
① GuardAlertActivity — finishWith("accepted", …) 직후 GuardRecheck.arm(ctx, level) 호출
② GuardRecheck (신규)
     arm(ctx, level)   수락 시각·level·오늘 밤 키를 SharedPreferences("guard")에 저장
     evaluate(ctx)     GuardService 60초 폴링이 부른다. 조건 전부 만족하면 발동:
                         · armed 상태다
                         · 수락 후 5분 경과
                         · 화면이 켜져 있다 (GuardService의 screenOn)
                         · 오늘 밤 재확인 횟수 < 2
                       발동: GuardNotifications.fire(ctx, level, …, cause = "recheck:accepted")
                       발동 후 disarm. 그 발동에서 다시 수락하면 arm이 다시 걸린다
     화면이 꺼지면 disarm — 잤다는 뜻이다
③ GuardAlertPolicy.plan — level >= 4 && overrideSilentAtL4 분기를 제거한다
④ GuardSettings — overrideSilentAtL4 게터·세터·키를 제거한다
⑤ GuardPlugin — getSettings/setSettings에서 그 필드를 뺀다. recheckStatus() 추가(확인용)
```

밤 키는 `GuardWatch`의 `nightKey`와 같은 방식(취침 창 기준)이 아니라 **귀속일**을 쓴다 — 재확인은 취침 창 밖에서도 일어난다.

### 재검토 트리거

실사용 2주. 재확인이 **매일 두 번** 오면 5분이 짧은 것이다. 반대로 재확인에서 매번 Override를 고르면 5분이 아니라 **수락 자체가 형식**이라는 뜻이고, 그때는 `[알겠습니다]` 문구를 다시 본다.

---

<a id="adr-027"></a>
## ADR-027. 모드 하향이란 무엇인가 — 단일 컬럼이 아니라 파라미터 벡터

**Status** `Accepted` · **Refines** ADR-019 · **Phase** 6 · 2026-08-05

### 맥락

ADR-019가 **"보호 모드 중 모드 하향 금지, 하향에도 사유 + 대기"**를 정했다.
그런데 **하향이 무엇인지는 정의하지 않았다.** 그 결과 `guard.setMode`는 지금 이렇게 되어 있다:

```
존재하는 키인가 → batch로 교체 → 끝
```

**부수 규칙 1도 2도 구현되지 않았다.** 마찰이 하나도 없다.
구현하려면 먼저 "어느 방향이 아래인가"에 답해야 한다.

`guard_modes`의 컬럼은 일곱이다:

```
max_level · risk_threshold · friction_mult · use_fsi · use_overlay · ai_daily_cap · sort
```

### 결정

**① 하향 = 강도 파라미터 중 하나라도 약해지는 것.**

```
max_level        낮아지면      약함
risk_threshold   높아지면      약함    ← 방향이 반대다
friction_mult    낮아지면      약함
use_fsi          1 → 0         약함
use_overlay      1 → 0         약함
ai_daily_cap     강도가 아니다 — 판정에서 제외
sort             표시 순서다 — 판정에서 제외
```

**어느 하나라도 약해지면 하향으로 본다.** 다섯이 전부 같거나 세지면 상향(또는 동일)이다.

**② "지금 보호 구간인가"를 새로 짜지 않는다.**
`services/guard.schedule()`이 이미 각 보호 일정의 `from`(보호 진입)과 `start`(일정 시각)를 계산한다.
그 구간에 `now`가 들어 있으면 보호 중이다. **판정식을 두 벌 두지 않는다.**

**③ 서버는 사유를 강제하고, 대기는 클라이언트가 건다.**
서버는 60초가 실제로 흘렀는지 알 수 없다. 사유 없는 하향을 `400`으로 막는 것이 서버가 할 수 있는 전부다.
Override의 대기도 이미 기기가 세고 서버엔 결과만 온다 — 같은 구조다.

### 근거

- **`sort`로 판정하면 표시 순서를 바꾸는 순간 하향 판정이 뒤집힌다.** 정렬은 UI의 문제이지 강도가 아니다
- **`max_level` 하나로 판정하면 우회로가 남는다.** `max_level`은 그대로 두고 `friction_mult`만 낮춘 모드를 만들면 마찰 없이 마찰이 사라진다. ADR-019가 막으려던 것이 정확히 이것이다
- **놓치는 것보다 과하게 잡는 편이 옳다.** 하향을 못 잡으면 Guard가 무력해지고, 과하게 잡으면 사유를 한 번 더 쓴다. 비용이 대칭이 아니다
- `ai_daily_cap`은 ADR-024가 **지출 통제**로 규정한 값이다. 예산을 줄이는 것은 Guard를 약화시키는 것과 다르다. 이것을 강도로 세면 비용 절감이 마찰을 부른다
- ②는 T-16이 귀속일을 `attributionOfIso` 하나에 맡긴 것과 같은 이유다. **두 벌이 되면 반드시 갈라진다**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| `sort` 순서로 판정 | 표시 순서 변경이 판정을 뒤집는다 |
| `max_level`만 비교 | `friction_mult` 우회로가 열린다 — 막으려던 것이 그대로 남는다 |
| `strength` 컬럼 신설 | 마이그레이션이 붙고, **어느 면에서는 세고 어느 면에서는 약한 모드**의 순위를 매길 수 없다 |
| 보호 구간 판정을 별도 함수로 | 데드라인 역산이 두 벌이 된다. 갈라지면 어느 쪽이 옳은지 알 수 없다 |
| 서버가 대기까지 강제 (2단계 커밋) | 중간 상태가 늘고, Override와 구조가 달라진다 |

### 감수하는 비용

**과잉 차단.** 어떤 면에서 세고 어떤 면에서 약한 모드로 옮기려면 — 이론적으로는 상향인데도 — 사유를 써야 한다.
모드가 `coach`·`secretary` 둘뿐인 지금은 발생하지 않는다. 셋째가 오면 그때 실제 사례로 판단한다.

**대기는 API를 직접 부르면 건너뛸 수 있다.** 사유는 못 건너뛴다.
자기 자신과의 약속에서 API를 직접 호출하는 우회는 이미 다른 층위의 문제다 — 설계 §6.2가 다루는 범위 밖이다.

### 구현 명세

**마이그레이션 하나** — 사유를 적을 자리가 없다.

```
me_history 에 reason TEXT NULL 추가
```

`me_history`는 주석이 이미 **"변경 궤적을 분석 입력으로 쓴다(§3)"**고 말한다.
모드 변경도 설정 변경이므로 여기 들어간다. 새 테이블을 만들지 않는다 —
그러면 자기 보정이 읽을 곳이 둘로 갈린다.

```
setMode(env, t, key, reason?)     ← 보호 구간 판정에 시간 맥락이 필요하다
  ① 존재 확인                                    (지금 있는 것)
  ② 하향인가 — 다섯 파라미터 비교
  ③ 하향이고 보호 구간 중       → 409  "보호 중에는 내릴 수 없어요"
  ④ 하향이고 사유가 없거나 빈 문자열 → 400  "왜 내리는지 적어주세요"
  ⑤ 교체 + me_history 기록 (field="guard_mode", reason)
  상향·동일은 ③④를 건너뛴다 — 사유도 대기도 없다
```

**상향에 마찰을 붙이지 않는다.** ADR-019 부수 규칙 1이 "상향은 자유"라고 명시했다.

**⑤는 방향과 무관하게 실행된다.** 기록은 마찰이 아니다.
하향만 남기면 이력이 `내림`·`내림`으로만 이어져 **사이에 올린 것이 보이지 않고**,
"두 번 내렸다"가 "한 번 내리고 계속 그 상태"로 읽힌다. 상향도 그 자체로 신호다 —
시험 기간에 스스로 강도를 올리는 것은 §6.5가 읽을 값이 있다.
`reason`은 하향에만 붙는다. `me_history`는 설정 변경의 궤적이고, 한 필드만 방향으로 거르면
같은 테이블 안에서 규칙이 갈린다.

### 재검토 트리거

모드가 셋을 넘어 **한 면은 세고 한 면은 약한** 조합이 실제로 나올 때.
또는 `me_history.reason`이 모드 말고 다른 설정 변경에도 필요해질 때 — 그때는 범용화가 맞는지 다시 본다.

---

<a id="adr-028"></a>
## ADR-028. 입력의 무게중심을 시스템으로 — 주체성 축소 원칙

**Status** `Accepted` · **Phase** 전 Phase · 2026-08-10
> ⚠️ **설계문서 갱신 필요.** v0.9는 사용자를 유일한 입력원으로 전제한다. ADR-018의 §6.4 갱신과 함께 v1.0에서 다룬다.

### 맥락
실사용에서 확인된 것: 앱을 직접 켜서 기록하고 알림을 수용해야 하는 수동적 역할이 실생활 영향의 상한을 만든다. 폰 기본 캘린더와의 이중 입력, 복잡한 구성 등 접근성 비용이 기록 자체를 막는다. 시스템의 존재 이유(외부 구속)가 사용자의 성실성(내부 구속)에 의존하는 모순.

### 결정
데이터를 두 종류로 나누고 다르게 다룬다.

| 종류 | 예 | 정책 |
|---|---|---|
| **행동 데이터** | 일정 · 기기 사용 · 화면 on/off · foreground app · 수면·활동 | **시스템이 수집한다.** 사용자 입력은 예외 경로 |
| **성찰 데이터** | score · feelings · 미루기 사유 · Override 사유 | **자동화하지 않는다.** 대신 하루 1회 순간으로 압축, 도달 채널에서 바로 응답 |

부수 원칙: **입력 노동은 없애되 결정권은 남긴다.** 시스템이 관찰하고 제안하며, 확정은 사용자가 한다(1탭). 특히 사전 서약(§6.2)이 걸리는 결정은 자동화 금지 — ADR-030.

### 근거
- 감지 계열(ADR-018·025)은 이미 이 방향이다. 이 ADR은 그것을 입력 전반의 원칙으로 승격한다
- 성찰 데이터를 자동화하지 않는 이유: score·feelings는 관찰이 아니라 자기 보고다. 추정치로 채우면 분석·자기 보정(§6.5)의 입력이 오염된다
- 결정권을 남기는 이유: Guard의 정당성은 "미리 정한 나"의 서약에서 나온다. 서약 없는 개입은 부과이고, 부과는 도구 이탈(§6.3)로 귀결된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 위젯·알림 개선으로 대응 | 증상 완화. 이중 입력과 수동 기록이라는 구조가 남는다 |
| 성찰 데이터도 AI 추정으로 자동화 | 자기 보고를 추정으로 대체하면 보정 입력이 오염된다. §6.5의 전제 훼손 |
| 전면 자동화(확정까지) | 사전 서약 원칙 위반. Guard 정당성 붕괴 |

### 감수하는 비용
동기화·권한·제안 UI라는 새 표면이 생긴다. 각 표면의 비용은 하위 ADR(029~034)에서 개별 평가한다.

### 재검토 트리거
자동 수집이 만든 데이터의 품질이 수동 입력보다 낮다고 확인될 때(중복·오분류가 수정 노동을 만들 때).

---

<a id="adr-029"></a>
## ADR-029. 캘린더 양방향 동기화 — 동기화 창은 열린 날만

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
일정의 자연스러운 입력처는 폰 기본 캘린더다. personal-os가 자체 일정 입력을 요구하는 한 이중 입력이 남고, 이중 입력은 어느 한쪽의 방치로 끝난다. 한편 personal-os의 events는 보호 규칙(protect_*)이 걸리는 곳이라 캘린더를 버릴 수도 없다.

양방향 동기화의 일반적 비용은 충돌 해소다(ADR-015가 CRDT를 과잉이라 기각한 이유). 그러나 이 도메인에는 일반 동기화에 없는 자산이 있다 — **마감된 날은 불변**이라는 규칙이 이미 있다.

### 결정
**양방향 동기화. 단 동기화 창 = 열린 날(오늘 포함)~미래 N일(기본 60, settings).**

```
열린 날   : 캘린더 ↔ personal-os 양방향. 캘린더가 편집 표면
마감된 날 : 동기화 영구 중단. personal-os 기록이 유일한 원본
           (이후 캘린더에서 과거를 고쳐도 무시)
```

**"열린 날은 캘린더와 공유하는 현재, 마감된 날은 personal-os만의 과거."** 귀속일 마감이 동기화 이탈 시점을 겸한다 — 새 개념을 만들지 않고 기존 불변성이 동기화 경계가 된다.

세부:

| 항목 | 결정 |
|---|---|
| 필드 스코프 | 제목 · 날짜 · 시각/종일만. protect_*는 앱 전용(캘린더로 안 나감). 위치·참석자·알림은 동기화 안 함 |
| 식별 | `events.ext_src('devcal')`·`ext_uid`. 반복 일정은 `<eventId>:<날짜>`로 인스턴스 단위 |
| 충돌 | 갱신 시각 LWW. 창이 좁고 단일 사용자라 실충돌은 드묾 — 해소 UI 없음 |
| 반복 일정 | `CalendarContract.Instances`로 창 범위만 전개해 개별 행으로. 앱→반복원본 수정은 금지(캘린더에서만) |
| 날짜 귀속 | 캘린더의 벽시계 날짜 그대로. 귀속일 재계산 금지 — 일정·보호 판정은 벽시계의 것(APP-PLAN 함정과 정합) |
| 삭제(캘린더→앱) | 열린 날 mirror 삭제. guard 이력 참조 시 삭제 대신 protect 해제+보존(개입 이력 불변) |
| 실행 주체 | **기기.** CalendarContract는 기기만 본다. 서버는 멱등 upsert 엔드포인트만 제공 |
| 시점 | 앱 열 때 + 하루 1회 보호 일정 pull **직전**(캘린더의 시험이 제안→서약→알람 예약 경로에 타도록) + 수동 새로고침 |
| 대상 캘린더 | 사용자가 기기 캘린더 목록에서 선택(1개 이상, settings) |

**단계화 — 8월은 읽기 방향만.**

| | 캘린더→앱 (읽기) | 앱→캘린더 (쓰기) |
|---|---|---|
| 시점 | **8월 W5 전반** | 9월 |
| 가치 | 이중 입력 소멸 + 보호 제안 재료 = 주체성 축소의 본체 | 앱 생성 일정의 캘린더 노출 |

읽기 단계 동안 devcal-소스 event는 **앱에서 읽기 전용**(수정은 캘린더에서). 두 저장소가 갈라질 경로를 쓰기 방향이 붙기 전까지 물리적으로 차단한다.

### 근거
- 동기화 창 한정이 양방향의 비용 문제를 구조로 푼다. 충돌 창이 "열린 날"로 좁혀지고, 과거 이력(양이 무한히 쌓이는 쪽)은 동기화 대상 자체가 아니다
- 마감 시점 확정은 설계 §1.3의 자연스러운 확장이다 — daily가 마감되며 그 날의 일정도 personal-os 기록으로 확정된다
- 캘린더가 입력 표면이 되면 보호 규칙의 재료(시험·마감 일정)가 등록 노동 없이 도착한다. ADR-025의 지적("보호 일정을 붙이는 UI도 없다")이 UI 신설이 아니라 흡수로 풀린다
- 읽기/쓰기 단계 분리로 8월 추가분이 최소화된다. 읽기는 서버 쪽이 upsert 하나라 Guard 크리티컬 패스와 접점이 없다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 캘린더=미래의 원본, 단방향 읽기만(원안) | 앱에서 만든 일정이 캘린더에 안 보인다. 표면이 둘로 남는 반쪽 해소 |
| personal-os 원본 유지 + 가져오기 | 이중 입력 부분 잔존. 접근성 문제의 근본 해결이 아님 |
| 전 기간 양방향(창 무제한) | 과거 편집 충돌이 불변성과 정면충돌. ADR-015가 기각한 복잡도가 그대로 돌아옴 |
| Google Calendar API 경유 | 계정 종속·네트워크 의존·쿼터. CalendarContract는 로컬이라 오프라인 정합(ADR-021 방향)과 일치 |

### 감수하는 비용
- 재설치 시 기기 쪽 매핑이 사라져 재연결 필요. `(ext_src, ext_uid)` 유니크 인덱스가 서버 쪽 중복은 막는다. 기존 수동 입력분과의 병합은 (날짜, 제목) 휴리스틱 1회
- 여러 날짜에 걸친 종일 일정은 v1에서 시작일 1건으로 축약
- 쓰기 방향이 붙기 전까지 devcal event는 앱에서 편집 불가 — 한 달 감수
- 마이그레이션 번호 소비: **0016_cal_sync**. 인증 마이그레이션은 착수 시 배정

### 재검토 트리거
9월 write-back 착수 시(LWW로 부족한 충돌이 실제로 관측되는지). 창 크기 N은 실사용 조정.

---

<a id="adr-030"></a>
## ADR-030. 보호 규칙은 자동 부착이 아니라 자동 제안

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
ADR-029로 시험·마감 일정이 캘린더에서 자동으로 들어온다. 다음 질문: 보호 규칙(protect_*)도 자동으로 붙일 것인가? 주체성 축소를 끝까지 밀면 자동 부착이지만, Guard의 정당성은 사전 서약(§6.2)에서 나온다.

### 결정
**결정론 키워드 매칭으로 제안하고, 확정은 사용자 1탭.**

```
가져온 일정 제목 ⊇ 키워드(settings `guard_kw`, 기본: 시험·마감·면접·발표·접수)
  → Today에 제안 카드 1회: "8/21 정보처리기사 — 보호 걸까요? [보호 설정 / 안 함]"
  → [보호 설정] = protect_* 기본값 부여 + 알람 예약 경로 진입
  → 무응답 시 다음 열림에 재노출, event 시작 후 소멸. 응답은 기록
```

카드는 outcome 확정 카드(APP-PLAN 4주)와 같은 패턴을 재사용한다. 신규 UI 최소.

### 근거
- 서약이 없는 개입은 부과다. 새벽에 발동한 Guard가 "내가 건 것"이 아니면 Override가 아니라 이탈을 부른다 — §6.3
- 매칭은 결정론이다. AI 분류를 넣지 않는 이유는 ADR-021과 같다 — 제안 경로가 서드파티 가용성에 걸릴 이유가 없다. 오분류 비용도 비대칭적으로 안전하다: 과잉 제안은 1탭 거절, 누락은 기존 수동 경로
- 1탭이면 주체성 비용은 사실상 0이고 서약의 형식은 보존된다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 완전 자동 부착 + 사후 해제 | 사전 서약 위반. 해제가 Override 마찰과 충돌하는 역설(자기가 안 건 보호를 마찰 내고 풀어야 함) |
| AI로 일정 성격 분류 | 제안 품질 이득 대비 가용성·비용. 키워드로 시작하고 10월 데이터로 판단 |
| 제안 없이 수동 유지 | ADR-025가 지적한 "보호 일정이 드물다" 문제가 그대로. 발동 루프가 계속 굶는다 |

### 감수하는 비용
키워드 목록 관리가 사용자 몫으로 남는다(기본값 제공으로 완화). 제안 카드 무시가 쌓이면 카드 자체가 소음 — 재노출은 열림당 1회로 제한.

### 재검토 트리거
10월. 제안 수락률이 기록되므로, 수락률이 매우 높으면(서약이 형식화되면) 자동 부착+통지로 내리는 것을 그때 다시 판단한다.

---

<a id="adr-031"></a>
## ADR-031. 기록 경로를 OS 표면으로 — share-target · 알림 액션

**Status** `Accepted` · **Phase** 7 · 2026-08-10

### 맥락
기록(할일·memo·하루 마감)은 앱을 열고 화면을 찾아 들어가야 한다. 위젯(Phase 5)이 일부를 풀지만 뒤로 밀렸고, 위젯도 결국 "보러 가는" 표면이다. OS에는 이미 도달해 있는 표면이 있다 — 공유 시트와 알림.

### 결정
기록 진입점 3종을 네이티브 표면에 만든다. **전부 9월. 전부 기존 엔드포인트 재사용(ADR-012 준용).**

| 표면 | 동작 | 서버 |
|---|---|---|
| share-target | 아무 앱에서 공유 → [memo / 할 일] 선택 → 저장 | `addMemo`·기존 task 생성. 신규 없음 |
| 알림 액션 | 리마인더 알림에 [완료]·[미루기] 버튼 | `complete`·`defer`. 409는 앱 열기로 폴백 |
| RemoteInput 마감 | 하루 마감 알림에서 score·한 줄 응답 → 마감 | 기존 close 경로 |

### 근거
- ADR-028의 성찰 데이터 압축("하루 1회, 도달 채널에서 바로")의 실행 형태가 RemoteInput 마감이다
- 공유 시트는 기록 마찰의 최저점이다 — 보던 것을 보내는 행위라 맥락 전환이 없다. **식사 기록(ADR-032이 자동화 불가로 판정)의 상한 경로이기도 하다**
- 기존 엔드포인트 재사용 이유는 ADR-012와 동일: 도메인 규칙(409·state·귀속일)이 한 곳에 산다
- 9월인 이유: 알림 액션·RemoteInput은 notify 아웃박스(번호 착수 시 배정) 위에 서고, 8월 W3~5는 Guard가 쓴다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Daily 위젯으로 해결 | RemoteViews에 텍스트 입력이 없어 어차피 알림 RemoteInput 우회가 필요(APP-PLAN Phase 5). 그럼 알림이 본체다 |
| 8월 W5 편입 | W5는 Guard 밀림 대비 버퍼 + 캘린더 읽기(ADR-029)가 선점 |
| 앱 내 입력 개선(진입 단축) | 앱을 연다는 전제가 남는다. ADR-028 위반 |

### 감수하는 비용
알림 액션의 409 처리·RemoteInput의 마감 실패 전달이 알림 UI 제약 안에서 거칠다. 실패 시 앱 열기 폴백으로 통일.

### 재검토 트리거
ADR-010 재검토(위젯 착수 시)와 함께 본다 — RemoteInput 마감이 자리 잡으면 Daily 위젯의 존재 이유가 좁아진다.

---

<a id="adr-032"></a>
## ADR-032. 건강 데이터는 Health Connect에서 로컬로 읽는다

**Status** `Accepted` · **Phase** 8 · 2026-08-10

### 맥락
lifestyle 데이터(수면·활동)를 세세한 입력 없이 얻어 분석·제시에 쓰고 싶다. 장애물로 보였던 것: 스마트워치 부재, API 비용. 실제로는 둘 다 전제가 아니다.

### 결정
**Android Health Connect를 유일한 건강 데이터 소스로 한다. 기기 로컬 읽기, 서버측 건강 API·클라우드 SDK 사용 안 함.**

| 층 | 내용 |
|---|---|
| 1차 (지금) | 폰 화면 on/off 기반 수면 추정 — §6.6에 이미 있는 항, W2 감지 수집이 재료. 워치 불필요 |
| 2차 (HC) | Health Connect에서 수면 세션(단계 포함)·걸음·심박 읽기. Samsung Health 등이 쓰는 데이터를 유형별 권한 승인 후 로컬로 |
| 우선순위 | HC에 실측 수면이 있으면 추정치보다 우선. `risk_snapshot`·분석 입력에 출처(`estimated`/`hc`) 표기 |

**식사는 자동 수집 대상에서 명시적으로 제외한다.** 어떤 소비자 기기도 식사를 자동 감지하지 못한다. 식사 기록의 상한은 초저마찰 수동 — share-target 사진/한 줄(ADR-031).

### 근거
- HC는 로컬 API라 네트워크 0 · API 비용 0 — ADR-021의 로컬 원칙과 정합
- Samsung Health SDK 직접 연동 대신 HC를 경유하면 제조사 종속이 없고, 워치를 나중에 들여도 코드 변경이 없다(워치가 HC에 쓰기 시작할 뿐)
- 워치는 전제조건이 아니라 정밀도 업그레이드다. 폰 추정(무료·이미 계획됨)으로 시작해, 추정치가 분석에 유효한지부터 확인하는 순서가 싸다

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| Samsung Health SDK 직접 연동 | 제조사 종속·심사. HC가 같은 데이터를 표준 경로로 준다 |
| 서버측 건강 플랫폼 API | 네트워크·비용·계정 종속. 로컬로 충분 |
| 식사 자동 감지 | 기술적으로 불가. 가능한 척 설계하면 빈 파이프라인만 남는다 |
| 워치 구매를 전제로 설계 | 전제가 아니다. 폰 추정으로 시작 가능 |

### 감수하는 비용
- HC 권한 승인 흐름(데이터 유형별)이라는 온보딩 표면 추가
- 추정치와 실측치의 이원화 — 출처 표기로 분석 오염 방지
- 데이터 품질이 기기·수면 습관(폰을 머리맡에 두는가)에 따라 다름
- **Fit 시리즈는 Wear OS가 아니라 Samsung Health 경유로만 데이터가 나온다** — Fit4 구매 전, Samsung Health→HC 동기화에 수면 세션이 실제로 도착하는지 확인

### 재검토 트리거
10월 분석에서 수면 항의 기여도 평가. 추정치가 무용하면 워치 도입을 그때 판단(반대가 아니라).

---

<a id="adr-033"></a>
## ADR-033. 학교 공지는 Worker가 수집한다 — fetch·diff·키워드 제안

**Status** `Superseded by` [ADR-037](#adr-037) · **Phase** 8 · 2026-08-10

### 맥락
일정·마감 정보가 학교 사이트 공지에 먼저 뜬다. 사람이 읽고 옮기는 노동이 남는다.

### 결정
**기존 cron에 수집기를 태운다.** 등록한 공개 게시판 URL을 fetch → 저장된 해시와 diff로 신규 글 판별 → 제목 키워드 매칭(결정론) → 아웃박스(notify) 적재 + 제안 카드 → 1탭으로 일정화(ADR-030 경로 합류).

**로그인 벽 뒤(포털·LMS)는 v1 제외.** 공개 게시판만.

### 근거
- cron·아웃박스·제안 카드·키워드 매칭 전부 기존 자산이다. 새로 짓는 것은 fetch·diff·파서뿐
- 공개 페이지라 권한·구조 문제가 없다 — "구조적으로 막혀 있을 것"이라는 예상과 달리 이 항목은 그냥 일이다
- 키워드 결정론으로 시작하는 이유는 ADR-030와 같다. 본문 AI 분석은 매칭된 글에만 사후로(비용 통제)

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| LMS 로그인 자동화 | 세션 유지·2FA·구조 변경에 취약. 유지보수가 편익을 넘는다 — v2 재검토 |
| 전 글 AI 분석 | 비용. 제목 키워드 필터 후 소량에만 |
| 제안 없이 자동 일정 등록 | ADR-030 위반. 오수집이 캘린더를 오염 |

### 감수하는 비용
게시판 HTML 구조가 바뀌면 파서가 깨진다 — 사이트별 파서는 소모품으로 취급(깨지면 고치는 운영 비용). 대상 게시판 목록 관리는 사용자.

### 재검토 트리거
공개 게시판만으로 커버리지가 부족하다고 확인될 때 로그인 벽을 다시 판단.

---

<a id="adr-034"></a>
## ADR-034. 메신저는 알림 캡처로 읽는다 — 기기에서 필터, 제안만

**Status** `Accepted` · **Phase** 8 · 2026-08-10

### 맥락
일정·과제 공지가 카톡 단톡방으로 온다. 카카오톡에는 메시지를 읽는 API가 없다 — "권한 등 구조적 문제가 있을 것"이라 예상됐던 항목. 실제로는 표준 우회 경로가 있고 권한 계급도 이미 확보한 급이다.

### 결정
**`NotificationListenerService`로 수신 알림 텍스트를 캡처한다. 처리는 전량 기기 로컬, 서버에는 필터 통과분만.**

```
알림 캡처 (기기)
 → 등록한 방/발신자 + 키워드 필터 (결정론, 기기 로컬)
 → 통과분만 서버로 → model_low 일정 추출 → 제안 카드 1탭 (ADR-030 경로 합류)
 → 비매칭 원문은 저장하지 않는다
```

### 근거
- 카톡 공식 API 부재에서 유일하게 실행 가능한 경로다
- 권한 계급이 UsageStats와 동일하다(특수 접근 + 사이드로드 제한된 설정 1회 해제) — 이미 다루는 계급
- 필터 후 소량이라 AI 비용이 통제된다(`ai_daily_cap` 계열). 오추출 비용은 1탭 거절로 비대칭 안전(ADR-030와 동형)
- "비매칭 미저장"은 ADR-022가 전체 활동 로깅을 기각한 것과 같은 사생활 최소화 원칙

### 기각한 대안
| 대안 | 기각 이유 |
|---|---|
| 카카오 API | 존재하지 않는다 |
| 접근성 서비스로 화면 읽기 | 권한 과잉 — ADR-018이 AccessibilityService를 기각한 논리 그대로 |
| 전 메시지 서버 저장 후 분석 | 사생활 과잉 + 비용. 필터가 기기에 있어야 하는 이유 |

### 감수하는 비용
- **무음 방은 캡처 불가**(알림이 안 뜬다). 알림 미리보기 설정에도 종속 — 커버리지가 구조적으로 부분적
- model_low 추출 품질 한계 — 제안이므로 치명적이지 않음
- 감각 계층 중 불확실성이 가장 크다 — Phase 8 내 착수 순서 마지막

### 재검토 트리거
실사용에서 캡처 누락률이 높으면(핵심 방이 무음인 경우) 이 경로의 기대치를 낮추고 share-target 수동(ADR-031)을 보완 경로로 승격.

---

<a id="adr-035"></a>
## ADR-035. Level 4의 "신규 작업 차단"은 막는 것이 아니라 내일로 돌리는 것

**Status** `Accepted` · **Refines** ADR-026 · **Phase** 6 · 2026-08-10

### 맥락

설계 §6.4가 Level 4의 개입 상한에 **"신규 작업 차단"**을 넣었다. 그런데 **구현이 없다.**
리포 전체에서 그 개념이 나오는 곳은 한 군데뿐이고, 그것이 **AI에게 주는 프롬프트**다:

```
"Level 3(화면 점유 + 알람)이 이미 발동한 상태에서,
 Level 4(신규 작업 차단까지)로 격상할 근거가 있는지만 판정한다."
```

**모델은 존재하지 않는 강제력을 근거로 판정하고 있다.** 격상돼도 아무 일도 일어나지 않는다.
그 판정으로 쌓인 `guard_events`는 §6.5가 읽을 전례인데, **"차단이 필요했다"고 판단한 근거와
실제로 벌어진 일이 다르다.**

그리고 §6.4의 "차단"은 ADR-026의 결정과 정면으로 부딪힌다:

> 이탈 경로에는 전부 대가가 있다 — **다만 막지는 않는다**

### 결정

**아래를 함께 정한다** — 하나만 떼면 나머지가 성립하지 않는다.

**① 걸리는 자리는 "작업 추가"가 아니라 "오늘 날짜가 붙는 순간"이다.**

날짜가 붙는 연산은 넷이고 **규칙이 서로 다르다:**

| 연산 | 오늘이 어떻게 붙나 | Level 4 중 |
|---|---|---|
| 날짜와 함께 생성 | 캘린더에서 연 그 날짜 | **오늘 칸이면 내일로 돌린다** |
| 날짜를 붙인다 | 피커에서 고른 날 — 빠른 추가·대기가 **둘 다 여기로 모인다** | **오늘을 못 고르게 한다** |
| 날짜를 옮긴다(미루기) | 지난 날의 일이면 **오늘이 하한**이다 | **오늘을 못 고르게 한다** |
| 날짜 없이 생성 | 안 붙는다 — 대기 | **손대지 않는다** |

**대기는 이미 이 ADR이 원하는 상태다.** 날짜가 없으니 Today에 뜨지 않고 오늘의 부담이 아니다 —
*"적어두고 자자"*의 정확한 형태이므로 **막으면 오히려 대기의 의미가 깨진다.**

**피커를 여는 자리는 여럿이지만 확정하는 자리는 하나다.** 게이트는 여는 쪽이 아니라
**고른 날이 실제로 붙는 쪽**에 건다 — 여는 자리를 세면 하나를 빠뜨렸을 때 조용히 새는 구멍이 남는다.

즉 게이트는 **날짜가 정해지는 자리**에 걸린다. 작업 생성 API를 감싸는 방식으로는 구현되지 않는다.

**미루기도 걸린다 — 그것이 새 작업이 아니라서 오히려 더 정확히 걸린다.**

미루기는 부담을 늘리지 않는다. 그러나 이 ADR이 다루는 해악은 부담의 **총량**이 아니라
**그것이 놓인 날**이고(§근거의 마지막 줄), 미루기는 **오직 그것만 하는 연산**이다.

새벽 세 시에 오늘은 **두 시간 남았다** — 하루 경계가 05:00이다.
오늘에 붙는 것은 종류를 가리지 않고 두 시간짜리 마감이 되고, 밀린 일을 당겨오면
**두 시간 뒤 다시 못 한 일이 된다.** 밀린 일을 하나 더 밀린 일로 만드는 것이
미루기의 목적일 리 없다. 그리고 새벽에 지난 일을 오늘로 당기는 것은 대개 계획이 아니라
**죄책감이 하는 일**이고, Level 4는 그 판단이 가장 흐린 시각이다.

**② 판정은 기기가 한다. 서버는 막지 않는다.**

발동한 주체가 기기이고, **"지금 떠 있는가"의 유일한 진실도 기기에 있다.**
서버는 `guard_events`가 올라온 뒤에야 알고, 오프라인이면 늦거나 이미 해제된 것을 모른다.

**서버가 모르는 것을 서버에 강제시키면 잘못 막는다.** 엉뚱하게 막히는 Guard는
막지 않는 Guard보다 나쁘다 — §6.3의 도구 이탈로 직행한다.

**③ "Level 4 중"은 화면의 수명이 아니라 마지막 격상 승인 + 30분이다.**

`GuardAlertActivity`의 살아 있는 참조(`live`)로 판정하면 **차단이 영원히 안 걸린다** —
수락이든 Override든 `finishWith()`가 참조를 지우므로, **정상 경로로 화면을 나오면 이미 `null`**이다.
그리고 앱에 와서 작업을 추가하는 것은 **화면을 나온 뒤**의 일이다.

그래서 **발동 시각을 `SharedPreferences`에 남기고 창으로 판정한다.**
`GuardWatch`의 `K_LAST_FIRE`·`REFIRE_MS`가 이미 그 모양이다.

**30분인 이유**: Level 4는 데드라인 +30분부터 30분 간격으로 뜬다.
창을 30분으로 두면 **다음 발동이 이어받아** 구간이 끊기지 않고, 발동이 멎으면 자연히 닫힌다.

**발동이 아니라 승인을 기준으로 재는 것이 맞다.** 승인은 검증 왕복만큼 발동보다 뒤이고,
그래서 창이 다음 발동을 **몇 초 넘겨서** 닫힌다 — 겹치는 쪽으로 어긋난다.
발동 시각으로 재면 반대로 **다음 발동 직전에 잠깐 열리는 틈**이 생긴다.
그리고 승인이 아예 없으면(오프라인·거부) Level 4가 아니므로 잴 것도 없다.

**④ 프롬프트를 사실로 고친다.** 모델에게 실제 강제력을 말한다.

**⑤ 말없이 옮기지 않는다.** 왜 내일이 됐는지 화면이 말한다.

이유 없이 날짜가 바뀌면 **고장으로 읽힌다.** 도구를 못 믿게 되는 것이 §6.3의 이탈 경로이므로,
막지 않으려고 고른 방식이 신뢰를 깎으면 남는 것이 없다.
형태는 티켓이 정한다 — 여기서 정하는 것은 **조용히 하지 않는다**까지다.

### 근거

- **"차단"이 지키려던 것은 "오늘 더 짊어지지 않는다"이지 "적지 못하게 한다"가 아니다.**
  Level 4는 이미 수면이 위험한 상태이고, 거기서 새 작업을 오늘에 넣는 것은
  **내일의 나에게 짐을 더하는 행동**이다. 날짜만 옮기면 그 해악이 사라진다
- **완전 차단은 스트레스를 만든다.** 급한 것이 생각났는데 적어둘 곳이 없으면
  그것을 붙들고 있느라 더 못 잔다 — 막으려던 것을 악화시킨다
- **마찰 후 통과는 잘못된 선택지를 만든다.** "대가를 치르고 오늘로 넣기"가 가능하면
  그것이 기본 경로가 된다. ADR-026이 Override에 마찰을 둔 것은 **이탈**을 다루기 위해서였고,
  새 작업 추가는 이탈이 아니라 **부담 증가**다 — 다른 종류에 같은 도구를 쓰지 않는다
- **막지 않으므로 ADR-026과 어긋나지 않는다.** 화면은 여전히 사용자의 것이고,
  적는 것도 자유다. 바뀌는 것은 **그것이 어느 날에 놓이는가** 하나다

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 완전 차단(§6.4 문구 그대로) | ADR-026 정면 위반. 적어둘 곳이 없으면 붙들고 있느라 더 못 잔다 |
| 마찰 후 통과(Override 형태) | "대가를 치르고 오늘로"가 기본 경로가 된다. 이탈과 부담 증가는 다른 종류다 |
| 서버가 막는다 | 서버는 발동 여부를 늦게·부정확하게 안다. 잘못 막는 Guard가 더 나쁘다 |
| 미루기는 예외 — 새 작업이 아니니까 | 막는 것이 **부담의 증가**가 아니라 **오늘에 붙는 것**이다. 미루기는 오직 그것만 한다 |
| 지금처럼 아무것도 안 한다 | **프롬프트가 거짓말을 계속한다.** 그 판정이 §6.5의 전례를 오염시킨다 |

### 감수하는 비용

**"차단"이라는 말이 정확하지 않게 된다.** 설계 §6.4의 문구는 v1.0에서 손봐야 한다 —
지금은 이 ADR이 그 위에 놓인다.

**기기가 안 떠 있으면(앱만 켜져 있고 발동 없음) 아무 제약도 없다.** 그것이 맞다 —
Level 4가 아니면 오늘에 넣는 것이 정상이다.

### 재검토 트리거

실사용에서 **"내일로 갔는데 오늘 해야 했다"**가 반복될 때. 그때는 날짜 이동이 아니라
다른 형태(예: 오늘에 넣되 표시를 다르게)를 본다.

---

## ADR-036. 예정 → 대기 간선은 만들지 않는다. 출구는 "멀리 미루기"다

**Status** `Accepted` · **Phase** 1·6 · 2026-08-14

### 맥락

T-35(세 번 밀린 일에 출구를 준다)가 *"대기로 되돌린다"*를 요점으로 삼았는데,
담당이 확인해 보니 **그 간선이 없고, 라우트 하나로 생기지도 않는다.**

```
is_waiting    예정이 하나도 없음              (0008_cancel_task.sql:37)
defer_count   예정 개수 − 1                   (같은 뷰)
```

**둘이 같은 것을 센다.** 세 번 미룬 일은 예정이 넷이므로, 대기로 만들려면 넷을 지워야 하고
지우면 `defer_count`가 0이 된다 — **팝업을 띄운 근거가 증발한다.**
그 항목들은 Missed 기록 그 자체이고(§1.2), 게다가 `trg_entries_frozen_del`이
마감된 날의 삭제를 막는다. **세 번 미뤘다는 것은 그 날들이 마감됐다는 뜻**이라,
지워야 할 것이 정확히 얼어 있다.

`scheduleTask`가 그 비대칭을 글자로 적어 뒀다 — *"이미 예정이 있는 task예요 — 이동은 미루기로"*.

### 결정

**간선을 만들지 않는다.** 세 번 밀린 일의 출구는 **`defer`의 2주 상한을 그 경우에만 푸는 것**이다.

```
pickMinMax 의 defer 상한   D+14      ← 지금
defer_count ≥ N 인 항목    상한 없음  ← 이 결정
```

**서버·스키마·트리거 무변경.** `defer`는 이미 있는 경로이고, 바뀌는 것은
**프런트가 고를 수 있는 날의 범위** 하나다.

### 근거

**대기와 "세 번 진 일"은 다른 상태다.** 대기는 아직 시작하지 않은 것이고 부담이 0이다.
세 번 미룬 일은 **세 번 부딪혔고 세 번 졌다** — 이력이 있고 부담이 있다.
둘을 같은 통에 넣으려 하자 **스키마가 세 겹으로 저항했다**(정의 충돌 · 트리거 · 21일 시계).
**그 저항이 신호다** — 애초에 같은 상태가 아니다.

**2주 상한의 취지를 깨뜨리지 않는다.** 그 상한은 *"미루기가 무기한 연기가 되지 않게"*인데,
`defer_count ≥ N`은 **그 취지가 이미 실패했다는 증거**다. 상한이 막고 있는 것은
무기한 연기가 아니라 **"지금 할 일이 아니다"라고 인정하는 일**이다.

**신호를 지우지 않는다.** `defer_count`가 하나 더 오르지만 그것이 사실이다 —
이 일은 네 번 미뤄졌다. 9~11월이 읽을 것을 지우면서 출구를 주지 않는다.

**9월 1일 전에 원장 구조를 흔들지 않는다.** 전례 기간이 코앞이고,
지금 있는 데이터는 *"세 번 미룬 일이 있다"*뿐이다. **구조를 바꿀 근거가 아직 없다.**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| `is_waiting`을 "열린 예정이 없다"로 | **Missed를 조용히 삼킨다.** 어제 못 한 일이 오늘 대기가 된다. 파생의 뜻이 소급해서 바뀌고 `waitingList`·21일 시계·analysis가 전부 그 뷰를 읽는다 |
| 대기를 셋째 상태로(칼럼 신설) | 원장은 살지만 **21일 시계가 두 뜻을 갖는다** — 앵커를 두면 "45일째", 옮기면 없던 연장 이력. **실사용이 요구할 때 한다** |
| 출구를 안 준다 (알림만) | 사용자가 이미 안다. 잔소리는 §6.3의 이탈로 간다 |

### 감수하는 비용

**팝업이 다시 뜬다.** 멀리 미뤄도 `defer_count`는 계속 오르므로 그 날이 오면 또 묻는다.
**그것이 맞다** — 여전히 안 하고 있으면 여전히 물어야 한다.

**"멀리"가 얼마인지는 사용자가 정한다.** 상한만 풀고 날짜는 피커에서 고른다.

### 재검토 트리거

**멀리 미루기를 써도 계속 밀리는 항목이 쌓일 때.** 그때는 `defer_count`와 그 간격이
데이터로 남아 있으므로 **셋째 상태(위 표 2행)를 근거를 가지고 결정할 수 있다.**
지금 없는 것은 그 근거다.

---

<a id="adr-037"></a>
## ADR-037. 학교 일정은 Moodle 캘린더를 토큰으로 읽는다

**Status** `Accepted` · **Supersedes** [ADR-033](#adr-033) · **Phase** 8 · 2026-08-17

### 맥락

ADR-033은 **공개 게시판만** 보기로 하고 로그인 벽 뒤를 v2로 미뤘다. 그 재검토 트리거가
발동했다 — *"공개 게시판만으로 커버리지가 부족하다고 확인될 때"*. **uclass의 과목별
공지·과제는 전부 로그인 뒤에 있고, 공개 게시판에는 아무것도 없다.**

실측이 전제 셋을 바꿨다.

| ADR-033의 전제 | 실측 |
|---|---|
| 로그인 벽은 세션 유지·2FA·구조 변경에 취약하다 | **로그인이 필요 없는 토큰 URL이 이미 있다** — `/calendar/export.php`의 `달력 URL 가져옴` |
| 사이트별 HTML 파서는 소모품이다 | **파서가 없다.** iCal은 표준 포맷 |
| 원천은 게시판 글이다 | **원천은 캘린더다.** 과제·실험보고서·발표 마감이 전 과목 캘린더에 이미 들어 있다 |

### 결정

**기존 cron이 사용자별 Moodle iCal 토큰 URL을 fetch한다. `UID`로 신규·변경·삭제를 가르고
제안 카드로 올린다(ADR-030 경로 합류).**

```
cron → GET {iCal 토큰 URL}
     → VEVENT 파싱
     → UID 기준 diff (신규 / LAST-MODIFIED 변경 / 사라짐)
     → 제안 카드 1탭 → 일정화
```

**비밀번호를 저장하지 않는다.** 사용자가 URL을 한 번 발급해 시크릿으로 넣는다.
**v1은 마감만 덮는다** — 공지사항 글(휴강·보강·시험 안내)은 캘린더에 들어오지 않는다.

### 근거

**① 실측이 원천을 바꿨다.** 2026년 4월 캘린더에 6과목이 전부 들어 있었다:

```
5주차 과제 (~4/7 23:00) 기한    실험2 결과보고서 기한    과제1) 자기 성장담 쓰기 기한
2차 실험 조별 레포트 기한        양자역학 과제 2 4월 18일 23:00 기한    중간 발표 자료 제출 기한
```

**교수님이 공지에 쓰든 주차별로 쓰든 상관없다** — Moodle에 활동으로 만들면 마감이 자동으로
캘린더에 들어간다. ADR-033이 *"교수님마다 달라 파서가 유연해야 한다"*로 떠안을 뻔한 문제가
**원천을 바꾸자 사라졌다.**

**② `UID`가 해시 diff보다 낫다.**

```
UID:10788@uclass.uos.ac.kr        ← 이벤트 id @ 호스트. 마감이 바뀌어도 같다
LAST-MODIFIED:20260817T130943Z    ← 변경 감지
```

해시는 *"무언가 바뀌었다"*까지고 `UID`는 *"어느 것이 바뀌었다"*다. **삭제도 잡힌다** —
해시로는 못 하는 것이고, 취소된 마감을 안 지우면 캘린더가 거짓말을 시작한다.

**③ 노이즈가 원천에서 이미 걸러진다.** 캘린더 **화면**은 하루 11건인데(안전교육 강좌 콘텐츠)
**iCal에는 0건**이다 — 테마가 화면에만 그리는 것이라 `event` 테이블에 없다. 별도 필터를 두지 않는다.

**④ 취소 가능한 열쇠다.** 토큰은 `/user/managetoken.php`에서 무효화된다.
비밀번호와 달리 유출이 계정 장악으로 가지 않는다.

#### 실측 2026-08-17 — 확인된 것과 아닌 것

```
✅ uclass = Moodle 4.5 (2024100713) · UBION coursemos 테마
✅ /calendar/export.php 활성 · 토큰 URL 발급 가능
✅ 내보내기가 실제 이벤트를 담는다        (개인 일정 1건으로 확인)
✅ 창 = -5일 ~ +365일 고정               (26/08/12 - 27/08/17)
✅ VEVENT 필드 = UID·SUMMARY·DESCRIPTION·CLASS·LAST-MODIFIED·DTSTAMP·DTSTART·DTEND
✅ DTSTART는 UTC(Z)
✅ 공지사항 글은 캘린더에 없다
❌ CATEGORIES가 없다 — 강좌 구분이 iCal에 실리는지 미확인
❌ 과제 due 이벤트의 SUMMARY 형식 미확인   (화면은 "… 기한")
❌ DTSTART가 마감 시각인지 미확인

⚠️ **미확인의 자리가 한 칸 앞이었다** (2026-09-01 실측). 위 셋은 *"실린 것을 어떻게 읽나"*를 묻는데,
**아직 안 갈린 것은 "무엇이 실리는가"다.**

```
개강일 09-01 10:00  수집 정상(not_calendar 없음 · last_error 빈 문자열)
last_seen_count 0   K_SEEN 은 parseIcal().length 그대로이고 필터가 없다
collected_items 0행  8/21 이래 한 건도 없다
```

**정상 달력을 받았고 그 안에 VEVENT가 0개다.** 8/17 표본도 VEVENT 하나뿐이었고 그것마저
사용자가 만든 개인 이벤트(`SUMMARY:test`)였다 — **강좌 이벤트는 그때도 없었다.**

**정규 수업(주간 시간표)이 Moodle 캘린더 이벤트가 아닐 가능성이 높다.** Moodle 캘린더에 실리는
것은 과제·퀴즈 마감과 코스 이벤트이고, **개강 첫날이라 마감이 아직 안 걸린 것과 모순되지 않는다.**

⚠️ **가르는 것은 iCal 원문 한 줄이고 사용자만 할 수 있다**(`UCLASS_ICAL_URL`은 시크릿이자 열쇠다):

```powershell
curl -s "<UCLASS_ICAL_URL>" | Select-String "BEGIN:VEVENT|^SUMMARY|^DTSTART"
```

```
VEVENT 0줄                    →  목록이 빈 것이 확정. 다음은 preset_what·preset_time 이 옛 학기 필터인가
줄이 있는데 collected 0행      →  그때 비로소 수집 경로의 결함이다
```
```

⚠️ **미확인 셋은 전부 같은 이유다 — 창 안에 실제 과제 이벤트가 없다.** 1학기 마감은 전부
과거고 2학기는 아직 안 열렸다. **개강 첫 과제가 답한다. 그전에 구현하면 검사가 추측을 검사한다.**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 아이디·비밀번호를 Worker에 저장하고 로그인 | ADR-033의 이유가 그대로 살아 있고, **되지도 않는다** — `/login/token.php`가 `redirecterrordetected`를 준다(인증·정책 플러그인이 리다이렉트한다) |
| Moodle 웹서비스 토큰(`moodle_mobile_app`) | REST는 켜져 있으나(`invalidtoken` 응답) **발급 경로가 위와 같은 이유로 막혔다.** 열리면 이쪽이 낫다 — 공지 글까지 한 문으로 들어온다. 재검토 트리거로 남긴다 |
| 강좌 페이지 HTML 파싱 | 로그인 세션이 필요해 **비밀번호 저장으로 되돌아간다.** 파서도 소모품 |
| 포럼 RSS | 사이트 전역은 ON이나 사용자 키가 0개고 포럼별 노출을 확인하지 못했다. **공지 글 v2의 첫 후보** |
| iCal을 폰 캘린더에 구독시키고 Phase 7 읽기로 우회 | 구독 캘린더는 잡동사니와 섞인다. 그리고 **Phase 7 게이트에 종속된다** — 독립 모듈이라는 Phase 8의 성질을 잃는다 |
| 전 이벤트 AI 분석 | ADR-033과 같다. 비용 — 그리고 마감은 이미 구조화돼 있어 AI가 필요 없다 |

### 감수하는 비용

- **공지사항 글이 안 들어온다.** 휴강·보강·시험 안내는 사람이 읽는다
- **창이 -5일 ~ +365일로 고정이다.** 사이트 설정이라 못 바꾼다 — **첫 수집 이전의 마감은 없는 셈**
- **토큰 유출 시 캘린더 전체가 읽힌다.** 무효화는 되지만 자동 회전은 없다
- **강좌 구분이 안 될 수 있다.** 그러면 제안 카드가 *"어느 과목"*을 못 쓴다

⚠️ **이 iCal을 폰 캘린더에 구독시키면 안 된다.** Phase 7이 폰 캘린더를 읽으므로
**같은 마감이 두 경로로 들어와 제안이 두 번 뜬다.** 원천은 하나여야 한다 —
기각한 대안 5행이 그 경로를 닫은 이유이기도 하고, 사용자가 편의로 구독을 걸면
설계가 모르는 사이에 되살아나는 종류다.

### 재검토 트리거

- **개강 첫 실제 과제 이벤트에서 강좌 구분이 안 되면.** `DESCRIPTION`·`SUMMARY`로 되는지 보고,
  안 되면 강좌 페이지 경로를 다시 판단한다
- **`token.php`의 `redirecterrordetected`가 사라지면** 웹서비스로 갈아탄다 — 공지 글까지 한 문이다
- **공지 글 누락이 실제로 아프면** 포럼 RSS를 v2로 연다

---

<a id="adr-043"></a>
## ADR-043. 두 번째 위젯은 홈에서 찍는다 — 점수는 요약과 짝으로만 온다

**Status** `Accepted` · **Refines** [ADR-041](#adr-041) · **Phase** 5 · 2026-08-26

### 맥락

ADR-041은 **첫 위젯이 눌리는지 재려고** 데이터를 안 그리는 버튼 하나를 지었다.
그 재검토 트리거는 *"'오늘 뭐 있나'가 실제로 아쉬우면 그때 데이터 그리는 위젯을 짓고,
**그때 비로소 토큰 UI·`/api/widget/*`·갱신 스케줄이 필요해진다**"*였다.

**그런데 그 셋 중 둘은 이미 서 있었다.**

```
AppWidgetProvider 등록   ✅ T-46
다크모드 색              ✅ T-46 (values-night — 이 리포 첫 것)
기기 토큰                ✅ GuardSync 가 baseUrl+token 을 SharedPreferences 에 들고 있다
                            웹이 configure 로 넣고, 위젯은 같은 프로세스라 그냥 읽는다
API 규격                 ✅ PUT /api/daily/feelings · PUT /api/daily/score · POST /api/logs
갱신 스케줄              ← 남은 것은 이것 하나다
```

**ADR-010이 "위젯 인프라 다섯"이라 적은 목록이 사실상 소진됐다.** Guard가 서버에 쓰려고
만든 토큰 경로와, 화면이 이미 쓰는 API가 그대로 위젯의 것이다 —
**`/api/widget/*`을 새로 만들 이유가 처음부터 없었다.**

### 결정 ① — 크기가 커질수록 찍을 것이 는다

```
최소   feelings 셋 (energy · stress · focus)
중간   위 + ★ 요약 한 줄 + daily score
최대   위 + 로그 쓰기(딥링크)
```

**눈금은 5칸**(`2·4·6·8·10`). `feelings.value`가 `REAL(1~10)`이라 스키마는 그대로고,
**한 칸이 손가락에 맞는 것이 해상도보다 먼저다.** 10칸으로 늘리는 것은 레이아웃만 바꾸면 된다.

⚠️ **`minSdk = 24`라 `RemoteViews(Map<SizeF,…>)`(API 31+)를 못 쓴다.**
레이아웃 **하나**에 세 블록을 두고 `onAppWidgetOptionsChanged`에서 `setViewVisibility`로 켠다.

### ★ 결정 ② — score 는 요약과 짝으로만 나타난다

**ADR-041이 일일 점수 위젯을 기각한 근거는 하나뿐이었다:**

> 위젯에서 점수만 매기면 T-44·T-45가 마감 화면에 넣은 요약을 안 읽게 된다 —
> ADR-040이 방금 세운 것을 되돌린다

**위젯이 그 요약을 함께 그리면 근거가 사라진다.** 읽고 나서 매기는 순서가 홈에서도 유지된다.
그래서 **요약과 score 는 같은 크기 단계에서 함께 켜지고 함께 꺼진다** — 한쪽만 있는 상태를 만들지 않는다.

⚠️ **feelings 셋에는 요약을 붙이지 않는다.** 그것은 **지금 몸 상태**이지 하루의 결산이 아니다.
읽고 매기는 것이 아니라 **그 순간을 그대로 옮기는 것**이라, 요약이 앞에 있으면 오히려 오염된다
(T-44가 평가어를 금지한 것과 같은 방향 — 매기기 전에 판단을 심지 않는다).

### ★ 결정 ③ — 찍힌 것처럼 보이고 안 찍히면 안 된다

**위젯은 토스트를 못 띄운다.** 탭한 칸을 낙관적으로 칠했는데 서버가 거부하면
**사용자는 찍은 줄 알고 넘어간다.** Guard가 T-38·T-39·T-40에서 세 번 겪은 모양이다.

```
성공   칠한 것이 유지된다
실패   칠한 것이 되돌아오고 · 그 사실이 위젯에 남는다
```

거부는 상상이 아니다 — **마감된 날에는 `feelings_frozen_ins`가 추가를 막아 409가 온다.**
그 하루의 마지막 탭이 조용히 사라지는 것이 이 위젯의 가장 흔한 실패다.

### 근거

- **홈에서 찍는 것과 앱을 여는 것은 다른 물건이다.** ADR-010이 위젯의 목적을
  *"빈번한 기록"*이라 적었을 때 가리킨 것이 앞쪽이다. T-46은 그 목적의 **대리 지표**를 쟀지
  목적 자체를 이룬 것이 아니다
- **남은 비용이 하나다.** 인프라 다섯 중 넷이 이미 서 있으므로, **미룰 이유가 비용에는 없다**
- **되돌리기가 싸다.** 요약을 함께 그리는 쪽으로 지으면, 3일 실사용이 어느 답을 주든
  손해가 없다 — **빼는 것은 `setViewVisibility` 한 줄이고, 나중에 넣는 것은 API 변경이다**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 눈금만 그리고 요약은 안 그린다 | **ADR-040을 되돌린다.** 빈 칸에 점수를 매기는 그 모양이다 |
| 3일 실사용을 기다렸다 짓는다 | 기다려서 갈리는 것이 **요약을 그리느냐 하나뿐**인데, 그리는 쪽이 되돌리기가 싸다 |
| `/api/widget/*` 를 새로 만든다 | 화면이 쓰는 API가 그대로 맞다. **두 벌이 되면 갈라진다** |
| 위젯 안에서 로그를 타이핑 | **`RemoteViews`에 `EditText`가 없다.** Android가 안 준다 — 딥링크뿐이다 |
| 한 지표씩 순환(작은 위젯 유지) | 네 번 왕복해야 하고, 위젯이 *"지금 어느 지표인지"* 상태를 든다. **홈에서 바로 찍는다는 목적이 흐려진다** |
| 10칸 유지(4×4) | 한 칸 약 28dp — 오조작이 늘고 **되돌리는 경로가 반드시 필요해진다** |

### 감수하는 비용

- **해상도가 절반이다**(5칸). 하루 한 번 찍는 값에 10단계가 필요한지는 아직 모른다 —
  아쉬우면 레이아웃만 바꾼다
- **홈 한 칸이 아니라 여러 칸을 쓴다.** 최대 크기는 로그 버튼까지 들어간다
- **위젯과 앱이 같은 값을 두 곳에서 그린다.** 한쪽에서 찍으면 다른 쪽이 따라와야 한다 —
  ★ 결정 ③과 같은 자리이고, 여기가 이 위젯의 가장 비싼 부분이다
- **위젯은 읽지 않고 받는다.** 문장 공장을 한 곳(`closeSummaryText`)에 두려면 웹이 만든 것을
  네이티브로 건네는 수밖에 없고, 그러면 **위젯이 아는 것은 앱이 마지막으로 알려 준 것**이다.
  대가는 둘 — **앱을 한 번도 안 연 기기에서는 눈금이 비고**, 다른 기기에서 찍은 값을 모른다.
  ⚠️ **소실은 아니다**(서버가 진실이고 `PUT`이 멱등이다). 낡아 보일 뿐이다
- **하루 경계(06:00)에 위젯이 스스로 비워져야 한다.** 남은 인프라 하나가 이것이다

### 재검토 트리거

- **위젯으로 찍은 뒤 앱에서 마감할 때 요약을 안 읽게 되면** — 결정 ②가 실패한 것이다.
  그때는 score 를 위젯에서 빼고 feelings 만 남긴다(`setViewVisibility` 한 줄)
- **오조작이 잦으면** — 5칸이어도 잘못 누른다는 뜻이고, **되돌리는 경로**가 다음 티켓이다
- **한 주 써서 위젯으로 찍는 일이 거의 없으면** — 접근성이 원인이 아니었다는 결론이
  ADR-041보다 강한 근거로 확정된다. 그때는 ADR-031(알림 액션)이 다음이고,
  **위젯 계열 전체를 접는 것**도 후보가 된다
- **10단계가 실제로 아쉬우면** 레이아웃만 바꾼다 — 스키마·API 무관
- **앱을 며칠 안 열어 위젯이 낡은 값을 보이면** — 그때 비로소 위젯이 서버를 **읽을** 이유가 생긴다.
  지금은 없다: 이 위젯은 찍는 물건이고, 눈금은 방금 찍은 것을 되비추는 보조다

---

<a id="adr-042"></a>
## ADR-042. 미루기는 마지막 출구가 아니다 — 강요된 미루기는 신호를 오염시킨다

**Status** `Accepted` · **Phase** 5 · 2026-08-25

### 맥락

**한 사건이 세 걸음으로 일어났다** (2026-08-25 실측, `20260817-001` "test" — 이 앱의 첫 task):

```
①  Works 어느 섹션에도 없다      지난 예정 + 미완 + defer_count 0 은 다섯 섹션 전부의 밖이다
②  Today 의 재배정 대기에서 미루기를 누른다   ← 유일하게 보이는 경로
③  날짜 말고 고를 것이 없다      지우려고 들어갔는데 8/27 로 미루고 나왔다
```

**그리고 `defer_count`가 0에서 1이 됐다.**

Works 안내문은 `미루기는 복사가 아니라 같은 일의 이동이라, 몇 번 미뤘는지가 그대로 신호가 돼요`라고
적혀 있다. **그 신호에 "지우려다 실패한 한 번"이 섞였다.** 12월에 §6.5가 읽을 때
이 1은 *"한 번 미룬 일"*로 읽히고, **그것은 사실이 아니다.**

### 왜 ①이 생기나 — 다섯 섹션이 각각 이 일을 놓친다

```sql
worksScheduled   e.date >= 오늘        지난 예정이라 없다
worksDeferring   defer_count > 0       아직 0이라 없다
waitingList      is_waiting = 1        예정이 있으니 0이라 없다
worksByPeriod    period_id 필요        기간이 없어 없다
worksDone        완료                  미완이라 없다
```

**다섯 조건이 각각 옳은데 합집합에 구멍이 난다.** `reassignQueue`(`latest_date < 오늘`)만
이 집합을 잡고, 그것은 Today에만 있다.

⚠️ **데이터가 사라진 것은 아니다.** Today에는 매일 떴다(`reassignQueue`는 마감 여부를 안 본다).
그러나 Works는 앱 스스로 **"정리하는 곳"**이라 부르는 화면이고,
**정리하러 간 사람이 그 일을 못 찾았다.**

### 결정 ① — Works 의 '이월 중'이 지난 예정도 잡는다

```
defer_count > 0   OR   latest_date < 오늘
```

**"이월 중"을 '몇 번 미뤘나'가 아니라 '넘어가고 있는가'로 읽는다.** 지난 날짜에
걸린 채 미완인 일은 **미룬 적이 없어도 이미 넘어가고 있다** — 이름이 가리키는 것이 그것이다.

### 결정 ② — 날짜 선택 화면에서 그 일로 돌아갈 수 있다

**미루기 모드의 pickbar에 출구를 하나 더 둔다: 그 task 의 상세 시트를 연다.**

```
지금   [ 날짜를 탭해 일정 정하기 — test ]                    [취소]
뒤     [ 날짜를 탭해 일정 정하기 — test · 이 일 보기 ]        [취소]
```

⚠️ **완료·취소·삭제 버튼을 pickbar에 복제하지 않는다.** 이미 상세 시트에 셋 다 있고,
**두 벌이 되면 갈라지고 갈라진 쪽이 틀린다** — 이 리포가 금지어 목록·기준선 숫자에서
같은 이유로 세운 규칙이다. **시트를 여는 한 줄이 전부다.**

⚠️ **시트를 열어도 pick 모드는 살아 있다.** 시트를 닫으면 캘린더로 돌아와 계속 고를 수 있다 —
*"역시 미루자"*가 한 단계도 안 는다.

⚠️ ★ **다만 시트에서 완료·취소·삭제가 성공하면 pick 을 푼다.** 대상이 사라졌는데 pick 이
남으면 **다음 탭이 없는 일을 미룬다.** 조용히 404가 뜨거나, 더 나쁘게는 엉뚱한 일이 옮겨간다.

### 근거

- **미루기 횟수는 이 앱이 판단에 쓰는 재료다.** ADR-036이 `defer_count ≥ CARRY_N`으로
  2주 상한을 푸는 근거로 삼고, 12월에 §6.5가 읽는다. **재료에 강요된 값이 섞이면
  그 위의 판단이 전부 어긋난다** — 파생값을 저장하지 않는 것과 같은 자리다
- **막다른 길은 사용자를 거짓말하게 만든다.** 나가는 문이 하나뿐이면 사람은 그 문으로 나가고,
  기록에는 *"그 문을 골랐다"*가 남는다. **ADR-026이 세운 것은 대가지 강제가 아니다**
- **①과 ②는 같은 사건의 두 얼굴이다.** 하나만 고치면 다른 쪽으로 다시 몰린다:
  ①만 고치면 Works 에서 찾아 시트를 열 수 있지만 Today 경로는 그대로 막다른 길이고,
  ②만 고치면 여전히 **Today 를 거쳐야만** 그 일에 닿는다

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| pickbar 에 완료·취소·삭제 버튼 셋 | **두 벌이 된다.** 시트에 이미 있다 — 여는 한 줄이면 된다 |
| Works 에 섹션을 새로 만든다 | 세그가 여섯이 된다. **'이월 중'의 뜻이 이미 이 집합을 덮는다** |
| `worksScheduled` 에 지난 예정을 넣는다 | *"예정"*이 과거를 뜻하게 된다 — 이름이 거짓이 된다 |
| 아무것도 안 한다 (취소하고 나가서 찾아라) | **이번에 실제로 `defer_count` 가 오염됐다.** 증거가 이미 있다 |
| 오염된 `defer_count` 를 되돌린다 | **기록은 사실의 기록이다.** 지나간 미루기는 지우지 않는다 — 구조를 고쳐 다음을 막는다 |

### 감수하는 비용

- **'이월 중'이 늘어난다.** 지난 예정에 걸린 것이 전부 들어오므로 Today 의 재배정 대기와
  많이 겹친다. **다른 목적의 두 화면이라 겹치는 것은 정상이다** — Today 는 *"오늘 뭘 할까"*,
  Works 는 *"전체를 정리"*다
- **pickbar 가 한 줄 길어진다.** 캘린더 위 좁은 띠라 문구를 짧게 유지해야 한다
- **pick 과 시트가 동시에 떠 있는 상태가 생긴다.** ★ 위의 해제 배선이 그 값을 치르는 자리다

### 재검토 트리거

- **'이월 중'이 너무 길어져 안 읽히면** — 그때는 두 갈래(`미룬 것` · `지난 것`)로 나누는 것이
  다음 후보다. **지금 나누지 않는 이유는 세그가 여섯이 되기 때문이고, 길이가 그보다 아프면 뒤집힌다**
- **시트를 여는 출구를 실제로 안 쓰면** — 문제는 출구가 아니라 **Works 에서 못 찾은 것**이었다는 뜻이고,
  결정 ①만 남기고 ②를 되돌린다
- **`defer_count` 가 또 원치 않게 오르면** — 남은 강요 경로가 있다는 뜻이다. 그 경로를 찾는다

---

<a id="adr-041"></a>
## ADR-041. 첫 위젯은 데이터를 그리지 않는다 — 버튼 하나로 시작한다

**Status** `Accepted` · **Refines** [ADR-010](#adr-010) · **Phase** 5 · 2026-08-24

### 맥락

ADR-010이 `Calendar → Tasks → Daily`를 정한 근거는 **가치가 아니라 비용**이었다:

> 첫 위젯이 일회성 인프라 비용을 전부 떠안는다 — AppWidgetProvider 등록 ·
> 기기 토큰 발급 UI · `/api/widget/*` 규격 · 다크모드 색 · 갱신 스케줄.
> **이 비용은 어느 위젯이 먼저 오든 동일하다**

그리고 목록에 `⚠️ 전제 변동`이 붙어 있다 — 위젯의 목적이 *"빈번한 기록"*으로 확정되면서
복잡도 순서와 목적 순서가 갈렸고, 9월에 판단하기로 했다.

**그런데 그 비용 목록을 다시 보면 다섯 중 셋이 "데이터를 그릴 때만" 든다.**

```
AppWidgetProvider 등록   항상 필요
다크모드 색              아이콘 하나면 거의 없다
기기 토큰 발급 UI        ← 데이터를 그릴 때만
/api/widget/* 규격       ← 데이터를 그릴 때만
갱신 스케줄              ← 데이터를 그릴 때만
```

**ADR-010의 전제("비용이 동일하다")는 위젯이 데이터를 그린다는 가정 위에 있었다.**

### 결정

**첫 위젯은 버튼 하나다. 데이터를 그리지 않는다.**

```
1×1 위젯 "+"  →  탭  →  앱의 할 일 추가 화면으로 딥링크
```

**대상은 Tasks다** — ADR-010의 복잡도 순서가 아니라 **목적 순서**를 따른다.
그 근거였던 비용 동등성이 위 표에서 깨졌으므로, 순서를 비용으로 정할 이유가 없어졌다.

⚠️ **일일 점수 위젯은 만들지 않는다.** 위젯에서 점수만 매기면
**T-44·T-45가 마감 화면에 넣은 요약을 안 읽게 된다** — ADR-040이 방금 세운 것을 되돌린다.
마감은 하루 한 번이고, 그 한 번은 앱을 열 값이 있다.

### 근거

- **인프라를 진짜 필요할 때 짓는다.** 토큰 UI·`/api/widget/*`·갱신 스케줄은
  **데이터를 그리기로 정한 뒤에** 만든다. 지금 지으면 쓰지도 않는 규격이 굳는다
- **할 일은 떠오를 때마다 생긴다.** 마감(하루 1회)과 달리 **빈도가 마찰에 곱해진다** —
  ADR-010이 *"빈번한 기록"*이라 적은 그 자리다
- **실사용이 다음을 정한다.** *"위젯이 실제로 눌리는가"*를 이 최소형이 답한다.
  안 눌리면 데이터 그리는 위젯도 안 눌린다 — **비싼 것을 짓기 전에 싼 것으로 잰다**
- Android 위젯은 **텍스트 입력을 못 한다**(RemoteViews에 `EditText`가 없다).
  어차피 입력은 앱 화면에서 하므로, **위젯이 데이터를 들 이유가 처음부터 약했다**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 일일 점수 위젯 | **ADR-040을 되돌린다.** 요약을 안 읽고 점수만 매기게 된다 |
| Calendar 위젯 먼저 (ADR-010 그대로) | 비용 동등성이 깨졌다. 그러면 목적으로 고르는 것이 맞다 |
| App Shortcut 으로 대신 | 길게 눌러야 나온다. **홈에 늘 보이는 것**이 이 결정의 값이다 |
| 처음부터 데이터를 그린다 | 인프라 넷이 한꺼번에 든다. **눌리는지도 모르는 채로** |

### 감수하는 비용

- **위젯이 아무 정보도 안 준다.** 홈에서 "오늘 뭐 있나"는 여전히 앱을 열어야 안다 —
  그것은 **다음 위젯이 답할 질문**이고, 이 위젯이 눌린 뒤에 정한다
- **APK가 필요하다.** 다만 무선 adb가 서 있어 유선 세션은 안 든다(ADR-039 §실측)
- 홈 화면 한 칸을 쓴다

### 재검토 트리거

- **한 주 써서 위젯을 거의 안 누르면** — 접근성이 원인이 아니었다는 뜻이고,
  데이터 그리는 위젯을 지어도 같다. 그때는 ADR-031(알림 액션)이 다음 후보다
- **누르는데 앱 화면이 느려서 막히면** — 딥링크 도착 지점을 가볍게 만드는 것이 먼저다
- **"오늘 뭐 있나"가 실제로 아쉬우면** 그때 데이터 그리는 위젯을 짓고,
  그때 비로소 토큰 UI·`/api/widget/*`·갱신 스케줄이 필요해진다

---

<a id="adr-040"></a>
## ADR-040. 마감은 빈 칸을 채우는 일이 아니라 확인하는 일이다

**Status** `Accepted` · **Refines** [ADR-028](#adr-028) · **Phase** 7 · 2026-08-22

### 맥락

**일일 기록이 계속 밀린다.** 2026-08-21 실측: `daily` 없음 · `logs 0` · `feelings []`.

**그런데 마찰은 이미 낮다.**

```ts
closeDay(env, t, kind: "manual" | "brief" | "auto", date?)
// score 도 feelings 도 요구하지 않는다. 버튼 하나면 닫힌다.
```

**그러므로 원인이 입력 비용이 아니다.** ADR-028이 처방한 방향(입력을 싸게)이
이 자리에는 이미 적용돼 있는데도 안 된다.

**진짜 모양은 이것이다:**

```
비용은 지금     자기 직전에 앱을 연다
값은 12월       §6.5 가 그때 읽는다
그 사이         아무것도 안 돌아온다
게다가          autoClose 가 대신 닫는다 — 미룬 대가가 지금 보이지 않는다
```

**시간 할인이다. 의지가 아니라 구조다.**

⚠️ **그리고 시각이 최악이다.** 하루 경계가 설정값(현재 06:00)이라 실제 마감 시점은
**자기 직전**이고, **가장 성찰하기 싫은 시각에 가장 성찰적인 일을 요구한다.**

### 결정

**마감 화면이 먼저 말한다. 사람은 그것을 확인하거나 고친다.**

```
지금     "오늘에 점수를 매긴다면"   →  빈 칸을 채워라
바꾸면   시스템이 그날을 한 줄로 말한다  →  이게 맞나 · 몇 점인가
```

**시스템이 판단하지 않는다.** 센 것만 말한다 — 한 것 · 남은 것 · 이월. **평가어를 쓰지 않는다**
(*"잘했어요"*·*"아쉬워요"*). 그 순간 점수가 오염되고, 이 앱은 거울이지 심판이 아니다.

### 근거

**① 원칙이 이미 있고 이 자리에만 적용이 안 됐다.** ADR-028의 *"입력의 무게중심을 시스템으로"*는
새 입력원(캘린더·감각기관)에만 적용됐다. **마감은 여전히 사람이 백지를 채운다.**

**② 새 데이터가 필요 없다.** `buildMech`가 이미 `done`·`missed`·`deferred`를 만든다 —
**마감 후에.** 같은 것을 **전에** 보여주는 것뿐이고, 화면에는 `S.today`로 이미 와 있다.

**③ 값이 지금 돌아온다.** 마감 버튼의 대가가 *"닫혔다"*에서 *"오늘이 어땠는지 알게 됐다"*로 바뀐다.
그것이 ③칸(넣어도 값이 안 온다)의 첫 조각이다.

**④ 점수 매기기 자체가 쉬워진다.** 빈 칸에 숫자를 넣는 것과 *"3개 했고 2개 남았다"*를 보고
매기는 것은 **다른 일**이다. 후자는 회상이 필요 없다.

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| AI가 그날을 요약한다 | 비용·지연. 재료가 이미 결정론으로 있다. **매일 도는 것에 AI를 넣지 않는다**(ADR-024와 같은 방향) |
| 자동 마감을 없앤다 | 밀린 날이 영영 안 닫힌다. `autoClose`는 원장의 불변 조건(*"과거 예정일에는 항상 closed daily가 존재한다"*)을 지킨다 |
| 마감을 강제한다 (안 하면 화면을 막는다) | **마찰은 Guard의 도구다**(ADR-026). 기록에 쓰면 앱을 안 열게 된다 |
| 알림으로 조른다 | 잔소리다. §6.3의 이탈 압력을 올린다. RemoteInput(ADR-031)은 **마찰을 낮추는 것**이지 조르는 것이 아니다 |
| 요약을 마감 후에 보여준다 | 지금이 그것이다. **결정을 돕지 못하고 통보만 된다** |

### 감수하는 비용

- **마감 시트가 무거워진다.** 읽을 것이 늘면 열기 싫어질 수 있다 — **세 줄을 넘지 않는다**
- ⚠️ **앵커링.** 시스템이 먼저 말하면 점수가 그 말에 물든다. 감수한다 —
  **안 하는 것보다 낫고**, 지금은 점수 자체가 없다
- **결정론 요약이라 틀린 날이 있다.** 사람이 고칠 수 있어야 한다

### 재검토 트리거

- **이래도 마감이 안 되면 원인이 ③이 아니다** — 그때는 ADR-031의 RemoteInput 마감(9월)이 답이고,
  그것도 안 되면 마감이라는 형식 자체를 다시 본다
- **앵커링이 실제로 보이면**(점수가 요약과 지나치게 붙으면) 요약을 점수 **뒤로** 옮긴다 —
  순서만 바꾸면 되는 자리로 만들어 둔다

---

<a id="adr-039"></a>
## ADR-039. 밤 실측은 두 종류다 — 실사용 조건과 알람 계층을 섞지 않는다

**Status** `Accepted` · **Phase** 6 · 2026-08-21

### 맥락

**밤을 세 번 쟀는데 세 번 다 다른 것을 쟀다.**

| 밤 | 조건 | 실제로 잰 것 |
|---|---|---|
| 8/11 | 충전 중 | Doze 아님 — 아무것도 |
| 8/18 | 보호 일정 + USB(01:50까지) | 얕은 Doze에서 5/5 발동 |
| 8/21 | 단일 알람 04:00 · 무선 · 미충전 | **깊은 Doze 2시간 35분 뒤 정시 발동** |

8/18을 *"USB 때문에 얕았다"*로 읽었는데 **그게 원인의 전부가 아니었다.**

```
min_time_to_alarm = 1h        (기기 실측)
  → 다음 wake-from-idle 알람이 1시간 안에 있으면 deep IDLE 진입 자체가 거부된다

schedule() 의 발동 = L2(−2h) · L2(−1h) · L3 · L4×6
  → 데드라인 1시간 전부터 마지막까지 전 구간이 60분 이하 간격
```

**보호 일정이 걸린 밤은 충전을 안 해도 깊은 Doze에 못 들어간다.** 설계상 그렇다.

### 결정

**두 질문을 다른 밤으로 나눠 잰다. 한 밤에 섞지 않는다.**

```
① 실사용 조건    보호 일정 그대로.  얕은 Doze 에서 발동·판정·기록이 완주하는가
② 알람 계층      앞뒤 1시간이 빈 단일 알람(scheduleAt).  깊은 Doze 를 통과하는가
```

**①이 실사용이고 ②는 상한이다.** ②가 실패해도 ①이 통과하면 Guard는 동작하고,
②가 통과해도 ①을 대신하지 못한다.

### 근거

**① 섞으면 과녁이 흐려진다.** 8/18은 보호 경로를 재려다 Doze 질문을 함께 물었고,
그래서 *"얕은 Doze였다"*의 원인을 충전 하나로 몰았다. 원인이 둘이면 하나를 고쳐도 안 바뀐다.

**② 실사용에서 만나는 것은 얕은 Doze다.** 보호 일정이 걸린 밤은 구조적으로 그렇고,
바꿀 수 있는 것이 아니다. ⚠️ **이것은 나쁜 소식이 아니다** — 깊이 안 들어가니
알람이 미뤄질 위험도 그만큼 작다.

**③ 깊은 Doze는 보호 일정이 **없는** 밤에 감지 경로(B)가 만난다.** 9~11월 실사용의
대부분은 일정을 안 걸어둔 밤이고, `watch:bedtime`은 거기서 뜬다. ②가 그 상한을 준다.

#### ★ "Doze를 뚫는다"가 아니라 "미리 나온다"

8/21 실측이 그 경계를 보여 줬다:

```
02:24  deep-idle 복귀 — 1시간 35분 연속
03:58  normal (alarm)        ← AlarmManager 가 알람 약 2분 전에 idle 에서 꺼낸다
04:00:00  발동 · shown · posted · 지각 0
04:43  다시 deep-idle
```

**발동 시점에는 이미 idle이 아니다.** 그래서 믿을 만한 것이고, 동시에
*"Doze 안에서 우리 코드가 돈다"*는 뜻이 **아니다.** 12월에 이 기록을 읽는 쪽이
둘을 헷갈리면 결론이 통째로 틀어진다.

⚠️ **그리고 이것이 8/18의 `timeout` 셋에 대한 가설을 준다.** idle에서 나온 직후에는
**모뎀이 아직 깨는 중**일 수 있고, 그때 6초는 짧다. T-37이 read를 12초로 늘린 것이
순서 문제뿐 아니라 이쪽도 함께 고쳤을 수 있다 — **다음 보호 밤에 `timeout`이
사라지는지가 그 검사다.**

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 한 밤에 둘 다 재기 | 8/18이 그렇게 해서 원인을 하나로 몰았다. **과녁이 흐려진다** |
| 보호 일정의 발동 간격을 벌려 깊은 Doze를 만들기 | **실사용을 실측에 맞추는 것이다.** 30분 간격은 ADR-026의 마찰 설계이지 실측 편의가 정할 것이 아니다 |
| `dumpsys deviceidle force-idle`로 대체 | 흉내다. 제조사 최적화가 그 위에 한 겹 더 있다 |
| ②를 실사용 경로(`schedule()`)로 재기 | **그 상황이 발생하지 않는다.** 보호 일정이 걸리면 깊은 Doze가 아니므로 물을 것이 없다 |

### 감수하는 비용

- **②는 테스트 경로(`scheduleAt`)로만 잴 수 있다.** 알람 계층(`setAlarmClock`·`RTC_WAKEUP`·
  `window=0`)이 같으므로 답은 이전되지만, **실사용 코드 경로 그대로는 아니다**
- **밤 하나에 질문 하나**라 검증이 느리다. 두 종류를 각각 다른 밤에 재야 한다
- ②는 사람을 새벽에 깨운다 — 그래서 자주 못 한다

### 재검토 트리거

- **다음 보호 밤(①)에서 `timeout`이 또 나오면.** 위 모뎀 가설이 틀린 것이고,
  그때는 `unavailable`의 이유 분포가 다음 판단의 재료다
- **`min_time_to_alarm`이 다른 기기에서 다르면.** 이 결정은 이 기기의 값에 기대고 있다
- 감지 경로(B)가 깊은 Doze에서 실제로 실패하면 — 그때는 폴링 주기가 설계 대상이 된다

---

<a id="adr-038"></a>
## ADR-038. 중첩 타임아웃은 바깥이 안보다 길다

**Status** `Accepted` · **Refines** [ADR-024](#adr-024) · **Phase** 6 · 2026-08-19

### 맥락

두 값이 서로를 모른 채 겹쳐 있다.

```
기기  GuardVerify.TIMEOUT_MS = 6_000    connectTimeout · readTimeout
서버  AI_TIMEOUT_MS          = 8_000    model_high race
```

**바깥(기기)이 안(서버)보다 먼저 포기한다.** 서버가 7초에 판정을 만들어도 기기는 6초에 끊었다.

**그래서 `server_timeout`은 한 번도 관측될 수 없다.** 서버가 그 신호를 주려면 8초 뒤에
답해야 하는데 기기는 6초에 끊기 때문이다. T-31이 `timeout`(기기)과 `server_timeout`(서버)을
*"Doze가 끊었나 / 모델이 늦었나"*로 갈라 뒀는데 **뒤쪽이 구조적으로 나올 수 없는 값이었다.**

⚠️ **이 ADR은 격상이 안 되는 원인을 설명하지 않는다.** 처음엔 그렇게 읽었으나 데이터가
반박했다 — 전수를 세어 보니 **왕복이 6초 안에 끝난 적이 열한 번 있다**(8/08·8/11의 `deny`,
그중 둘은 `ai_used=1`로 기록됐다). 순서가 뒤집힌 것은 **잠재 결함이지 두 밤의 원인이 아니다.**

**진짜로 열려 있는 것은 따로 있다 — 판정 22건 중 `approve`가 0이다.**

```
deny                 11    ← 모델이 닿았고, 격상을 거부했다
unavailable/timeout   3    ← 8/18 밤. 이쪽이 네트워크다
unavailable/(이유없음) 6    ← 8/11 밤. 0016 이전이라 이유가 안 남았다
unavailable/cap       2    ← secretary 의 ai_daily_cap=0
approve               0
```

**`deny` 열한 번은 네트워크 문제가 아니다.** 그 사유가 어디에도 남지 않아
*"모델이 왜 거부했는가"*를 지금 알 수 없다 — **관측이 없는 쪽이 하나 더 있다.**
이 ADR의 범위 밖이고, 별건으로 연다.

**관측이 한쪽으로만 열려 있으면 진단이 그쪽으로 쏠린다** — 이 ADR을 처음 쓸 때
실제로 그렇게 쏠렸다. 그것이 순서를 바로잡아야 하는 이유이기도 하다.

### 결정

**중첩된 타임아웃은 바깥이 안보다 길다. 안쪽이 자기 실패를 말할 시간을 남긴다.**

```
기기 readTimeout  >  서버 AI_TIMEOUT_MS + 여유
```

두 값 중 **어느 쪽을 옮길지는 티켓이 정한다** — 이 ADR이 정하는 것은 *순서*다.
숫자를 여기 적으면 코드와 두 벌이 된다(§기준선 보고 규칙과 같은 이유).

**그리고 안쪽 값을 바꿀 때 바깥을 함께 보는 것을 규칙으로 둔다.** 지금은 두 상수가
서로 다른 언어·다른 저장소에 있어 **한쪽만 고쳐도 아무 검사가 빨간불이 되지 않는다.**

### 근거

- **`server_timeout`이 관측 가능해지는 것 자체가 값이다.** 그게 나와야 *"모델이 느리다"*와
  *"기기가 못 닿았다"*가 갈리고, 12월에 §6.5가 읽을 때 대응이 달라진다.
  **이 ADR이 사는 이유는 이것 하나다** — 성능이 아니라 관측이다
- **경계 사례에서 지출이 새는 것도 막는다.** 서버가 6~8초에 판정을 만들면 **모델은 불렀는데**
  기기가 못 받아 `ai_used=0`으로 기록된다. 상한 전체가 뚫린 것은 아니지만
  (`ai_used=1`은 실제로 기록된다) **그 구간의 호출만 안 세어진다**
- **fail-closed는 그대로다.** 이 결정은 *승인 기준*을 건드리지 않는다. 판정을 못 받으면
  여전히 Level 3이고, 바뀌는 것은 **판정을 받을 기회가 실제로 있는가**뿐이다

### 기각한 대안

| 대안 | 기각 이유 |
|---|---|
| 밤에는 AI 없이 결정론으로 격상 | ADR-024의 fail-closed를 뒤집는다. **원인이 진단 오류였는데 원칙을 바꾸는 것은 순서가 틀렸다** — 고치고 나서도 안 되면 그때 판단한다 |
| 서버 타임아웃만 줄인다(8→4초) | 순서는 맞지만 **모델에게 주는 시간이 준다.** 격상이 더 어려워질 수 있다 — 어느 쪽을 옮길지는 실측이 정한다 |
| 예약 시각에 미리 판정해 캐시 | 네트워크가 있는 시각에 판정한다는 발상은 옳지만, **판정 재료가 발동 시점의 것이 아니게 된다.** 원인이 타임아웃 순서로 밝혀진 지금 필요하지 않다 |
| 재시도 | 새벽에 두 배 깨운다. 마찰은 Guard의 도구이지 버그 회피 수단이 아니다 |

### 감수하는 비용

**기기가 더 오래 기다린다.** 개입 화면은 이미 떠 있고 검증은 백그라운드 스레드라
사용자가 기다리는 시간은 아니다 — 늘어나는 것은 격상이 화면에 반영되기까지의 지연이다.

⚠️ **ADR-035 ③의 "마지막 격상 승인 + 30분"이 그만큼 뒤로 밀린다.** 승인이 늦어지면
Level 4 구간의 시작도 늦다. 겹치는 쪽으로 어긋나므로 안전한 방향이지만, **두 값이 연동된다는
사실은 남는다.**

### 재검토 트리거

**순서를 바로잡은 뒤에도 격상이 안 되면.** 그때 나오는 `server_timeout`·`network`의 분포가
다음 판단의 재료다 — *"모델이 느리다"*면 model_high 교체, *"기기가 못 닿는다"*면
그때야말로 Doze·절전이 원인이고 위 표 3행(사전 판정 캐시)을 다시 연다.
