# T-85 — 시간표는 학기마다 쌓인다

**발행** Cowork · 2026-09-24 · **담당** Claude Code · **상태** ✅ 구현·검사 완료 (2026-09-24 · 배포 대기)
**근거** [`docs/pos-obsidian-design.md`](../pos-obsidian-design.md) DEC-27 · DEC-30 · ADR-045 ② · [T-58](T-58-timetable.md)
**서버만 · 마이그레이션 없음 · APK 무관 · 프런트 무변경**

---

## 목표

새 시간표를 저장해도 다른 학기의 규칙이 남게 한다. 저장은 학기 범위가 겹치는 규칙만 바꾸고, `GET /api/timetable`은 대표 학기 하나만 돌려준다. 과목명은 NFC로 저장한다.

**왜** — 지금 저장은 전체 교체다(`stClearTimetable`). 다음 학기 시간표를 넣는 순간 이번 학기 규칙이 지워지고, 수업은 규칙에서 전개되므로(ADR-045 ②) **지난 날짜를 열면 그날의 수업이 사라진다.** Obsidian 연동이 과목 이름의 기준으로 쓰는 정본 과목명의 이력도 함께 사라진다(설계 DEC-27).
**다음 학기 시간표를 넣기 전에 배포돼야 한다.** 그 뒤에는 이번 학기 규칙을 되살릴 수 없다.

## 먼저 확인한다

```
□ list()·replace()를 부르는 곳이 GET·PUT /api/timetable 라우트 둘뿐인가
□ classesIn()이 규칙 전부를 읽고, expand()가 규칙마다 학기 범위로 거르는가
  (그렇다면 쌓인 규칙이 다른 학기의 날짜로 새지 않는다. 전개 쪽은 고칠 것이 없다)
□ 화면(openTimetable)이 GET 응답의 rules를 편집 초안으로 쓰고,
  ttSave가 PUT 응답의 rules.length를 토스트에 쓰는가
□ smoke의 T-58 절 뒤에서 그 학기 시간표를 쓰는 절이 어디인가(아침 재료 등).
  새 검사가 그 학기를 건드리면 안 된다
```

## 할 일

① **저장 — 겹치는 학기만 바꾼다.** `stClearTimetable` 대신, 새 범위와 겹치는 규칙만 지운다.

```
겹침:  term_start <= 새 term_end  AND  term_end >= 새 term_start
```

지우기와 넣기는 지금처럼 한 batch다. 빈 규칙(`rules: []`)으로 저장하면 그 범위와 겹치는 학기만 비워진다.

② **조회 — 대표 학기 하나.** `GET /api/timetable`은 대표 학기의 규칙과 범위만 준다.

```
대표 학기 = 오늘(귀속일 t.d)을 포함하는 학기
          → 없으면 term_start가 가장 늦은 학기
          → 규칙이 없으면 { rules: [], term: null }  (지금과 같다)
```

대표 학기를 고르는 부분은 순수 함수로 뺀다. 검사가 오늘을 바꿔 가며 부른다.
라우트가 `TimeCtx`를 넘기도록 `src/index.ts` 한 줄을 고친다.

③ **저장 응답 — 방금 저장한 학기.** `PUT`은 저장한 범위의 규칙과 범위를 돌려준다. 화면 토스트("N칸")가 이 값을 쓴다.

④ **과목명 NFC.** `validate()`에서 과목명을 `trim()`한 뒤 `normalize("NFC")`한다(설계 DEC-30). 이미 저장된 행은 고치지 않는다.

⑤ `stClearTimetable`은 쓰는 곳이 없어지면 지운다. SQL은 `src/db/index.ts`에만 둔다.

## 범위 — 이 파일들만 고친다

```
src/services/timetable.ts   ①②③④
src/db/index.ts             ①⑤ 겹침 삭제 문장
src/index.ts                ② GET 라우트가 TimeCtx를 넘긴다 (한 줄)
test/smoke.ts               검사
docs/api-surface.md         재생성 (GET·PUT의 뜻이 바뀐다)
```

`public/`은 바꾸지 않는다. 바꿔야 할 것 같으면 멈추고 보고한다.

## 금지

| 하지 말 것 | 왜 |
|---|---|
| 새 마이그레이션 | `timetable_rules`에 학기 범위가 이미 있다. 스키마가 필요해 보이면 멈추고 보고한다 |
| GET이 모든 학기의 규칙을 준다 | 화면이 그 규칙을 편집 초안으로 쓴다. 여러 학기가 섞인 초안을 한 범위로 저장하면 지난 학기 규칙이 이번 학기로 복제된다 |
| 겹치지 않는 학기의 규칙을 지운다 | 그것을 남기는 것이 이 티켓의 목적이다 |
| 과목명으로 학기를 가른다 | 같은 과목을 다른 학기에 다시 들을 수 있다. 학기는 범위로만 가른다 |
| 학기 범위를 코드에 박는다 | T-58 검사 8(날짜 리터럴 스캐너) · `validate()`의 주석 |
| `expand`·`classesIn`의 거르는 규칙을 바꾼다 | 이미 규칙마다 학기 범위로 거른다 |
| 전개한 수업을 저장한다 | ADR-045 ② · 원칙 1 |
| 검사가 T-58 학기를 바꾼다 | 뒤의 절이 그 시간표를 쓴다. 새 검사는 겹치지 않는 학기만 더하고 끝에 치운다 |

## 읽을 것

- ADR-045 ② — 규칙(과목·요일·시각·학기 범위)을 저장하고, 날짜는 조회할 때 전개한다
- 설계 `docs/pos-obsidian-design.md` DEC-27 · DEC-30 · §6.1(시간표 이력, 테스트 "정본 이력")
- `test/smoke.ts` §T-58 — 검사 4가 규칙 행 수 8을 센다. 새 검사는 그 절 뒤에 둔다
- 함정 12 — 검사 fixture에 고정 날짜를 쓰지 않는다. 학기 범위는 오늘(`D`)에서 상대로 잡는다

## 완료 조건

```
typecheck 통과 · smoke 497 → 497+N · front 523 그대로 · 실패 0 · verify exit 0
```

T-58 학기를 A(오늘 포함)라 한다. B는 A가 끝난 뒤의 학기, B′는 B와 겹치지만 범위가 다른 학기다.

```
1  ★ B를 저장해도 A의 규칙이 남는다 (행 수 = A + B)                  ← 본체
2  ★ A 안의 지난 날짜(/api/days/:date)에 A의 수업이 뜬다             ← 1의 짝. 행만 남고 전개되지 않으면 소용없다
3  ★ B 안의 날짜에는 B의 수업만 뜬다 (A가 새지 않는다)
4  ★ GET은 A만 준다 (칸 수 8 · 범위 A)
5  ★ B′를 저장하면 B만 B′로 바뀌고 A는 그대로다 (행 수 = A + B′)
6  ★ 대표 학기 고르기(순수 함수): 오늘을 포함하는 학기 / 없으면 가장 늦게 시작하는 학기 / 규칙이 없으면 null
7  ★ PUT 응답은 저장한 학기다 (B′를 저장하면 B′의 칸 수와 범위)
8  ★ NFD로 들어온 과목명이 NFC로 저장된다
```

검사가 끝나면 B′의 범위로 빈 규칙을 저장해 치우고, 행 수가 A(8)로 돌아왔는지 센다.

**변이**

```
전체 삭제로 되돌린다                      → 1 · 2 · 4 · 5가 죽는다
겹침 조건을 뒤집는다                      → 1 · 2 · 4 · 5가 죽는다
겹침 대신 범위가 같은 학기만 지운다        → 5만 죽는다
GET이 모든 학기를 준다                    → 4만 죽는다
대표 학기를 늘 "가장 늦게 시작"으로 고른다 → 4 · 6이 죽는다
PUT 응답을 대표 학기로 돌려준다           → 7만 죽는다
expand의 학기 거름을 뺀다                 → 3이 죽는다 (T-58 검사 7도)
NFC 정규화를 뺀다                         → 8만 죽는다
```

변이가 붙었는지와 되돌아갔는지를 둘 다 본다(함정 16).

## 확인 절차 (사용자)

```powershell
npm run deploy
```

```
□ 설정 → 시간표를 열면 이번 학기 시간표가 전과 같다 (칸 수 · 학기 범위)
□ 오늘 화면의 수업이 전과 같다
□ 🌙 진짜 판정은 다음 학기 시간표를 넣는 날이다.
     넣은 뒤 이번 학기의 지난 날짜(예: 9월의 수요일)를 캘린더에서 열어 수업이 남아 있는가
```

## 이 티켓이 안 하는 것

- 정본 과목 목록 API(모든 학기). Obsidian 연동 S2에서 연다(설계 §5.5).
- 시간표 화면에서 학기를 골라 보거나 지난 학기를 고치는 UI.
- 이미 저장된 과목명을 NFC로 바꾸는 일.

---

## 보고 (담당이 채운다)

```
티켓: T-85
바꾼 파일: src/services/timetable.ts, src/db/index.ts, src/index.ts, test/smoke.ts, docs/api-surface.md
기준선: typecheck 통과 · smoke 497 → 506 · front 523 그대로 · 실패 0 · verify exit 0
설계와 어긋난 점: 없음
막힌 것: 없음
```

### §먼저 확인한다 — 네 줄 전부 참이었다. 티켓의 전제가 코드와 맞는다

```
□→✅ list()·replace()를 부르는 곳이 GET·PUT /api/timetable 라우트 둘뿐인가
     src/index.ts:199(GET) · 203(PUT) 둘뿐이다. 그 밖에 부르는 곳은 replace() 자신이
     끝에서 list()를 부르던 한 줄이었고, ③이 그것을 없앴다(방금 저장한 학기를 직접 만든다).

□→✅ classesIn()이 규칙 전부를 읽고, expand()가 규칙마다 학기 범위로 거르는가
     db.timetableRules = `SELECT * FROM timetable_rules ORDER BY …` — 학기 필터가 없다.
     expand()가 규칙마다 `date < r.term_start || date > r.term_end` 로 거른다.
     ★ 그래서 전개 쪽은 한 줄도 안 고쳤다. **검사 3이 그 사실을 못 박는다.**

□→✅ 화면이 GET 응답의 rules를 편집 초안으로 쓰고, ttSave가 PUT 응답의 rules.length를 쓰는가
     app.js:1373 `ttDraft = (cur.rules||[]).map(...)` · 1377-1378이 `cur.term`으로 날짜 칸까지 채운다.
     app.js:1403 `toast(... ${(saved.rules||[]).length}칸)`.
     ★ 둘 다 참이라 **public/은 한 줄도 안 고쳤다** — 대표 학기를 주면 화면이 전과 같이 뜬다.

□→✅ smoke의 T-58 절 뒤에서 그 학기 시간표를 쓰는 절이 어디인가
     §T-60(아침 재료)이 A를 재료로 쓴다 — smoke.ts:2938 주석이 그렇게 적고 있다. §T-71도 그렇다.
     ★ 그래서 새 검사는 A와 **겹치지 않는** 학기(B=[D+70,D+160] · B′=[D+75,D+170])만 쓰고
     절 끝에서 치운다. **검사 9가 치워졌는지를 센다** — 안 치워지면 §T-60이 남의 규칙을 쥔다.
```

### 검사 9를 좁혔다 — 남의 명제를 업고 있었다

처음 쓴 9는 `응답칸 === 0`을 함께 셌다. 그것은 **7의 명제**(응답 = 방금 저장한 학기)이고,
업고 있으면 7을 겨냥한 M6이 9까지 죽여 **9가 자기 몫을 못 센다**(`AGENT-CHAIN` §8 — 표본 탓).
빼고 원장만 보게 했더니 **M6이 7만 죽인다.** 못 보게 된 것은 없다 — 7이 이미 계약을 진다.

### 티켓 표에 없는 변이 둘을 더했다 (M9 · M10) — 혼자 죽기 시험

⚠️ **티켓 표대로만 돌리면 검사 2와 9는 *혼자 죽는 변이가 하나도 없다*.**
9는 M1·M2·M3에 얹혀 죽고, 2는 M1·M2·M7에 얹혀 죽는다 —
그러면 *"남이 이미 센 것을 다시 쓴 문장"*과 구별되지 않는다(`AGENT-CHAIN` §8).

```
M9   빈 규칙이면 지우지 않는다        →  9만 죽었다.  9는 센다
M10  학기 거름의 *아래* 끝만 뺀다     →  2만 죽었다.  2는 센다
```

★ **M10이 티켓 표의 빈칸을 메운다.** 학기 거름은 끝이 둘인데 M7은 **둘을 한꺼번에** 없애
2와 3을 함께 죽인다 — 한쪽만 없애면 갈린다(§5 *"대칭이 있는 것은 양쪽을 각각 적는다"*).

### 변이 배터리 — 열 중 열이 예상표와 일치

**돌리기 전에 예상을 적고 대조했다**(T-83). 매 판 **붙었는지 · 되돌아갔는지**를 바이트로 봤고
(함정 16 · T-79), **총계가 열 판 모두 506**이라 러너는 매번 살아 있었다(T-79 M7).

| 변이 | 예상 | 실제 | |
|---|---|---|---|
| M1 전체 삭제로 되돌린다 | 1·2·4·5·9 | 1·2·4·5·9 | 일치 |
| M2 겹침 조건을 뒤집는다 | 1·2·4·5·9 | 1·2·4·5·9 | 일치 |
| M3 범위가 같은 학기만 지운다 | 5·9 | 5·9 | 일치 |
| M4 GET이 모든 학기를 준다 | 4 | 4 | 일치 |
| M5 대표를 늘 "가장 늦게 시작"으로 | 4·6 | 4·6 | 일치 |
| M6 PUT 응답을 대표 학기로 | 7 | 7 | 일치 |
| M7 expand의 학기 거름을 뺀다 | 2·3 (+T-58 7) | 2·3 (+T-58 7) | 일치 |
| M8 NFC 정규화를 뺀다 | 8 | 8 | 일치 |
| M9 빈 규칙이면 미삭제 | 9 | 9 | 일치 |
| M10 거름의 아래 끝만 뺀다 | 2 | 2 | 일치 |

⚠️ **M1·M2는 T-85 밖에서 셋을 더 죽였다**(§T-60 1·2 · §T-71 1 — 전부 `수업칸=0`).
★ **겹침이 아니라 표본 탓이다**(`AGENT-CHAIN` §8 — 필연인지 표본 탓인지 가르는 시험):
그 셋은 **A의 시간표를 픽스처로 업고 있고**, M1·M2는 A를 통째로 지운다.
**다른 변이(M4~M10)에서 그 셋이 안 죽으므로** 서로 다른 것을 센다는 증거가 배터리 안에 있다.
⚠️ 그래서 그 셋을 좁히지 않았다 — 좁히려면 §T-60이 자기 시간표를 따로 깔아야 하고,
**그것은 이 티켓의 §금지(검사가 T-58 학기를 바꾼다)에 걸린다.**

### 도구를 두 번 의심해서 두 번 걸렀다 (함정 17 · 18)

1. **토큰 훑기가 낸 `0건`.** 신규 문서 둘에서 0건이 나왔다 — 스캐너에 **알려진 양성 한 줄**
   (경로·토큰·메일 주소)과 **평문 한글 한 줄**을 먹여 하나는 물고 하나는 안 무는 것을 보고서야
   *"없다"*로 적었다.
2. ⚠️ **CRLF 측정이 거짓말을 했다.** `grep -c $'\r'`를 **명령 치환 안에서** 부르자
   `$'\r'`가 빈 패턴이 되어 **`CR줄 = 전체줄`**(244/244 · 1169/1169 …)이 나왔다.
   ★ 화면에서 그것은 *"이 리포는 전부 CRLF다"*로 읽힌다 — **함정 17이 적은 `655` 그 자리다.**
   알려진 정답 둘(LF 파일 0 · CRLF 파일 2)을 먹이자 갈렸고, **바이너리로 다시 세니 전부 LF**였다.
   (`grep -cU`는 치환 **밖**에서만 맞는다. 안에 넣으면 똑같이 거짓말한다 — 그것도 확인했다.)
   ⚠️ 이것이 중요했던 이유: 작업 트리가 LF라야 **변이 앵커의 `\n`이 문다**(T-78이 물린 자리).

### 안 한 것 · 짚은 것

- **`public/`은 한 줄도 안 고쳤다**(티켓 §범위대로). 화면은 대표 학기를 전과 같은 모양으로 받는다.
- **마이그레이션 없음** — `timetable_rules`에 학기 범위가 이미 있다. 스키마가 필요한 자리가 없었다.
- `stClearTimetable`은 쓰는 곳이 없어져 지웠다(⑤). 이제 학기 하나를 비우는 길은
  **그 범위로 `rules: []`를 저장하는 것** 하나뿐이고, 검사 9가 그 길을 지킨다.
- ⚠️ **범위 밖이라 안 고친 것 하나 — Cowork 자리다.** `app.js:4548`이 `$("#tt-save").onclick = () => run(ttSave)`로
  **프라미스를 버린다**(함정 14의 ★★ — T-80이 `switchTab`에서 물린 바로 그 모양).
  지금은 front 검사가 이 경로를 왕복으로 안 타서 드러나지 않는다. **고치지 않았고 적어 올린다.**
