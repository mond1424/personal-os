# T-37 — 기기가 서버보다 먼저 포기하지 않는다

**발행** Cowork · 2026-08-19 · **담당** Claude Code · **작음 · 지금**
**근거** [ADR-038](../../APP-ADR.md#adr-038) · **APK 필요** (Kotlin 상수)

---

## 무엇이 어긋났나

**두 상수가 서로를 모른 채 겹쳐 있고, 바깥이 안보다 먼저 끊는다.**

```
기기  android/.../guard/GuardVerify.kt:29    TIMEOUT_MS    = 6_000   (connect·read 각각)
서버  src/services/guard.ts:507              AI_TIMEOUT_MS = 8_000   (model_high race)
```

**그래서 `server_timeout`은 관측될 수 없다.** 서버가 그 값을 주려면 8초 뒤에 답해야 하는데
기기는 6초에 끊는다. T-31이 만든 구별 — `timeout`(기기가 못 닿음) vs `server_timeout`(모델이 늦음) —
의 **한쪽이 도달 불가능한 값**이고, 그 둘은 대응이 다르다.

⚠️ **이것이 격상 0회의 원인은 아니다.** 판정 전수를 세면 그렇지 않다:

```
deny 11 · unavailable/timeout 3 · unavailable/(이유없음) 6 · unavailable/cap 2 · approve 0
```

`deny` 열한 번은 **왕복이 6초 안에 끝났다는 뜻**이고 그중 둘은 `ai_used=1`로 기록됐다.
**`ai_used` 기록 경로는 정상이고 일일 상한도 작동한다.** 이 티켓은 성능이 아니라
**관측 하나를 되살리는 것**이다 — 범위를 그렇게 좁게 잡는다.

## 왜 지금

**9~11월이 §6.5의 전례를 쌓는 기간이고, 그때 `approve`가 왜 안 나오는지를 읽어야 한다.**
지금 `unavailable`이 오면 *"Doze가 끊었나 · 모델이 늦었나"*를 가를 수 없다 —
8/18 밤의 `timeout` 셋이 정확히 그 상태였다. **경계가 흐린 채로 세 달을 쌓으면
12월에 그 세 달이 못 읽는 데이터가 된다.**

작고, APK 한 번에 묶이면 끝난다.

## 할 일

### ① 순서를 바로잡는다

**바깥이 안보다 길다.** 어느 값을 옮길지는 담당이 정하되, 근거를 보고에 적는다.

권고: **기기 쪽을 늘린다.** 개입 화면은 이미 떠 있고 검증은 백그라운드 스레드라
(`GuardNotifications.kt:233` `Thread { … }`) **사용자가 기다리는 시간이 아니다.**

⚠️ **`connectTimeout`과 `readTimeout`을 같은 값으로 두지 않는다.**

```
connectTimeout   짧게 — 연결이 안 되는 것은 진짜 네트워크 부재다. 빨리 알수록 낫다
readTimeout      길게 — 서버가 생각 중인 시간이다. 여기가 서버 예산보다 커야 한다
```

지금은 둘이 같은 상수를 쓴다. **뜻이 다른 둘을 한 이름으로 두면 한쪽을 고칠 때 다른 쪽이 딸려 간다.**

### ② 두 값이 갈라지는 것을 검사가 막는다

**이게 이 티켓의 본체다.** 지금은 **한쪽만 고쳐도 아무 검사가 빨간불이 되지 않는다** —
서로 다른 언어·다른 파일에 있고 서로를 모른다.

`test/smoke.ts`가 두 파일을 읽어 비교한다:

```
① 기기 readTimeout > 서버 AI_TIMEOUT_MS 인가
② ★ 스캐너가 살아 있는가        ← 합성 값으로 실제로 잡는지 확인 (T-36 ②의 자리)
③ 두 상수를 각각 찾았는가        ← 정규식이 못 찾으면 ①이 조용히 통과한다
```

**②·③이 ①의 짝이다.** *"부등호가 참이다"*를 세는 검사는 **둘 다 못 찾아도 참**이 될 수 있다
(`undefined > undefined`는 false지만, 파싱 실패를 0으로 채우면 참이 된다) — AGENT-CHAIN §5.

### ③ `server_timeout`이 관측 가능해진 것을 확인한다

고친 뒤 **서버 타임아웃을 일부러 짧게(예: 100ms) 만들어 한 번 발동**시키면
`ai_unavailable_reason`이 `server_timeout`으로 와야 한다. **와야 T-31의 구별이 살아난다.**
확인 후 되돌린다. (이건 변이 검사이지 커밋할 변경이 아니다.)

## 범위

```
android/.../guard/GuardVerify.kt    TIMEOUT_MS 분리 · 값 조정
test/smoke.ts                       스캐너 3건
```

**서버 무변경이 기본이다** — 서버 값을 옮기는 쪽을 고르면 그 근거를 보고에 적는다.
**마이그레이션 없다.** `ai_used`·`ai_verdict`·`ai_unavailable_reason` 칼럼은 그대로다
(0016의 CHECK에 `server_timeout`이 이미 있다 — 확인하고 쓴다).

## 금지

| 하지 말 것 | 왜 |
|---|---|
| fail-closed를 뒤집기 | ADR-024다. **원인이 진단 오류였는데 원칙을 바꾸는 것은 순서가 틀렸다** |
| 재시도 붙이기 | 새벽에 두 배 깨운다. 마찰은 Guard의 도구이지 버그 회피 수단이 아니다 |
| 사전 판정 캐시 | ADR-038 §기각한 대안 3행. 판정 재료가 발동 시점의 것이 아니게 된다 |
| 숫자를 ADR·CLAUDE.md에 적기 | 코드와 두 벌이 된다(§기준선 보고 규칙) |
| `connect`와 `read`를 한 상수로 두기 | 뜻이 다르다. 한쪽을 고칠 때 다른 쪽이 딸려 간다 |
| `ai_used` 기록 경로 손대기 | **그 값은 정상이다** — 8/11의 `deny` 둘이 `ai_used=1`로 남아 있다. 판정을 못 받았을 때의 0은 맞는 기록이다 |
| `approve`가 0인 것을 여기서 다루기 | 별건이다(T-38). **원인이 다르고 고치는 자리도 다르다** |

## 완료 조건

```
typecheck 통과 · smoke 306 → 309 이상 · front 291(변화 없음) · 실패 0 · verify exit 0
Kotlin: assembleRelease BUILD SUCCESSFUL · npx cap sync android 성공
```

**변이 — 이 티켓의 짝**

| 변이 | 기대 |
|---|---|
| 기기 값을 서버보다 작게 되돌린다 | ①이 죽는다 |
| 스캐너를 눈멀게 한다 | **①은 초록으로 남고 ★만 죽는다** ← 이게 핵심이다 |
| 서버 상수 이름을 바꾼다 | ③이 "못 찾았다"로 죽는다 |

## 확인 절차 (사용자) — APK 설치 후, 밤에

```
□ 낮에 testNotify({level:4}) 로 한 번   → ai_verdict 가 approve 인가
□ ai_used 가 1로 기록되는가             ← 상한이 이제 오른다
□ 두 번 더 불러 상한이 실제로 걸리는가   ← coach 의 cap 은 5
```

⚠️ **밤 실측 전날 낮에는 하지 않는다** — 상한이 이제 진짜로 오르므로 그 밤의 예산을 태운다.
귀속일 경계가 06:00이라 **01:30~05:00은 전날 낮과 같은 날**이다.

---

## 보고 (담당이 채운다)

```
티켓: T-37
바꾼 파일: android/guard/GuardVerify.kt   TIMEOUT_MS → CONNECT_TIMEOUT_MS · READ_TIMEOUT_MS
           test/smoke.ts                 스캐너 4건
           android/guard/GuardEventQueue.kt · GuardNotifications.kt   낡은 '6초' 주석 (아래)
           migrations/0016_...sql        주석 두 줄에서 숫자를 뺐다 (SQL 무변경 · 아래)
기준선: typecheck 통과 · smoke 313 → 317 · front 291(변화 없음) · 실패 0 · verify exit 0
        (T-38 을 먼저 받아 306 → 313 이었다. 이 티켓 단독 증가분은 4건)
        Kotlin assembleRelease BUILD SUCCESSFUL (56s) · [signing] release SHA-256 확인
        npx cap sync android ✅ (Sync finished in 1.542s)
어느 값을 옮겼고 왜: **기기 쪽을 늘렸다. 서버 무변경(8초).**
        서버를 줄이면 모델에게 주는 시간이 줄어 격상이 더 어려워진다 —
        ADR-038 §기각한 대안 2행이 "어느 쪽을 옮길지는 실측이 정한다"라고 남긴 자리인데,
        **아직 실측이 없다.** 없는 상태에서 안쪽을 줄이는 것은 관측을 늘리려고 판정 기회를
        줄이는 것이라 방향이 반대다. 기기 쪽은 백그라운드 스레드라 비용이 거의 0이다.
connect / read 를 어떻게 갈랐나:
        connect 4초 — 연결 자체가 안 되는 것은 진짜 네트워크 부재다. 빨리 알수록 낫다.
                      3초는 Doze 에서 라디오가 깨는 시간을 감안해 짧다고 봤다.
        read   12초 — AI_TIMEOUT_MS(8초) + 여유 4초. 서버는 그 예산 **위에** 컨텍스트
                      조립과 왕복이 얹히므로 여유는 그쪽 몫이다.
        최악 대기 = 4 + 12 = 16초. 전부 백그라운드다.
server_timeout 이 실제로 관측됐나 (§할 일 ③): **못 했다 — 이 층이 할 수 있는 일이 아니다.**
        서버 타임아웃을 100ms 로 낮추려면 그 상태로 **배포**해야 하고 배포는 사용자가 한다.
        그다음 폰에서 발동시켜야 하는데 APK 도 아직 안 깔렸다. **아래 §확인 절차로 옮긴다.**
```

## 어긋난 것 · 넘긴 것

**① 변이 표의 둘째 줄이 실제와 다르다 — 더 안전한 쪽으로.**

티켓은 *"스캐너를 눈멀게 하면 **①은 초록으로 남고** ★만 죽는다"*라고 적었다.
실제로는 **①도 함께 죽는다**(314/3). §할 일 ②가 경고한 *"파싱 실패를 0으로 채우면 참이 된다"*를
피해 **못 찾으면 `null`을 그대로 두었기** 때문이다 — `null > 8000`은 false라 ①이 조용히
통과하지 못한다. 티켓의 기대는 "0으로 채웠을 때"의 모양이고, 그 모양을 안 만들었다.

**변이 셋을 실제로 돌렸다.**

| 변이 | 기대 | 실제 |
|---|---|---|
| 기기 값을 서버보다 작게 (`READ 6_000`) | ①이 죽는다 | **316/1** — ①만 죽고 `read=6000 server=8000`을 찍는다 ✅ |
| 스캐너를 눈멀게 (`TS_AI` 정규식 오염) | ①은 초록 · ★만 죽는다 | **314/3** — ③이 `server=null`로 이름을 대고 ①·★도 죽는다 ⚠️ |
| connect를 read와 같게 (한 상수로 되돌린 효과) | — | **316/1** — 셋째 검사만 죽는다 ✅ |

**② 범위 밖 두 가지를 건드렸다. 둘 다 숫자 하나짜리 주석이다.**

- `GuardEventQueue.kt`·`GuardNotifications.kt`에 **"6초"가 세 군데 더** 있었다.
  값이 여기서 바뀌므로 그대로 두면 **오늘 만든 낡은 문서**가 된다. 숫자를 빼고 상수를 가리켰다.
  ⚠️ `GuardEventQueue.amendFire`에는 경고를 하나 더 달았다 — **기다리는 창이 6초에서 최악
  16초가 됐다.** 그 사이에 `flush()`가 돌면 판정이 서버에 안 실린다(원래 있던 위험이 넓어졌다).
- `migrations/0016`의 주석 두 줄이 `6초`·`8초`를 적고 있었다. **SQL은 손대지 않았고**
  주석에서 숫자만 뺐다(이미 적용된 마이그레이션이라 최소로). **되돌리길 원하면 말해 달라.**

**③ 갈랐지만 세지는 못한다 — 알아 둘 한계.**

`connect`와 `read`는 이제 값이 다르지만 **자바가 둘 다 `SocketTimeoutException`을 던지므로
기록에는 여전히 `timeout` 하나**로 남는다. 갈라 세려면 이유 목록에 값을 더해야 하고,
그 목록은 0016의 CHECK가 강제하는데 **SQLite는 CHECK를 고치려면 테이블을 다시 만들어야 한다** —
`guard_events`는 개입 이력 영구 보존이라 옮기지 않는다. 이 티켓이 되살리는 갈래는
`timeout`(기기) vs `server_timeout`(서버)이고 **그쪽은 이제 실제로 갈린다.**

## §확인 절차에 더할 것 (담당 → 사용자)

**위 §확인 절차는 그대로 두었다**(Cowork 발행분). 아래 둘만 보탠다.

**A. §할 일 ③ — `server_timeout` 변이 (배포가 필요해 이 층이 못 한다)**

```
1  src/services/guard.ts 의 AI_TIMEOUT_MS 를 100 으로 낮춘다   ← 커밋하지 않는다
2  npm run deploy
3  폰에서 testNotify({ level: 4 })
4  guardEvents 의 ai_unavailable_reason 이 server_timeout 인가   ← 여기가 과녁
5  8_000 으로 되돌리고 다시 deploy
```

**4번이 `timeout`으로 오면 고친 것이 안 먹은 것이다** — 그건 기기가 먼저 끊었다는 뜻이고,
APK 가 안 깔렸거나 값이 안 들어간 것이다. `server_timeout`이 와야 T-31 의 구별이 살아난다.

⚠️ **APK 를 깐 뒤에 한다.** 옛 APK 는 여전히 6초에 끊으므로 5단계를 다 밟아도 `timeout`이 온다.

**B. §확인 절차 2번 줄의 전제 하나 — 확인 요청**

```
□ ai_used 가 1로 기록되는가   ← 상한이 이제 오른다
```

**"상한이 이제 오른다"는 이 티켓의 성과가 아니다.** `ai_used` 기록 경로는 원래 정상이고
(8/08·8/11 의 `deny` 둘이 `ai_used=1`로 남아 있다 — 티켓 §무엇이 어긋났나가 직접 그렇게 적었다),
8/17 낮에 상한이 0이었던 것은 `secretary` 모드였기 때문이다. **확인 자체는 유효하니 그대로 하되,
그 화살표는 지운다.** 같은 오진이 `FIELD-TEST-NIGHT.md`·`APP-BUILD.md` 에 있었고 오늘 정정했다.
