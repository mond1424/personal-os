# T-73 — Compose 스파이크: 캘린더 하나로 전환 비용을 잰다

**발행** Cowork · 2026-09-14 · **담당** Claude Code · **큼**
**근거** [`APP-PLAN` §6 Compose 스파이크](../../APP-PLAN.md) · [§7 12월 네이티브 전환 결정](../../APP-PLAN.md) · ADR-017
**지금 쓰는 앱 무변경 · 마이그레이션 없음 · 배포 무관 · `src/`·`public/` 무변경**

---

## ★★ 이 티켓은 만드는 티켓이 아니라 **재는 티켓**이다

**`APP-PLAN`이 *"코드 변환 1주일"* 을 검증 가능한 가설이라고 적어 뒀다.** 이 티켓이 그 가설을 잰다.

⚠️⚠️ **그래서 실패해도 성공이다.** *"Compose 로는 이게 안 된다"* 는 **결과이지 잘못이 아니고**,
그것이 12월에 전환을 **안 하는** 근거가 된다.

★★★ **담당이 "성공시켜야 한다"고 느끼면 그 순간 측정이 오염된다.**
막힌 자리를 우회해서 그림만 맞추면 **12월 결정이 거짓 재료 위에 선다** — 이 리포가
`ignored` 40건으로 이미 한 번 겪은 일이다(ADR-047 §정정).

---

## 왜 캘린더인가

**가장 어렵기 때문이다.** 배경 밴드 경계선 모델(설계 §2.2) · 셀 동적 공간 예산 ·
3-pane 슬라이드 · 날짜 팝업. **여기가 되면 나머지는 쉽고, 여기서 막히면 전부 막힌다.**

★ 그래서 **여기서 나온 막힘 목록이 전체 전환 비용의 상한**이다.

## ★★ 재는 것은 코드 작성이 아니라 **튜닝 재현**이다

`APP-PLAN`이 못 박았다 — **이 숫자들이 제품이다:**

```
AXIS_LOCK 20 · 축비 1.9 · TRACK_RATIO 0.35 · FLICK 0.5
CAL_GAP 20 · STRETCH_MAX 90 · STRETCH_K 0.42 · STRETCH_BACK_MS 460
```

⚠️ **그 값을 그대로 옮기면 안 된다.** Compose 는 제스처 시스템이 달라
(`pointerInput` / `Animatable` / `nestedScroll`) **값이 넘어가지 않는다. 다시 유도해야 한다.**

★★ **값을 복사해 넣고 "됐다"고 하는 것이 이 티켓의 가장 큰 실패 모양이다** —
그러면 잰 것이 *"코드가 도나"* 가 되고, **재려던 것은 안 쟀다.**

---

## ★★★ 판정자는 사용자다 — 담당이 "같다"고 말할 수 없다

**"같게 느껴지는가"는 화면만이 답한다.** `verify`도 스크린샷도 이걸 못 잰다.

```
담당이 만든다  →  사용자가 폰에서 만져 본다  →  "아직 다르다" / "같다"
                                                 ★ 이 왕복을 센다
```

⚠️ **"다르다"가 왔을 때 *무엇이* 다른지를 사용자가 말로 못 할 수 있다** — 그게 정상이다.
**그때 담당이 물어야 할 것은 느낌이 아니라 자리다:**

```
언제 다른가    손 뗄 때 / 끌 때 / 경계에서 튕길 때 / 멈출 때
어느 축인가    가로 / 세로 / 대각선에서 축이 안 잠긴다
```

★ **왕복 한 번마다 무엇을 바꿨는지 적는다.** 그 기록이 §보고의 절반이다.

---

## 할 일

### ① 붙일 자리 — 지금 쓰는 앱에 영향 0

⚠️ **사용자는 서명된 릴리스 APK를 매일 쓴다. 그것이 흔들리면 안 된다.**

```
제약    지금 앱의 진입점·화면·동작이 한 글자도 안 바뀐다
방법    ★ 담당이 정한다 (디버그 소스셋 · applicationIdSuffix · 별도 액티비티 …)
```

⚠️ **릴리스 APK에 스파이크가 섞이면 그 자체가 회귀다.** 어느 길을 골랐는지 §보고에 적는다.

### ② 데이터는 고정 픽스처로

**실 D1 에 붙이지 않는다.** 6주치 셀을 손으로 박아 넣는다.

★ **재려는 것은 제스처이지 데이터 파이프라인이 아니다.** 서버를 붙이면
*"네트워크가 느려서 버벅인 것"* 과 *"제스처가 다른 것"* 이 화면에서 같아진다.
⚠️ **픽스처에 고정 날짜를 쓰지 마라**(함정 12) — 상대로 잡는다.

### ③ 만들고, 보여 주고, 왕복한다

**사용자가 "같다"고 할 때까지.** 또는 **막혀서 더 못 갈 때까지.**

⚠️ **막히면 우회하지 말고 멈추고 적어라.** 막힘이 이 티켓의 산출물이다.

---

## 산출물 — ★ 셋 중 셋째가 본체다

```
①  캘린더가 Compose 로 뜬다                    기초. 이것만으로는 아무것도 안 쟀다
②  사용자가 만져서 "같다"고 한다                ★ 튜닝 재현이 됐는가
③  ★★ 막힘 목록                                ← 12월 결정의 재료
```

**③의 형식** — 한 줄에 하나, 넷을 적는다:

```
무엇을 하려 했나  |  Compose 에서 왜 안 됐나  |  어떻게 했나  |  대가
```

`어떻게 했나`는 셋 중 하나다: **해결 · 우회 · 못 함.**
⚠️ **"우회"와 "해결"을 섞지 마라** — 우회는 나머지 화면에서도 다시 나온다.

---

## 범위

```
android/  스파이크 자리 (담당이 정한 격리 방식 안에서만)
docs/tickets/T-73-*.md   §보고
```

**마이그레이션 없음 · `src/` 무변경 · `public/` 무변경 · `test/` 무변경.**

## 금지

| 하지 말 것 | 왜 |
|---|---|
| **지금 앱의 화면·진입점 건드리기** | ⚠️ **매일 쓰는 앱이다.** 스파이크가 제품을 흔들면 그 자체가 회귀 |
| 여덟 숫자를 그대로 복사해 넣기 | ⚠️ **값이 안 넘어간다.** 그러면 재려던 것을 안 잰 것이다 |
| 막힌 자리를 조용히 우회하기 | ★★ **막힘이 산출물이다.** 감추면 12월 결정이 거짓 위에 선다 |
| 담당이 "같다"고 판정하기 | **화면만이 답한다.** 판정자는 사용자다 |
| 실 D1·서버에 붙이기 | 느린 네트워크와 다른 제스처가 화면에서 같아진다 |
| `guard/` 아래 손대기 | 위임 금지 영역(`AGENT-CHAIN` §4) |
| 스파이크를 릴리스 빌드에 넣기 | ①의 제약 위반 |

## 완료 조건

```
지금 앱 무변경     ★ smoke·front 가 한 칸도 안 움직인다 — 움직였으면 범위를 넘은 것이다
                     (T-71 에서 front 가 안 움직인 것이 검사였던 것과 같다)
assembleRelease    통과 (함정 13 — verify 는 Android 를 안 본다)
③ 막힘 목록        ★ 비어 있으면 안 된다. 0건이면 "왜 0인가"가 보고다
```

⚠️ **기준선은 `STATE.md` §기준선이 원장이다.**
⚠️ **③이 0건으로 나오면 함정 18을 의심해라** — *"막힌 게 없다"* 와 *"막힘을 안 셌다"* 는
화면에서 같다. **캘린더를 고른 이유가 가장 어렵기 때문인데 0건이면 뭔가 안 본 것이다.**

## ★ 판정 — 계획의 2~3일 : 1주는 **사람 기준이다**

⚠️ **담당은 사람보다 빠르므로 그 선을 그대로 쓰면 항상 "2~3일"이 나온다.**
**그 수는 이 티켓의 자가 아니다.**

**대신 자로 쓰는 것:**

```
★ 막힘 목록의 "우회" + "못 함" 개수        ← 나머지 화면에서 다시 나올 것들
★ 사용자 왕복 횟수                          ← "아직 다르다" 가 몇 번 왔나
   그중 축·경계·튕김에서 온 것이 몇인가     ← 제스처 시스템 차이의 크기
```

⚠️ **판정선은 이 티켓이 긋지 않는다.** 첫 스파이크라 비교할 이전 값이 없다 —
**이번 수가 기준이 된다.** 긋는 척하면 그게 추측이다.
★ **해석은 Cowork 가 한다.** 담당은 수를 들고 온다.

## 확인 절차 (사용자)

```
□ 지금 쓰는 앱이 그대로다 (스파이크를 깔아도)     ← 먼저 이것부터
□ 스파이크 캘린더를 만져 본다 — 몇 번이고 "다르다"고 말해 준다
□ ★ 다를 때 "언제" 다른지만 말하면 된다 (손 뗄 때 / 끌 때 / 튕길 때 / 멈출 때)
```

⚠️ **"같다"고 빨리 말해 주지 마라.** 그 한마디가 12월 결정을 바꾼다.

---

## ★ 이 티켓이 안 하는 것

- **전환을 결정하지 않는다.** 재기만 한다. 결정은 `APP-PLAN` §7, 12월이다.
- **나머지 화면을 안 만든다.** 캘린더 하나다.
- **스파이크를 제품으로 안 키운다.** ⚠️ 커밋은 하되 **버릴 코드**로 다룬다 —
  여기 든 공을 아까워하는 것이 12월 판단을 흐린다.
- **설계문서·`APP-PLAN`을 안 고친다.** 결과를 §7의 판단 입력으로 얹는 것은 Cowork 자리다.

---

## 보고 (담당이 채운다)

#### 라운드 0 — 만들어서 폰에 올릴 수 있는 상태 (2026-09-14)

**사용자 판정 전이다.** 아래 ③ 막힘 목록의 "대가"에서 *화면이 답할 것*으로 적힌 줄은
전부 미정이고, 왕복이 채운다.

---

### ① 격리 — 세 겹. 릴리스 빌드가 스파이크를 **구조적으로 못 담는다**

```
1겹  별도 Gradle 모듈            :spike
2겹  별도 applicationId           dev.mond1424.personalos.spike   (폰에 따로 깔린다)
3겹  release 변이 자체를 비활성화  androidComponents.beforeVariants { enable = false }
```

**기존 파일 중 건드린 것은 `android/settings.gradle`의 `include ':spike'` 한 줄뿐이다.**
(루트 `build.gradle`도 안 열었다 — Compose 컴파일러 플러그인은 모듈 안 `plugins {}`로
Gradle Plugin Portal 에서 받는다.)

**근거는 실측이다. 주장이 아니다:**

| 물음 | 확인한 것 | 답 |
|---|---|---|
| 릴리스 빌드에 섞이나 | `gradlew assembleRelease` 로그에서 `:spike:` 태스크 수 | **0** |
| 릴리스 APK 가 바뀌나 | `:app:packageRelease` | **UP-TO-DATE**(재패키징 안 함) |
| 서명이 그대로인가 | `[signing] release SHA-256=` | `C1:D8:…:A6` — `STATE.md` 기록과 같다 |
| `cap sync` 가 한 줄을 지우나 | `@capacitor/cli/dist/android/update.js:168` | **`capacitor.settings.gradle` 만 쓴다** — `settings.gradle` 은 안 건드린다 |

⚠️ **설치해도 지금 앱은 안 흔들린다** — applicationId 가 다르고 서명도 다르다(디버그 키).
**같은 앱을 덮어쓰는 경로가 아예 없다.**

---

### ② 무엇을 만들었나

`android/spike/` — **버릴 코드다.** 실 D1·서버에 안 붙는다. 픽스처는 전부 오늘 기준 상대(함정 12).

```
Tuning.kt          여덟 숫자를 "다시 유도한" 기록 — 값이 아니라 없어진 자리를 적었다
Fixture.kt         6주치 고정 픽스처 (기간 4개가 서로 다르게 겹친다 · 셀 예산이 갈리도록 섞었다)
Band.kt            배경 밴드 경계선 모델(설계 2.2) → Compose Path
CalendarScreen.kt  6주 그리드 · 월 캐러셀 · 셀 동적 공간 예산 · 날짜 팝업 · 범례 · 기간 목록
SpikeActivity.kt   진입점 (402dp 폭 제한 = 웹 .phone 과 같은 프레임)
```

**빌드**: `:spike:assembleDebug` BUILD SUCCESSFUL · APK `android/spike/build/outputs/apk/debug/spike-debug.apk`

---

### ★★ 여덟 숫자를 어디로 보냈나 — **한 개도 그대로 안 넣었다**

| 웹 | Compose 에서 해당하는 자리 | 1차 유도 |
|---|---|---|
| `AXIS_LOCK 20` | `LocalViewConfiguration.touchSlop` — **플랫폼이 준다** | 내가 안 정한다. 화면에 실제 dp 를 찍어 둔다 |
| `축비 1.9` | **없다** | 방향별 슬롭이 각자 재고 먼저 넘은 축이 가져간다 |
| `TRACK_RATIO 0.35` | `PagerDefaults.flingBehavior(snapPositionalThreshold)` | **기본값 0.5 를 그대로 쓴다** |
| `FLICK 0.5` | **없다** | 속도는 임계가 아니라 decay 스플라인의 입력이다 |
| `CAL_GAP 20` | `pageSpacing` | **20.dp — 이것만 넘어간다**(아래 단위 분석) |
| `STRETCH_MAX 90` | 플랫폼 stretch overscroll | 셋 다 지웠다 |
| `STRETCH_K 0.42` | 〃 | 〃 |
| `STRETCH_BACK_MS 460` | 〃 | 〃 |

★ **1차 유도의 질문은 하나다: *"플랫폼 기본값이 웹 튜닝을 대신할 수 있는가."***
그래서 일부러 Compose 가 주는 값을 그대로 썼다. 여기서 사용자가 *"다르다"* 고 말하는 자리가
**제스처 시스템 차이의 크기**다.

★ **단위 분석 — 이 하나가 여덟을 둘로 가른다:**
웹은 WebView 안 CSS px 로 잰다. **1 CSS px = 1 dp** 다. 그래서
**레이아웃 상수는 숫자가 그대로 넘어가고(dp 로 읽으면 된다), 제스처 상수는 안 넘어간다** —
재는 주체가 브라우저 포인터 이벤트에서 Compose 의 `pointerInput` 과 fling 스플라인으로
통째로 바뀌기 때문이다. `CAL_GAP` 만 넘어간 것이 우연이 아니다.

---

### ③ ★★ 막힘 목록

**형식**: 무엇을 하려 했나 | Compose 에서 왜 안 됐나 | 어떻게 했나 | 대가

#### A. 제스처 — **여기가 본체다**

| # | 무엇을 | 왜 안 됐나 | 어떻게 | 대가 |
|---|---|---|---|---|
| A1 | 축 잠금 임계를 20 으로 | `HorizontalPager`/`PagerState` 어디에도 슬롭을 넣는 자리가 없다. 제스처 감지기가 `LocalViewConfiguration` 에서 직접 읽는다 | **우회** — 플랫폼 값을 쓴다 | **약 8dp 로 2.5배 예민하다.** 맞추려면 `pointerInput` 을 손으로 다시 짜야 하고 **그 순간 `HorizontalPager` 를 버린다** |
| A2 | 축비 1.9 (더 확실한 가로만) | **개념이 없다.** 두 방향 슬롭을 각각 재고 먼저 넘은 축이 가져간다 | **못 함** | 대각선 손짓의 판정이 다르다. ★ 어느 쪽으로 다른지는 화면이 답한다 |
| A3 | 거리 판정 0.35 | `snapPositionalThreshold` 가 같은 자리다 | **해결** | ⚠️ **같은 숫자가 같은 뜻이 아니다.** 웹은 *거리 OR 속도* 이분 판정이고, Compose 는 **decay 로 도착점을 먼저 구한 뒤** threshold 를 댄다 |
| A4 | 속도 임계 0.5 px/ms | **파라미터가 없다.** `flingBehavior` 인자는 다섯이고(실측 — `javap`) 속도 임계는 그중에 없다: `state · snapDistance · decaySpec · snapAnimationSpec · threshold` | **못 함** | 속도는 *"어디까지 미끄러질지"* 로만 쓰인다. 임계를 원하면 `flingBehavior` 를 직접 구현 |
| A5 | 경계 스트레치 세 값 | `OverscrollConfiguration` 에 **`glowColor` 와 `drawPadding` 밖에 없다**(실측 — `javap`). 양·감쇠·복귀 시간은 비공개 | **우회** — 플랫폼 효과를 그대로 쓴다 | **파라미터로는 못 맞춘다.** `LocalOverscrollFactory` 로 **통째 교체**는 공개돼 있다 = 맞추려면 다시 구현 |

#### B. 구조·렌더 — **여기는 오히려 줄었다**

| # | 무엇을 | 웹에서 무엇이 있었나 | 어떻게 | 대가 |
|---|---|---|---|---|
| B1 | 5-pane 돌려 쓰기 | `calBusy` · `calGen` · `transitionend` 유실 대비 타이머 · pane 회전 · `panesAlreadyCurrent` | **해결 — 통째로 사라졌다.** 페이저가 자기가 재활용한다 | 없음 |
| B2 | 밴드 SVG | `viewBox 700x96` + `preserveAspectRatio="none"` 때문에 **가로만 늘어나서** 둥근 마감 반지름을 `700/실제폭` 만큼 키우는 보정(`capRx`)이 있었다 | **해결** — 실제 픽셀에 직접 그린다. 보정이 통째로 없다 | 없음 |
| B3 | 끌고 난 직후의 click 삼키기 | `dragBlockUntil = Date.now() + 60` | **해결** | 없음 — 드래그가 소비한 포인터는 클릭을 안 만든다 |
| B4 | 셀 글자 크기 | CSS px 은 **시스템 글꼴 배율을 안 탄다**. Compose 의 `sp` 는 탄다 | **우회** — `dp.toSp()` | 스파이크가 배율을 무시한다. ★ 뒤집으면 **웹이 접근성 글꼴 배율을 무시하고 있었다**는 뜻이고, 그건 이 티켓 밖의 별도 문제다 |
| B5 | 중복 `id` 사고(함정 15) | `#cal-list` 가 둘이라 숨은 칸에 썼다 | **해당 없음** | Compose 엔 전역 선택자가 없어 **그 사고 모양 자체가 없다** |

#### C. 빌드·격리

| # | 무엇을 | 왜 | 어떻게 | 대가 |
|---|---|---|---|---|
| C1 | 지금 앱 파일 0개 수정 | 모듈을 Gradle 에 알리는 한 줄은 불가피 | **우회** | `settings.gradle` 한 줄. `cap sync` 가 안 지운다(실측) |
| C2 | Kotlin 2.1.0 에서 Compose | 2.0 부터 Compose 컴파일러가 별도 플러그인이다 | **해결** — 모듈 안 `plugins {}` | 루트 `build.gradle` 무변경 |

#### D. ★ 안 쟀다 — **0건이 아니라 "아직"이다**

| # | 무엇 | 왜 중요한가 |
|---|---|---|
| D1 | **가로 캐러셀 안의 가로 캐러셀** | 웹은 탭 전환(바깥)과 달 넘기기(안)가 **둘 다 가로**고, `SWIPE_EDGE_RATIO 0.2` 로 *"가장자리 20%는 바깥 것"* 이라고 손으로 갈랐다. **Compose 의 중첩 가로 페이저는 이 티켓에서 안 건드렸다** — 스파이크에 바깥 탭이 없기 때문이다. ★ **남은 것 중 가장 어려운 자리로 보인다** |
| D2 | 다크 모드 | 웹은 변수 짝으로 한다(함정 5). 스파이크는 라이트만 |
| D3 | 날짜 선택 모드(pick) | 캘린더가 *"날짜를 탭해 일정 정하기"* 로 바뀌는 상태. 제스처 차단 규칙이 붙는다 |
| D4 | 실제 데이터 · 로딩 · 캐시 세대 | 일부러 뺐다(티켓 ②) |

⚠️ **D 는 "막힘 0건"이 아니다.** 재려던 것 중 **안 댄 자리**다 — 함정 18 의 *"없다 와 못 봤다"* 를
가르려고 따로 뒀다.

---

### 왕복 기록

| # | 사용자가 말한 것 | 무엇을 바꿨나 |
|---|---|---|
| 0 | (아직 없다 — 설치 대기) | — |

★ **축·경계·튕김에서 온 것: 0 / 전체: 0** (판정 전)

---

### 숫자

```
smoke  452 → 452 (변화 없음)
front  467 → 467 (변화 없음)
```

★ **움직이지 않은 것이 검사다.** `src/` · `public/` · `test/` · `migrations/` 를 한 줄도 안 열었으므로
움직였으면 범위를 넘은 것이다(T-70 · T-71 과 같은 모양).

```
gradlew assembleRelease   BUILD SUCCESSFUL · :spike: 태스크 0 · packageRelease UP-TO-DATE
                          [signing] release SHA-256=C1:D8:…:A6
gradlew :spike:assembleDebug   BUILD SUCCESSFUL
```

⚠️ `verify` 는 Android 를 안 본다(함정 13) — 그래서 `assembleRelease` 를 따로 돌렸다.

---

### ★ 판정용 수 (해석은 Cowork 가 한다)

```
막힘 "우회"  4   (A1 · A5 · B4 · C1)
막힘 "못 함"  2   (A2 · A4)
막힘 "해결"  5   (A3 · B1 · B2 · B3 · C2)
안 쟀다      4   (D1~D4)
사용자 왕복  0   (판정 전)
```

⚠️ **"해결" 다섯 중 셋(B1·B2·B3)은 코드가 *줄어서* 해결됐다.** 그 셋은 **웹이 손으로 하던
DOM 재사용·viewBox 보정·클릭 삼킴**이고, 네이티브가 그냥 갖고 있는 것이다.
★ **반대로 "못 함" 둘은 전부 제스처 판정이고, 그것이 이 앱의 제품이다.**

---

### 확인 절차 (사용자)

```powershell
adb install -r android\spike\build\outputs\apk\debug\spike-debug.apk
```

⚠️ **`-r` 이 지금 앱을 건드리지 않는다** — 패키지 이름이 달라 **다른 앱**으로 깔린다.
폰 런처에 **`OS Spike`** 라는 이름으로 하나 더 생긴다.

```
□ 먼저 이것부터 — 지금 쓰는 Personal OS 가 그대로다 (아이콘 · 권한 · 알람)
□ OS Spike 를 열어 달을 넘겨 본다. 몇 번이고 "다르다"고 말해 준다
□ ★ 다를 때 "언제" 다른지만 말하면 된다:
     손 뗄 때 / 끌 때 / 경계에서 튕길 때 / 멈출 때
     가로 / 세로 / 대각선에서 축이 안 잠긴다
□ 맨 위 회색 한 줄에 이 폰의 touchSlop 이 dp 로 찍혀 있다 — 그 수를 알려 주면
  A1 의 "2.5배"가 실제로 몇 배인지 갈린다 (OEM 이 바꿀 수 있는 값이다)
```

⚠️ **"같다"고 빨리 말해 주지 마라.** 그 한마디가 12월 결정을 바꾼다.

---

### 이 층이 적지 않는 것

**설치 여부 · 판정.** 둘 다 사용자가 하고 이 층은 모른다(`CLAUDE.md` §사람이 하는 것의 상태).
⚠️ **무선 디버깅은 지금 안 열려 있다** — `ADB_MDNS_OPENSCREEN=1` 로 다시 물어서도 0건이었다
(함정 18 대로 백엔드를 바꿔 한 번 더 물었다). **그래서 설치는 사용자 손이다.**
